{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "command": "echo \"Use edit_file or edit_symbol instead -- they don't require reading the file first.\""
      }
    ]
  }
}
