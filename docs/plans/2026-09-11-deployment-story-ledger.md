# Deployment Story Verification Ledger

Plan: `docs/plans/2026-09-11-deployment-story-plan.md`. Reuse a row only when the scope label matches and the commit SHA equals the current HEAD.

## Verification Ledger

| Invariant | Command | Scope Label | Commit SHA | Result | Timestamp (UTC) | Evidence Reused |
|---|---|---|---|---|---|---|
| Batch A (tasks 1 to 5) leaves the whole workspace green: 2208 passed, 13 ignored CLI tests passed | `cargo xtask test dev` | dev | 622c616d | pass (39.2 s wall) | 2026-09-11T21:02:00Z | no |
| Plugin tests green after the manifest and hook change: 20 pass, 0 fail | `node --test --test-reporter=tap hooks/session-start.test.cjs hooks/installers-no-hooks.test.cjs hooks/run.test.cjs` (julie-plugin) | plugin-tests | working tree on d741d4c (julie-plugin), committed as bbd2c53 | pass | 2026-09-11T20:56:45Z | no (worker run, Task 5 report) |
| Packed Linux archive runs through `run.cjs`: sidecar beside the binary, embedding child ready on poll 1, `fast_search` 6 hits, service stopped | Task 6 steps (see `.razorback` report and the finding) | live | 622c616d (julie), bbd2c53 (julie-plugin) | pass, except `initialize` lacked `instructions` (fixed below) | 2026-09-11T21:06:00Z | no |
| Service `initialize` carries the agent instructions | `cargo nextest run --lib initialize_over_http_carries_the_agent_instructions` | worker-red-green | working tree on 622c616d | RED then pass | 2026-09-11T21:12:00Z | no |
| Plugin launcher picks the highest archive version when several match | `node --test hooks/run.test.cjs` (julie-plugin), then `rm -rf bin/x86_64-unknown-linux-gnu && node hooks/run.cjs --version` | worker-red-green | working tree on 6ed348b (julie-plugin) | RED (actual `julie-v7.18.0`) then pass; live run extracted 8.0.0 | 2026-09-11T21:29:00Z | no |
| Shim binds tool calls to `JULIE_WORKSPACE` when set | `cargo nextest run --lib shim_workspace_root_honors_julie_workspace_env` | worker-red-green | working tree on 9e38fdba | RED (no such function) then pass | 2026-09-11T22:05:00Z | no |
| Six harness checks: each harness starts the plugin launcher and one `fast_search` answers `:52 find_default_sidecar_binary function` | see the finding's harness table (`claude -p`, `codex exec`, `agy -p`, `opencode run`, `hermes chat -q`, `cursor-agent -p`) | live | service built from 9e38fdba, packed shim from 622c616d, 97a4111 (plugin launcher) | pass 6/6; Codex and Antigravity pass only from a user-level config entry, so the plugin no longer declares a server for them (plugin 193cb03) | 2026-09-11T22:08:00Z | no |
| Harness configs restored | `cmp` against the backups for `~/.codex/config.toml`, `~/.hermes/config.yaml`, `~/.gemini/config/mcp_config.json`, `~/.cursor/mcp.json`; `codex plugin remove` and `marketplace remove`; `agy plugin uninstall`; `install-opencode.cjs --uninstall`; `~/.config/opencode/opencode.json` deleted | live | n/a | pass, byte for byte; no plugin install remains | 2026-09-11T22:09:00Z | no |
| Branch gate (src changed, escalated from dev): `cargo fmt --check`; `cargo clippy --workspace --all-targets`; whole workspace, CLI tests, dogfood set | `cargo fmt --check`; `cargo clippy --workspace --all-targets`; `cargo xtask test full` | full | 18403740 | pass: fmt clean; clippy 0 errors, pre-existing warnings only (item 5); full 5/5 commands in 41.7 s, dogfood 69 passed | 2026-09-11T22:11:20Z (full), 2026-09-11T22:12:45Z (clippy) | no |
| Plugin tests green at the final plugin HEAD: 21 pass, 0 fail | `node --test hooks/*.test.cjs` (julie-plugin) | plugin-tests | 1dec280 (julie-plugin) | pass | 2026-09-11T22:34:18Z | no |
| Release shim honors `JULIE_WORKSPACE` from a foreign directory | `cd /tmp && printf '<initialize, initialized, fast_search>' \| JULIE_WORKSPACE=/home/murphy/source/julie target/release/julie-server` (release build of 18403740; output in `scratchpad/shim-env-livecheck.jsonl`) | live | 18403740 | pass: version 8.0.0, 1,960 instruction bytes, 6 hits, first `crates/julie-pipeline/src/embeddings/native/launch.rs:52` | 2026-09-11T22:32:29Z | no |
| Docs contract tests after the review fixes to README, release notes, finding, and ledger | `cargo xtask test dev` | dev | working tree on 18403740 (docs only), committed as the finding commit | pass 3/3 commands in 15.5 s: 2210 passed, 13 CLI tests passed. First run failed on a stray `/tmp/.julie` marker left by the mistaken `/tmp` shim run; removed, rerun clean | 2026-09-11T22:33:49Z | no |
