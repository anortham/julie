# Codebase Cleanup Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use razorback:subagent-driven-development when subagent delegation is available. Fall back to razorback:executing-plans for single-task, tightly-sequential, or no-delegation runs.

**Goal:** Remove dead and test-only production code, reduce duplicate cleanup and path policy, and split the widest runtime/search/test surfaces into deeper modules with smaller caller obligations.

**Architecture:** Execute in staged cleanup tracks. Start with false-confidence deletions and no-op tests, then introduce typed request/path/error contracts before splitting high-blast-radius runtime, search, editing, and watcher modules.

**Tech Stack:** Rust, rmcp, Tantivy, SQLite/rusqlite, tree-sitter, cargo-nextest, Julie MCP tools.

**Architecture Quality:** High architecture impact. The primary issues are shallow or overly wide interfaces: `JulieServerHandler`, `ManageWorkspaceTool`, `SearchIndex`, `JulieWorkspace`, search orchestration, editing tools, and test harnesses expose too much implementation detail to callers and tests.

---

## Evidence Summary

Review inputs:
- Local evidence: `cargo check --all-targets` passed on `f92bead5`; `cargo clippy --all-targets` surfaced 454 `julie-extractors` warnings and 229 `julie` library warnings.
- Size evidence: 53 production/xtask files exceed 500 lines; 24 `src/tests` files exceed 1000 lines.
- Julie evidence: `manage_workspace stats` reported 6,916 files and 190,247 symbols.
- Subagent passes: dead/test-only review, architecture-boundary review, duplicate-policy review, and test-surface review.

High-confidence cleanup signals:
- `src/tracing/mod.rs` is production code used only by tests and contains mock/stub comments.
- `WorkspaceCommand` is defined but unused; live `manage_workspace` uses `operation: String` plus optional fields.
- `clear_cache_for_test` is a production `*_for_test` API with definition-only refs. Follow-up verification found `is_autocommit_for_test` and `loaded_workspace_file_watcher_running_for_test` still have real test callers, so they are not Phase 1 deletion candidates.
- Tests include no-op/stub modules that pass without asserting behavior.
- Search, workspace, editing, and daemon lifecycle code have repeated path/status/error/output policies.

## Architecture Quality

**Affected modules:** `src/handler.rs`, `src/tools/workspace/commands/**`, `src/search/index.rs`, `src/tools/search/**`, `src/workspace/mod.rs`, `src/tools/editing/**`, `src/watcher/**`, `src/daemon/database.rs`, `src/tests/**`.

**Caller-facing interface:** MCP tools, CLI subcommands, workspace registry rows, search results, editing tool results, test fixtures, and daemon/session lifecycle helpers.

**Depth/locality check:** Current high-risk modules are wide and shallow. Callers often know raw workspace status strings, path normalization rules, mutex-backed database/search internals, or tool-specific error classification. This spreads policy across tools and tests.

**Test surface:** Tests should move toward MCP tool calls, CLI calls, and typed helper APIs. Unit tests remain useful for pure policy modules such as path resolution, status parsing, search filtering, and edit preparation.

**Seams/adapters:** New seams are justified only where they reduce repeated caller obligations: typed manage-workspace requests, workspace path resolution, tool error mapping, edit preparation, search test-exclusion policy, and test harness helpers.

**Rejected shortcuts:** Do not split `SearchIndex`, `JulieServerHandler`, or watcher runtime first. They are high-blast-radius and need typed contracts/harness cleanup before large moves. Do not keep no-op tests as placeholders.

**Architecture risk:** high.

## Candidate Plan

### Candidate 1: Delete Or Quarantine `CrossLanguageTracer`

**Files:** `src/tracing/mod.rs`, `src/lib.rs`, `src/tests/core/tracing.rs`, `src/tests/integration/tracing.rs`.

**Current friction:** `CrossLanguageTracer` is production code with only test callers. `trace_data_flow` callers are in tracing tests only. The file contains comments such as “mock flow to make tests pass” and stub methods `find_direct_relationship` / `find_pattern_match`.

**Deletion test:** Removing `pub(crate) mod tracing` should not affect production if tests are migrated or removed.

**Proposed module/interface:** Delete `src/tracing/mod.rs` unless a real caller-facing tracing tool exists. If any behavior is still useful, re-express it through `TraceCallPathTool` / `call_path` with real symbol and relationship fixtures.

**Why this improves locality/leverage:** Removes a false production surface and forces tracing behavior through the tool agents actually use.

**Test surface:** `cargo nextest run --lib tracing`; replacement tests should call `call_path` or the current trace tool through handler/MCP.

**Risk:** medium.

**Recommendation:** first cleanup batch.

### Candidate 2: Remove No-Op And Copied-Pattern Tests

**Files:** `src/tests/tools/smart_read.rs`, `src/tests/regression_prevention_tests.rs`, `src/tests/main_error_handling.rs`, `src/tests/integration/search_regression_tests.rs`.

**Current friction:** `smart_read.rs` contains tests returning `Ok(())` with placeholder comments and no assertions. `regression_prevention_tests.rs` has an empty RED-phase test. `main_error_handling.rs` tests copied `EnvFilter` and `MockDatabase` patterns rather than current production behavior; `update_workspace_statistics` no longer exists in production. `search_regression_tests.rs` has commented ranking tests.

**Deletion test:** Deleting or rewriting these tests should not reduce real behavior coverage because they currently do not execute caller-facing behavior or have stale targets.

**Proposed module/interface:** Replace with behavior tests through `GetSymbolsTool`, `FastSearchTool`, `logging::install_file_tracing`, or remove when no current behavior remains.

**Why this improves locality/leverage:** Test output stops overstating coverage. Future coverage work targets real interfaces.

**Test surface:** Exact tests for the replacement behavior. If removing only, `cargo nextest run --lib regression_prevention_tests smart_read main_error_handling search_regression_tests` should show removed filters rather than passing stubs.

**Risk:** low.

**Recommendation:** first cleanup batch.

### Candidate 3: Prune Dead Test-Only Production APIs

**Files:** `src/workspace/mutation_gate.rs`, `src/database/mod.rs`, `src/handler.rs`.

**Current friction:** Verified definition-only ref:
- `clear_cache_for_test` at `src/workspace/mutation_gate.rs:162`.

Follow-up verification disproved the original broader claim:
- `is_autocommit_for_test` at `src/database/mod.rs:275` is called by `src/tests/daemon/symbol_db_pooled_test.rs`.
- `loaded_workspace_file_watcher_running_for_test` at `src/handler.rs:1291` is called by `src/tests/core/workspace_init.rs`.

**Deletion test:** Remove `clear_cache_for_test` and run `cargo check --all-targets`; no caller should break.

**Proposed module/interface:** Delete definition-only helpers. For used `*_for_test` helpers, consolidate behind test harness modules instead of adding more public production methods.

**Why this improves locality/leverage:** Reduces test-only holes in production interfaces.

**Test surface:** `cargo check --all-targets`, plus exact tests if any replacement helper is added.

**Risk:** low.

**Recommendation:** first cleanup batch.

### Candidate 4: Typed `manage_workspace` Request And Status Contract

**Files:** `src/tools/workspace/commands/mod.rs`, `src/cli_tools/commands.rs`, `src/daemon/database.rs`, `src/tools/workspace/commands/registry/**`.

**Current friction:** `WorkspaceCommand` exists but has only a definition ref. Live API uses `ManageWorkspaceTool { operation: String, ...optional fields... }`. Workspace statuses are raw strings such as `pending`, `indexing`, `ready`, and `error`, with string comparisons in cleanup and registration code.

**Deletion test:** If the typed enum were wired in, invalid argument combinations disappear from command handlers and CLI validation becomes a parser concern.

**Proposed module/interface:** Keep the public MCP JSON shape for compatibility, but parse it into an internal `ManageWorkspaceRequest` enum plus `WorkspaceStatus`. Put `targets_primary()`, required-argument validation, and daemon-only classification on the typed request.

**Why this improves locality/leverage:** Callers stop re-learning which optional fields are valid for which operation. Registry cleanup stops comparing raw status strings.

**Test surface:** JSON deserialization/validation tests and MCP `manage_workspace` calls for invalid op, missing args, register/index/open/refresh/remove/list/clean/health.

**Risk:** high.

**Recommendation:** first structural refactor before broad workspace cleanup.

### Candidate 5: Shared Workspace Path Resolver

**Files:** `src/tools/symbols/primary.rs`, `src/tools/symbols/target_workspace.rs`, `src/tools/refactoring/rename.rs`, `src/tools/workspace/commands/index.rs`.

**Current friction:** Absolute/relative/canonicalized/native/relative-Unix path handling is repeated with slightly different fallback behavior. Some paths fall back to raw input on conversion errors.

**Deletion test:** A shared resolver should remove duplicated path logic from get-symbols primary/target, rename scope, and index command path handling.

**Proposed module/interface:** `WorkspacePathResolver::resolve_file_input(input, root) -> ResolvedWorkspacePath { absolute_native, relative_unix, existed }`. Add explicit outside-workspace and missing-file behavior.

**Why this improves locality/leverage:** Tools stop owning path invariants individually.

**Test surface:** `get_symbols` primary and target absolute/relative paths; outside-workspace rejection; `rename_symbol(scope=...)` acceptance/rejection parity; index command root resolution.

**Risk:** high.

**Recommendation:** first structural refactor, after typed manage-workspace request.

### Candidate 6: Shared Workspace Target Error And Metrics Contract

**Files:** `src/tools/navigation/resolution.rs`, `src/handler/tools/*.rs`, `src/tools/navigation/call_path.rs`, `src/handler.rs`.

**Current friction:** Target workspace failures map differently by tool: wrappers use `internal_error`, substring-based `invalid_params`, success text diagnostics, or metric fallback. This makes behavior hard to reason about and hard to test consistently.

**Deletion test:** Tool wrappers should not need to inspect workspace error strings.

**Proposed module/interface:** Typed `WorkspaceParam` and `ToolErrorKind`, plus one `map_tool_error` adapter for handler wrappers. Metrics binding should receive a typed target resolution result.

**Why this improves locality/leverage:** Error classification and telemetry attribution become one policy.

**Test surface:** Invalid workspace id through `fast_refs`, `deep_dive`, `get_context`, `get_symbols`, `blast_radius`, and `call_path`; target-workspace metrics binding assertions.

**Risk:** high.

**Recommendation:** structural refactor after path resolver.

### Candidate 7: Search Test-Exclusion Policy

**Files:** `src/search/index.rs`, `src/tools/search/execution.rs`, `src/tools/search/line_mode.rs`.

**Current friction:** Lexical search excludes tests by path and role; semantic KNN uses `SearchFilter::matches_symbol_result`, which excludes by path only. Line-mode has separate candidate filtering.

**Deletion test:** One `SearchFilter::matches_test_policy(path, role)` should be used by lexical, semantic, hybrid, and line-mode paths.

**Proposed module/interface:** Centralize test exclusion policy in `SearchFilter` or a small `search::filtering` module.

**Why this improves locality/leverage:** Search backends stop diverging on inline test symbols in production paths.

**Test surface:** `fast_search(backend="semantic", exclude_tests=true)`, hybrid, and lexical fixtures with inline test-role symbols in production paths.

**Risk:** medium-high.

**Recommendation:** search cleanup batch; run dogfood if ranking/scoring shifts.

### Candidate 8: Integrate Line Matches Into Search Execution

**Files:** `src/tools/search/mod.rs`, `src/tools/search/line_mode.rs`, `src/tools/search/execution.rs`.

**Current friction:** Full output enriches unified hits through line-mode and swallows errors. Locations mode runs unified search, then conditionally re-runs line-mode to preserve old content-search behavior. Language/file/test filters exist in both paths.

**Deletion test:** `FastSearchTool::call_tool` should execute one search plan whose result includes optional line matches and explicit degradation metadata.

**Proposed module/interface:** Make line matches part of `SearchExecutionResult`, with one candidate filter and a trace field for line enrichment failure.

**Why this improves locality/leverage:** Removes additive old/new search paths and makes zero-hit/degraded behavior visible.

**Test surface:** `fast_search(return_format="locations")` content queries, path queries, file-pattern queries, language-filtered line results, and zero-hit trace assertions.

**Risk:** high.

**Recommendation:** after Candidate 7.

### Candidate 9: Prepare Editing Work Once

**Files:** `src/tools/editing/edit_file.rs`, `src/tools/editing/rewrite_symbol.rs`, `src/handler/tools/edit_file.rs`, `src/handler/tools/rewrite_symbol.rs`.

**Current friction:** `success_metrics_metadata` resolves paths, reads files, applies edits, and computes diffs; `call_tool` then repeats the same work. `rewrite_symbol` calls `prepare_rewrite` once for metrics and again for the actual call.

**Deletion test:** A request should have exactly one prepared edit/rewrite object per handler invocation.

**Proposed module/interface:** `PreparedEdit` and `PreparedRewrite` produced once by tool wrapper, then consumed by metrics and render/apply logic.

**Why this improves locality/leverage:** Removes duplicate file reads/parses and closes race windows between metrics preparation and commit.

**Test surface:** Dry-run/apply outputs unchanged; failure metrics fields preserved; race test where file changes between prepare and commit.

**Risk:** medium-high.

**Recommendation:** independent cleanup batch.

### Candidate 10: Shared Language Detection Policy

**Files:** `crates/julie-extractors/src/pipeline.rs`, `crates/julie-extractors/src/language_spec/specs.rs`, `src/tools/editing/rewrite_symbol.rs`, `src/tools/workspace/indexing/file_policy.rs`.

**Current friction:** Extractor source-aware detection can classify `.h` as C++; language specs map `.h` to C by default. `rewrite_symbol` uses extension-only detection. Indexing policy adds filename/fallback rules for Dockerfile, Makefile, TOML, JSON, and shell names.

**Deletion test:** Indexing, live rewrite parsing, watcher indexing, and external extraction should ask one policy for language/parser support.

**Proposed module/interface:** `LanguageDetectionPolicy::{Indexing, ParserRequired, SourceAware}` backed by `LanguageSpec`.

**Why this improves locality/leverage:** Language behavior stops drifting across tools.

**Test surface:** Index and rewrite C++ `.h`; Dockerfile/Makefile/Cargo.toml classification through indexing/watcher/external extract; unsupported-parser error messages.

**Risk:** high.

**Recommendation:** separate extractor/language batch.

### Candidate 11: Split Wide Runtime Modules

**Files:** `src/handler.rs`, `src/workspace/mod.rs`, `src/daemon/workspace_pool.rs`, `src/watcher/runtime.rs`, `src/watcher/mod.rs`, `src/daemon/database.rs`, `xtask/src/changed.rs`.

**Current friction:**
- `JulieServerHandler`: 2,565 LOC, 186 symbols, 121 direct refs to the handler symbol.
- `JulieWorkspace`: exposes DB/search/watcher/provider/config runtime fields.
- `SearchIndex`: 2,239 LOC, mixes Tantivy adapter, document shaping, query execution, compatibility marker, locking, and ranking helpers.
- `DaemonDatabase`: 1,336 LOC, mixes migrations, workspace registry, cleanup events, tool calls, search compare, codehealth, and id migration.
- Watcher runtime constructor has many shared-state parameters and mixes scheduling, dedupe, repair, retry, overflow, and gate handling.

**Deletion test:** Each split should move policy out of callers, not just move code between files.

**Proposed module/interface:**
- `ToolRuntime` facets: workspace resolver, store/search access, metrics, embeddings, spillover.
- `WorkspaceLayout`, `WorkspaceRuntime`, `WorkspaceHealthReporter`.
- `daemon/database/{migrations,workspaces,tool_calls,search_compare,codehealth}` behind `DaemonDatabase`.
- `WatcherContext`, `QueueScheduler`, `RepairCoordinator`, `ProjectionRetryQueue`.
- `changed::{collect,select,render}` plus declarative route rules.

**Why this improves locality/leverage:** These splits are valuable after typed contracts exist because callers can depend on narrower capabilities instead of raw runtime objects.

**Test surface:** Existing subsystem buckets: daemon DB, workspace init, watcher integration, xtask changed tests, and affected MCP tool tests.

**Risk:** high.

**Recommendation:** later structural batches, not first.

### Candidate 12: Test Harness And Fixture Cleanup

**Files:** `src/tests/**`, `src/tests/fixtures/**`, `fixtures/databases/julie-snapshot`.

**Current friction:** 24 test files exceed 1000 lines. MCP session helpers, temp workspace setup, raw `Symbol` literals, SQL row inserts, and text extraction repeat across daemon/search/workspace tests. `fixtures/databases/julie-snapshot/symbols.db` is about 96MB and includes generated artifacts.

**Deletion test:** Adding harness helpers should shrink repeated setup without making behavior harder to see.

**Proposed module/interface:**
- `src/tests/harness/mcp_session.rs`: session creation, initialized notification, roots request/response, tool call, text extraction.
- `src/tests/harness/db_fixture.rs`: `FixtureDb`, `FileBuilder`, `SymbolBuilder`, `RelationshipBuilder`, `IdentifierBuilder`.
- Split large tests by behavior only after helpers land.
- Convert ignored tests into deterministic exact tests or explicit manual diagnostics outside normal discovery.

**Why this improves locality/leverage:** Tests become shorter and assert caller-facing behavior instead of private state.

**Test surface:** Harness self-tests plus migrated exact tests; lead runs `cargo xtask test changed` after each coherent migration batch.

**Risk:** medium.

**Recommendation:** parallel with first cleanup batches, but do not migrate huge files before the helpers exist.

## Execution Order

1. **False-confidence cleanup:** Candidates 1, 2, and 3. Remove prototype/stub surfaces and no-op tests first so later coverage signals mean something.
2. **Workspace contracts:** Candidates 4, 5, and 6. Typed requests, status, paths, and errors reduce duplicated policy before wider workspace refactors.
3. **Focused behavior cleanup:** Candidates 7, 8, 9, and 10. Search policy, line-mode integration, editing prepare-once, and language detection are independent enough for separate workers.
4. **Harness cleanup:** Candidate 12. Add harness helpers early, then migrate large tests in behavior slices.
5. **Wide module splits:** Candidate 11. Split handler/workspace/search/watcher/daemon DB only after contracts and harnesses reduce the blast radius.

## Execution Progress

Completed on branch `cleanup/codebase-cleanup-2026-05-24`:

- **Candidate 1:** deleted prototype `CrossLanguageTracer` production module and its tests.
- **Candidate 2:** deleted stale/decorative test modules and removed their xtask routes.
- **Candidate 3:** removed only verified-unused `clear_cache_for_test`; retained the two helpers that still have real test callers.
- **Candidate 4:** replaced dead `WorkspaceCommand` with typed internal `ManageWorkspaceOperation` / `ManageWorkspaceRequest`, keeping public MCP JSON flat.
- **Candidate 5:** completed the safe first slice: shared tool file-input resolver for `get_symbols` primary/target and rename scope normalization. Workspace command root resolution remains intentionally separate.
- **Candidate 6:** completed the safe first slice: typed resolver-created workspace failures and `deep_dive` type-based invalid-params classification. Broad wrapper normalization and metrics attribution remain separate.
- **Candidate 7:** unified symbol-result test exclusion for semantic/hybrid KNN by honoring path OR metadata role `test`; line-mode behavior remains intentionally unchanged.
- **Candidate 8:** completed the first trace slice: locations-only content queries now replace unified symbol/file execution hits with line hits, refresh trace top hits/result counts, and preserve current locations text/fallback behavior. Lead review fixed exposed line-hit language metadata for non-Rust files.
- **Candidate 9:** prepared edit/rewrite work once per handler invocation via `PreparedEdit` / `PreparedRewrite`, with metrics and apply/render consuming the same prepared object. Lead review removed obsolete dead wrapper methods.
- **Candidate 10:** completed the first language slice: public source-aware extractor language detection, app-level content-aware indexing wrapper, and content-aware detection in rewrite, indexing-core, and watcher paths. Resolver same-language scoring remains path-only and is still a separate slice.
- **Candidate 12:** completed the first safe helper slice: shared MCP `CallToolResult` text extraction helper, migrated `get_symbols.rs`, `get_symbols_smart_read.rs`, and `search_context_lines.rs`.

Post-review fixes completed:

- **Candidate 8:** line-mode locations now use indexed language metadata instead of re-checking file extensions, so source-aware C++ `.h` headers survive `language="cpp"` filtering and still return content line hits.
- **Candidate 9:** public MCP `edit_file` validation now rejects empty `old_text` before file I/O on both direct and handler paths. Failed apply metrics now force `applied=false` for `edit_file` and `rewrite_symbol`.
- **Candidate 9:** `EditingTransaction` now serializes same-process writes by normalized target path. `commit_if_unchanged` re-checks after any previous Julie writer commits, and refactoring callers with original content now use checked commits instead of blind last-writer-wins writes.
- **Candidate 10:** `.h` source-aware detection no longer treats C identifiers named like C++ keywords as C++ syntax. The decision compares C and C++ parse diagnostics and keeps `.h` as C unless C++ parses more cleanly.
- **Candidate 10:** `rename_symbol` now uses source-aware language detection for live AST rewrites, fixing C++ `.h` parse refusal.
- **Candidate 10:** external extraction now has end-to-end C++ `.h` coverage when discovery groups a header as C but content proves C++.
- **Search API cleanup:** legacy `SearchIndex::search_content`, `SearchIndex::search_files`, `SearchDocument::symbol_from_parts`, `SearchDocument::file_from_parts`, and their content/file wrapper result types are now test-only where verified. The one non-test `xtask` caller was moved to `SearchDocument::for_file`.
- **Claude CLI verification follow-up:** a fresh Claude review approved the plan/fix slice and found only low-severity residuals. The real residual was closed by making `MultiFileTransaction` acquire the same per-path edit locks as `EditingTransaction`. The parser-init fallback is now logged before falling back to the conservative `::` token heuristic. The prepare-failure `applied=false` concern was already covered by base metadata and was pinned with handler assertions.
- **Candidate 8:** full-output and locations line enrichment now record `applied`, `no_matches`, or `failed` trace status, line match counts, no-match diagnostics, and failure text in handler telemetry. The previous silent fallback on line-mode errors is now visible in `SearchTrace`.
- **Candidate 8 review follow-up:** fresh Claude review approved the observability slice with low residuals. Follow-up fixed the actionable items: documented raw line-match count semantics, made snippet scope-rescue non-propagation explicit, and made failed enrichment clear stale companion fields with a focused trace test.
- **Candidate 10:** resolver same-language scoring now uses stored file-language metadata for caller files, falling back to extension detection only when metadata is absent. This closes the remaining source-aware `.h` gap where C++ headers were still treated as C during relationship resolution. Fresh Claude review approved the slice; follow-up documented the file-row-before-resolution dependency and pinned missing-row extension fallback with a test.
- **Candidate 12:** direct MCP session tests now share JSON-line helpers for sending responses, reading server messages, and answering `ListRootsRequest`. The slice removes duplicated roots-response blocks from `get_symbols`, `fast_search`, and workspace health/list tests, and folds touched local text-extraction duplicates into `tests::helpers::mcp::call_tool_result_text`.
- **Candidate 12 review follow-up:** fresh Claude review found no wire-format, deadlock, lifetime, or JSON-RPC regressions. The actionable cleanup residual was inconsistent text-extraction consolidation in touched files; `line_mode.rs` and `global_targeting.rs` now alias the shared helper instead of keeping local duplicate implementations.
- **Candidate 12:** added shared test DB row builders for `FileInfo`, `Symbol`, `Relationship`, and `Identifier` under `src/tests/helpers/db/`, split below the 500-line new-file limit, and migrated the first row-literal clusters in `external_extract::operations` and `blast_radius_determinism_tests`.
- **Candidate 12 review follow-up:** fresh Claude review first caught that untracked helper files were missing from the review diff. After intent-to-add exposed the helper source, the review approved the slice and verified migrated defaults preserve prior row semantics. A speculative-setter cleanup trimmed unused builder methods before the final dev fallback.
- **Candidate 11:** completed the first safe runtime split by moving daemon DB schema migrations from `src/daemon/database.rs` to `src/daemon/database/migrations.rs`. `DaemonDatabase::open` remains the only caller, and public daemon DB APIs stayed unchanged.
- **Candidate 12:** migrated the dashboard intelligence `FileInfo` fixture helper to the shared DB row builder, adding an explicit `symbol_count` setter to preserve the prior row shape. Raw SQL analytics edge cases stayed local and visible.
- **Candidate 11/12 review follow-up:** fresh Claude review approved the combined daemon migration split and dashboard helper slice with no findings after verifying migration parity and explicit dashboard fixture overrides.
- **Candidate 11:** split daemon DB search-compare persistence into `src/daemon/database/search_compare.rs`, moving the four search-compare methods and row/input types behind stable `database` module re-exports.
- **Candidate 11 review follow-up:** fresh Claude review approved the search-compare split with no findings, verifying SQL parity, transactional behavior, type re-exports, and private module access.
- **Candidate 12:** extended `SymbolBuilder` with parent ID, code context, and annotations setters, then migrated `early_warning_report_tests` file/symbol fixtures to shared DB builders while preserving metadata, parent, annotation, span, byte, and content fields.
- **Candidate 12 review follow-up:** fresh Claude review approved the early-warning fixture migration with no findings, verifying builder defaults and fixture field parity.
- **Candidate 11:** split daemon DB tool-call persistence into `src/daemon/database/tool_calls.rs`, moving insert/history/success-rate/prune/search-analysis methods and `SearchToolCallRow` behind a stable `database` module re-export.
- **Candidate 11 review follow-up:** fresh Claude review approved the tool-call split with no findings, verifying SQL parity, timestamp/cutoff behavior, re-export stability, and private module access.
- **Candidate 11:** split daemon DB codehealth snapshot persistence into `src/daemon/database/codehealth.rs`, moving snapshot insert/read/history/materialization methods and `CodehealthSnapshot` row types behind stable `database` module re-exports. `migrate_workspace_ids` remains in the root daemon DB module because it still coordinates multiple tables.
- **Candidate 11 review follow-up:** fresh Claude review approved the codehealth split with no findings, verifying `now_unix`/private field visibility, stable re-export paths, SQL parity, lock-ordering docs, and the decision to leave workspace-ID migration in the root module.
- **Candidate 12:** migrated `test_quality_tests` pipeline setup clusters to shared DB row builders and storage APIs for metadata update, identifier evidence, empty evidence config fallback, no-match fallback, and no-substring matching tests. The slice preserves behavior through `compute_test_quality_metrics`; builder-backed symbols now use canonical `Symbol` storage defaults instead of raw SQL omitted nullable span columns.
- **Candidate 12 review follow-up:** fresh Claude review approved the test-quality fixture migration with no findings, verifying the span/default deltas are inert for `compute_test_quality_metrics`, identifier containment survives `bulk_store_identifiers`, and scope stayed inside the selected setup clusters.
- **Candidate 12:** migrated the first `test_linkage_tests` setup cluster to shared DB row builders and storage APIs for relationship-based linkage and identifier-only linkage. Later linkage edge-case raw SQL remains local. Lead review removed decorative `reference_score` updates after verifying `compute_test_linkage` does not read that column.
- **Candidate 12 review follow-up:** fresh Claude review approved the first linkage fixture migration with no findings, verifying `reference_score` removal is safe, builder defaults match the raw rows for linkage behavior, and later raw SQL edge cases remain untouched.
- **Candidate 11:** split daemon DB workspace registry storage into `src/daemon/database/workspaces.rs`, moving workspace CRUD/stat/session/cleanup-event methods and workspace row types behind stable `database` module re-exports. `normalize_workspace_paths` and `migrate_workspace_ids` remain in the root daemon DB module because they are startup/migration repair paths.
- **Candidate 11 review follow-up:** fresh Claude review approved the workspace registry split with no findings, verifying re-export stability, private parent access from the submodule, method/SQL parity, and the decision to keep startup/migration repair in the root module.
- **Candidate 12:** migrated the remaining ordinary `test_quality_tests` pipeline setup clusters for no-body, metadata preservation, and fixture-not-applicable cases to shared DB row builders and storage APIs. Lead cleanup removed redundant builder default setters from adjacent migrated tests; persisted metadata assertions remain raw SQL by design.
- **Candidate 12 review follow-up:** fresh Claude review approved the remaining test-quality fixture migration with no findings. The first schema run exhausted turns and the second semantic review used malformed keys, so the final strict no-tool rerun produced schema-valid approval.
- **Candidate 12:** migrated the next `test_linkage_tests` edge-case setup cluster for uncovered production symbols and test-to-test relationship exclusion to shared DB row builders and storage APIs. Symbol confidence is set explicitly where raw SQL previously relied on the schema default; relationship default setters were trimmed after lead review.
- **Candidate 12 review follow-up:** fresh Claude review approved the linkage edge-case fixture migration with no actionable findings. A semantic review first returned informational notes in the findings array, so the final strict rerun recorded schema-valid approval.
- **Candidate 12:** migrated the `test_linkage_tests` linked-tests cap setup loop to shared DB row builders and storage APIs. New symbols preserve the prior schema-default confidence, and relationships rely on builder defaults for calls/confidence while keeping file path and line numbers explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the linkage cap fixture migration with no findings. The first review returned only a partial JSON object, so the final strict rerun recorded schema-valid approval.
- **Candidate 12:** migrated the `test_linkage_tests` class-name similarity fallback setup cluster to shared DB row builders and storage APIs. The slice preserves method/csharp/visibility/test metadata/identifier targeting shape; decorative `reference_score` values were not carried over because `compute_test_linkage` does not read them for this path.
- **Candidate 12 review follow-up:** fresh Claude review approved the class-name similarity fixture migration with no findings. The first review returned only a partial JSON object, so the final strict rerun recorded schema-valid approval.
- **Candidate 12:** migrated the `test_linkage_tests` language-scoped ambiguity guard setup cluster to shared DB row builders and storage APIs. The slice preserves rust/python language partitioning, test metadata, visibility, identifier shape, and symbol confidence while leaving output assertion SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the ambiguity-guard fixture migration with no findings. The first review returned only a partial JSON object, so the final strict rerun recorded schema-valid approval.
- **Candidate 12:** migrated the `test_linkage_tests` class-inherits-method-linkage setup cluster to shared DB row builders and storage APIs. The slice preserves class/method parentage, csharp visibility, test metadata, and relationship shape while leaving output assertion SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the parent-inheritance fixture migration with no findings. The first review exhausted the turn cap, so the final strict rerun recorded schema-valid approval.
- **Candidate 12:** migrated the `test_linkage_tests` deduplication-across-strategies identifier fixture from raw SQL to the shared identifier builder and storage API. The slice preserves the duplicate `test_1` to `prod_1` identifier edge and leaves output assertions explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the deduplication fixture migration with no findings, verifying builder row semantics, `bulk_store_identifiers` usage, plan wording, and verification evidence.
- **Candidate 12:** split the late linkage edge cases into `src/tests/analysis/linkage_edge_cases_tests.rs` before larger fixture migrations. `test_linkage_tests.rs` is now 584 lines and the new edge-case file is 407 lines, keeping both below the 1000-line test-file cap.
- **Candidate 12 review follow-up:** fresh Claude review approved the linkage edge-case file split with no findings, verifying test discovery, moved-body parity, git visibility, line counts, and remaining raw SQL plan accuracy.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` tied-score fallback setup cluster to shared symbol/identifier builders and storage APIs. The slice preserves C# method symbols, test metadata, identifier fallback targeting, and deterministic output assertions while leaving the metadata reset SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the tied-score fallback fixture migration with no findings, verifying row semantics, identifier NULL-target fallback, plan accuracy, and verification evidence.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` stale-cleanup setup cluster to shared symbol/relationship builders and storage APIs. The slice preserves parent/child/test relationships and leaves the stale-linkage `DELETE FROM relationships` behavior SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the stale-cleanup fixture migration with no findings, verifying parent/child/test row semantics, relationship shape, and ledger accuracy.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` scorable-filter setup cluster to shared symbol/relationship builders and storage APIs. The slice preserves test-case, fixture-setup, and legacy-test metadata roles while leaving caller-facing assertions unchanged.
- **Candidate 12 review follow-up:** fresh Claude review approved the scorable-filter fixture migration with no findings, verifying metadata roles, relationship file/line shape, ledger accuracy, and remaining raw SQL list.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` direct best-confidence setup cluster to shared symbol/relationship builders and storage APIs. The slice preserves test-quality confidence metadata, production/test visibility, and relationship file/line shape while leaving output assertion SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the direct best-confidence fixture migration with no findings, verifying row shape, metadata preservation, intentional `reference_score` removal, plan wording, and verification evidence.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` parent-aggregation setup cluster to shared symbol/relationship builders and storage APIs. The slice preserves C# class/method parentage, child test-quality confidence metadata, and relationship file/line shape while leaving parent linkage assertion SQL explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the parent-aggregation fixture migration with no findings, verifying C# parent/child shape, metadata preservation, intentional `reference_score` removal, and plan/ledger accuracy.
- **Candidate 12:** migrated the `linkage_edge_cases_tests` default-confidence setup cluster to shared symbol/relationship builders and storage APIs. The slice preserves missing confidence inside `test_quality` metadata so linkage still falls back to the default confidence value.
- **Candidate 12 review follow-up:** fresh Claude review approved the default-confidence fixture migration with no findings, verifying omitted nested confidence, explicit symbol confidence preservation, plan wording, and verification evidence.
- **Candidate 12:** added `set_symbol_reference_scores` to the shared DB test helpers and migrated `change_risk_tests` setup to symbol builders plus explicit reference-score updates. The helper rejects missing symbol IDs, preserving the centrality signal that `compute_change_risk_scores` reads.
- **Candidate 12 review follow-up:** fresh Claude review approved the reference-score helper and change-risk fixture migration with no findings, verifying the missing-ID guard, centrality preservation, migrated row shape, and ledger accuracy.
- **Candidate 12:** migrated the `get_context_task_inputs` identifier-caller fixture from raw SQL to `identifier_builder` plus `bulk_store_identifiers`, preserving caller/target linkage, byte span, columns, and confidence.
- **Candidate 12 review follow-up:** fresh Claude review approved the get-context task-input identifier fixture migration with no findings, verifying row-shape preservation, plan wording, and verification evidence.
- **Candidate 12:** migrated the remaining `get_context_graph_expansion_tests` identifier fixtures from raw SQL to `identifier_builder` plus `bulk_store_identifiers`. The slice preserves TypeScript type-usage rows, TypeScript call-target rows, Rust call/member-access filtering rows, and SQL-wildcard regression names/spans.
- **Candidate 12 review follow-up:** fresh Claude review approved the graph-expansion identifier fixture migration with no findings, verifying builder defaults, per-cluster row semantics, wildcard regression preservation, `valid_symbol_ids=None` behavior, and ledger accuracy.
- **Candidate 12:** migrated the `deep_dive_regression_tests` qualified-method identifier helper from raw SQL to `identifier_builder` plus `bulk_store_identifiers`, preserving the call kind, Rust language default, fixed span/bytes, containing symbol IDs, and confidence for the qualified and noisy caller rows.
- **Candidate 12 review follow-up:** fresh Claude review approved the deep-dive regression identifier helper migration with no findings, verifying row-shape parity, `IdentifierKind::Call` serialization, optional containing-symbol preservation, call-site coverage, and ledger accuracy.
- **Candidate 12:** migrated the `deep_dive_tests` identifier fallback helper from raw SQL to `identifier_builder` plus `bulk_store_identifiers`, and typed its seven call-site clusters with `IdentifierKind::Call`. The slice preserves fixed spans/bytes, optional containing symbols, Rust language default, confidence, and no target/code-context values.
- **Candidate 12 review follow-up:** fresh Claude review approved the deep-dive helper migration with no findings, verifying row-shape parity, benign `INSERT OR REPLACE` behavior for unique IDs, `valid_symbol_ids=None` containment preservation, ten call-site conversions across seven tests, and ledger accuracy.
- **Candidate 12:** migrated `test_get_identifiers_by_names_kinds_excluding_containers_filters_rows` from raw symbol/identifier inserts to shared builders. This core query fixture uses only supported function/call/type-usage/member-access rows; import-kind raw SQL remains elsewhere because `IdentifierKind` has no import variant.
- **Candidate 12 review follow-up:** fresh Claude review approved the core identifier query fixture migration with no findings, verifying symbol and identifier row parity, `valid_symbol_ids=None` reference preservation, the no-import-variant note, and ledger scope accuracy.
- **Candidate 12 re-review:** fresh Claude re-reviewed remaining raw SQL inventory and found no `src/tests/analysis` raw setup inserts left. It classified `database_row_mapping.rs` malformed row fixtures, target-workspace `import` identifier fixtures, and schema-default tests as intentional raw SQL, then identified `vector_storage.rs`, `workspace/index_embedding_tests.rs`, `metrics/query_tests.rs`, selected `database.rs` reference-score loops, and `hybrid_search_tests.rs` as the next migration candidates.
- **Candidate 12:** migrated `vector_storage.rs` test symbol setup to shared file/symbol builders. Lead debugging preserved the old `INSERT OR IGNORE` file behavior by checking `get_file_hash` before `store_file_info`, because `store_file_info` uses `INSERT OR REPLACE` and would otherwise cascade-delete earlier same-file symbols.
- **Candidate 12 review follow-up:** fresh Claude review approved the vector storage fixture migration with no findings, verifying the file-row cascade-delete root cause, the `get_file_hash` guard, benign builder default deltas for vector tests, ledger accuracy, and the `hybrid_search_tests` doc-comment caveat.
- **Candidate 12:** migrated `workspace/index_embedding_tests.rs` file/symbol setup to shared builders, preserving the old `INSERT OR IGNORE` file behavior in the shared helper with a `get_file_hash` guard and explicitly restoring `reference_score = 1.0` through `set_symbol_reference_scores`.
- **Candidate 12 review follow-up:** fresh Claude review approved the workspace embedding fixture migration with no findings, verifying reference-score restoration, duplicate-file semantics, FK order, builder row mapping, and plan-note scope.
- **Candidate 12:** migrated `metrics/query_tests.rs` file/symbol setup to shared builders, preserving centrality ordering by restoring `reference_score`, preserving raw-schema confidence default with `.confidence(1.0)`, and keeping metadata as object maps.
- **Candidate 12 review follow-up:** fresh Claude review approved the metrics query fixture migration with no findings, verifying file row shape, symbol fields, metadata object shape, explicit confidence, reference-score restoration, centrality fallback coverage, and plan evidence.
- **Candidate 12:** migrated the two `database.rs` `get_reference_scores` score-read fixtures to shared symbol builders plus explicit `set_symbol_reference_scores`. Schema-default and compute-score raw SQL remains untouched because those tests intentionally exercise database defaults or score computation inputs.
- **Candidate 12 database per-cluster review:** read-only subagent review classified additional relationship-only, constructor, type-usage, and path-sensitive centrality fixtures in `database.rs` as builder-safe when migrated in small exact-test batches. It also re-confirmed `test_reference_score_defaults_to_zero` and raw `kind='import'` identifier rows must stay raw because builders would change the surface under test.
- **Candidate 12 review follow-up:** fresh Claude review approved the database get-reference-score fixture migration with no findings.
- **Candidate 12:** migrated the first two `database.rs` relationship-only reference-score computation fixtures to shared symbol/relationship builders. The slice preserves weighted `calls/imports/uses` scoring and self-reference exclusion, using `line_number(0)` to match the raw relationship schema default.
- **Candidate 12 review follow-up:** fresh Claude review approved the relationship-only database fixture migration with no findings.
- **Candidate 12:** migrated the `database.rs` outgoing-only reference-score computation fixture to shared symbol/relationship builders. The slice preserves sender zero-score and receiver call-weight behavior with the same stored function rows and a `calls` relationship.
- **Candidate 12 review follow-up:** fresh Claude review approved the outgoing-only database fixture migration with no findings.
- **Candidate 12:** migrated the `database.rs` interface/base-class propagation fixtures to shared symbol/relationship builders, preserving C# class/interface kinds and implements/extends/calls/uses relationship shapes.
- **Candidate 12 review follow-up:** fresh Claude review approved the interface/base propagation fixture migration with no findings.
- **Candidate 12:** migrated the `database.rs` constructor propagation fixture to shared builders, preserving public visibility, constructor parent_id, C# file paths/spans/bytes, and instantiates/uses relationships.
- **Candidate 12 review follow-up:** fresh Claude review approved the constructor propagation fixture migration with no findings.
- **Candidate 12:** migrated the `database.rs` type-usage Step 1b centrality fixtures to shared file/symbol/identifier builders. The raw import-identifier fixture remains raw because `IdentifierKind` has no `Import` variant; a read-only subagent and local `compute_reference_scores` inspection verified identifier byte columns are inert for these type-usage scoring paths.
- **Candidate 12 review follow-up:** fresh Claude review approved the type-usage Step 1b fixture migration with no actionable findings, verifying row-shape parity, inert identifier byte defaults, the raw import-kind exception, and ledger accuracy.
- **Candidate 12:** migrated the `database.rs` path-sensitive reference-score fixtures for test-file deweighting and C header-to-implementation propagation to shared file/symbol/relationship builders, preserving test-path inputs, C header/function row shapes, relationship weights, and centrality assertions.
- **Candidate 12 review follow-up:** fresh Claude review approved the path-sensitive reference-score fixture migration with no actionable findings, verifying file paths, spans/bytes, relationship kinds, line-number inertness, and ledger accuracy.
- **Candidate 12:** migrated `test_delete_embeddings_for_symbol_ids_only_removes_requested_rows` in `database.rs` to shared file/symbol builders, preserving the embedding deletion target rows while keeping file-row count defaults explicit.
- **Candidate 12 review follow-up:** fresh Claude review approved the embedding-deletion fixture migration with no actionable findings, verifying file row fields, symbol row spans/bytes/kinds, embedding row behavior, and ledger accuracy.
- **Candidate 12:** added `store_file_info_if_missing` and `SymbolBuilder::doc_comment` to the shared DB test helpers, then migrated `hybrid_search_tests.rs` file/symbol setup to shared builders while preserving doc comments, signatures, metadata roles, spans, bytes, and duplicate-file `INSERT OR IGNORE` behavior. The same file-row helper replaced the copied guard in `vector_storage.rs` and `workspace/index_embedding_tests.rs`.
- **Candidate 12 review follow-up:** fresh Claude review approved the hybrid fixture migration with no actionable findings. Its only note was possible confidence drift; local schema verification showed raw inserts omitted `confidence` into `DEFAULT 1.0`, so the explicit builder `.confidence(1.0)` calls preserve parity.
- **Candidate 12:** migrated the remaining analysis raw file-row helpers in `change_risk_tests.rs`, `test_linkage_tests.rs`, and `linkage_edge_cases_tests.rs` to `store_file_info_if_missing` plus `file_info_builder`. Also migrated `database_row_mapping.rs` valid FK setup rows to shared file/symbol builders while keeping malformed kind/visibility rows raw.
- **Candidate 12:** split oversized `hybrid_search_tests.rs` into behavior-focused modules under `src/tests/tools/hybrid_search_tests/`: reciprocal-rank fusion merge tests, hybrid orchestration/provider degradation tests, weight-profile wiring tests, and KNN conversion tests. Natural-language query detection tests moved to `query_classification_tests.rs`. The adjacent xtask changed-file mapper now routes the new split module paths to `tools-search-hybrid` instead of falling back to the full dev tier.
- **Candidate 12:** split oversized `search/line_mode.rs` into behavior-focused modules under `src/tests/tools/search/line_mode/`: basic/default search, missing-index readiness messages, primary rebind/cache/swap behavior, and filter behavior. The split removes the unused local `extract_workspace_id` helper and keeps the existing `line_` changed-file routing narrow.
- **Candidate 12:** split oversized `search/tantivy_index_tests.rs` into behavior-focused modules under `src/tests/tools/search/tantivy_index_tests/`: lifecycle/basic operations, ranking/tokenization, relaxed fallback, schema compatibility, tokenizer compatibility, and writer lifecycle. The split preserves the ignored tokenizer-mismatch bug-documentation test and keeps existing `tantivy_` changed-file routing narrow.
- **Candidate 12:** split oversized `search/tantivy_path_prior_tests.rs` into behavior-focused modules under `src/tests/tools/search/tantivy_path_prior_tests/`: query-shape gating, language layouts, path classifiers, scoring invariants, language affinity, and test-intent override behavior. The split preserves all 34 tests, avoids an ignored `test_*` child filename by using `intent.rs`, and keeps existing `tantivy_` changed-file routing narrow.
- **Candidate 12 review follow-up:** fresh Claude review approved the path-prior split with no findings, verifying 34-test preservation, no duplicate or missing `#[test]` functions, child module discovery, `intent.rs` git visibility, `tantivy_` prefix routing, and plan/ledger accuracy.
- **Candidate 11/13:** extracted inline watcher filtering tests from `src/watcher/filtering.rs` into `src/tests/integration/watcher_filtering.rs`, reducing the production file from 836 lines to 240 and satisfying the project rule that tests live under `src/tests/`. The slice also removed the verified-stale commented `test_git_context` module line from `src/tests/mod.rs`. Lead debugging updated `core-fast`, changed-file routing, and the checked manifest contract after the first changed gate exposed stale runner filters. Fresh Claude review approved with no findings.
- **Candidate 13:** removed the verified small dead-code cluster: dead `line_mode_search`, the now-unreachable line-output formatter/module/test and `LineModeSearchResult::workspace_label`, unused `KNOWN_CODE_EXTENSIONS` plus re-export, stale daemon `#[allow(dead_code)]` attributes, and stale `is_source_language` wiring. The line-output deletion followed compile warning evidence after removing the wrapper; live line-mode callers use `line_mode_matches` plus current `format_content_locations_only`/snippet enrichment paths.
- **Candidate 13 review follow-up:** fresh Claude review approved the dead-code slice with no findings, verifying the removed symbols have no production callers, deleted formatter coverage was private unreachable behavior, daemon helpers/fields are live, and the `is_source_language` helper wiring is byte-for-byte behavior-preserving.
- **Candidate 13:** removed extractor placeholder tests that had no assertions or executable behavior: Python assignment/import/type/identifier/decorator/relationship placeholders and the empty standalone Python assignments test module. Replaced Ruby extractor creation's `assert!(true)` with a real constructor assertion against `base().file_path`.
- **Candidate 13:** removed `JulieServerHandler::workspace_root`, the stale construction-time root field that was only asserted by tests. Tests now assert the session current root and startup hint directly, matching the production source of truth.
- **Candidate 13:** removed `JulieServerHandler::primary_database_and_search_index` after migrating the remaining stale-index tests to `primary_pooled_database` and `primary_pooled_database_and_search_index`.
- **Candidate 12:** split the `test_quality_tests` integration/body edge clusters into `quality_pipeline_tests.rs` and `quality_body_analysis_tests.rs`. The original `test_quality_tests.rs` dropped from 1416 to 981 lines, under the test-file cap, while the new files are 257 and 89 lines. The new filenames avoid the repo's ignored `test_*.rs` pattern.
- **Candidate 12:** split oversized `deep_dive_tests.rs` along its existing deserialization, formatting, and data-test groupings into a 14-line root module plus behavior-focused child modules under `src/tests/tools/deep_dive_tests/`. Every split test file is now under the 1000-line test cap; largest child is `data_tests/identifiers_query_similarity.rs` at 850 lines. The adjacent changed-file mapper routes split deep-dive test paths to `tools-deep-dive` so the slice stays off the dev fallback path.
- **Candidate 12 review follow-up:** read-only subagent audit and fresh Claude review both approved the deep-dive split with no findings. They verified 65 original test functions preserved exactly once, valid parent/private helper visibility, git-visible child modules, narrow `tools-deep-dive` changed-file routing, and plan/ledger line-count accuracy.
- **Candidate 12:** split oversized `blast_radius_determinism_tests.rs` into a 247-line root helper/module file plus behavior-focused child modules under `src/tests/tools/blast_radius_determinism_tests/`: seed resolution, relationship walk/ranking, walk/budget semantics, and rendering/output integration. All 11 test functions remain covered, every file is under the 1000-line test cap, and existing `src/tests/tools/blast_radius...` changed-file routing keeps the slice in `tools-blast-spillover` without an xtask mapper change.
- **Candidate 12:** split the stateful primary-rebind/swap-window and prepared-rewrite cluster out of `rewrite_symbol_tests.rs` into `src/tests/tools/editing/rewrite_symbol_tests/stateful.rs`. The root file is now 838 lines, the child module is 350 lines, all 27 test functions remain covered, and existing `src/tests/tools/editing/` changed-file routing keeps the slice in `tools-editing` without an xtask mapper change.
- **Candidate 12:** split oversized `embedding_metadata_enrichment.rs` into a 59-line root helper/module file plus behavior-focused child modules under `src/tests/core/embedding_metadata_enrichment/tests/`: documentation excerpt normalization, variable budgeting/default scoring, test-symbol filtering, and batch enrichment. All 42 test functions remain covered, every file is under the 1000-line test cap, child filenames avoid the repo's ignored `test_*.rs` pattern, and existing `src/tests/core/embedding*` changed-file routing keeps the slice in `core-embeddings` without an xtask mapper change.
- **Candidate 12 review follow-up:** fresh Claude review approved the embedding metadata enrichment split with no required findings. It verified 42 tests preserved exactly once, inline `mod tests` child path resolution, parent/private helper visibility through `use super::*`, git-visible child filenames, `core-embeddings` changed-file routing, and plan/ledger accuracy. Its only non-blocking note was that the budget-selection test-symbol exclusion case belongs defensibly in `symbol_filtering.rs` because the behavior under test is exclusion.
- **Candidate 12:** split oversized `tools/workspace/discovery.rs` into a 53-line root helper/module file plus behavior-focused child modules under `src/tests/tools/workspace/discovery/`: vendor-pattern detection, `.julieignore` generation, indexable-file discovery, blacklist/file filtering, and daemon-home exclusion. All 40 test functions remain covered, every file is under the 1000-line test cap, child filenames avoid the repo's ignored `test_*.rs` pattern, and existing `src/tests/tools/workspace/` changed-file routing keeps the slice in `tools-workspace` without an xtask mapper change.
- **Candidate 12:** split oversized `tools/workspace/mod_tests/part5.rs` into flat include siblings `part5.rs`, `part6.rs`, and `part7.rs`. This preserves the existing include-series helper visibility and test paths while bringing the files to 376, 393, and 385 lines; existing `src/tests/tools/workspace/` changed-file routing keeps the slice in `tools-workspace` without an xtask mapper change.
- **Candidate 12:** split oversized `daemon/roots.rs` into behavior-focused child modules under `src/tests/daemon/roots/`: initial binding, roots-list-changed refresh, secondary-target handling, startup deferral, and deferred-auto-index/sensitive-cwd guards. The parent module now keeps only shared roots helpers and module wiring, all 30 test attributes remain present, and existing `src/tests/daemon/` changed-file routing keeps the slice in the daemon bucket.
- **Candidate 12 review follow-up:** fresh Claude review approved the daemon roots split with no findings, verifying 30 tests preserved exactly once, parent/private helper visibility through `use super::*`, git-visible child modules, file-size caps, daemon changed-file routing, and updated plan/ledger accuracy.
- **Candidate 12:** migrated ordinary raw dashboard intelligence symbol and relationship setup rows for `get_top_symbols_by_centrality`, `get_file_hotspots`, and `get_aggregate_stats` to shared DB builders/storage APIs. All 25 dashboard intelligence tests passed; the two remaining raw file inserts intentionally exercise NULL `line_count` and empty-language aggregate behavior.
- **Candidate 12:** split oversized `tools/workspace/global_targeting.rs` into a 153-line parent helper/module file plus behavior-focused child modules under `src/tests/tools/workspace/global_targeting/`: target activation, list/stats, deferred explicit targets, global remove, resolution failures, open lifecycle, rebind/index behavior, deferred sessions, and primary swap guards. All 40 test functions remain covered, every file is under the 1000-line test cap, child filenames avoid the repo's ignored `test_*.rs` pattern, and `xtask` changed-file routing now maps the split child paths to `tools-workspace-targeting` instead of the broad workspace bucket.
- **Candidate 12:** split oversized `tools/target_workspace_fast_refs_tests.rs` inside its existing private `tests` module into behavior-focused child modules under `src/tests/tools/target_workspace_fast_refs_tests/tests/`: limit/exact-definition behavior, kind filtering, identifier reference discovery, async target-workspace parity, and qualified-name lookup. All 16 test functions remain covered, every file is under the 1000-line test cap, child filenames avoid the ignored `test_*.rs` pattern, and `xtask` changed-file routing now maps split child paths to `tools-fast-refs` instead of a dev fallback.
- **Candidate 12:** split oversized `dashboard/integration.rs` into a 401-line parent helper/module file plus behavior-focused child modules under `src/tests/dashboard/integration/`: signals pages, metrics/pages, static/search/project routes, and status health rendering. All 22 test functions remain covered, every file is under the 1000-line test cap, child filenames avoid the ignored `test_*.rs` pattern, and existing `src/tests/dashboard/` changed-file routing keeps the slice in the dashboard bucket.
- **Candidate 12:** split oversized `daemon/database.rs` by moving the workspace-status/path-normalization tail tests into `src/tests/daemon/database/tests/workspace_status.rs`. The parent is now 934 lines and the child is 143 lines; all five moved tests remain under the existing private `tests` module with `use super::*`, and existing `src/tests/daemon/` changed-file routing keeps the slice in the daemon bucket.
- **Candidate 12:** split oversized `daemon/http_transport.rs` by moving the restart-pending admission test into `src/tests/daemon/http_transport/tests/restart_pending.rs`. The parent is now 895 lines and the child is 110 lines; the moved test remains under the existing private `tests` module with `use super::*`. `xtask` changed-file routing now maps split child paths to `transport` instead of the broad daemon bucket.

Remaining candidates:

- **Candidate 8:** dashboard preview rendering was re-verified as already implemented and tested. A fresh re-review found the remaining unified line-match integration is design-first, not implementation-ready: `execute_search_unified` returns only unified hits plus trace, `FastSearchTool::execute_with_trace` still does post-execution line enrichment and locations reruns, `line_mode_matches` owns separate filtering/scope-rescue/zero-hit attribution, and dashboard search still calls line mode separately. Do not mix this with ranking changes; write a focused design/spec before implementation.
- **Candidate 10:** completed. Continue to reject path-only `.h` assumptions in new language-detection or relationship-resolution code.
- **Candidate 11:** daemon DB root is now below the 500-line implementation target, and `src/watcher/filtering.rs` is now 240 lines with no inline tests. Broad handler/search/workspace-pool splits still wait on stronger contracts and harnesses.
- **Candidate 12:** targeted builder migrations have removed the high-value ordinary raw setup clusters from the reviewed files. Explicit stay-raw categories are now verified: malformed row-mapping fixtures in `database_row_mapping.rs`; target-workspace identifier fixtures that pass `kind='import'` because `IdentifierKind` has no `Import` variant; schema/default tests such as `test_reference_score_defaults_to_zero`; assertion SQL, metadata-reset SQL, stale-relationship deletion SQL, schema/migration SQL, ignored concurrency stress SQL, and analytics edge-case SQL. `src/tests/dashboard/intelligence.rs` ordinary symbol and relationship setup rows are now migrated; its remaining raw file inserts are intentional aggregate-query edge cases.
- **Candidate 12 re-review:** after the test-quality, deep-dive, blast-radius, rewrite-symbol, embedding-metadata-enrichment, workspace-discovery, workspace `mod_tests/part5`, daemon roots, global-targeting, target-workspace fast_refs, dashboard integration, daemon database workspace-status, and daemon HTTP transport restart-pending splits, current `src/tests/**/*.rs` files still above the 1000-line test cap are `core/database.rs`, `core/handler.rs`, `integration/watcher_handlers.rs`, `integration/real_world_validation.rs`, `core/workspace_init.rs`, `integration/stale_index_detection.rs`, and `integration/watcher.rs`. Defer lifecycle, daemon, workspace, schema, and integration-heavy splits until they have explicit design/ownership because they encode shared invariants.
- **Candidate 12 next split detail:** `src/tests/analysis/test_quality_tests.rs`, `src/tests/tools/deep_dive_tests.rs`, `src/tests/tools/blast_radius_determinism_tests.rs`, `src/tests/tools/editing/rewrite_symbol_tests.rs`, `src/tests/core/embedding_metadata_enrichment.rs`, `src/tests/tools/workspace/discovery.rs`, `src/tests/tools/workspace/mod_tests/part5.rs`, `src/tests/daemon/roots.rs`, `src/tests/tools/workspace/global_targeting.rs`, `src/tests/tools/target_workspace_fast_refs_tests.rs`, `src/tests/dashboard/integration.rs`, `src/tests/daemon/database.rs`, and `src/tests/daemon/http_transport.rs` are now below the test-file cap. The next candidate should be chosen after the fresh scout of the remaining over-cap files; the list is now dominated by core database/handler, workspace lifecycle, stale-index, watcher, and real-world integration surfaces.
- **Candidate 13 dead-code re-review:** remove-now helper cleanup is complete: extractor placeholders, `handler.workspace_root`, and `primary_database_and_search_index` are gone. Defer `text_search_impl` and target-workspace fixture isolation because those are active test harness compatibility/fixture debts, not simple dead code.
- **Next priority:** finish Claude review and commit for the `daemon/http_transport.rs` restart-pending split, then use the fresh remaining-slice scout before choosing the next over-cap file.

## Verification Strategy

**Project source of truth:** `AGENTS.md`, `RAZORBACK.md`, and `docs/TESTING_GUIDE.md`.

**Worker red/green scope:** Exact test by name: `cargo nextest run --lib <exact_test_name> 2>&1 | tail -10`.

**Worker ceiling:** Workers may run exact tests only. The lead owns `cargo xtask test changed`, specialist tiers, and `cargo xtask test dev`.

**Worker gate invariant:** Each worker must state the behavior their exact test proves. Deletion-only workers must state the deleted symbol/file and prove no remaining caller through `cargo check --all-targets` or exact compile/test scope assigned by the lead.

**Lead affected-change scope:** `cargo xtask test changed` after each coherent batch.

**Branch gate:** `cargo xtask test dev` before handoff. Add specialist gates below when triggered.

**Replay/metric evidence:** Search ranking, zero-hit, and dogfood metrics are hard gates only when the candidate changes search/scoring/tokenization behavior. Report-only metrics must be labeled as report-only.

**Escalation triggers:**
- Search/scoring/tokenization changes: add `cargo xtask test dogfood`.
- Startup/workspace/system behavior: add `cargo xtask test system`.
- Daemon/watcher/lifecycle/restart behavior: add `cargo xtask test reliability`.
- Extractor/language-detection changes: add `cargo xtask test bucket extractors` and `cargo xtask certify tree-sitter --check`; add parser-upgrade bucket if parser dependencies move.
- Xtask routing changes: run the exact xtask tests first, then `cargo xtask test changed`.

**Assigned verification failure:** Workers stop and report when assigned verification fails unless the task explicitly changes that gate.

**Verification ledger:** Record invariant, command, scope label, commit SHA, result, timestamp, and evidence reuse.

## Model Routing

**Project source of truth:** `RAZORBACK.md`.

**Strategy tier:** Cleanup sequencing, architecture decisions, candidate triage, contract shape.
- Harness mapping: use `RAZORBACK.md`; Codex route is `gpt-5.5` medium/high when selectable.

**Implementation tier:** Bounded local edits with clear tests, such as deleting unused helpers or adding a pure resolver.
- Harness mapping: use `RAZORBACK.md`; Codex route is `gpt-5.5` low/medium when selectable.

**Mechanical tier:** Formatting, docs-only updates, fixture metadata with no gate ownership.
- Harness mapping: use `RAZORBACK.md`; Codex route is `gpt-5.4-mini` low/medium when selectable.

**Gate-interpretation reviewer:** Search behavior, daemon lifecycle, workspace cleanup safety, public MCP/CLI compatibility, and failed exact tests.
- Harness mapping: use `RAZORBACK.md`; Codex route is `gpt-5.5` high when selectable.

**Escalation tier:** Search ranking, watcher/daemon concurrency, language detection across extractors, shared lifecycle, and public protocol compatibility.
- Harness mapping: use `RAZORBACK.md`; Codex route is `gpt-5.5` high/xhigh when selectable.

**Worker eligibility:** Use workers when file ownership is narrow, tests are exact, and no shared invariant needs interpretation.

**Escalation triggers:** Public MCP/CLI contract changes, lifecycle/concurrency, search ranking, language detection, failed-worker diagnosis, or weak/missing tests.

**Mechanical exclusion:** Mechanical workers cannot own failing tests, replay evidence, metrics, or acceptance gates.

**Unsupported harness behavior:** If the harness cannot choose per-agent model/reasoning, use `inherit` and state that in the worker report.

## Verification Ledger

| Invariant | Command | Scope Label | Commit SHA | Result | Timestamp (UTC) | Evidence Reused |
|---|---|---|---|---|---|---|
| Current branch type-checks before planning cleanup | `cargo check --all-targets` | planning-baseline | f92bead5 | pass | 2026-05-24T15:44:19Z | no |
| Clippy signal collected for cleanup inventory; warnings are report evidence, not an acceptance gate | `cargo clippy --all-targets --message-format short` | planning-inventory | f92bead5 | report-only: 454 julie-extractors lib warnings; 229 julie lib warnings; 386 julie lib-test warnings including duplicates | 2026-05-24T15:44:19Z | no |
| Candidate 9 formatting preserved | `cargo fmt --check` | affected-change | 97ab2ea98608 | pass | 2026-05-24T18:18:00Z | no |
| Candidate 9 type-checks after prepare-once editing changes | `cargo check --all-targets` | affected-change | 97ab2ea98608 | pass | 2026-05-24T18:18:00Z | no |
| Candidate 9 edit-file prepared flow preserves editing behavior and stale-target protection | `cargo xtask test bucket tools-editing` | affected-change | 97ab2ea98608 | pass: tools-editing bucket | 2026-05-24T18:16:00Z | no |
| Candidate 9 handler metadata still records editing/telemetry contracts | `cargo xtask test bucket core-handler-telemetry` | affected-change | 97ab2ea98608 | pass: core-handler-telemetry bucket | 2026-05-24T18:17:00Z | no |
| Candidate 12 helper extraction compiles and preserves formatting | `cargo check`; `cargo fmt --check` | affected-change | 2e81be351214 | pass | 2026-05-24T18:25:00Z | no |
| Candidate 12 migrated get_symbols helpers preserve behavior | `cargo xtask test bucket tools-get-symbols` | affected-change | 2e81be351214 | pass: tools-get-symbols bucket | 2026-05-24T18:23:00Z | no |
| Candidate 12 migrated search-context helper preserves behavior | `cargo xtask test bucket tools-search-context` | affected-change | 2e81be351214 | pass: tools-search-context bucket | 2026-05-24T18:24:00Z | no |
| Candidate 8 locations line-hit trace preserves line-mode locations and search formatting | `cargo xtask test bucket tools-search-line`; `cargo xtask test bucket tools-search-format-quality` | affected-change | 7287ce35ef31 | pass: tools-search-line and tools-search-format-quality buckets | 2026-05-24T18:43:00Z | no |
| Candidate 8 file/path and semantic locations stay unified-only | `cargo xtask test bucket tools-search-file-mode`; `cargo xtask test bucket tools-search-hybrid` | affected-change | 7287ce35ef31 | pass: tools-search-file-mode and tools-search-hybrid buckets | 2026-05-24T18:44:00Z | no |
| Candidate 10 source-aware extractor language detection preserves extractor contract | `cargo xtask test bucket extractors` | expensive-specialist | 071bc8ccfebb | pass: extractors bucket | 2026-05-24T18:52:00Z | no |
| Candidate 10 content-aware indexing/watcher changes preserve system behavior | `cargo xtask test system` | expensive-specialist | 071bc8ccfebb | pass: system tier | 2026-05-24T18:55:00Z | no |
| Post-review edit validation/metrics regressions are fixed through the public MCP handler path | `cargo nextest run --lib tests::core::handler::test_edit_file_empty_old_text_validation_precedes_file_io`; `cargo nextest run --lib tests::core::handler::test_edit_file_failed_apply_metrics_record_applied_false`; `cargo nextest run --lib tests::core::handler::test_rewrite_symbol_failed_apply_metrics_record_applied_false` | worker-red-green | working-tree on 98e075a0 | pass | 2026-05-24T19:25:27Z | no |
| Same-process checked edit commits serialize per file and preserve stale-target rejection | `cargo nextest run --lib test_single_file_commit_if_unchanged_serializes_julie_writers`; `cargo nextest run --lib test_single_file_transaction_rejects_changed_target_before_commit`; `cargo nextest run --lib test_edit_file_apply_rejects_changed_target_before_commit`; `cargo nextest run --lib test_prepared_rewrite_rejects_changed_target_before_commit` | worker-red-green | working-tree on 98e075a0 | pass | 2026-05-24T19:25:27Z | no |
| Source-aware `.h` language fixes preserve C headers, C++ header rename, external extraction, and indexed language line hits | `cargo nextest run -p julie-extractors --lib test_detect_language_for_source_preserves_c_headers_with_cpp_keyword_identifiers`; `cargo nextest run --lib tests::tools::refactoring::rename_symbol::test_rename_symbol_cpp_h_header_uses_source_aware_parser`; `cargo nextest run --lib tests::tools::search::fast_search_regression_tests::content_locations_cpp_h_language_filter_keeps_line_hits`; `cargo nextest run --lib extract_scan_routes_cpp_h_header_through_source_aware_detection`; `cargo nextest run --lib test_extract_files_for_indexing_records_cpp_language_for_cpp_h_header_grouped_as_c` | worker-red-green | working-tree on 98e075a0 | pass | 2026-05-24T19:25:27Z | no |
| Test-only search API cleanup preserves xtask build and direct wrapper/constructor coverage | `cargo check -p xtask`; `cargo nextest run --lib test_search_files_prefers_exact_basename_over_fragment_matches`; `cargo nextest run --lib three_tokens_no_single_file_contains_all_falls_back_to_or`; `cargo nextest run --lib test_add_file_content_and_search`; `cargo nextest run --lib tantivy_indexed_qualified_name_found_by_partial_token`; `cargo nextest run --lib test_incremental_indexing_preserves_tantivy_file_content` | affected-change | working-tree on 98e075a0 | pass | 2026-05-24T19:25:27Z | no |
| Post-review editing/refactoring handler/search/language affected buckets pass | `cargo xtask test bucket tools-editing`; `cargo xtask test bucket tools-refactoring`; `cargo xtask test bucket tools-search-line`; `cargo xtask test bucket tools-search-file-mode`; `cargo xtask test bucket tools-search-format-quality`; `cargo xtask test bucket tools-search-tantivy`; `cargo xtask test bucket tools-search-unified`; `cargo xtask test bucket tools-workspace`; `cargo xtask test bucket extractors`; `cargo nextest run --lib tests::external_extract -- --skip search_quality`; `cargo xtask test bucket core-fast`; `cargo xtask test bucket core-handler-telemetry` | affected-change | working-tree on 98e075a0 | pass; external_extract included one nextest LEAK report but zero failures | 2026-05-24T19:25:27Z | no |
| Full affected diff passes dev fallback and dogfood search-quality after post-review fixes | `cargo fmt --check`; `cargo xtask test changed` | affected-change + dogfood | working-tree on 98e075a0 | pass: changed fell back to 36 buckets including dev and search-quality; 675.8s total | 2026-05-24T19:37:38Z | no |
| Claude CLI independently verifies plan and post-review fix slice; low real residuals are addressed | `claude -p --no-session-persistence --dangerously-skip-permissions --tools "Read,Bash" --model opus --effort high --max-turns 40 --max-budget-usd 10.00 ...` | gate-review | 5a4e3b1c | approve with low residuals: one fixed, one made observable, one verified already covered | 2026-05-24T19:48:34Z | no |
| Claude follow-up fixes preserve multi-file edit serialization, handler metadata, and extractor behavior | `cargo check --all-targets`; `cargo nextest run --lib test_multi_file_transaction_serializes_with_single_file_checked_writer`; `cargo nextest run --lib tests::core::handler::test_edit_file_empty_old_text_validation_precedes_file_io`; `cargo nextest run --lib tests::core::handler::test_rewrite_symbol_metrics_include_symbol_span_and_failure_kind`; `cargo nextest run -p julie-extractors --lib test_detect_language_for_source_preserves_c_headers_with_cpp_keyword_identifiers`; `cargo xtask test bucket tools-editing`; `cargo xtask test bucket core-fast`; `cargo xtask test bucket extractors` | affected-change | working-tree on 5a4e3b1c | pass; an initial typo filter `test_rewrite_symbol_metrics_include_input_and_rewrite_outcome` ran zero tests and was replaced by the correct exact test | 2026-05-24T19:48:34Z | no |
| Claude follow-up affected diff passes selected buckets | `cargo fmt --check`; `cargo xtask test changed` | affected-change | working-tree on 5a4e3b1c | pass: changed selected extractors, tools-editing, core-fast; 63.4s total | 2026-05-24T19:50:18Z | no |
| Candidate 8 line enrichment trace status is recorded for success, no-match, locations, and telemetry paths | `cargo nextest run --lib content_full_output_trace_records_line_enrichment_success`; `cargo nextest run --lib content_full_output_trace_records_line_enrichment_no_matches`; `cargo nextest run --lib content_locations_trace_uses_line_hits_without_matching_line_text`; `cargo nextest run --lib test_fast_search_metadata_serializes_line_enrichment_fields` | worker-red-green | working-tree on a96bbac19f62 | pass | 2026-05-24T20:10:44Z | no |
| Candidate 8 line enrichment observability preserves typecheck, formatting, search formatting, line-mode, and telemetry buckets | `cargo check --all-targets`; `cargo fmt --check`; `cargo xtask test bucket tools-search-format-quality`; `cargo xtask test bucket tools-search-line`; `cargo xtask test bucket core-handler-telemetry` | affected-change | working-tree on a96bbac19f62 | pass | 2026-05-24T20:10:44Z | no |
| Candidate 8 full changed diff preserves all affected search and telemetry buckets | `cargo xtask test changed` | affected-change | working-tree on a96bbac19f62 | pass: changed selected 12 buckets; 177.5s total | 2026-05-24T20:14:31Z | no |
| Claude CLI reviews Candidate 8 line-enrichment observability slice | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on a96bbac19f62 | approve with low residuals; F1-F3 addressed, F4-F5 classified as low/non-blocking after local verification | 2026-05-24T20:24:28Z | no |
| Candidate 8 review follow-up preserves trace enum/status behavior and affected search buckets | `cargo nextest run --lib record_line_enrichment_failed_clears_stale_companion_fields`; `cargo nextest run --lib line_enrichment_status_serializes_snake_case`; `cargo check --all-targets`; `cargo fmt --check`; `cargo xtask test bucket tools-search-promotion`; `cargo xtask test changed` | affected-change | working-tree on a96bbac19f62 | pass: changed selected 12 buckets; 178.5s total | 2026-05-24T20:24:28Z | no |
| Candidate 10 resolver uses stored caller language metadata and DB lookup returns requested file languages | `cargo nextest run --lib test_get_file_languages_by_paths_returns_requested_languages`; `cargo nextest run --lib test_resolve_batch_prefers_content_aware_cpp_h_caller_language` | worker-red-green | working-tree on 55e81df81561 | pass | 2026-05-24T20:44:40Z | no |
| Candidate 10 resolver language scoring preserves database/workspace/init buckets | `cargo check --all-targets`; `cargo fmt --check`; `cargo xtask test bucket core-database`; `cargo xtask test bucket tools-workspace`; `cargo xtask test bucket tools-workspace-targeting`; `cargo xtask test bucket workspace-init` | affected-change | working-tree on 55e81df81561 | pass | 2026-05-24T20:44:40Z | no |
| Candidate 10 full changed diff and dogfood search quality pass after resolver scoring change | `cargo xtask test changed`; `cargo xtask test dogfood` | affected-change + dogfood | working-tree on 55e81df81561 | pass: changed fell back to dev, 35 buckets, 516.8s; dogfood 2 buckets, 226.7s | 2026-05-24T20:44:40Z | no |
| Claude CLI reviews Candidate 10 resolver language-scoring slice | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 55e81df81561 | approve; residuals addressed with dependency comment and missing-row fallback test | 2026-05-24T21:01:59Z | no |
| Candidate 10 review follow-up preserves fallback behavior and final affected/dogfood gates | `cargo nextest run --lib test_resolve_batch_falls_back_to_extension_language_when_caller_file_missing`; `cargo check --all-targets`; `cargo fmt --check`; `cargo xtask test changed`; `cargo xtask test dogfood` | affected-change + dogfood | working-tree on 55e81df81561 | pass: changed fell back to dev, 35 buckets, 530.4s; dogfood 2 buckets, 227.7s | 2026-05-24T21:01:59Z | no |
| Candidate 12 direct MCP roots helper preserves migrated get_symbols/search/workspace session behavior | `cargo nextest run --lib test_get_symbols_primary_wrapper_resolves_roots_before_reading`; `cargo nextest run --lib test_manage_workspace_list_triggers_roots_resolution_when_primary_missing`; `cargo nextest run --lib test_fast_search_primary_wrapper_resolves_roots_before_searching`; `cargo nextest run --lib test_manage_workspace_health_triggers_roots_resolution_when_primary_missing` | worker-red-green | working-tree on cbf1cbd26ac1 | pass | 2026-05-24T21:38:21Z | no |
| Candidate 12 helper cleanup preserves typecheck, formatting, and affected direct-session buckets | `cargo check --all-targets`; `cargo fmt --check`; `cargo xtask test bucket tools-get-symbols`; `cargo xtask test bucket tools-workspace-targeting`; `cargo xtask test bucket tools-search-line`; `cargo xtask test bucket tools-workspace` | affected-change | working-tree on cbf1cbd26ac1 | pass; post-review reran tools-workspace-targeting and tools-search-line after text-extraction consolidation | 2026-05-24T21:38:21Z | no |
| Claude CLI reviews Candidate 12 direct MCP helper slice | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high --max-turns 1 ...` | gate-review | working-tree on cbf1cbd26ac1 | reviewed clean for wire format, deadlocks, lifetimes, and JSON-RPC behavior; actionable text-extraction consolidation residual addressed | 2026-05-24T21:38:21Z | no |
| Candidate 12 final changed diff passes dev fallback after review follow-up | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on cbf1cbd26ac1 | pass: changed fell back to dev, 34 buckets, 512.3s total | 2026-05-24T21:38:21Z | no |
| Candidate 12 DB row builders preserve default row semantics and migrated fixture behavior | `cargo nextest run --lib test_file_info_builder_sets_stable_defaults`; `cargo nextest run --lib test_symbol_builder_overrides_metadata_and_span`; `cargo nextest run --lib test_relationship_and_identifier_builders_cover_common_reference_rows`; `cargo nextest run --lib extract_force_rebuild_is_atomic_after_extraction_success`; `cargo nextest run --lib extract_delete_clears_cross_file_identifier_targets`; `cargo nextest run --lib extract_bulk_insert_nulls_dangling_parent_id`; `cargo nextest run --lib test_blast_radius_surfaces_identifier_only_callers`; `cargo nextest run --lib test_walk_impacts_traverses_extends_relationships`; `cargo nextest run --lib test_walk_impacts_identifier_edges_choose_strongest_kind_without_replacing_relationships`; `cargo nextest run --lib test_walk_impacts_caps_identifier_fanout_for_common_names`; `cargo nextest run --lib test_blast_radius_is_deterministic_across_repeated_calls` | worker-red-green | working-tree on f0bfb4372967 | pass | 2026-05-24T22:13:11Z | no |
| Candidate 12 DB row builder migration preserves external extraction and blast-radius affected areas | `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::external_extract -- --skip search_quality`; `cargo xtask test bucket tools-blast-spillover` | affected-change | working-tree on f0bfb4372967 | pass: external_extract 43 passed with 3 leaky reports and zero failures; tools-blast-spillover passed | 2026-05-24T22:13:11Z | no |
| Claude CLI reviews Candidate 12 DB row builder slice with helper source included | `git add -N src/tests/helpers/db/mod.rs src/tests/helpers/db/rows.rs src/tests/helpers/db/tests.rs`; `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high --max-turns 1 ...` | gate-review | working-tree on f0bfb4372967 | approve after verifying builder defaults and migrated row parity; earlier review without untracked helper files was treated as incomplete | 2026-05-24T22:13:11Z | no |
| Candidate 12 DB row builder final diff passes dev fallback after review trim | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on f0bfb4372967 | pass: changed fell back to dev, 34 buckets, 504.1s total | 2026-05-24T22:13:11Z | no |
| Candidate 11 daemon migration split preserves daemon DB migration behavior | `cargo nextest run --lib test_daemon_db_create_and_migrate`; `cargo nextest run --lib test_migration_003_drops_legacy_pairings_and_preserves_workspaces`; `cargo nextest run --lib test_daemon_db_idempotent_open`; `cargo nextest run --lib tests::daemon::database -- --skip search_quality` | affected-change | working-tree on 1abd537fe101 | pass | 2026-05-24T22:41:31Z | no |
| Candidate 12 dashboard FileInfo builder migration preserves fixture defaults and dashboard intelligence behavior | `cargo nextest run --lib test_file_info_builder_sets_stable_defaults`; `cargo nextest run --lib test_file_info_builder_overrides_symbol_count`; `cargo nextest run --lib test_get_file_hotspots_returns_ordered_by_composite_score`; `cargo nextest run --lib test_get_aggregate_stats_counts_files_and_symbols`; `cargo nextest run --lib tests::dashboard::intelligence -- --skip search_quality` | worker-red-green | working-tree on 1abd537fe101 | pass; worker first verified RED for missing `symbol_count` setter | 2026-05-24T22:41:31Z | no |
| Candidate 11/12 combined split/helper diff preserves formatting, typecheck, daemon/dashboard buckets, and affected dev fallback | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket daemon`; `cargo xtask test bucket dashboard`; `cargo xtask test changed` | affected-change | working-tree on 1abd537fe101 | pass: changed fell back to dev, 34 buckets, 523.5s total | 2026-05-24T22:41:31Z | no |
| Claude CLI reviews Candidate 11 daemon migration split and Candidate 12 dashboard helper slice | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 1abd537fe101 | approve with no findings; first response approved but missed schema keys, strict rerun returned schema-valid approval | 2026-05-24T22:41:31Z | no |
| Candidate 11 search-compare persistence split preserves daemon DB and dashboard callers | `cargo nextest run --lib test_insert_and_list_search_compare_runs_and_cases`; `cargo nextest run --lib test_all_dashboard_pages_return_200`; `cargo nextest run --lib tests::daemon::database -- --skip search_quality` | affected-change | working-tree on 63bc30b00759 | pass | 2026-05-24T22:53:50Z | no |
| Candidate 11 search-compare split preserves formatting, typecheck, daemon/dashboard buckets, and changed selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket daemon`; `cargo xtask test bucket dashboard`; `cargo xtask test changed` | affected-change | working-tree on 63bc30b00759 | pass: changed selected daemon only, 61.4s total | 2026-05-24T22:53:50Z | no |
| Claude CLI reviews Candidate 11 search-compare split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 63bc30b00759 | approve with no findings; optional changed gate was already run and passed | 2026-05-24T22:53:50Z | no |
| Candidate 12 early-warning fixture migration preserves builder setters and analysis behavior | `cargo nextest run --lib test_symbol_builder_overrides_parent_context_and_annotations`; `cargo nextest run --lib test_symbol_builder_overrides_metadata_and_span`; `cargo nextest run --lib tests::helpers::db::tests`; `cargo nextest run --lib tests::analysis::early_warning_report_tests -- --skip search_quality` | worker-red-green | working-tree on ee1dfadd8146 | pass; first run verified RED for missing `parent_id` setter | 2026-05-24T23:07:27Z | no |
| Candidate 12 early-warning fixture migration preserves formatting, typecheck, analysis bucket, and affected dev fallback | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on ee1dfadd8146 | pass: changed fell back to dev, 34 buckets, 516.0s total | 2026-05-24T23:07:27Z | no |
| Claude CLI reviews Candidate 12 early-warning fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on ee1dfadd8146 | approve with no findings; suggested full helper DB module check was run and passed | 2026-05-24T23:07:27Z | no |
| Candidate 11 tool-call persistence split preserves daemon DB, metrics, dashboard, and search-analysis callers | `cargo nextest run --lib test_insert_and_query_tool_calls`; `cargo nextest run --lib test_insert_tool_call_persists_metadata_json`; `cargo nextest run --lib test_prune_old_tool_calls`; `cargo nextest run --lib tests::tools::metrics::tool_calls_db_tests -- --skip search_quality`; `cargo nextest run --lib test_metrics_page_renders_aggregated_tool_history`; `cargo nextest run --lib test_metrics_page_counts_failed_tool_calls_in_success_rate`; `cargo nextest run --lib test_query_metrics_formats_null_input_bytes`; `cargo nextest run --lib tests::dashboard::search_analysis -- --skip search_quality` | affected-change | working-tree on 43893740b3e2 | pass | 2026-05-24T23:15:30Z | no |
| Candidate 11 tool-call split preserves formatting, typecheck, selected buckets, and changed selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket tools-metrics`; `cargo xtask test bucket daemon`; `cargo xtask test bucket dashboard`; `cargo xtask test changed` | affected-change | working-tree on 43893740b3e2 | pass: changed selected daemon only, 61.4s total | 2026-05-24T23:15:30Z | no |
| Claude CLI reviews Candidate 11 tool-call split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 43893740b3e2 | approve with no findings; requested check and changed gate were already passed | 2026-05-24T23:15:30Z | no |
| Candidate 11 codehealth persistence split preserves snapshot insert/read/history/materialization and workspace-ID migration behavior | `cargo nextest run --lib test_snapshot_and_retrieve_codehealth`; `cargo nextest run --lib test_snapshot_history`; `cargo nextest run --lib test_get_latest_snapshot_returns_none_when_empty`; `cargo nextest run --lib test_snapshot_codehealth_from_symbols_db`; `cargo nextest run --lib test_migrate_workspace_ids_updates_all_tables`; `cargo nextest run --lib test_migrate_workspace_ids_merges_when_target_workspace_exists`; `cargo nextest run --lib tests::daemon::database -- --skip search_quality` | affected-change | working-tree on 9d7d01ec275e | pass | 2026-05-24T23:25:53Z | no |
| Candidate 11 codehealth split preserves formatting, typecheck, daemon bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket daemon`; `cargo xtask test changed` | affected-change | working-tree on 9d7d01ec275e | pass: changed selected daemon only, 61.4s total | 2026-05-24T23:25:53Z | no |
| Claude CLI reviews Candidate 11 codehealth split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 9d7d01ec275e | approve with no findings; verified SQL parity, re-export stability, private module access, and migration scope | 2026-05-24T23:27:53Z | no |
| Candidate 12 test-quality fixture migration preserves pipeline behavior for metadata update, identifier evidence, fallback, and exact identifier matching | `cargo nextest run --lib test_pipeline_integration_updates_metadata`; `cargo nextest run --lib test_pipeline_integration_with_identifier_evidence`; `cargo nextest run --lib test_empty_evidence_config_falls_back_to_regex`; `cargo nextest run --lib test_identifier_evidence_without_matches_falls_back_to_regex_body`; `cargo nextest run --lib test_identifier_evidence_no_substring_matching` | worker-red-green | working-tree on 54ca28912a3e | pass | 2026-05-24T23:35:46Z | no |
| Candidate 12 test-quality fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 54ca28912a3e | pass: changed selected analysis only, 1.0s total | 2026-05-24T23:35:46Z | no |
| Claude CLI reviews Candidate 12 test-quality fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 54ca28912a3e | approve with no findings; verified span/default delta is inert, identifier containment survives storage API, and scope stayed narrow | 2026-05-24T23:37:51Z | no |
| Candidate 12 test-linkage fixture migration preserves relationship-based and identifier-only linkage behavior | `cargo nextest run --lib test_compute_linkage_relationship_linkage`; `cargo nextest run --lib test_identifier_only_linkage` | worker-red-green | working-tree on bd640c7a135c | pass | 2026-05-24T23:46:23Z | no |
| Candidate 12 test-linkage fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on bd640c7a135c | pass: changed selected analysis only, 1.0s total | 2026-05-24T23:46:23Z | no |
| Claude CLI reviews Candidate 12 first linkage fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on bd640c7a135c | approve with no findings; verified `reference_score` removal, builder row defaults, and narrow scope | 2026-05-24T23:48:18Z | no |
| Candidate 11 workspace registry split preserves workspace CRUD, stats, sessions, cleanup events, normalize sentinels, and workspace-ID migration behavior | `cargo nextest run --lib test_upsert_and_get_workspace`; `cargo nextest run --lib test_increment_decrement_session_count`; `cargo nextest run --lib test_reset_all_session_counts`; `cargo nextest run --lib test_update_workspace_stats`; `cargo nextest run --lib test_update_vector_count`; `cargo nextest run --lib test_insert_and_list_cleanup_events`; `cargo nextest run --lib test_cleanup_event_log_is_capped_at_fifty_rows`; `cargo nextest run --lib test_delete_workspace_with_root_path`; `cargo nextest run --lib test_upsert_workspace_path_conflict_updates_status`; `cargo nextest run --lib test_upsert_workspace_allows_upgrade_to_ready`; `cargo nextest run --lib test_normalize_workspace_paths_fixes_slashes_and_status`; `cargo nextest run --lib test_normalize_workspace_paths_skips_pending_without_symbols`; `cargo nextest run --lib test_normalize_restores_ready_on_all_platforms`; `cargo nextest run --lib test_migrate_workspace_ids_updates_all_tables`; `cargo nextest run --lib test_migrate_workspace_ids_merges_when_target_workspace_exists`; `cargo nextest run --lib tests::daemon::database -- --skip search_quality` | affected-change | working-tree on 3480b5b3be9d | pass | 2026-05-24T23:59:31Z | no |
| Candidate 11 workspace registry split preserves formatting, typecheck, daemon bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket daemon`; `cargo xtask test changed` | affected-change | working-tree on 3480b5b3be9d | pass: changed selected daemon only, 61.4s total | 2026-05-24T23:59:31Z | no |
| Claude CLI reviews Candidate 11 workspace registry split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 3480b5b3be9d | approve with no findings; verified re-export stability, private submodule access, SQL parity, and root-owned startup/migration repair scope | 2026-05-25T00:01:28Z | no |
| Candidate 12 remaining test-quality pipeline fixture migration preserves metadata update, identifier evidence, no-body, metadata preservation, and fixture-not-applicable behavior | `cargo nextest run --lib test_pipeline_integration_updates_metadata`; `cargo nextest run --lib test_pipeline_integration_with_identifier_evidence`; `cargo nextest run --lib test_pipeline_integration_no_body`; `cargo nextest run --lib test_pipeline_integration_preserves_existing_metadata`; `cargo nextest run --lib test_pipeline_integration_fixture_not_applicable` | worker-red-green | working-tree on f147d0aac95d | pass | 2026-05-25T00:14:49Z | no |
| Candidate 12 remaining test-quality pipeline fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on f147d0aac95d | pass: changed selected analysis only, 1.0s total; final rerun ignored docs/memories and selected analysis | 2026-05-25T00:19:37Z | no |
| Claude CLI reviews Candidate 12 remaining test-quality fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high --max-turns 1 ...` | gate-review | working-tree on f147d0aac95d | approve with no findings; earlier read-tool run hit max turns and semantic rerun approved with malformed key names, so strict no-tool rerun supplied schema-valid approval | 2026-05-25T00:19:05Z | no |
| Candidate 12 linkage edge-case fixture migration preserves uncovered-symbol and test-to-test exclusion behavior | `cargo nextest run --lib test_uncovered_symbol_has_no_test_linkage_key`; `cargo nextest run --lib test_test_to_test_relationships_excluded` | worker-red-green | working-tree on 84c67252711a | pass | 2026-05-25T00:29:53Z | no |
| Candidate 12 linkage edge-case fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 84c67252711a | pass: changed selected analysis only, 1.0s total; final rerun ignored plan doc and selected analysis | 2026-05-25T00:34:00Z | no |
| Claude CLI reviews Candidate 12 linkage edge-case fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 84c67252711a | approve with no findings; prior semantic review approved but used informational findings, so strict rerun supplied schema-valid approval | 2026-05-25T00:33:35Z | no |
| Candidate 12 linkage cap fixture migration preserves linked-test count and display cap behavior | `cargo nextest run --lib test_linked_tests_capped_at_five` | worker-red-green | working-tree on 9e364e51495d | pass | 2026-05-25T00:39:24Z | no |
| Candidate 12 linkage cap fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 9e364e51495d | pass: changed selected analysis only, 1.0s total; final rerun ignored plan doc and selected analysis | 2026-05-25T00:41:22Z | no |
| Claude CLI reviews Candidate 12 linkage cap fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 9e364e51495d | approve with no findings; first review emitted only `findings`, so strict rerun supplied schema-valid approval | 2026-05-25T00:40:59Z | no |
| Candidate 12 linkage class-similarity fixture migration preserves name-match fallback behavior | `cargo nextest run --lib test_name_match_prefers_class_name_similarity` | worker-red-green | working-tree on df483d47f1d8 | pass | 2026-05-25T00:49:41Z | no |
| Candidate 12 linkage class-similarity fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on df483d47f1d8 | pass: changed selected analysis only, 1.0s total; final rerun ignored plan doc and selected analysis | 2026-05-25T00:51:55Z | no |
| Claude CLI reviews Candidate 12 linkage class-similarity fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on df483d47f1d8 | approve with no findings; first review emitted only `findings`, so strict rerun supplied schema-valid approval | 2026-05-25T00:51:30Z | no |
| Candidate 12 linkage ambiguity-guard fixture migration preserves same-language fallback and cross-language exclusion behavior | `cargo nextest run --lib test_name_match_ambiguity_guard_is_language_scoped` | worker-red-green | working-tree on de3bc57b06d3 | pass | 2026-05-25T00:58:50Z | no |
| Candidate 12 linkage ambiguity-guard fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on de3bc57b06d3 | pass: changed selected analysis only, 1.0s total; final rerun ignored plan doc and selected analysis | 2026-05-25T01:01:57Z | no |
| Claude CLI reviews Candidate 12 linkage ambiguity-guard fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on de3bc57b06d3 | approve with no findings; first review emitted only `findings`, so strict rerun supplied schema-valid approval | 2026-05-25T01:01:29Z | no |
| Candidate 12 linkage parent-inheritance fixture migration preserves method linkage and parent aggregation behavior | `cargo nextest run --lib test_class_inherits_method_linkage` | worker-red-green | working-tree on 75ef98e96e91 | pass | 2026-05-25T01:08:26Z | no |
| Candidate 12 linkage parent-inheritance fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 75ef98e96e91 | pass: fmt applied indentation-only fix, changed selected analysis only, 1.1s total; final rerun ignored plan doc and selected analysis | 2026-05-25T01:11:06Z | no |
| Claude CLI reviews Candidate 12 linkage parent-inheritance fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 75ef98e96e91 | approve with no findings; first review exhausted turn cap, so strict rerun supplied schema-valid approval | 2026-05-25T01:10:40Z | no |
| Candidate 12 linkage deduplication fixture migration preserves duplicate identifier deduplication behavior | `cargo nextest run --lib test_deduplication_across_strategies` | worker-red-green | working-tree on 2e0cf493980b | pass | 2026-05-25T01:19:57Z | no |
| Candidate 12 linkage deduplication fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 2e0cf493980b | pass: changed selected analysis only, 1.0s total; final rerun ignored plan doc and selected analysis | 2026-05-25T01:23:29Z | no |
| Claude CLI reviews Candidate 12 linkage deduplication fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 2e0cf493980b | approve with no findings; first review tried tool use and hit max turns, strict no-tool rerun approved builder semantics, plan wording, and verification evidence | 2026-05-25T01:22:36Z | no |
| Candidate 12 linkage edge-case file split preserves moved test behavior | `cargo nextest run --lib test_name_match_fallback_is_deterministic_on_tied_scores`; `cargo nextest run --lib test_compute_linkage_clears_stale_symbol_and_parent_linkage`; `cargo nextest run --lib test_scorable_filter_excludes_fixture_includes_test_case_and_legacy`; `cargo nextest run --lib test_best_confidence_present_in_linkage_output`; `cargo nextest run --lib test_parent_aggregation_includes_best_confidence`; `cargo nextest run --lib test_best_confidence_defaults_when_metadata_absent` | worker-red-green | working-tree on f607ca943832 | pass | 2026-05-25T01:33:33Z | no |
| Candidate 12 linkage edge-case file split preserves formatting, typecheck, analysis bucket, changed-file selection, and file-size cap | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed`; `wc -l src/tests/analysis/test_linkage_tests.rs src/tests/analysis/linkage_edge_cases_tests.rs` | affected-change | working-tree on f607ca943832 | pass: changed selected analysis; final rerun ignored plan doc; line counts 584 and 407 | 2026-05-25T01:36:40Z | no |
| Claude CLI reviews Candidate 12 linkage edge-case file split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on f607ca943832 | approve with no findings; verified moved-body parity, module discovery, git-visible filename, line counts, and remaining raw SQL plan accuracy | 2026-05-25T01:35:20Z | no |
| Candidate 12 linkage tied-score fallback fixture migration preserves deterministic name-match behavior | `cargo nextest run --lib test_name_match_fallback_is_deterministic_on_tied_scores` | worker-red-green | working-tree on 3ab99e838cab | pass; rerun after `cargo fmt` | 2026-05-25T01:45:06Z | no |
| Candidate 12 linkage tied-score fallback fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 3ab99e838cab | pass: fmt applied builder-block formatting, changed selected analysis only; final rerun ignored plan doc | 2026-05-25T01:48:08Z | no |
| Claude CLI reviews Candidate 12 linkage tied-score fallback fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 3ab99e838cab | approve with no findings; verified row semantics, identifier NULL-target fallback, plan accuracy, and verification evidence | 2026-05-25T01:46:47Z | no |
| Candidate 12 linkage stale-cleanup fixture migration preserves stale relationship removal and parent linkage clearing behavior | `cargo nextest run --lib test_compute_linkage_clears_stale_symbol_and_parent_linkage` | worker-red-green | working-tree on 1cb8e4ad0537 | pass; rerun after `cargo fmt` | 2026-05-25T01:53:57Z | no |
| Candidate 12 linkage stale-cleanup fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 1cb8e4ad0537 | pass: fmt applied relationship-builder formatting, changed selected analysis only; final rerun ignored plan doc | 2026-05-25T01:57:02Z | no |
| Claude CLI reviews Candidate 12 linkage stale-cleanup fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 1cb8e4ad0537 | approve with no findings; verified parent/child/test row semantics, relationship shape, and ledger accuracy | 2026-05-25T01:55:50Z | no |
| Candidate 12 linkage scorable-filter fixture migration preserves fixture exclusion and legacy inclusion behavior | `cargo nextest run --lib test_scorable_filter_excludes_fixture_includes_test_case_and_legacy` | worker-red-green | working-tree on d9b3d92c28c0 | pass | 2026-05-25T02:04:05Z | no |
| Candidate 12 linkage scorable-filter fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on d9b3d92c28c0 | pass: changed selected analysis only; final rerun ignored plan doc | 2026-05-25T02:07:11Z | no |
| Claude CLI reviews Candidate 12 linkage scorable-filter fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on d9b3d92c28c0 | approve with no findings; verified metadata roles, relationship file/line shape, ledger accuracy, and remaining raw SQL list | 2026-05-25T02:05:59Z | no |
| Candidate 12 linkage direct best-confidence fixture migration preserves test-quality confidence output | `cargo nextest run --lib test_best_confidence_present_in_linkage_output` | worker-red-green | working-tree on 9dfc8f767ca0 | pass; lead rerun after `cargo fmt` | 2026-05-25T02:14:54Z | no |
| Candidate 12 linkage direct best-confidence fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 9dfc8f767ca0 | pass: fmt applied relationship-builder indentation; changed selected analysis only; final rerun ignored plan doc | 2026-05-25T02:17:56Z | no |
| Claude CLI reviews Candidate 12 linkage direct best-confidence fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 9dfc8f767ca0 | approve with no findings; verified row shape, metadata preservation, intentional `reference_score` removal, plan wording, and verification evidence | 2026-05-25T02:16:55Z | no |
| Candidate 12 linkage parent-aggregation fixture migration preserves parent best-confidence aggregation | `cargo nextest run --lib test_parent_aggregation_includes_best_confidence` | worker-red-green | working-tree on f73636a6bdd2 | pass | 2026-05-25T02:25:23Z | no |
| Candidate 12 linkage parent-aggregation fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on f73636a6bdd2 | pass: changed selected analysis only; final rerun ignored plan doc | 2026-05-25T02:28:00Z | no |
| Claude CLI reviews Candidate 12 linkage parent-aggregation fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on f73636a6bdd2 | approve with no findings; verified C# parent/child shape, metadata preservation, intentional `reference_score` removal, and plan/ledger accuracy | 2026-05-25T02:27:01Z | no |
| Candidate 12 linkage default-confidence fixture migration preserves missing-confidence fallback behavior | `cargo nextest run --lib test_best_confidence_defaults_when_metadata_absent` | worker-red-green | working-tree on 8e99dce9f363 | pass; lead rerun after `cargo fmt` | 2026-05-25T02:33:08Z | no |
| Candidate 12 linkage default-confidence fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed`; `rg -n "execute_batch|INSERT INTO symbols|INSERT INTO relationships|INSERT INTO identifiers" src/tests/analysis/linkage_edge_cases_tests.rs` | affected-change | working-tree on 8e99dce9f363 | pass: fmt applied relationship-builder indentation; changed selected analysis only; raw setup insert search found no matches; final rerun ignored plan doc | 2026-05-25T02:36:02Z | no |
| Claude CLI reviews Candidate 12 linkage default-confidence fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 8e99dce9f363 | approve with no findings; verified omitted nested confidence, explicit symbol confidence preservation, plan wording, and verification evidence | 2026-05-25T02:35:04Z | no |
| Candidate 12 reference-score helper rejects bad fixture IDs and updates scores | `cargo nextest run --lib test_set_symbol_reference_scores_updates_scores`; `cargo nextest run --lib test_set_symbol_reference_scores_rejects_missing_symbol` | worker-red-green | working-tree on 0452c0ef502c | pass; worker RED first saw unresolved import before helper implementation | 2026-05-25T02:56:47Z | no |
| Candidate 12 change-risk fixture migration preserves centrality-driven risk scoring | `cargo nextest run --lib test_compute_change_risk_scores` | worker-red-green | working-tree on 0452c0ef502c | pass; lead rerun after `cargo fmt` | 2026-05-25T02:56:47Z | no |
| Candidate 12 change-risk fixture migration preserves formatting, typecheck, analysis bucket, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test bucket analysis`; `cargo xtask test changed` | affected-change | working-tree on 0452c0ef502c | pass: changed fell back to dev because shared helpers changed; final rerun ignored plan doc; 34 buckets passed in 502.2s | 2026-05-25T03:08:42Z | no |
| Claude CLI reviews Candidate 12 reference-score helper and change-risk fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 0452c0ef502c | approve with no findings; verified missing-ID guard, centrality preservation, migrated row shape, and ledger accuracy | 2026-05-25T02:59:14Z | no |
| Candidate 12 get-context task-input identifier fixture migration preserves caller summary output | `cargo nextest run --lib test_run_pipeline_reports_identifier_callers_in_pivot_summary` | worker-red-green | working-tree on 88bbe6602d6e | pass; lead applied migration after worker report did not match clean worktree; rerun after `cargo fmt` | 2026-05-25T03:16:40Z | no |
| Candidate 12 get-context task-input identifier fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 88bbe6602d6e | pass: changed selected tools-get-context-graph only, 22.1s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 22.1s | 2026-05-25T03:24:09Z | no |
| Claude CLI reviews Candidate 12 get-context task-input identifier fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 88bbe6602d6e | approve with no findings; verified row equivalence, `valid_symbol_ids=None` preservation of caller/target IDs, plan wording, and ledger accuracy | 2026-05-25T03:24:09Z | no |
| Candidate 12 get-context graph expansion identifier fixture migration preserves identifier graph behavior | `cargo nextest run --lib test_expand_graph_finds_neighbors_via_identifiers_when_no_relationships`; `cargo nextest run --lib test_expand_graph_preserves_identifier_call_relationship_kind`; `cargo nextest run --lib test_identifier_incoming_edges_filters_kind_and_excluded_containers_before_expansion`; `cargo nextest run --lib test_expand_graph_identifier_names_with_sql_wildcards_do_not_overmatch` | worker-red-green | working-tree on 1ceb5e5cc838 | pass: all four exact tests passed after migration; `rg` confirmed no raw identifier inserts remain in the file | 2026-05-25T03:31:09Z | no |
| Candidate 12 get-context graph expansion identifier fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 1ceb5e5cc838 | pass: changed selected tools-get-context-graph only, 22.0s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 21.8s | 2026-05-25T03:35:17Z | no |
| Claude CLI reviews Candidate 12 get-context graph expansion identifier fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 1ceb5e5cc838 | approve with no findings; verified builder defaults, per-cluster row semantics, wildcard regression preservation, `valid_symbol_ids=None`, and ledger accuracy | 2026-05-25T03:34:05Z | no |
| Candidate 12 deep-dive regression identifier fixture migration preserves qualified-method fallback behavior | `cargo nextest run --lib test_deep_dive_regression_qualified_method_identifier_fallback_avoids_bare_name_noise` | worker-red-green | working-tree on 343cdac58a72 | pass; rerun after `cargo fmt`; `rg` confirmed no raw identifier inserts remain in the file | 2026-05-25T03:39:12Z | no |
| Candidate 12 deep-dive regression identifier fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 343cdac58a72 | pass: changed selected tools-deep-dive only, 1.9s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 1.9s | 2026-05-25T03:43:28Z | no |
| Claude CLI reviews Candidate 12 deep-dive regression identifier fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 343cdac58a72 | approve with no findings; verified row-shape parity, `IdentifierKind::Call` serialization, optional containing-symbol preservation, call-site coverage, and ledger accuracy | 2026-05-25T03:42:37Z | no |
| Candidate 12 deep-dive helper migration preserves identifier fallback and test-ref behavior | `cargo nextest run --lib test_identifier_fallback_adds_refs`; `cargo nextest run --lib test_identifier_fallback_deduplicates`; `cargo nextest run --lib test_identifier_fallback_filters_own_file_definition_line`; `cargo nextest run --lib test_build_context_populates_test_refs_at_full_depth`; `cargo nextest run --lib test_build_context_no_test_refs_at_overview`; `cargo nextest run --lib test_build_context_populates_test_refs_at_context_depth`; `cargo nextest run --lib test_build_context_uses_test_symbol_metadata_for_test_refs` | worker-red-green | working-tree on 2ee09b6a2d7c | pass: all seven exact helper tests passed; explorer independently verified all helper callers are `IdentifierKind::Call`; `rg` confirmed no raw identifier inserts remain in the file | 2026-05-25T03:47:41Z | no |
| Candidate 12 deep-dive helper migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 2ee09b6a2d7c | pass: changed selected tools-deep-dive only, 1.9s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 1.9s | 2026-05-25T03:51:52Z | no |
| Claude CLI reviews Candidate 12 deep-dive helper identifier fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 2ee09b6a2d7c | approve with no findings; verified row-shape parity, benign `INSERT OR REPLACE` for unique IDs, `valid_symbol_ids=None`, ten call-site conversions, and ledger accuracy | 2026-05-25T03:50:57Z | no |
| Candidate 12 core identifier query fixture migration preserves filtering behavior | `cargo nextest run --lib test_get_identifiers_by_names_kinds_excluding_containers_filters_rows` | worker-red-green | working-tree on 53fdddffcd09 | pass; rerun after `cargo fmt` | 2026-05-25T03:55:32Z | no |
| Candidate 12 core identifier query fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 53fdddffcd09 | pass: changed selected core-database only, 1.5s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 1.5s | 2026-05-25T03:59:05Z | no |
| Claude CLI reviews Candidate 12 core identifier query fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 53fdddffcd09 | approve with no findings; verified symbol and identifier row parity, `valid_symbol_ids=None`, no import variant, and ledger scope accuracy | 2026-05-25T03:58:01Z | no |
| Claude CLI re-reviews Candidate 12 remaining raw SQL inventory and plan direction | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Bash" --max-turns 35 --model opus --effort high ...` | gate-review | d6822f7260ed | approve direction: classified stay-raw categories, found no analysis raw setup inserts, and identified next migration candidates; verified via `rg -nc "INSERT INTO (identifiers|symbols|relationships)" src/tests/tools src/tests/analysis src/tests/core` | 2026-05-25T04:13:36Z | no |
| Candidate 12 vector storage fixture migration preserves embedding deletion/count behavior | `cargo nextest run --lib test_delete_embeddings_for_file`; `cargo nextest run --lib tests::core::vector_storage` | worker-red-green | working-tree on d6822f7260ed | initial module run failed because `store_file_info` replaced same-file rows and cascaded symbol deletion; fixed with `get_file_hash` guard; final module run passed 21 tests | 2026-05-25T04:13:36Z | no |
| Candidate 12 vector storage fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on d6822f7260ed | pass: changed selected core-fast only, 16.9s total; final rerun after Claude ledger doc edit ignored plan doc and passed in 16.5s | 2026-05-25T04:18:24Z | no |
| Claude CLI reviews Candidate 12 vector storage fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on d6822f7260ed | approve with no findings; verified cascade-delete root cause, `get_file_hash` guard, benign builder default deltas, ledger accuracy, and doc-comment caveat | 2026-05-25T04:16:58Z | no |
| Candidate 12 workspace embedding fixture migration preserves embedding-pipeline test setup | `cargo nextest run --lib tests::tools::workspace::index_embedding_tests`; `rg -n "INSERT INTO symbols|INSERT OR IGNORE INTO files" src/tests/tools/workspace/index_embedding_tests.rs` | worker-red-green | working-tree on 10225e70 | pass: six workspace embedding tests passed before and after `cargo fmt`; raw setup insert search returned no matches | 2026-05-25T04:25:52Z | no |
| Candidate 12 workspace embedding fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 10225e70 | pass: changed selected `tools-workspace`, 9 commands, 29.5s total after plan doc edit; final diff whitespace check passed | 2026-05-25T04:28:16Z | no |
| Claude CLI reviews Candidate 12 workspace embedding fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on 10225e70 | approve with no findings; verified reference-score restoration, duplicate-file semantics, FK order, builder row mapping, and plan-note scope | 2026-05-25T04:25:52Z | no |
| Candidate 12 metrics query fixture migration preserves centrality fallback ordering | `cargo nextest run --lib query_metrics_uses_centrality_for_legacy_or_unknown_sorts`; `rg -n "INSERT INTO files|INSERT INTO symbols|execute_batch|params!" src/tests/tools/metrics/query_tests.rs` | worker-red-green | working-tree on 07302114 | pass: exact test passed after warning cleanup; raw setup insert search returned no matches | 2026-05-25T04:33:28Z | no |
| Candidate 12 metrics query fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed` | affected-change | working-tree on 07302114 | pass: changed selected `tools-metrics`, 1 command, 0.6s total; final rerun after plan doc edit ignored plan doc | 2026-05-25T04:38:36Z | no |
| Claude CLI reviews Candidate 12 metrics query fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 07302114 | approve with no findings; verified file row shape, symbol fields, metadata object shape, explicit confidence, reference-score restoration, centrality fallback coverage, and plan evidence | 2026-05-25T04:37:35Z | no |
| Candidate 12 database get-reference-score fixtures preserve score-read behavior | `cargo nextest run --lib test_get_reference_scores_batch`; `cargo nextest run --lib test_get_reference_scores_large_batch`; `rg -n "INSERT INTO symbols .*reference_score|reference_score\\)" src/tests/core/database.rs` | worker-red-green | working-tree on b9424797 | pass: both exact tests passed; targeted raw explicit-reference-score insert search returned no matches | 2026-05-25T04:43:49Z | no |
| Candidate 12 database get-reference-score fixtures preserve formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on b9424797 | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc; diff whitespace check passed | 2026-05-25T04:46:58Z | no |
| Candidate 12 database per-cluster review classifies remaining database raw setup | read-only subagent review of `src/tests/core/database.rs` raw setup and helper APIs | gate-review | working-tree on b9424797 | identified additional builder-safe relationship-only, constructor, type-usage, and path-sensitive centrality clusters; reconfirmed default-score and import-identifier rows stay raw | 2026-05-25T04:45:58Z | no |
| Claude CLI reviews Candidate 12 database get-reference-score fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on b9424797 | approve with no findings | 2026-05-25T04:45:58Z | no |
| Candidate 12 database relationship-only compute-score fixtures preserve weighting and self-ref behavior | `cargo nextest run --lib test_compute_reference_scores_weighted`; `cargo nextest run --lib test_compute_reference_scores_excludes_self_refs` | worker-red-green | working-tree on 7ac3068c | pass: exact tests verified calls/imports/uses weighting and self-reference exclusion | 2026-05-25T04:52:08Z | no |
| Candidate 12 database relationship-only compute-score fixtures preserve formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 7ac3068c | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc; diff whitespace check passed | 2026-05-25T04:54:00Z | no |
| Claude CLI reviews Candidate 12 database relationship-only fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 7ac3068c | approve with no findings | 2026-05-25T04:53:31Z | no |
| Candidate 12 database outgoing-only compute-score fixture preserves zero/incoming behavior | `cargo nextest run --lib test_compute_reference_scores_zero_for_no_incoming` | worker-red-green | working-tree on c0f36bf3 | pass: exact test verified sender outgoing-only zero score and receiver call score | 2026-05-25T04:56:31Z | no |
| Candidate 12 database outgoing-only compute-score fixture preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on c0f36bf3 | pass: changed selected `core-database`, 1 command, 1.6s total; final rerun after plan doc edit ignored plan doc; diff whitespace check passed | 2026-05-25T04:57:56Z | no |
| Claude CLI reviews Candidate 12 database outgoing-only fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on c0f36bf3 | approve with no findings | 2026-05-25T04:57:20Z | no |
| Candidate 12 database interface/base propagation fixtures preserve centrality inheritance behavior | `cargo nextest run --lib test_compute_reference_scores_propagates_interface_centrality_to_implementations`; `cargo nextest run --lib test_compute_reference_scores_propagates_base_class_centrality` | worker-red-green | working-tree on c0e3b6be | pass: exact tests verified interface implementation centrality and base-class extension centrality | 2026-05-25T05:02:03Z | no |
| Candidate 12 database interface/base propagation fixtures preserve formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on c0e3b6be | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc; diff whitespace check passed | 2026-05-25T05:03:40Z | no |
| Claude CLI reviews Candidate 12 database interface/base propagation fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on c0e3b6be | approve with no findings | 2026-05-25T05:03:01Z | no |
| Candidate 12 database constructor propagation fixture preserves parent centrality behavior | `cargo nextest run --lib test_compute_reference_scores_propagates_constructor_centrality` | worker-red-green | working-tree on c70a0df4 | pass: exact test verified constructor centrality and parent-class inheritance | 2026-05-25T05:06:33Z | no |
| Candidate 12 database constructor propagation fixture preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on c70a0df4 | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc; diff whitespace check passed | 2026-05-25T05:08:06Z | no |
| Claude CLI reviews Candidate 12 database constructor propagation fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on c70a0df4 | approve with no findings; verified ids, names, kinds, csharp paths/spans/bytes, public visibility, constructor parent_id, relationship ids/from/to/kinds, and compute-score assertion path | 2026-05-25T05:07:27Z | no |
| Candidate 12 database type-usage Step 1b fixture migration preserves identifier centrality behavior | `cargo nextest run --lib test_compute_reference_scores_includes_type_usage_identifiers`; `cargo nextest run --lib test_compute_reference_scores_includes_constants_with_type_usage`; `cargo nextest run --lib test_compute_reference_scores_qualified_name_identifiers`; `cargo nextest run --lib test_compute_reference_scores_escapes_like_wildcards`; `cargo nextest run --lib test_step1b_identifier_boost_excludes_test_file_symbols` | worker-red-green | working-tree on d31176303327 | pass: exact tests verified type-usage boost, constants, qualified names, wildcard safety, and test-file symbol exclusion | 2026-05-25T05:20:04Z | no |
| Candidate 12 database type-usage fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on d31176303327 | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc and passed in 1.5s; diff whitespace check passed | 2026-05-25T05:25:10Z | no |
| Claude CLI reviews Candidate 12 database type-usage Step 1b fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high --max-turns 15 ...` | gate-review | working-tree on d31176303327 | approve with no actionable findings; verified row-shape parity, inert identifier byte defaults, raw import-kind exception, and ledger accuracy; earlier lower-turn-cap structured runs exhausted before final JSON | 2026-05-25T05:24:24Z | no |
| Candidate 12 database path-sensitive reference-score fixture migration preserves deweighting and header propagation behavior | `cargo nextest run --lib test_centrality_deweights_test_file_symbols`; `cargo nextest run --lib test_compute_reference_scores_propagates_header_to_implementation` | worker-red-green | working-tree on f29752033379 | pass: exact tests verified test-file deweighting and C header-to-implementation 70% propagation | 2026-05-25T05:29:56Z | no |
| Candidate 12 database path-sensitive fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on f29752033379 | pass: changed selected `core-database`, 1 command, 1.5s total; final rerun after plan doc edit ignored plan doc and passed in 1.5s; diff whitespace check passed | 2026-05-25T05:34:05Z | no |
| Claude CLI reviews Candidate 12 database path-sensitive reference-score fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high --max-turns 20 ...` | gate-review | working-tree on f29752033379 | approve with no actionable findings; verified file paths, spans/bytes, relationship kinds, line-number inertness, weight semantics, and ledger accuracy; an earlier lower-turn-cap structured run exhausted before final JSON | 2026-05-25T05:33:14Z | no |
| Candidate 12 database embedding-deletion fixture migration preserves targeted embedding deletion behavior | `cargo nextest run --lib test_delete_embeddings_for_symbol_ids_only_removes_requested_rows` | worker-red-green | working-tree on 9fa0c9a0583d | pass: exact test verified empty deletion, selected deletion count, remaining embedding count, and surviving symbol id | 2026-05-25T05:36:26Z | no |
| Candidate 12 database embedding-deletion fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 9fa0c9a0583d | pass: final sequential rerun after plan doc edit selected `core-database`, ignored plan doc, passed in 1.5s; diff whitespace check passed | 2026-05-25T05:40:32Z | no |
| Claude CLI reviews Candidate 12 database embedding-deletion fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "" --model opus --effort high ...` | gate-review | working-tree on 9fa0c9a0583d | approve with no actionable findings; verified file fields, symbol spans/bytes/kinds, FK/embedding behavior, and ledger accuracy | 2026-05-25T05:39:25Z | no |
| Candidate 12 hybrid fixture migration preserves doc comments, metadata roles, KNN conversion, and shared file-row guard behavior | `cargo nextest run --lib test_store_file_info_if_missing_preserves_existing_file_row`; `cargo nextest run --lib test_symbol_builder_overrides_doc_comment`; `cargo nextest run --lib test_hybrid_search_none_provider_returns_keyword_results`; `cargo nextest run --lib test_hybrid_search_filters_semantic_results_by_language_and_file_pattern`; `cargo nextest run --lib test_hybrid_search_exclude_tests_filters_semantic_results`; `cargo nextest run --lib test_hybrid_search_exclude_tests_filters_metadata_test_semantic_results`; `cargo nextest run --lib test_hybrid_search_with_weight_profile_uses_weighted_merge`; `cargo nextest run --lib test_hybrid_search_none_profile_uses_uniform_merge`; `cargo nextest run --lib test_hybrid_search_weight_profile_keyword_only_graceful`; `cargo nextest run --lib test_knn_to_search_results_converts_correctly`; `cargo nextest run --lib test_knn_to_search_results_uses_metadata_role_for_inline_tests`; `cargo nextest run --lib test_knn_to_search_results_skips_missing_symbols`; `cargo nextest run --lib test_knn_score_clamp_negative_distance`; `cargo nextest run --lib test_store_and_count_embeddings`; `cargo nextest run --lib test_clear_embeddings_on_separate_db_does_not_affect_other`; `cargo nextest run --lib test_embedding_count_reflects_total_vectors_not_run_count`; `rg -n "INSERT(?: OR [A-Z]+)? INTO (files|symbols|relationships|identifiers)" src/tests/tools/hybrid_search_tests.rs src/tests/core/vector_storage.rs src/tests/tools/workspace/index_embedding_tests.rs` | worker-red-green | working-tree on afcdf9dffccc | pass: exact tests passed; raw setup insert search returned no matches | 2026-05-25T06:06:56Z | no |
| Candidate 12 hybrid fixture migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on afcdf9dffccc | pass: changed fell back to dev because shared helper files changed; 34 buckets passed in 535.5s; diff whitespace check passed | 2026-05-25T06:06:56Z | no |
| Claude CLI reviews Candidate 12 hybrid fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...`; `rg -n "confidence" src/database src/tests/helpers/db/rows.rs` | gate-review | working-tree on afcdf9dffccc | approve with no actionable findings; verified helper guard, doc-comment setter, migrated row shape, metadata, and plan ledger. Local schema check resolved Claude's confidence note: `symbols.confidence` defaults to 1.0, so explicit builder confidence preserves raw-insert parity | 2026-05-25T06:10:49Z | no |
| Candidate 12 analysis/database row-helper migration preserves analysis linkage/risk and malformed-row mapping behavior | `cargo nextest run --lib test_compute_change_risk_scores`; `cargo nextest run --lib test_linked_tests_capped_at_five`; `cargo nextest run --lib test_name_match_ambiguity_guard_is_language_scoped`; `cargo nextest run --lib test_class_inherits_method_linkage`; `cargo nextest run --lib test_name_match_fallback_is_deterministic_on_tied_scores`; `cargo nextest run --lib test_compute_linkage_clears_stale_symbol_and_parent_linkage`; `cargo nextest run --lib test_scorable_filter_excludes_fixture_includes_test_case_and_legacy`; `cargo nextest run --lib test_best_confidence_present_in_linkage_output`; `cargo nextest run --lib test_parent_aggregation_includes_best_confidence`; `cargo nextest run --lib test_best_confidence_defaults_when_metadata_absent`; `cargo nextest run --lib test_db_row_to_symbol_unknown_kind_does_not_silently_coerce`; `cargo nextest run --lib test_db_row_to_symbol_unknown_visibility_does_not_silently_drop`; `cargo nextest run --lib test_db_row_to_relationship_unknown_kind_does_not_silently_coerce`; `rg -n "INSERT(?: OR [A-Z]+)? INTO (files|symbols|relationships|identifiers)" src/tests/analysis/change_risk_tests.rs src/tests/analysis/test_linkage_tests.rs src/tests/analysis/linkage_edge_cases_tests.rs src/tests/core/database_row_mapping.rs` | worker-red-green | working-tree on 6c7bf1640646 | pass: exact tests passed; raw setup search now shows only intentionally malformed row-mapping inserts in these files | 2026-05-25T06:33:10Z | no |
| Candidate 12 analysis/database row-helper migration preserves formatting, typecheck, and changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 6c7bf1640646 | pass: changed fell back to dev because `database_row_mapping.rs` has no narrow bucket mapping; 34 buckets passed in 515.6s; diff whitespace check passed | 2026-05-25T06:33:10Z | no |
| Claude CLI reviews Candidate 12 analysis/database row-helper migration | first `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...`; retry with `--fallback-model sonnet` after 529 overload | gate-review | working-tree on 6c7bf1640646 | approve with no code changes recommended; verified analysis file helper parity, row-mapping raw/builder boundary, explicit confidence parity, and ledger accuracy. Non-blocking plan wording nit addressed | 2026-05-25T06:40:15Z | no |
| Candidate 12 hybrid test split preserves moved test discovery, behavior buckets, and changed-file routing | `cargo check --all-targets`; `cargo nextest run --lib hybrid_search_tests`; `cargo nextest run --lib query_classification_tests`; `cargo nextest run -p xtask changed_tests_route_hybrid_search_split_modules_to_hybrid_bucket`; `wc -l src/tests/tools/hybrid_search_tests/*.rs src/tests/tools/query_classification_tests.rs` | worker-red-green | working-tree on a4935099 | pass: hybrid filter 26 tests, query classification filter 11 tests, xtask mapping test verified RED before mapper fix and GREEN after; split file line counts are 264/6/558/298/200 and query classification is 137 | 2026-05-25T07:00:11Z | no |
| Candidate 12 hybrid test split preserves formatting, typecheck, and affected changed-file selection | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on a4935099 | pass: initial changed run exposed unmapped split paths and fell back to dev, 34 buckets in 499.2s; after xtask mapping fix, changed selected `xtask-runner`, `tools-search-hybrid`, and `tools-format-filter`, 3 buckets in 25.8s; final post-review rerun passed the same 3 buckets and diff whitespace check passed | 2026-05-25T07:07:59Z | no |
| Claude CLI reviews Candidate 12 hybrid test split and changed-file mapping | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high --fallback-model sonnet ...` | gate-review | working-tree on a4935099 | approve with no correctness regressions; verified moved test discovery, module declaration, xtask routing, plan/ledger accuracy, and no untracked review blind spots. Cosmetic stale KNN wording in `rrf_merge.rs` was fixed before final gate rerun | 2026-05-25T07:07:59Z | no |
| Candidate 12 line-mode test split preserves moved search behavior and file-size caps | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::tools::search::line_mode -- --skip search_quality`; `cargo xtask test bucket tools-search-line`; `wc -l src/tests/tools/search/line_mode/*.rs`; `git diff --check` | affected-change | working-tree on 4b2bb370 | pass: moved module filter ran 30 tests with 30 passed and 1 non-failing leaky report; tools-search-line bucket passed in 16.0s; split files are 273/480/310/64/657 lines; diff whitespace check passed | 2026-05-25T07:18:04Z | no |
| Candidate 12 line-mode test split preserves changed-file routing | `cargo xtask test changed` | affected-change | working-tree on 4b2bb370 | pass: changed selected only `tools-search-line`; final rerun after Claude ledger edit again selected only `tools-search-line` and passed in 16.8s | 2026-05-25T07:23:31Z | no |
| Claude CLI reviews Candidate 12 line-mode test split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high --fallback-model sonnet ...` | gate-review | working-tree on 4b2bb370 | approve with no correctness regressions; verified 19 test function signatures preserved, helper bodies unchanged and visible to child modules, `line_` changed-file routing, plan/ledger accuracy, and no untracked review blind spots. Response content was useful but not schema-shaped | 2026-05-25T07:22:20Z | no |
| Candidate 12 Tantivy index test split preserves moved tests and file-size caps | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::tools::search::tantivy_index_tests -- --skip search_quality`; `cargo xtask test bucket tools-search-tantivy`; `wc -l src/tests/tools/search/tantivy_index_tests/*.rs`; `git diff --check` | affected-change | working-tree on 43252d0a | pass: initial module filter exposed duplicate language/delete tests from overlapping ranges; after removing duplicates, module filter ran 30 non-ignored tests and passed; tools-search-tantivy passed in 5.0s; split files are 149/8/291/384/189/174/147 lines; diff whitespace check passed | 2026-05-25T07:29:58Z | no |
| Candidate 12 Tantivy index test split preserves changed-file routing | `cargo xtask test changed` | affected-change | working-tree on 43252d0a | pass: changed selected only `tools-search-tantivy`; final rerun after Claude ledger edit selected only `tools-search-tantivy` and passed in 4.9s | 2026-05-25T07:37:27Z | no |
| Claude CLI reviews Candidate 12 Tantivy index test split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high --fallback-model sonnet ...` | gate-review | working-tree on 43252d0a | approve with no correctness regressions; verified all 31 original test functions preserved with no duplicates, ignored tokenizer-mismatch test intact, `tantivy_` changed-file routing, plan/ledger accuracy, and no untracked review blind spots. Response content was useful but not schema-shaped | 2026-05-25T07:33:36Z | no |
| Candidate 12 Tantivy path-prior test split preserves moved tests and file-size caps | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::tools::search::tantivy_path_prior_tests -- --skip search_quality`; `cargo xtask test bucket tools-search-tantivy`; `wc -l src/tests/tools/search/tantivy_path_prior_tests/*.rs`; `git diff --check` | affected-change | working-tree on 8a55dd41 | pass: initial check exposed two dropped `#[test]` attributes from range boundaries; after fixing, focused module filter ran all 34 tests and passed; tools-search-tantivy passed in 4.7s; split files are 114/126/271/35/232/267/52 lines; diff whitespace check passed | 2026-05-25T07:44:36Z | no |
| Candidate 12 Tantivy path-prior test split preserves changed-file routing | `cargo xtask test changed` | affected-change | working-tree on 8a55dd41 | pass: changed selected only `tools-search-tantivy` for the deleted flat file and all split child modules; renamed the ignored `test_intent.rs` child to `intent.rs` so every new file is visible without forced add; final rerun after Claude ledger edit ignored the plan doc and passed in 4.7s | 2026-05-25T07:49:19Z | no |
| Claude CLI reviews Candidate 12 Tantivy path-prior test split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high --fallback-model sonnet ...` | gate-review | working-tree on 8a55dd41 | approve with no findings; verified all 34 original `#[test]` functions preserved exactly once, helper/module visibility, `intent.rs` git visibility and `.gitignore` avoidance, `tantivy_` changed-file routing, and plan/ledger accuracy. Response content was useful but not schema-shaped | 2026-05-25T07:48:07Z | no |
| Candidate 11/13 watcher filtering test extraction preserves filtering behavior and runner routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::integration::watcher_filtering -- --skip search_quality`; `cargo nextest run -p xtask changed_tests_route_watcher_filtering_to_core_fast_bucket manifest_contract_tests_checked_in_manifest_uses_exact_bucket_specs`; `cargo xtask test bucket core-fast`; `cargo xtask test bucket xtask-runner`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on edcc9595 | pass: moved 13 inline watcher filtering tests to `src/tests/integration/watcher_filtering.rs`; production `src/watcher/filtering.rs` is now 240 lines and moved test file is 596 lines; first changed run exposed stale `core-fast` filter, second exposed checked-manifest drift, both root causes fixed; final changed gate fell back to dev because `src/tests/mod.rs` changed and passed 34 buckets in 502.0s | 2026-05-25T08:20:16Z | no |
| Claude CLI reviews Candidate 11/13 watcher filtering test extraction | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high --fallback-model sonnet ...` | gate-review | working-tree on edcc9595 | approve with no findings; verified all 13 inline tests moved exactly once with attributes/assertions preserved, production filtering code only lost the test module, `src/tests/mod.rs` registration/stale-comment removal, `core-fast` route and manifest consistency, and plan/ledger accuracy. Response content was useful but not schema-shaped | 2026-05-25T08:23:31Z | no |
| Candidate 13 small dead-code cleanup preserves line-mode, daemon, search-reranker, formatting, and affected search-quality behavior | `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib stage_counts_zero_on_happy_path`; `cargo nextest run --lib every_line_match_satisfies_the_caller_filters_primary`; `cargo nextest run --lib test_reranker_source_language_phrase_bonus_requires_non_docs_role`; `cargo xtask test changed` | affected-change + dogfood | working-tree on b1d3ce09d261 | pass: check clean after deleting newly-exposed line-output dead branch; exact tests passed; changed fell back to dev due unmapped shared tool files and passed 35 buckets including daemon and search-quality in 652.5s | 2026-05-25T08:54:00Z | no |
| Claude CLI reviews Candidate 13 small dead-code cleanup | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --effort high ...` | gate-review | working-tree on b1d3ce09d261 | approve with no findings; verified removed symbols had no production callers, line-output coverage was private unreachable behavior, daemon fields/helpers are live, helper wiring is behavior-preserving, and no stale references/routing gaps remain | 2026-05-25T08:54:00Z | no |
| Candidate 12 test-quality split preserves pipeline integration and body-analysis behavior while bringing the source file under the test-file cap | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_pipeline_integration_updates_metadata`; `cargo nextest run --lib test_pipeline_integration_with_identifier_evidence`; `cargo nextest run --lib test_comment_assertions_not_counted`; `cargo xtask test bucket analysis`; `cargo xtask test changed`; `wc -l src/tests/analysis/test_quality_tests.rs src/tests/analysis/quality_pipeline_tests.rs src/tests/analysis/quality_body_analysis_tests.rs` | affected-change | working-tree on 845248be3449 | pass: exact moved tests passed; analysis bucket passed; changed selected only analysis and passed in 1.0s; line counts are 981/257/89 | 2026-05-25T09:05:03Z | no |
| Claude CLI reviews Candidate 12 test-quality split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --fallback-model sonnet --effort high ...` | gate-review | working-tree on 845248be3449 | approve with one minor documentation finding; verified moved test equivalence, metadata `HashMap` semantics, module registration, and gitignore-safe filenames. Corrected line-count row/body from 246 to 257 after review | 2026-05-25T09:12:17Z | no |
| Candidate 12 deep-dive split preserves moved tests, module wiring, file-size caps, and changed-file routing | `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_deep_dive_accepts_symbol_name_alias`; `cargo nextest run --lib test_header_shows_file_line_kind`; `cargo nextest run --lib test_find_symbol_by_name`; `cargo nextest run --lib test_identifier_fallback_adds_refs`; `cargo nextest run --lib test_full_depth_output_under_token_budget`; `cargo nextest run -p xtask changed_tests_deep_dive_split_modules_route_to_deep_dive_bucket`; `cargo xtask test bucket tools-deep-dive`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/deep_dive_tests/*.rs src/tests/tools/deep_dive_tests/*/*.rs || true`; `wc -l src/tests/tools/deep_dive_tests.rs src/tests/tools/deep_dive_tests/*.rs src/tests/tools/deep_dive_tests/*/*.rs` | affected-change | working-tree on 9777dc930cc7 | pass: exact moved tests passed; xtask route test passed; `tools-deep-dive` passed; `changed` selected `xtask-runner` and `tools-deep-dive` and passed in 13.1s; final rerun after review/ledger edits ignored the plan doc and passed in 12.5s; no new file is ignored; split files are 14/17/68/102/224/231/319/567/690/850 lines; read-only subagent audit verified 65 original test functions preserved exactly once | 2026-05-25T09:27:22Z | no |
| Claude CLI reviews Candidate 12 deep-dive split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --fallback-model sonnet --effort high ...` | gate-review | working-tree on 9777dc930cc7 | approve with no findings; verified 65 original tests preserved exactly once, parent/private helper visibility via `use super::*`, root and child module registration, narrow `tools-deep-dive` routing for split paths, and plan/ledger line-count accuracy. Response content was useful but not schema-shaped | 2026-05-25T09:31:30Z | no |
| Candidate 12 blast-radius determinism split preserves walk/ranking/rendering tests and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_file_path_seeds_filter_noisy_structural_symbols_but_symbol_ids_are_exact`; `cargo nextest run --lib test_rank_impacts_prioritizes_and_labels_extends_relationships`; `cargo nextest run --lib test_walk_impacts_identifier_edges_choose_strongest_kind_without_replacing_relationships`; `cargo nextest run --lib test_blast_radius_surfaces_identifier_only_callers`; `cargo xtask test bucket tools-blast-spillover`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/blast_radius_determinism_tests/*.rs || true`; `wc -l src/tests/tools/blast_radius_determinism_tests.rs src/tests/tools/blast_radius_determinism_tests/*.rs` | affected-change | working-tree on a444ac1478b0 | pass: representative exact tests passed across seed, relationship, walk-semantics, and rendering modules; `tools-blast-spillover` passed in 2.7s; after the review-note rename to `walk_semantics`, `cargo fmt --check`, `cargo check --all-targets`, the walk-semantics exact test, `tools-blast-spillover`, and `changed` passed again; final `changed` selected only `tools-blast-spillover` and ignored the plan doc; no new child file is ignored; split files are 247/181/385/191/91 lines; read-only subagent review approved the candidate and grouping | 2026-05-25T09:41:15Z | no |
| Claude CLI reviews Candidate 12 blast-radius determinism split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model opus --fallback-model sonnet --effort high ...`; retried directly with `--model sonnet` after Opus 529 overload | gate-review | working-tree on a444ac1478b0 | approve with no required findings; verified 11 tests preserved exactly once, module wiring, parent/private helper visibility, `src/tests/tools/blast_radius` routing, git-visible child files, line counts, and plan accuracy. Non-blocking grouping note addressed by renaming `identifier_walk.rs` to `walk_semantics.rs`; documentation evidence note addressed in this row | 2026-05-25T09:52:33Z | no |
| Candidate 12 rewrite-symbol stateful/prepared split preserves moved tests and editing routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_rewrite_symbol_uses_current_primary_db_after_rebind`; `cargo nextest run --lib test_rewrite_symbol_keeps_primary_binding_snapshot_across_swap_window`; `cargo nextest run --lib test_prepared_rewrite_drives_metrics_and_dry_run_output`; `cargo nextest run --lib test_prepared_rewrite_rejects_changed_target_before_commit`; `cargo nextest run --lib test_rewrite_symbol_replace_body_via_live_parse`; `cargo nextest run --lib test_rewrite_symbol_rejects_stale_index`; `cargo nextest run --lib test_rewrite_symbol_file_path_filter_uses_suffix_not_substring`; `cargo xtask test bucket tools-editing`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/editing/rewrite_symbol_tests/*.rs || true`; `wc -l src/tests/tools/editing/rewrite_symbol_tests.rs src/tests/tools/editing/rewrite_symbol_tests/*.rs` | affected-change | working-tree on 473256813c03 | pass: four moved stateful/prepared tests passed under `rewrite_symbol_tests::stateful`; retained live-edit/file-path smoke tests passed in the root module; `tools-editing` passed in 23.9s; `changed` selected only `tools-editing` and passed in 24.2s; final rerun after review/ledger edits ignored the plan doc and passed in 24.6s; no new child file is ignored; line counts are 838/350; 27 test attributes preserved | 2026-05-25T10:00:39Z | no |
| Claude CLI reviews Candidate 12 rewrite-symbol stateful/prepared split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 473256813c03 | approve with no required findings; verified 23 root tests + 4 child tests = 27 preserved exactly once, `mod stateful` discovery, parent-private helper access via `use super::*`, child-scoped `poll`/`Arc` imports, existing `src/tests/tools/editing/` routing, and plan/ledger accuracy | 2026-05-25T10:07:40Z | no |
| Candidate 12 embedding metadata enrichment split preserves moved tests, module wiring, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_extract_doc_excerpt_jsdoc_block`; `cargo nextest run --lib test_select_budgeted_variables_penalizes_default_values_across_signature_styles`; `cargo nextest run --lib test_select_budgeted_variables_excludes_test_variables`; `cargo nextest run --lib test_prepare_batch_enriches_struct_with_field_signatures`; `cargo xtask test bucket core-embeddings`; `cargo xtask test changed`; `git check-ignore -v src/tests/core/embedding_metadata_enrichment/tests/*.rs || true`; `wc -l src/tests/core/embedding_metadata_enrichment.rs src/tests/core/embedding_metadata_enrichment/tests/*.rs`; `git diff --check` | affected-change | working-tree on 5be2ea71d6f4 | pass: representative exact tests passed across doc excerpt, variable budgeting, symbol filtering, and batch enrichment modules; initial check exposed Rust inline-module path resolution and ignored `test_symbol_filtering.rs`, both fixed before final gates; `core-embeddings` passed in 4.3s; `changed` selected only `core-embeddings` and passed in 4.3s; no new child file is ignored; split files are 59/365/95/104/544 lines | 2026-05-25T10:22:53Z | no |
| Claude CLI reviews Candidate 12 embedding metadata enrichment split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 5be2ea71d6f4 | approve with no required findings; verified 42 tests preserved exactly once, inline `mod tests` child path resolution, parent/private helper visibility, git-visible child filenames, `core-embeddings` changed-file routing, file-size caps, and plan/ledger accuracy. Non-blocking placement note for `test_select_budgeted_variables_excludes_test_variables` accepted because the tested behavior is symbol exclusion | 2026-05-25T10:26:54Z | no |
| Candidate 12 workspace discovery split preserves moved tests, module wiring, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_analyze_vendor_patterns_does_not_flag_packages_directory`; `cargo nextest run --lib test_generate_julieignore_file_creates_file_with_correct_format`; `cargo nextest run --lib test_discover_indexable_files_respects_gitignore`; `cargo nextest run --lib test_should_index_file_rejects_blacklisted_filenames`; `cargo nextest run --lib test_should_index_file_rejects_paths_under_julie_home`; `cargo nextest run --lib tests::tools::workspace::discovery -- --skip search_quality`; `cargo xtask test bucket tools-workspace`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/workspace/discovery/*.rs || true`; `wc -l src/tests/tools/workspace/discovery.rs src/tests/tools/workspace/discovery/*.rs`; `git diff --check` | affected-change | working-tree on a9663f271291 | pass: representative exact tests passed across vendor, julieignore, discovery, filtering, and daemon-home modules; module filter ran all 40 discovery tests, then reran after restoring the original empty-file helper body; initial check exposed a guessed `ManageWorkspaceTool` helper and was fixed by restoring the original field set from HEAD; `tools-workspace` passed in 30.2s; final `changed` after the plan/helper edits selected only `tools-workspace` and passed in 30.0s; no new child file is ignored; split files are 53/102/141/208/174/368 lines | 2026-05-25T10:44:31Z | no |
| Claude CLI reviews Candidate 12 workspace discovery split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on a9663f271291 | approve with no required findings; verified 40 tests preserved exactly once, root helper/module wiring, parent/private helper visibility, git-visible child filenames, `tools-workspace` changed-file routing, file-size caps, and plan/ledger accuracy. Non-blocking helper-content note was addressed by restoring the original empty-file helper body | 2026-05-25T10:42:04Z | no |
| Candidate 13 extractor placeholder cleanup removes no-op tests while preserving extractor gates | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run -p julie-extractors --lib tests::python::extractor -- --skip search_quality`; `cargo nextest run -p julie-extractors --lib tests::python::decorators -- --skip search_quality`; `cargo nextest run -p julie-extractors --lib tests::python::relationships -- --skip search_quality`; `cargo nextest run -p julie-extractors --lib tests::ruby::extractor -- --skip search_quality`; `cargo xtask test bucket extractors`; `cargo xtask test changed`; `rg -n "This test is placeholder|fn test_.*placeholder|fn test_placeholder|assert!\\(true\\)" crates/julie-extractors/src/tests/python crates/julie-extractors/src/tests/ruby -g '*.rs' || true`; `git diff --check` | affected-change | working-tree on f9e8ff00c276 | pass: placeholder search returned no matches after removing seven no-op Python placeholders and the empty assignments module, Ruby extractor creation now asserts `base().file_path`, exact Python/Ruby module filters passed, extractor bucket passed in 25.3s, changed selected only `extractors` and passed in 20.4s | 2026-05-25T10:50:14Z | no |
| Claude CLI reviews Candidate 13 extractor placeholder cleanup | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on f9e8ff00c276 | approve with no required findings; verified deleted Python assignments module has no dangling declaration, no substantive Python extractor/decorator/relationship tests were removed, Ruby constructor test now has a meaningful `base().file_path` assertion, and plan/ledger state is accurate. Non-blocking copied-check-ignore note was a misread of the adjacent workspace-discovery row | 2026-05-25T10:54:36Z | no |
| Candidate 12 workspace mod_tests split preserves moved tests, include wiring, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_startup_noop_repair_does_not_mark_catchup_active_while_planning -- --skip search_quality`; `cargo nextest run --lib test_refresh_treats_semantic_version_drift_as_full_reindex -- --skip search_quality`; `cargo nextest run --lib test_incremental_indexing_detects_empty_database -- --skip search_quality`; `cargo xtask test bucket tools-workspace`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/workspace/mod_tests/part6.rs src/tests/tools/workspace/mod_tests/part7.rs || true`; `wc -l src/tests/tools/workspace/mod_tests/part5.rs src/tests/tools/workspace/mod_tests/part6.rs src/tests/tools/workspace/mod_tests/part7.rs src/tests/tools/workspace/mod_tests.rs`; `git diff --check` | affected-change | working-tree on 4b1f1b8a473b | pass: representative exact tests passed across retained part5, moved part6, and moved part7 sections; `tools-workspace` passed in 30.2s; `changed` selected only `tools-workspace` and passed in 30.1s, then final rerun after plan/review edits ignored the plan doc and passed in 30.4s; no new child file is ignored; split files are 376/393/385 lines plus a 7-line include root; all 14 original test functions remain present exactly once | 2026-05-25T11:04:25Z | no |
| Claude CLI reviews Candidate 12 workspace mod_tests split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 4b1f1b8a473b | approve with no findings; verified untracked `part6.rs`/`part7.rs`, all 14 original test functions preserved exactly once, split-boundary comments/attributes intact, new files not ignored, line counts below cap, include wiring, `tools-workspace` routing, and plan/ledger accuracy. Response content was useful but not schema-shaped because Claude returned an evidence object outside the requested schema | 2026-05-25T11:08:54Z | no |
| Candidate 13 handler workspace root removal preserves session-root behavior and broad handler-dependent gates | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib handler_construction_sets_workspace_root`; `cargo nextest run --lib test_handler_uses_provided_workspace_root`; `cargo nextest run --lib test_loaded_workspace_swap_intent_invalidates_loaded_and_current_state_before_async_gap`; `rg -n "handler\\.workspace_root|pub\\(crate\\) workspace_root|workspace_root: workspace_root\\.clone\\(\\)" src/handler.rs src/tests/core/handler.rs src/tests/core/workspace_init.rs || true`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 97e644f06ee1 | pass: direct stale-field search returned no matches; exact tests passed, including the session-root fallback assertion updated to compare against `workspace_startup_hint().path`; changed fell back to dev because `src/handler.rs` is shared infrastructure and passed 35 buckets in 550.4s; diff whitespace check passed | 2026-05-25T11:25:46Z | no |
| Claude CLI reviews Candidate 13 handler workspace root removal | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 97e644f06ee1 | approve with no findings; verified the handler field and constructor initializers are gone, remaining `workspace_root` names belong to other structs, test replacements preserve new-for-test temp-root, explicit `new(workspace_root)` fallback, and swap-gap startup-hint fallback invariants, and plan/ledger text is accurate. Response content was useful but not schema-shaped because Claude returned an evidence object outside the requested schema | 2026-05-25T11:30:39Z | no |
| Candidate 13 primary database/search-index helper removal preserves stale-index upgrade behavior | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib test_primary_workspace_repair_plan_reports_semantic_version_changed`; `cargo nextest run --lib test_v690_upgrade_preserves_existing_tantivy_docs_on_first_edit`; `rg -n "primary_database_and_search_index" src/handler.rs src/tests/integration/stale_index_detection.rs src/tests -g '*.rs' || true`; `cargo xtask test changed`; `git diff --check` | affected-change | working-tree on 448af5c3e7c4 | pass: legacy helper search returned no matches after migrating stale-index tests to pooled DB accessors; exact stale-index tests passed; changed fell back to dev because `src/handler.rs` is shared infrastructure and also ran integration for `stale_index_detection`, passing 35 buckets in 647.8s; diff whitespace check passed | 2026-05-25T11:47:33Z | no |
| Claude CLI reviews Candidate 13 primary database/search-index helper removal | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 448af5c3e7c4 | approve with no findings; verified zero stale helper refs, primary DB version writes remain visible before repair planning, v6.9 upgrade test still checks Tantivy docs through the loaded workspace search index without writer-lock conflict, pooled `SymbolDatabase` sees canonical revision and `alpha_new`, and plan/ledger text is accurate. Response wrapped the schema JSON in explanatory text plus an evidence table | 2026-05-25T11:55:17Z | no |
| Candidate 12 daemon roots split preserves moved roots tests, helper visibility, file-size caps, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::daemon::roots`; `cargo xtask test changed`; `wc -l src/tests/daemon/roots.rs src/tests/daemon/roots/*.rs` | affected-change | working-tree on c51345e8 | pass: roots module filter ran 25 platform-runnable tests with 25 passed and one existing nextest leak note; changed selected only daemon and passed in 61.4s; line counts are 149/287/539/762/754/601, all below the test-file cap | 2026-05-25T12:04:13Z | no |
| Claude CLI reviews Candidate 12 daemon roots split and plan update | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on c51345e8 | approve with no findings; verified 30 tests preserved exactly once, helper visibility through child modules, no ignored child files, file-size caps, daemon changed-file routing, plan no longer lists roots over-cap, and dashboard intelligence raw-SQL candidate evidence | 2026-05-25T12:11:59Z | no |
| Candidate 12 dashboard intelligence fixture migration preserves dashboard analytics behavior and removes ordinary raw symbol/relationship setup | `cargo fmt`; `cargo check --all-targets`; `cargo nextest run --lib tests::dashboard::intelligence`; `cargo xtask test changed`; `rg -n "INSERT INTO (symbols|relationships)|INSERT INTO symbols|INSERT INTO relationships" src/tests/dashboard/intelligence.rs || true`; `rg -n "INSERT INTO files" src/tests/dashboard/intelligence.rs` | affected-change | working-tree on 4ea02906 | pass: exact dashboard intelligence module passed 25 tests; changed selected dashboard only and passed in 12.2s; raw symbol/relationship setup search returned no matches; two raw file inserts remain for NULL line-count and empty-language aggregate edge cases | 2026-05-25T12:17:35Z | no |
| Claude CLI reviews Candidate 12 dashboard intelligence fixture migration | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 4ea02906 | approve with no blocking findings; verified row semantics, reference_score preservation, inert span/byte fields for dashboard analytics, intentional remaining raw file edge cases, and plan evidence. Low documentation/span-comment nits were addressed before commit | 2026-05-25T12:24:03Z | no |
| Candidate 12 global-targeting split preserves moved tests, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::tools::workspace::global_targeting -- --skip search_quality`; `cargo nextest run -p xtask changed_tests_route_global_targeting_split_modules_to_targeting_bucket`; `cargo xtask test bucket tools-workspace-targeting`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/workspace/global_targeting/*.rs || true`; `wc -l src/tests/tools/workspace/global_targeting.rs src/tests/tools/workspace/global_targeting/*.rs`; `find src/tests -name '*.rs' -print0 | xargs -0 wc -l | awk '$1 > 1000 {print}'`; `git diff --check` | affected-change | working-tree on 1d51e240 | pass: module filter ran 40 tests with 40 passed and 2 non-failing nextest LEAK notes; route test verified RED before mapper fix and GREEN after; targeting bucket passed in 35.7s; final changed rerun after the Claude review fix selected `xtask-runner` plus `tools-workspace-targeting` and passed in 47.8s; no child files are ignored; split line counts are 153/239/397/125/477/570/147/525/153/165; remaining over-cap scan no longer includes `global_targeting.rs`; format and diff whitespace checks passed | 2026-05-25T12:39:22Z | no |
| Claude CLI reviews Candidate 12 global-targeting split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 1d51e240 | approve with one low finding; verified 40 tests preserved exactly once, helper visibility, `#[serial]` placement, git-visible child names, targeting prefix routing, and plan/ledger accuracy. Finding fixed by changing the routing test path from fictional `activation.rs` to real `target_activation.rs`, then rerunning the exact xtask test | 2026-05-25T12:42:42Z | no |
| Candidate 12 target-workspace fast_refs split preserves moved tests, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::tools::target_workspace_fast_refs_tests -- --skip search_quality`; `cargo nextest run -p xtask changed_tests_route_target_workspace_fast_refs_split_modules_to_fast_refs_bucket`; `cargo xtask test bucket tools-fast-refs`; `cargo xtask test changed`; `git check-ignore -v src/tests/tools/target_workspace_fast_refs_tests/tests/*.rs || true`; `wc -l src/tests/tools/target_workspace_fast_refs_tests.rs src/tests/tools/target_workspace_fast_refs_tests/tests/*.rs`; `find src/tests -name '*.rs' -print0 | xargs -0 wc -l | awk '$1 > 1000 {print}'`; `git diff --check` | affected-change | working-tree on 168f6382 | pass: module filter ran 16 tests with 16 passed; route test verified RED before mapper fix and GREEN after; `tools-fast-refs` passed in 1.7s; final `changed` selected `xtask-runner` plus `tools-fast-refs` and passed in 12.5s; no child files are ignored; split line counts are 343/394/158/259/161/209; remaining over-cap scan no longer includes `target_workspace_fast_refs_tests.rs`; format and diff whitespace checks passed | 2026-05-25T12:56:20Z | no |
| Claude CLI reviews Candidate 12 target-workspace fast_refs split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 168f6382 | approve with no defects; verified 16 tests preserved exactly once, nested module pathing under the existing private `tests` module, `use super::*` helper visibility, gitignore-safe child names, `tools-fast-refs` prefix routing, and plan/ledger accuracy. Response content was useful but not schema-shaped | 2026-05-25T13:00:27Z | no |
| Candidate 12 dashboard integration split preserves moved tests, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::dashboard::integration -- --skip search_quality`; `cargo xtask test bucket dashboard`; `cargo xtask test changed`; `git check-ignore -v src/tests/dashboard/integration/*.rs || true`; `wc -l src/tests/dashboard/integration.rs src/tests/dashboard/integration/*.rs`; `find src/tests -name '*.rs' -print0 | xargs -0 wc -l | awk '$1 > 1000 {print}'`; `git diff --check` | affected-change | working-tree on dfa22f7d | pass: module filter ran 22 tests with 22 passed; dashboard bucket passed in 12.5s; changed selected only `dashboard` and passed in 11.6s; no child files are ignored; split line counts are 401/317/158/231/222; remaining over-cap scan no longer includes `dashboard/integration.rs`; format and diff whitespace checks passed | 2026-05-25T13:06:19Z | no |
| Claude CLI reviews Candidate 12 dashboard integration split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on dfa22f7d | approve with no defects; verified 22 tests preserved exactly once, child module pathing and `use super::*` helper visibility, gitignore-safe child names, dashboard bucket routing, plan/ledger accuracy, and no behavior/test-discovery risks. Local `wc -l` recheck confirmed the parent is 401 lines after review noted a possible cosmetic count mismatch | 2026-05-25T13:11:41Z | no |
| Candidate 12 daemon database workspace-status split preserves moved tests, file-size caps, git visibility, and changed-file routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run --lib tests::daemon::database::tests::workspace_status -- --skip search_quality`; `cargo nextest run --lib tests::daemon::database -- --skip search_quality`; `cargo xtask test bucket daemon`; `cargo xtask test changed`; `git check-ignore -v src/tests/daemon/database/tests/workspace_status.rs || true`; `wc -l src/tests/daemon/database.rs src/tests/daemon/database/tests/workspace_status.rs`; `find src/tests -name '*.rs' -print0 | xargs -0 wc -l | awk '$1 > 1000 {print}'`; `git diff --check` | affected-change | working-tree on 2ce68141 | pass: workspace-status filter ran 5 tests with 5 passed; daemon database module ran 34 tests with 34 passed; daemon bucket passed in 61.4s; changed selected only daemon and passed in 61.5s; no child file is ignored; split line counts are 934/143; remaining over-cap scan no longer includes `daemon/database.rs`; format and diff whitespace checks passed | 2026-05-25T13:22:46Z | no |
| Claude CLI reviews Candidate 12 daemon database workspace-status split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on 2ce68141 | approve with no defects; verified five moved tests preserved exactly once, child module pathing under the private `tests` module, `use super::*` helper visibility, gitignore-safe child filename, daemon changed-file routing, over-cap inventory, and plan/ledger accuracy. Response content was useful but not schema-shaped | 2026-05-25T13:25:53Z | no |
| Candidate 12 HTTP transport restart-pending split preserves moved test, file-size caps, git visibility, and transport routing | `cargo fmt`; `cargo fmt --check`; `cargo check --all-targets`; `cargo nextest run -p xtask changed_tests_routes_http_transport_paths_to_transport_bucket`; `cargo nextest run --lib test_http_julie_session_rejects_new_sessions_after_restart_pending_with_active -- --skip search_quality`; `cargo nextest run --lib tests::daemon::http_transport -- --skip search_quality`; `cargo xtask test bucket transport`; `cargo xtask test changed`; `git check-ignore -v src/tests/daemon/http_transport/tests/restart_pending.rs || true`; `wc -l src/tests/daemon/http_transport.rs src/tests/daemon/http_transport/tests/restart_pending.rs`; `find src/tests -name '*.rs' -print0 | xargs -0 wc -l | awk '$1 > 1000 {print}'`; `git diff --check` | affected-change | working-tree on df46c781 | pass: route test failed RED with `transport, daemon`, then passed after mapper prefix; exact moved test passed; HTTP transport module ran 18 tests with 18 passed; transport bucket passed in 3.4s; changed selected `xtask-runner` plus `transport` and passed in 20.1s; no child file is ignored; split line counts are 895/110; remaining over-cap scan no longer includes `daemon/http_transport.rs` | 2026-05-25T13:32:59Z | no |
| Claude CLI reviews Candidate 12 HTTP transport restart-pending split | `claude -p --no-session-persistence --dangerously-skip-permissions --output-format json --json-schema ... --tools "Read,Bash" --model sonnet --effort high ...` | gate-review | working-tree on df46c781 | approve with no defects; verified moved test preserved exactly once, child module pathing under the private `tests` module, `use super::*` helper visibility, child module test-only inheritance, gitignore-safe child filename, transport prefix routing, route test coverage, over-cap inventory, line counts, and plan/ledger accuracy. Response content was useful but not schema-shaped | 2026-05-25T13:35:47Z | no |
