# Tree-Sitter Review Findings Status

Reviewed against `main` at `0bcc7c48` on 2026-05-07.

> **Note (2026-06-02):** The parser inventory, certification, golden fixtures, and capability tests have migrated to the external [`anortham/julie-extractors`](https://github.com/anortham/julie-extractors) repo. Source paths in the Evidence column below that start with `crates/julie-extractors/...` now live there. All findings recorded here were closed before the migration.

This file tracks concrete review findings that were raised during the v7.8.0 tree-sitter review. The point is to keep these out of chat-only memory. Generated certification reports show capability gaps, but they do not cover every storage, ID, span, or performance defect below.

## Current Findings

| ID | Severity | Finding | Current status | Evidence | Closure |
| --- | --- | --- | --- | --- | --- |
| TS-RF-001 | Critical | Resolved pending relationship IDs can collapse multiple callsites from the same caller to the same callee. | Closed. Resolved relationship IDs now include pending file path and line number in addition to caller, target, and kind, so same caller/callee calls on different lines do not collide. | `src/tools/workspace/indexing/resolver.rs`, `src/tests/tools/workspace/resolver.rs`, `src/tests/core/batch_resolver.rs` | Verified by `test_build_resolved_relationship_keeps_distinct_callsites_distinct` and `test_resolve_batch_keeps_same_caller_target_calls_distinct_by_line`. |
| TS-RF-002 | High | Embedded HTML and Vue symbols get remapped spans but may keep stale span-derived IDs. | Closed. Embedded HTML script/style and Vue style remaps refresh span-derived IDs. HTML embedded script remapping also rekeys child `parent_id` references after host offsets are applied. Shared normalization coverage still proves broader symbol, relationship, identifier, structured pending, and type-row rekeying. | `crates/julie-extractors/src/html/scripts.rs`, `crates/julie-extractors/src/vue/style.rs`, `crates/julie-extractors/src/base/results_normalization.rs`, `crates/julie-extractors/src/tests/html/script_style.rs`, `crates/julie-extractors/src/tests/path_identity.rs`, `crates/julie-extractors/src/tests/vue/mod.rs` | Verified by `test_html_inline_script_offset_rekeys_child_parent_ids`, `test_vue_style_delegates_to_css_extractor_with_offsets`, `test_rekey_normalized_locations_preserves_distinct_same_start_symbols_and_type_rows`, and `test_symbol_ids_do_not_collide_for_same_row_column_different_spans`. |
| TS-RF-003 | High | Vue identifier extraction reparses the SFC per identifier and leaves byte ranges section-relative. | Closed. Vue identifier extraction now parses the SFC once per extraction pass, carries the current script section offset through identifier creation, writes file-relative byte and line spans, and computes containing symbols from the adjusted host span. | `crates/julie-extractors/src/vue/identifiers.rs`, `crates/julie-extractors/src/tests/vue/script_setup.rs`, `crates/julie-extractors/src/tests/vue/mod.rs` | Verified by `test_vue_identifier_offsets_use_the_current_script_section`, `test_vue_identifier_extraction_parses_script_section_once`, and `test_vue_script_setup_callsite_ids_are_file_relative`. |
| TS-RF-004 | High | Vue `<script setup>` line positions need exact file-relative coverage. | Closed. Script setup variables, imports, function declarations, and standalone macro symbols now have exact file-relative byte, line, column, and ID assertions. | `crates/julie-extractors/src/vue/script_setup.rs`, `crates/julie-extractors/src/tests/vue/script_setup.rs` | Verified by `tests::vue::script_setup`, including `test_script_setup_line_numbers_are_file_relative`, `test_script_setup_define_props_and_emits`, `test_script_setup_imports`, and `test_script_setup_function_declarations`. |
| TS-RF-005 | High | HTML embedded body offset detection can bind to matching attribute text. | Closed. Embedded body offsets now come from the embedded text node's tree-sitter bounds, so duplicated attribute text no longer steals the span. | `crates/julie-extractors/src/html/scripts.rs`, `crates/julie-extractors/src/tests/html/script_style.rs` | Verified by `test_html_inline_script_and_style_offsets_ignore_attribute_collisions`, which checks both byte and line/column mapping for duplicate script/style bodies. |
| TS-RF-006 | Medium | SQL view-source and trigger-target relationships are not implemented. | Closed. SQL now emits same-file `view_source` relationships from views to source tables and `trigger_target` relationships from triggers to target tables. SQL still records pending cross-file FK support as a separate `pending_relationships` gap. | `crates/julie-extractors/src/sql/relationships.rs`, `crates/julie-extractors/src/sql/schema_relationships.rs`, `crates/julie-extractors/src/tests/sql/relationships.rs`, `fixtures/extraction/sql/basic/expected.json`, `fixtures/extraction/capabilities.json`, `docs/LANGUAGE_CERTIFICATION_REPORT.md` | Verified by `test_sql_view_and_trigger_relationships_target_real_tables`, `tests::sql::relationships`, and SQL golden fixture evidence for `view_source` and `trigger_target`. |
| TS-RF-007 | Medium | Regex fallback symbols use the root node span. | Closed. Fallback lookaround and unicode-property extraction now synthesizes `NormalizedSpan` values from source text match ranges and creates fallback symbols from those spans instead of the root parser node. | `crates/julie-extractors/src/regex/mod.rs`, `crates/julie-extractors/src/regex/patterns.rs`, `crates/julie-extractors/src/tests/regex/task15.rs` | Verified by `test_regex_constructs_have_distinct_symbol_kinds`, which asserts exact byte and line spans for fallback lookaround and unicode-property symbols. |
| TS-RF-008 | Medium | Capability gates can still represent target behavior as an open gap instead of executable evidence. | Closed for this review scope. The SQL `relationships` gap is now removed only after executable golden evidence proves `view_source` and `trigger_target`, and the capability matrix has a regression that fails if that stale SQL gap returns. Other unrelated capability gaps remain documented release debt. | `crates/julie-extractors/src/tests/capability_matrix.rs`, `fixtures/extraction/capabilities.json`, `fixtures/extraction/sql/basic/expected.json`, `docs/LANGUAGE_CERTIFICATION_REPORT.md` | Verified by `capability_matrix_sql_relationship_gap_closes_with_view_and_trigger_evidence` and the tree-sitter certification report. |

## Already Tracked Elsewhere

- SQL pending-relationship gaps appear in `docs/LANGUAGE_CERTIFICATION_REPORT.md`.
- HTML and regex pending-relationship exceptions appear in `docs/LANGUAGE_CERTIFICATION_REPORT.md`.
- Vue remediation history appeared in the now-removed historical findings bundle and still appears in `docs/plans/2026-05-06-tree-sitter-extractor-audit-remediation.md`, but the exact Vue status rows above were not previously captured as current review findings.

## Verification Rule

Do not close a row here from memory. Close it only when the fix has:

- A focused regression test that fails before the fix and passes after it.
- The relevant golden or certification evidence updated when the finding affects reported extractor capability.
- A link to the commit or plan ledger entry that records the verification.
