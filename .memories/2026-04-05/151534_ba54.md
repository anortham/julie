---
id: checkpoint_ba54af99
timestamp: 2026-04-05T15:15:34.824Z
tags:
  - code-review
  - bug-fix
  - edit-tools
  - watcher
  - embeddings
  - freshness
  - dedup
  - per-workspace
  - web-research
  - validation
  - integration-tests
  - cross-review
  - codex
git:
  branch: main
  commit: 92baedac
  files:
    - docs/superpowers/plans/2026-04-05-code-review-fixes.md
summary: "Complete: All 7 Code Review Findings Fixed + Codex Cross-Review"
type: checkpoint
impact: All 7 freshness/correctness gaps in edit tools, watcher, and embeddings
  are now closed. edit_symbol is safe to use under stale indexes, concurrent
  workspaces don't interfere with each other's embeddings, and the web-research
  skill no longer leaks full pages into context.
evidence:
  - "cargo xtask test dev: 8 buckets passed (243s) - run after final fixes"
  - "cargo check: zero errors"
  - 13 edit_symbol tests pass (9 unit + 4 integration)
  - "Codex GPT-5.4 review: 4 findings, 3 fixed, 1 acknowledged"
symbols:
  - EditSymbolTool
  - check_file_freshness
  - check_if_indexing_needed
  - IncrementalIndexer::start_watching
  - spawn_workspace_embedding
  - check_bracket_balance
  - EditFileTool
next: Consider running cargo xtask test full for broader pre-merge confidence.
  The remaining TODO item is dry-run diff truncation for very large edits.
---

## Complete: All 7 Code Review Findings Fixed + Codex Cross-Review

### Summary

External model reviewed recent Julie changes and found 7 issues across edit tools, watcher, embeddings, and web-research skill. All validated against source code, planned, implemented via subagent-driven development, and cross-reviewed by Codex (GPT-5.4).

### Fixes Delivered (10 commits)

**High priority:**
1. **edit_symbol freshness guard** - blake3 hash comparison before applying indexed line ranges; refuses stale edits with clear error message
2. **Session-connect deletion detection** - `check_if_indexing_needed` now detects files deleted while daemon was down via `indexed_files - workspace_files`
3. **Watcher dedup coalescing** - re-queues events instead of dropping them; uses `break` (not `continue`) to avoid busy-spin in inner loop

**Medium priority:**
4. **Per-workspace embedding cancellation** - `embedding_tasks` HashMap keyed by workspace_id replaces single `embedding_task` slot; also fixed force-index to cancel target workspace (not primary)
5. **Web-research skill context leak** - combined fetch+save into single step that pipes directly to file; removed fallback-to-Read language (filewatcher must work, period)
6. **edit_symbol line-granularity docs** - tool description now states line-based limitation

**Low/Medium:**
7. **Bracket balance downgrade** - `Option<String>` warning instead of `Result<()>` hard reject

**Codex cross-review fixes:**
- Watcher busy-spin (`continue` -> `break`)
- Force-index cancels wrong workspace (compute target ws_id from path)
- Edit tools self-invalidation (update file hash after successful write)

### Integration tests added
4 end-to-end tests: replace via index, stale index rejection, insert_after dry-run, symbol not found

### Verification
- `cargo xtask test dev`: 8 buckets passed, 243s, zero failures (run twice)
- `cargo check`: zero compilation errors
- All subagent and integration tests green
