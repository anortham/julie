---
id: checkpoint_2bcd605a
timestamp: 2026-05-24T16:31:06.583Z
tags:
  - manage_workspace
  - typed-request
  - tdd
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: fb391a90
  files:
    - src/handler.rs
    - src/tests/mod.rs
    - src/tests/tools/workspace/manage_workspace_request.rs
    - src/tools/workspace/commands/mod.rs
    - src/tools/workspace/commands/registry/open.rs
    - src/tools/workspace/commands/registry/refresh_stats.rs
    - src/tools/workspace/commands/registry/register_remove.rs
    - src/tools/workspace/mod.rs
summary: Typed manage_workspace request parsing
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Typed parsing is local to workspace command dispatch and handler
  preflight; public MCP schema remains unchanged.
evidence:
  - "RED: `cargo nextest run --lib
    manage_workspace_request_parses_valid_operations_with_live_fields 2>&1 |
    tail -40` failed before implementation with unresolved
    `ManageWorkspaceOperation` / `ManageWorkspaceRequest`."
  - "GREEN: `cargo nextest run --lib manage_workspace_request` passed 4/4."
  - "Affected: `cargo nextest run --lib
    tests::tools::workspace::global_targeting -- --skip search_quality` passed
    34/34, with nextest reporting 2 leaky tests."
  - "Affected: `cargo nextest run --lib tests::core::handler -- --skip
    search_quality` passed 49/49, with nextest reporting 1 leaky test."
  - "Build: `cargo check --all-targets` passed."
  - "Format: `cargo fmt --check` passed."
next: Commit the typed request slice with the new focused tests.
---

## Typed manage_workspace request parsing

- **WHAT:** Replaced the unused `WorkspaceCommand` with internal `ManageWorkspaceOperation` and `ManageWorkspaceRequest`, preserving the public flat `ManageWorkspaceTool` JSON shape.
- **WHY:** Candidate 4 first slice needed one validated internal parser so dispatch and handler preflight stop duplicating raw operation-string matches.
- **HOW:** Added `TryFrom<&ManageWorkspaceTool>` validation for required fields, shared operation helpers for handler preflight, and threaded parsed `force`/`detailed`/target fields through internal dispatch and handlers.
- **IMPACT:** `manage_workspace` now parses once before dispatch, handler preflight uses the same operation parser, and focused tests cover flat serde shape, live field capture, validation errors, and preflight classification.
