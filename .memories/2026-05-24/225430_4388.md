---
id: checkpoint_43889ff2
timestamp: 2026-05-24T22:54:30.614Z
tags:
  - cleanup
  - candidate-11
  - daemon
  - search-compare
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 63bc30b0
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/daemon/database.rs
    - src/daemon/database/search_compare.rs
summary: Split Daemon Search Compare Persistence
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Continues Candidate 11 runtime decomposition with low blast radius and
  stable public API.
evidence:
  - cargo nextest run --lib test_insert_and_list_search_compare_runs_and_cases
  - cargo nextest run --lib test_all_dashboard_pages_return_200
  - cargo nextest run --lib tests::daemon::database -- --skip search_quality
  - cargo fmt --check
  - cargo check --all-targets
  - cargo xtask test bucket daemon
  - cargo xtask test bucket dashboard
  - "cargo xtask test changed: pass, selected daemon only, 61.4s"
  - "Claude CLI structured review: approve, no findings"
next: Commit this slice, then reassess remaining Candidate 11
  tool_calls/codehealth and Candidate 12 analysis fixture helpers.
confidence: 5
---

## Split Daemon Search Compare Persistence

### WHAT
- Moved daemon search-compare persistence out of `src/daemon/database.rs` into `src/daemon/database/search_compare.rs`.
- Moved `insert_search_compare_run`, `replace_search_compare_cases`, `list_search_compare_runs`, `list_search_compare_cases`, and their row/input types together.
- Added stable re-exports from `src/daemon/database.rs` so external imports keep working.
- Updated the cleanup plan and verification ledger.

### WHY
- Candidate 11 needs bounded structural runtime splits before larger handler/search/watcher changes.
- Search-compare persistence is a coherent domain slice with stable callers and focused coverage.

### HOW
- Kept the same `impl DaemonDatabase` methods in a child module, accessing `self.conn` and `now_unix` through the existing private module boundary.
- Preserved SQL, parameter ordering, transaction behavior, row mapping, and public type names.
- Verified dashboard and daemon callers after the move.

### IMPACT
- `src/daemon/database.rs` is another ~190 lines smaller.
- Daemon DB now has separate modules for migrations and search-compare persistence.
- Claude reviewed the move and found no issues.
