---
id: checkpoint_f25adf51
timestamp: 2026-05-24T23:16:11.491Z
tags:
  - cleanup
  - candidate-11
  - daemon
  - tool-calls
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: "43893740"
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/daemon/database.rs
    - src/daemon/database/tool_calls.rs
summary: Split Daemon Tool Call Persistence
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Continues low-risk Candidate 11 daemon DB decomposition with stable
  public APIs.
evidence:
  - cargo nextest run --lib test_insert_and_query_tool_calls
  - cargo nextest run --lib test_insert_tool_call_persists_metadata_json
  - cargo nextest run --lib test_prune_old_tool_calls
  - cargo nextest run --lib tests::tools::metrics::tool_calls_db_tests -- --skip
    search_quality
  - cargo nextest run --lib test_metrics_page_renders_aggregated_tool_history
  - cargo nextest run --lib
    test_metrics_page_counts_failed_tool_calls_in_success_rate
  - cargo nextest run --lib test_query_metrics_formats_null_input_bytes
  - cargo nextest run --lib tests::dashboard::search_analysis -- --skip
    search_quality
  - cargo fmt --check
  - cargo check --all-targets
  - cargo xtask test bucket tools-metrics
  - cargo xtask test bucket daemon
  - cargo xtask test bucket dashboard
  - "cargo xtask test changed: pass, selected daemon only, 61.4s"
  - "Claude CLI structured review: approve, no findings"
next: Commit this slice, then reassess remaining codehealth DB split and
  SQL-heavy test fixture clusters.
confidence: 5
---

## Split Daemon Tool Call Persistence

### WHAT
- Moved daemon tool-call persistence from `src/daemon/database.rs` into `src/daemon/database/tool_calls.rs`.
- Moved insert, insert-with-input-bytes, success-rate, history aggregation, pruning, search-analysis listing, and `SearchToolCallRow` together.
- Re-exported `SearchToolCallRow` from `daemon::database` to preserve existing imports.
- Updated the cleanup plan and verification ledger.

### WHY
- Candidate 11 is decomposing daemon DB by table/domain before attempting broader runtime splits.
- Tool-call persistence was the next coherent slice after migrations and search-compare storage.

### HOW
- Kept `impl DaemonDatabase` methods in a child module with access to `self.conn` and `now_unix`.
- Preserved SQL, timestamp cutoff math, aggregation semantics, row shape, method signatures, and public import paths.
- Verified daemon DB, metrics, dashboard, search-analysis, and changed gates.

### IMPACT
- `src/daemon/database.rs` is another ~200 lines smaller.
- Daemon DB persistence is now split into migrations, search_compare, and tool_calls modules.
- Claude reviewed and approved the move with no findings.
