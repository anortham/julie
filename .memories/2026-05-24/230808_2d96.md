---
id: checkpoint_2d966c5c
timestamp: 2026-05-24T23:08:08.022Z
tags:
  - cleanup
  - candidate-12
  - analysis
  - fixtures
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: ee1dfadd
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/early_warning_report_tests.rs
    - src/tests/helpers/db/rows.rs
    - src/tests/helpers/db/tests.rs
summary: Reused DB Builders In Early Warning Tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Candidate 12 fixture reuse now covers early-warning analysis tests while
  preserving raw SQL edge cases for later narrow clusters.
evidence:
  - "cargo nextest run --lib
    test_symbol_builder_overrides_parent_context_and_annotations: first failed
    before setter existed, then passed"
  - cargo nextest run --lib test_symbol_builder_overrides_metadata_and_span
  - cargo nextest run --lib tests::helpers::db::tests
  - cargo nextest run --lib tests::analysis::early_warning_report_tests --
    --skip search_quality
  - cargo fmt --check
  - cargo check --all-targets
  - cargo xtask test bucket analysis
  - "cargo xtask test changed: pass, dev fallback, 34 buckets, 516.0s"
  - "Claude CLI structured review: approve, no findings"
next: Commit this slice, then reassess remaining Candidate 11
  tool_calls/codehealth and Candidate 12 SQL-heavy analysis clusters.
confidence: 5
---

## Reused DB Builders In Early Warning Tests

### WHAT
- Added `SymbolBuilder` setters for `parent_id`, `code_context`, and `annotations`.
- Added helper coverage for parent/context/annotation overrides.
- Migrated `src/tests/analysis/early_warning_report_tests.rs` file and symbol fixture constructors to `file_info_builder` and `symbol_builder`.
- Updated the cleanup plan and verification ledger.

### WHY
- Candidate 12 is reducing repeated test fixture literals while keeping edge-case SQL visible.
- Early-warning tests had local `FileInfo` and `Symbol` constructors that matched the shared DB builder shape but needed a few explicit setters to preserve semantics.

### HOW
- Kept previous literal values explicit: file hash, size, timestamps, line counts, content, symbol spans, bytes, signatures, confidence, parent IDs, metadata, code context, and annotations.
- Left raw SQL-heavy analysis tests untouched pending narrower review.
- Ran exact helper and early-warning tests, analysis bucket, and dev fallback because shared test helpers changed.

### IMPACT
- Removes another local fixture-literal cluster from analysis tests.
- Makes the shared DB symbol builder cover annotation-bearing symbols used by analysis/report tests.
- Claude reviewed the migration and found no issues.
