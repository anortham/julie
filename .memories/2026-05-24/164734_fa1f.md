---
id: checkpoint_fa1f87c4
timestamp: 2026-05-24T16:47:34.963Z
tags:
  - cleanup
  - paths
  - get-symbols
  - rename
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 0485b624
  files:
    - src/tests/core/paths.rs
    - src/tools/refactoring/rename.rs
    - src/tools/symbols/primary.rs
    - src/tools/symbols/target_workspace.rs
    - src/utils/paths.rs
summary: Implemented Candidate 5 First Slice
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Reduces duplicated path normalization in three tool file-input call
  sites while preserving caller-specific fallback contracts.
evidence:
  - "RED: `cargo nextest run --lib
    test_resolve_workspace_file_input_handles_tool_file_paths 2>&1 | tail -40`
    failed on missing `resolve_workspace_file_input`."
  - "GREEN/final: helper test passed 1/1."
  - "Final focused checks passed: get_symbols path normalization,
    file-not-found, target-workspace relative paths after primary rebind, rename
    absolute file scope, cargo check --all-targets, cargo fmt --check."
symbols:
  - resolve_workspace_file_input
  - WorkspaceFileInputResolution
  - get_symbols_from_primary
  - get_symbols_from_target_workspace
  - normalize_scope_file_path
next: Lead-owned batch gates remain pending; this worker did not run xtask
  changed/dev/full per instructions.
confidence: 5
---

## Implemented Candidate 5 First Slice

- **WHAT:** Added a shared `resolve_workspace_file_input` helper for tool file-input normalization and routed `get_symbols` primary/target plus rename file-scope normalization through it.
- **WHY:** These handlers duplicated absolute/relative canonicalization and relative Unix query path conversion while needing distinct fallback behavior at caller boundaries.
- **HOW:** Kept the helper in `src/utils/paths.rs` next to `to_relative_unix_style`, returning absolute path, relative query `Result`, and a canonicalization flag. Callers still own their existing fallback branches, existence checks, and target-workspace retry behavior.
- **IMPACT:** Tool file input handling now has one resolver for absolute/relative/`.`/`..`/missing/traversal/tilde cases without changing workspace command root semantics.
