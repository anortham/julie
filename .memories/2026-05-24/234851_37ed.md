---
id: checkpoint_37ed1729
timestamp: 2026-05-24T23:48:51.889Z
tags:
  - cleanup
  - tests
  - fixtures
  - candidate-12
  - test-linkage
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: bd640c7a
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/test_linkage_tests.rs
summary: Migrated first test-linkage fixture setup cluster
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib test_compute_linkage_relationship_linkage
  - cargo nextest run --lib test_identifier_only_linkage
  - cargo fmt --check
  - cargo check --all-targets
  - cargo xtask test bucket analysis
  - cargo xtask test changed
  - fresh Claude review approved with no findings
next: Commit this slice, then reassess whether to continue with later linkage
  clusters or switch to a lead-owned daemon workspace registry split.
---

## Migrated first test-linkage fixture setup cluster

### WHAT
- Replaced the first ordinary raw SQL setup cluster in `src/tests/analysis/test_linkage_tests.rs` with shared DB row builders and storage APIs.
- Covered `setup_test_db` relationship-linkage fixtures and `test_identifier_only_linkage` identifier-only fixtures.
- Updated cleanup plan progress, remaining-candidate notes, and verification ledger.

### WHY
- Continues Candidate 12 by reducing raw SQL setup in large analysis tests without touching later SQL-heavy edge cases.

### HOW
- Used `file_info_builder`, `symbol_builder`, `relationship_builder`, `identifier_builder`, `store_file_info`, `store_symbols`, `store_relationships`, and `bulk_store_identifiers`.
- Preserved span, visibility, metadata, relationship kind, target/containing symbol IDs, and assertion behavior.
- Removed decorative `reference_score` updates after verifying `compute_test_linkage` does not read `reference_score`; fresh Claude agreed.
- Left later raw SQL clusters untouched for separate review.

### IMPACT
- The first linkage setup path now uses shared fixture builders.
- `src/tests/analysis/test_linkage_tests.rs` remains under the 1000-line test-file limit at 845 lines.
- Remaining Candidate 12 work should continue cluster-by-cluster, especially for later linkage edge cases.
