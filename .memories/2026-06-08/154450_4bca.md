---
id: checkpoint_4bcac625
timestamp: 2026-06-08T15:44:50.130Z
git:
  branch: main
  commit: c05184d0
  files:
    - .codex/config.toml
    - docs/plans/2026-06-08-daemon-orphaned-cleanup.md
summary: "Task 1: Deleted orphaned DaemonPaths methods"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
---

## Task 1: Deleted orphaned DaemonPaths methods

### WHAT
Deleted 14 orphaned methods from `DaemonPaths` in `crates/julie-core/src/paths.rs`:
- `daemon_shutdown_event()`, `daemon_pid()`, `daemon_lock()`, `daemon_startup_lock()`, `daemon_singleton_lock()`, `daemon_log()`, `daemon_db()`, `daemon_port()`, `daemon_mcp_transport()`, `daemon_mcp_token()`, `discovery_file()`, `token_file()`, `daemon_state()`, `migration_state()`

### KEPT
All critical methods: `julie_home_hash()`, `registry_db()`, `embedding_host_socket()`, `embedding_host_lock()`, `embedding_host_pipe_name()`, `project_log_dir()`, `ensure_dirs()`, VCS root markers, workspace paths.

### TEST UPDATES
- Removed `daemon_shutdown_event()` assertion from `embedding_host_pipe_name_is_deterministic_and_prefixed` test in `crates/julie-core/src/tests/paths.rs`
- Deleted orphaned `src/tests/daemon/shutdown_event.rs` (tested deleted method + deleted ShutdownEvent type)
- Removed `pub mod shutdown_event` from `src/tests/daemon/mod.rs`

### VERIFICATION
- `cargo build --bins` -- PASSES
- `cargo check` -- PASSES (no warnings)
- `cargo nextest run -p julie-core` -- 147/147 PASS, 1 skipped
