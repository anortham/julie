---
id: checkpoint_b9e60f03
timestamp: 2026-05-11T20:31:25.420Z
tags:
  - merge
  - release-evidence
  - tree-sitter
  - main
git:
  branch: main
  commit: f548160e
  files:
    - docs/LANGUAGE_CERTIFICATION_REPORT.md
    - docs/LANGUAGE_REAL_WORLD_EVIDENCE.json
    - docs/LANGUAGE_REAL_WORLD_EVIDENCE.md
summary: Merged body-span branch to main and refreshed release evidence
briefId: julie-world-class-systems-program
type: checkpoint
impact: Release evidence is refreshed for merged main. Evidence refresh files
  need committing before tagging.
evidence:
  - "`git merge --ff-only extractor-trust-contract-v1` on `main` succeeded."
  - Initial post-merge cert check failed due stale evidence, then was fixed by
    regenerating evidence/report.
  - "`cargo xtask certify tree-sitter --real-world --profile release --out
    docs/LANGUAGE_REAL_WORLD_EVIDENCE.json` passed: 22 verified repos, 0
    skipped."
  - "`cargo xtask certify tree-sitter --out
    docs/LANGUAGE_CERTIFICATION_REPORT.md && cargo xtask certify tree-sitter
    --check` passed."
  - "`cargo xtask test nano` passed: core-database and core-fast buckets."
symbols:
  - tree_sitter_certification
  - LANGUAGE_REAL_WORLD_EVIDENCE
next: Commit refreshed `docs/LANGUAGE_CERTIFICATION_REPORT.md`,
  `docs/LANGUAGE_REAL_WORLD_EVIDENCE.json`, and `.md`, then evaluate
  version/release-note/tag readiness.
---

## Merged body-span branch to main and refreshed release evidence

### WHAT
Fast-forwarded local `main` to `extractor-trust-contract-v1`, then regenerated tree-sitter certification evidence on merged main using the full release real-world profile.

### WHY
The user asked to merge Julie so a new release can be tagged if ready. Post-merge cert check exposed that certification reports are HEAD/evidence sensitive, so release evidence had to be regenerated on the merged commit before tagging.

### HOW
- Fetched origin, checked out `main`, pulled `origin/main` with `--ff-only`, and merged `extractor-trust-contract-v1` with `--ff-only`.
- Initial post-merge `cargo xtask certify tree-sitter --check` failed because generated evidence was stale relative to merged HEAD.
- Re-ran full release evidence: `cargo xtask certify tree-sitter --real-world --profile release --out docs/LANGUAGE_REAL_WORLD_EVIDENCE.json`.
- Regenerated certification report and reran cert check.
- Ran nano gate after evidence refresh.

### IMPACT
Local Julie `main` now contains the body-span work and has release-profile real-world evidence for 22 verified repos, 0 skipped. Certification report is current and nano gate passed.
