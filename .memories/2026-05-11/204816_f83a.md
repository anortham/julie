---
id: checkpoint_f83a3839
timestamp: 2026-05-11T20:48:16.671Z
tags:
  - release
  - v7.9.0
  - version-bump
  - verification
git:
  branch: main
  commit: 3173e95d
  files:
    - Cargo.lock
    - Cargo.toml
    - docs/release-notes/v7.9.0.md
    - docs/site/index.html
summary: Prepared Julie v7.9.0 release version bump
briefId: julie-world-class-systems-program
type: checkpoint
impact: Ready to commit release prep and tag v7.9.0.
evidence:
  - "`cargo check` passed with `julie v7.9.0`."
  - "`cargo xtask certify tree-sitter --check` passed after version bump."
  - "`cargo xtask test nano` passed after version bump."
  - "`cargo xtask test dev` passed: 32 buckets."
  - "`cargo xtask test system` passed: 6 buckets."
  - "`cargo xtask test dogfood` passed: dogfood repo index and search-quality
    buckets."
  - "Release-profile real-world evidence is already current on main: 22 verified
    repos, 0 skipped."
symbols:
  - Cargo.toml
  - docs/release-notes/v7.9.0.md
next: Commit release prep, push main, create annotated tag v7.9.0, push tag,
  then verify/edit GitHub release notes.
---

## Prepared Julie v7.9.0 release version bump

### WHAT
Bumped Julie from `7.8.5` to `7.9.0`, updated the docs site footer, added substantive `docs/release-notes/v7.9.0.md`, and refreshed `Cargo.lock` via `cargo check`.

### WHY
The body-span/extractor contract work changed extractor output shape and persisted schema, so it warrants a minor release rather than another patch tag.

### HOW
- Updated `Cargo.toml` package version to `7.9.0`.
- Updated `docs/site/index.html` footer to `v7.9.0`.
- Added v7.9.0 release notes covering extractor contract version, body-span/hash semantics, capability matrix changes, SQLite schema migration, release evidence, artifacts, and upgrade notes.
- Ran release-relevant gates before commit/tag.

### IMPACT
The repo is ready for a release-prep commit, push to `main`, tag `v7.9.0`, and GitHub release workflow trigger.
