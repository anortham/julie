---
id: checkpoint_d93da7f4
timestamp: 2026-06-24T01:30:56.417Z
tags:
  - dependency-update
  - julie-extractors
  - extractor-dep
  - verification
  - engine-version
git:
  branch: main
  commit: "01964765"
  files:
    - .codex/config.toml
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/Cargo.toml
    - crates/julie-index/Cargo.toml
    - crates/julie-pipeline/Cargo.toml
    - crates/julie-runtime/Cargo.toml
    - crates/julie-tools/Cargo.toml
    - src/tools/workspace/indexing/engine_version.rs
summary: Updated Julie to julie-extractors v2.5.3
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Julie consumes the current extractor package and preserves index drift
  detection for the upstream extraction-contract change.
symbols:
  - SEMANTIC_INDEX_ENGINE_VERSION
  - test_semantic_index_engine_version_includes_extraction_contract
next: If release work is desired, follow the Julie patch release flow
  separately; no push/release was performed.
---

## Updated Julie to julie-extractors v2.5.3

- **WHAT:** Repinned all Julie `julie-extractors` dependency surfaces from `v2.5.1` to `v2.5.3`, updated `Cargo.lock`, and synced `SEMANTIC_INDEX_ENGINE_VERSION` to the upstream `EXTRACTION_CONTRACT_VERSION` marker `2026-06-20...file-derived-component-symbols-v1`.
- **WHY:** Upstream `julie-extractors v2.5.3` is published and Julie needed to consume the newer extractor contract while preserving stored-index drift detection.
- **HOW:** Verified the live upstream release/tag, used the existing engine-version regression test as the RED/GREEN guard, and ran the extractor dependency integration bucket plus changed/dev verification.
- **IMPACT:** Julie now compiles/tests against `julie-extractors v2.5.3`; existing indexes will see the extractor contract marker change and reindex when needed.

**Verification:**
- `cargo nextest run --lib test_semantic_index_engine_version_includes_extraction_contract` failed before the marker update with the expected old/new contract mismatch, then passed after the update.
- `cargo check` passed.
- `cargo xtask test bucket extractor-dep-integration` passed on the final diff.
- `cargo xtask test changed` passed; it fell back to the dev tier and ran 29 buckets successfully.
- `rustfmt --check src/tools/workspace/indexing/engine_version.rs` passed.
- `git diff --check` passed.

**Notes:** Repo-wide `cargo fmt --check` still fails because of unrelated pre-existing formatting drift across many files, so full formatting was not applied. Julie MCP refresh for `/Users/murphy/source/julie` hit a Tantivy writer-lock projection-backfill error; Miller and CLI verification covered the code change.
