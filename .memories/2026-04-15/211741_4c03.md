---
id: checkpoint_4c032100
timestamp: 2026-04-15T21:17:41.496Z
tags:
  - benchmark
  - task-3
  - julie
  - baselines
  - cli
git:
  branch: main
  commit: f4132cea
  files:
    - .claude/settings.local.json
    - .codex/config.toml
    - xtask/src/runner.rs
    - xtask/tests/runner_tests.rs
summary: Completed Task 3 for benchmark corpus and baselines plan
planId: tree-sitter-hardening-completion-sprint
type: checkpoint
context: Executing the cross-repo benchmark corpus and baselines plan. Task 3
  required a dedicated Julie worktree and also had to absorb user changes made
  on Julie main.
decision: Emit Miller-compatible per-case result rows from Julie directly,
  instead of leaving a custom export shape for Task 4 to translate.
alternatives:
  - Keep the custom `query_results`-only export and adapt it later in Miller,
    rejected because it would push complexity into the comparison layer and
    weaken the contract between systems
impact: Makes the Julie exporter a cleaner producer contract and lowers the
  complexity of the upcoming comparison-tooling task.
evidence:
  - "Focused Julie export tests passed: 16 passed"
  - Julie CLI `export-baseline` prints JSON with `results` entries carrying
    latency, tokens, and quality
symbols:
  - run_baseline_export
  - setup_handler_for_export
  - baseline_query_catalog
  - ExportResult
  - ExportSummary
next: "Implement Task 4 in the Miller worktree: extend comparison tooling, docs,
  and Make targets for cross-system baselines."
confidence: 5
unknowns:
  - Whether Task 5 should run Julie baselines from this worktree directly or
    wait for a Julie commit before capturing checked-in artifacts
---

## Completed Task 3 for benchmark corpus and baselines plan

Finished the Julie-side fixture-backed baseline export command in the dedicated Julie worktree.

- **What:** Added a Julie `export-baseline` CLI command in `/Users/murphy/source/julie/.worktrees/baseline-export` and aligned its output with Miller's per-case result schema
- **Why:** Phase 0 needs comparable Miller and Julie baseline artifacts. Scraping `cargo test` output would be brittle and wasteful, so Julie needed a direct machine-readable export path backed by its existing fixture-driven dogfood setup
- **How:** Added `src/baseline_export.rs`, CLI wiring in `src/cli.rs` and `src/main.rs`, catalog tests under `src/tests/tools/search_quality/`, then corrected the first implementation because it emitted a custom `query_results` format without Miller-style `results` rows. The final export now includes system identity, corpus identity, per-case IDs, latency, token metrics, and `answer_quality_per_token`. I also pulled the user's uncommitted `xtask` runner noise-reduction changes from Julie main into the worktree and verified them there
- **Impact:** Task 4 can now compare Miller and Julie artifacts without inventing a translation layer from scratch. The Julie side now exposes stable machine-readable baseline data instead of only pass/fail dogfood tests

## Verification

- `cargo test --lib baseline_export` → `16 passed`
- `cargo test --lib query_catalog` → `5 passed`
- `cargo test -p xtask runner_tests` → `32 passed`
- `cargo run -- export-baseline` → prints JSON with Miller-compatible top-level identity and `results` entries

## Notable correction

- The first export implementation was rejected because it only emitted `query_results` plus a summary
- The final version adds a Miller-compatible `results` array with:
  - `system_id = julie`
  - `corpus_id = julie-dogfood-v1`
  - `case_id`
  - `latency`
  - `tokens`
  - `quality.answer_quality_per_token`

## Current state

- Miller Task 1 and Task 2 are complete in `/Users/murphy/source/miller/.worktrees/benchmark-corpus-and-baselines`
- Julie Task 3 is complete in `/Users/murphy/source/julie/.worktrees/baseline-export`
- Next: extend Miller comparison tooling and docs for cross-system baselines
