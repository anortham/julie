---
id: checkpoint_b89821b2
timestamp: 2026-04-15T16:15:49.954Z
tags:
  - treesitter
  - extractors
  - crash-recovery
  - verification
  - integration-flake
git:
  branch: treesitter-world-class-finish
  commit: ccef17e9
  files:
    - crates/julie-extractors/README.md
    - crates/julie-extractors/src/base/extractor.rs
    - crates/julie-extractors/src/base/mod.rs
    - crates/julie-extractors/src/base/types.rs
    - crates/julie-extractors/src/csharp/di_relationships.rs
    - crates/julie-extractors/src/csharp/member_type_relationships.rs
    - crates/julie-extractors/src/csharp/relationships.rs
    - crates/julie-extractors/src/factory.rs
    - crates/julie-extractors/src/gdscript/enums.rs
    - crates/julie-extractors/src/java/relationships.rs
    - crates/julie-extractors/src/javascript/assignments.rs
    - crates/julie-extractors/src/javascript/functions.rs
    - crates/julie-extractors/src/javascript/relationships.rs
    - crates/julie-extractors/src/kotlin/relationships.rs
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/lua/relationships.rs
    - crates/julie-extractors/src/manager.rs
    - crates/julie-extractors/src/php/functions.rs
    - crates/julie-extractors/src/php/members.rs
    - crates/julie-extractors/src/php/namespaces.rs
    - crates/julie-extractors/src/php/relationships.rs
    - crates/julie-extractors/src/php/types.rs
    - crates/julie-extractors/src/python/functions.rs
    - crates/julie-extractors/src/python/helpers.rs
    - crates/julie-extractors/src/python/relationships.rs
    - crates/julie-extractors/src/routing_identifiers.rs
    - crates/julie-extractors/src/routing_relationships.rs
    - crates/julie-extractors/src/routing_symbols.rs
    - crates/julie-extractors/src/rust/functions.rs
    - crates/julie-extractors/src/tests/api_surface.rs
summary: Recovered tree-sitter hardening continuation state
planId: tree-sitter-hardening-completion-sprint
type: checkpoint
context: Crash recovery during tree-sitter hardening completion work in the
  isolated worktree.
decision: Treat the extractor slice as recovered, but block sign-off until the
  fresh full-tier integration failure is investigated.
alternatives:
  - Ignore the failing full-tier run and preserve the old optimistic review
    artifact, rejected because that would misrepresent the branch state.
  - Expand the current task into fixing the stale-index integration pollution
    immediately without first recording the recovered extractor state, rejected
    because another crash would lose the narrowed diagnosis and fresh evidence.
impact: Extractor-local verification is green again, while the remaining
  repo-wide blocker is isolated to stale-index integration interference.
evidence:
  - cargo fmt --check
  - cargo clippy --quiet
  - cargo test -p julie-extractors --lib -> 1593 passed; 0 failed; 7 ignored
  - "cargo xtask test dev -> SUMMARY: 9 buckets passed in 377.4s"
  - cargo xtask test full -> integration failed on stale_index_detection
    fresh-index tests
  - cargo test --lib tests::integration::stale_index_detection -- --nocapture ->
    12 passed; 0 failed
  - cargo test --lib tests::integration -- --skip search_quality -> 139 passed;
    2 failed; 5 ignored
symbols:
  - BaseExtractor::find_doc_comment
  - find_parent_class_id
  - StructuredPendingRelationship::new
  - tests::java::cross_file_relationships::test_cross_file_method_call_creates_pending_relationship
  - tests::java::cross_file_relationships::test_cross_file_constructor_call_creates_pending_relationship
  - tests::kotlin::cross_file_relationships::test_method_call_on_external_type_creates_pending_relationship
  - check_if_indexing_needed
next: Investigate which earlier integration test pollutes stale-index freshness
  checks before rerunning `cargo xtask test full` again.
confidence: 4
unknowns:
  - Which earlier integration test or shared subsystem state causes the
    stale-index tests to fail inside the broader integration bucket.
  - Whether the polluter is branch-local or due unrelated concurrent work
    already present in the dirty worktree.
---

## Recovered tree-sitter hardening continuation state

Resumed the `treesitter-world-class-finish` worktree after a crash, re-verified the suspected remaining extractor blockers, and updated the review artifact to match the current truth.

- **What changed:**
  - Verified the previously suspected Python parent-id and doc-comment regressions are green in the worktree.
  - Fixed the Kotlin cross-file pending-call regression by updating the test to assert the structured-display contract (`helper.compute`) instead of the pre-hardening degraded legacy form (`compute`).
  - Tightened Java cross-file pending-call regressions so they now require receiver-qualified pending names and structured target assertions (`Helper.process`, `calc.getValue`) instead of allowing stale degraded names.
  - Re-ran extractor-local verification and refreshed the world-class review artifact.
  - Investigated the fresh full-tier failure and narrowed it to an order-dependent integration-bucket issue in `stale_index_detection`.

- **Why:**
  - The pre-crash review state was stale and partially wrong. The branch needed fresh evidence before any completion claim, and the review artifact still incorrectly claimed clean sign-off.

- **How:**
  - Reproduced the known targeted regressions one by one.
  - Used Julie/deep-dive plus targeted cargo tests to distinguish real code regressions from stale test expectations.
  - Ran fresh verification commands: `cargo fmt --check`, `cargo clippy --quiet`, `cargo test -p julie-extractors --lib`, `cargo xtask test dev`, and `cargo xtask test full`.
  - After `cargo xtask test full` failed, narrowed with exact stale-index tests, the stale-index file, and the integration bucket command to isolate the failure mode.

- **Impact:**
  - Extractor-local work is back in a trustworthy state with fresh green evidence.
  - The branch is **not** ready for repo-owner world-class sign-off yet because full-tier verification still fails outside the extractor crate.
  - The remaining blocker is now sharply defined: `stale_index_detection` passes alone and as a file, but fails when run inside the broader integration bucket, including with `--test-threads=1`, which points to order-dependent integration-state pollution rather than an extractor correctness gap.
