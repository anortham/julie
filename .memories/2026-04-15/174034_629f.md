---
id: checkpoint_629f1a39
timestamp: 2026-04-15T17:40:34.543Z
tags:
  - checkpoint
  - treesitter
  - extractors
  - main-merge
  - verification
  - worktree-cleanup
git:
  branch: main
  commit: a7cf232b
  files:
    - .claude/settings.local.json
    - .codex/config.toml
summary: Tree-Sitter Hardening Landed On Main
planId: tree-sitter-hardening-completion-sprint
type: checkpoint
context: The extractor hardening work had been validated in a feature worktree,
  but branch refs and dirty worktrees were out of sync, so the validated
  implementation was not safely represented on `main`.
decision: Promote the worktree changes into commits first, then fast-forward
  `main` while protecting the dirty primary checkout with a stash.
alternatives:
  - Merge or cherry-pick from a dirty worktree directly onto `main`, which
    risked mixing in stale root-only edits and losing local state.
  - Leave the validated work in the worktree, which would keep the
    implementation outside normal branch history and make future sessions
    brittle.
impact: "`main` is now the source of truth for the world-class tree-sitter
  hardening work, with the feature branch removed and verification recorded on
  the merged branch."
evidence:
  - "`git merge --ff-only treesitter-world-class-finish` updated `main` to
    `a7cf232b`"
  - "`cargo xtask test full` on `main`: `SUMMARY: 12 buckets passed in 643.1s`"
  - "`git worktree remove
    /Users/murphy/source/julie/.worktrees/treesitter-world-class-finish`
    succeeded"
  - "`git branch -d treesitter-world-class-finish` succeeded"
  - "`git stash list -1` shows `stash@{0}: On main: pre-merge-main-local`"
symbols:
  - tests::integration::documentation_indexing::test_documentation_indexing_basic
  - tests::integration::documentation_indexing::test_documentation_deduplication
  - tests::integration::documentation_indexing::test_documentation_update_on_change
  - tests::integration::documentation_indexing::test_multiple_sections_from_single_file
  - tests::integration::stale_index_detection::test_fresh_index_no_reindex_needed
  - tests::integration::stale_index_detection::test_fresh_index_with_extensionless_text_files_needs_no_reindex
  - tests::integration::daemon_lifecycle::tests::test_daemon_reaches_ready_before_slow_embedding_init_completes
next: Optional next step is to clear or reapply `stash@{0}` only if any of the
  old dirty-main changes still matter; the merged tree-sitter work itself is
  already on `main`.
confidence: 5
unknowns:
  - The saved stash may contain obsolete copies of files that are now committed
    on `main`; it should be inspected before any reapply.
  - Pre-existing warning noise from sidecar/HF/resource-tracker output still
    exists during long integration and dogfood runs.
---

## Tree-Sitter Hardening Landed On Main

### What
The tree-sitter hardening work is no longer stranded in the `treesitter-world-class-finish` worktree.

- Committed the feature work into two real commits:
  - `4a46549d` `fix(extractors): close remaining hardening gaps`
  - `a7cf232b` `test(integration): stabilize hardening verification`
- Fast-forwarded `main` to `a7cf232b`
- Removed the feature worktree and deleted branch `treesitter-world-class-finish`
- Preserved local-only config edits in the main worktree
- Saved the pre-merge dirty main state in `stash@{0}` as `pre-merge-main-local`

### Why
The work had been validated but was still split between a feature worktree and stray edits in the primary checkout, which meant the repo history did not reflect the real state of the implementation. That was useless for future work and risky to leave around.

### How
- First converted the worktree state into committed branch history
- Pulled the stray root-only hardening artifacts back into the feature branch
- Re-verified the integration hardening after adding `embedding_env` serialization to env-mutating integration tests
- Stashed the dirty main checkout, fast-forwarded `main` to the feature branch tip, restored the local config files, then removed the merged worktree and deleted the branch

### Impact
- `main` now contains the tree-sitter hardening work as real git history
- Fresh branch-level verification was run on `main`
- The implementation is no longer trapped in a side worktree
- Only local config dirt remains in the main checkout; code changes are merged and verified

