---
id: checkpoint_684ca47c
timestamp: 2026-05-30T02:19:42.944Z
tags:
  - miller-bridge
  - test-role
  - phase-1
  - wiring
  - extract-contract
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: 387a5174
  files:
    - src/indexing_core/extraction.rs
    - src/tests/external_extract/operations.rs
    - src/tools/workspace/indexing/pipeline.rs
    - src/watcher/handlers.rs
summary: Test-role enrichment Phase 1 wired — test_role now on the extract path
  Miller reads
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "RED: extract_scan_persists_test_role_metadata_across_languages left:0
    right:1 -> GREEN"
  - external_extract 48/48, analysis 142/142, watcher integration 69/69
  - "changed 20/21 buckets; rebind failure reproduced identically on stashed
    clean tree (task #33 pollution)"
next: Phase 2 cross-language detection + Phase 3 call-style capture via
  TeamCreate team; lead owns regression + commits.
---

## Test-role enrichment Phase 1 wired — test_role now on the extract path Miller reads

Second sibling plan (`docs/plans/2026-05-29-test-role-enrichment-for-miller.md`) rides the SAME `(28,2)` contract epoch as the literals/type_args batch. Verified its core finding still held: `classify_symbols_by_role` ran ONLY at `pipeline.rs:54` (live daemon), so the extract DB Miller reads had `is_test` but no `test_role`, and test CLASSES were never flagged.

- **WHAT:** Hoisted `classify_symbols_by_role` into the shared chokepoint `extract_files_for_indexing_with_records` (`src/indexing_core/extraction.rs`), right beside the literals carrier gate, sharing one `LanguageConfigs::load_embedded()`. This single site covers BOTH the live pipeline and the external-extract CLI. Added the same classify to the watcher (`src/watcher/handlers.rs`, made `results` mut). Removed the now-redundant `pipeline.rs:54` call (single source of truth).
- **WHY:** The extract CLI path never classified — Miller would re-pin to `(28,2)` expecting `test_role` and get nothing. This makes the `(28,2)` epoch actually complete for the test-role half.
- **HOW:** TDD. New extract-path integration test `extract_scan_persists_test_role_metadata_across_languages` (operations.rs) writes C# MSTest + Python pytest fixtures, scans, and asserts `json_extract(metadata,'$.test_role')`: [TestClass]→test_container+is_test, [TestInitialize]→fixture_setup, [TestMethod]→test_case, @pytest.fixture→fixture_setup, test_*→test_case (convention), production symbols→NULL. RED (left:0 right:1, container unclassified) → GREEN.
- **IMPACT:** Test classes ([TestClass]/@Nested) now get `is_test`+`test_role` in the extract — the previously-impossible container signal (per-extractor is_test is callable-only). Miller can shrink its path-heuristic fallback.

### Verification
- external_extract 48/48, analysis 142/142, watcher integration 69/69 green.
- `cargo xtask test changed`: 20/21 buckets pass. Sole failure `tools-workspace-targeting::rebind_index` is the PRE-EXISTING `/tmp/.julie` pollution bug (task #33): stashed my diff, ran on clean HEAD (classify block confirmed absent), test failed IDENTICALLY (left:Some("tmp_11fe14a5") right:Some("workspace-b_...")). Orthogonal to test-role classification.

### Next
Phase 2 (cross-language base-type containers + real framework detection for c/cpp/gdscript/lua/qml/r/zig) and Phase 3 (call-style capture for Dart/R) via a TeamCreate team. Contract bump already at 2 (no further bump; test_role rides the same epoch).
