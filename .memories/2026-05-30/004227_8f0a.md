---
id: checkpoint_8f0a8155
timestamp: 2026-05-30T00:42:27.460Z
tags:
  - miller-bridge
  - literals
  - phase-3b
  - extractors
  - breadth
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: e7d70578
  files:
    - crates/julie-extractors/examples/cmd_probe.rs
    - crates/julie-extractors/src/c/identifiers.rs
    - crates/julie-extractors/src/cpp/identifiers.rs
    - crates/julie-extractors/src/qml/identifiers.rs
    - crates/julie-extractors/src/tests/c/literals.rs
    - crates/julie-extractors/src/tests/c/mod.rs
    - crates/julie-extractors/src/tests/cpp/literals.rs
    - crates/julie-extractors/src/tests/cpp/mod.rs
    - crates/julie-extractors/src/tests/qml/literals.rs
    - crates/julie-extractors/src/tests/qml/mod.rs
    - crates/julie-extractors/src/tests/zig/literals.rs
    - crates/julie-extractors/src/tests/zig/mod.rs
    - crates/julie-extractors/src/zig/identifiers.rs
    - languages/c.toml
    - languages/cpp.toml
    - languages/qml.toml
    - languages/zig.toml
summary: Phase 3b literal-capture breadth — 23/26 applicable languages committed
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
next: Wait for batch-a 'cpp green' + batch-c bash/powershell reports; then
  commit, extend polyglot integration test, bump EXTRACT_CONTRACT_VERSION 1→2,
  run regression, final commit.
confidence: 4
---

## Phase 3b literal-capture breadth — 23/26 applicable languages committed

Carrier-gated string-literal capture (Miller bridge Phase 3) rolled out across languages via a 3-batch agent team (batch-a/b/c). Extractors emit a `Literal` per string call-arg config-free (kind=Other, verbatim carrier); `src/` `classify_literals_by_carrier` gates by per-language `[literal_carriers]` TOML and drops non-carriers.

### Committed this session (branch feat/miller-bridge-extraction-enrichments)
- `8781fe38` Go + Ruby
- `b05b2da6` JS, Vue, Elixir, Swift, R
- `330f66d6` VB.NET + Razor
- `166e75aa` GDScript + Lua
- `669c1edf` Kotlin + Scala
- `f5000a76` **fix**: embed vbnet.toml in load_embedded (was the 1 lang of 34 with no embedded config → its carriers never loaded → gate silently dropped all VB.NET literals). Bumped count test 33→34, added per-family breadth probe to carrier test.
- `f7a7f899` Rust (call_expression + sqlx macro_invocation arm) + Dart
- `e7d70578` Java + PHP

### Remaining (in flight)
- **cpp** (batch-a): 3/5 green; 2 fail on bare-`identifier` + `template_function` callee shapes — hint sent.
- **bash + powershell** (batch-c): command-grammar arm (commands.rs), just assigned (reassigned off batch-b to balance load).
- Verified-N/A (8, grammar-evidenced): SQL, CSS, HTML, Regex, JSON, TOML, YAML, Markdown.

### Key decisions
- **Last-segment carrier matching** (`carrier_matches`): config entry matches by exact full carrier OR carrier's last dot-segment, so bare `query` matches `pool.query`/`conn.execute` without enumerating receivers.
- **Rust sqlx macros**: batch-c twice deferred the macro arm; I insisted (dominant Rust SQL form) — now landed + tested.
- Per-language carrier curation reviewed against real library APIs; anti-flood calls documented (R bare GET dropped vs base-R get(); Swift bare `run` kept for SQLite.swift).

### Final steps after last 3 langs land
1. Commit batch-a (c/cpp/zig/qml) once cpp green; commit batch-c (bash/powershell).
2. Extend `src/tests/external_extract/operations.rs` polyglot test for distinct arm shapes (macro/command/member_call/function_call) end-to-end.
3. Bump `EXTRACT_CONTRACT_VERSION` 1→2 (metadata.rs:13) in lockstep with schema 28 — FINAL commit only.
4. Drive ledger to 100%, clean scratch example probe files (ast_probe.rs, ast_probe2.rs, rust_macro_probe.rs, cmd_probe.rs), run `cargo xtask test dev` + extractors bucket regression, notify Miller gate owner.
5. Off-path task #33: pre-existing workspace-rebind /private/tmp resolution bug (separate fix, not this branch).
