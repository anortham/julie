---
id: checkpoint_fa3e0802
timestamp: 2026-05-30T01:48:44.062Z
tags:
  - miller-bridge
  - phase-3b
  - extract-contract
  - literals
  - release-coordination
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: cde90c3f
  files:
    - docs/plans/2026-05-29-extraction-enrichments-for-miller-bridge.md
    - src/external_extract/metadata.rs
    - src/tests/external_extract/info.rs
summary: Phase 3b complete — extract contract bumped to v2 (Miller bridge)
briefId: fts-phase-2-unified-schema-implementation
type: decision
decision: Bump EXTRACT_CONTRACT_VERSION 1→2 in the final batch commit, lockstep
  with schema 28, guarded by a tripwire test; classify the cli bootstrap failure
  as pre-existing env flakiness, not a regression.
alternatives:
  - "Bump the contract earlier (rejected: Miller's exact-equality gate would
    reject intermediate (26,2)/(27,2) releases)"
  - "Skip the lockstep guard test (rejected: silent desync of the bridge is
    exactly the failure mode to prevent)"
  - "Treat the cli bucket failure as a blocking regression (rejected: passes
    45/45 with the diff present; no causal path from a constant bump to
    workspace init)"
evidence:
  - cargo nextest run --lib tests::external_extract → 47 passed
  - "RED: extract_contract_version_and_schema_... left:1 right:2; GREEN after
    bump"
  - clean tree full cli bucket 45/45; with-diff full cli bucket 45/45 (run 1)
  - /tmp/.julie and /private/tmp/.julie present (mtime 20:23 today) —
    find_workspace_root pollution
next: Commit code+doc+checkpoint, backfill commit SHA into the plan doc's
  version-triple line (amend), then notify the user (Miller gate owner) that
  contract v2 is ready to re-pin at M4.
---

## Phase 3b complete — extract contract bumped to v2 (Miller bridge)

Final closeout of the Miller-bridge extraction-enrichments batch. Carrier-gated string-literal capture is now at a **100% implemented-or-verified-N/A ledger** across all 34 languages, and the reader-facing contract dial is bumped.

- **WHAT:** Bumped `EXTRACT_CONTRACT_VERSION` 1→2 (`src/external_extract/metadata.rs:13`) in lockstep with `LATEST_SCHEMA_VERSION = 28`. Added a TDD lockstep tripwire `extract_contract_version_and_schema_are_at_phase3b_coordinated_values` (`src/tests/external_extract/info.rs`) that pins both dials so any future drift trips red and forces a coordinated Miller-gate update.
- **WHY:** v2 advertises the new carrier-gated `literals` table (URL/SQL/route records passed to HTTP/DB-client calls, interpolation normalized to `{}`). Miller (`~/source/codesearch`) gates ingestion on exact-equality of this constant, so the bump had to land ONLY in the final commit (no intermediate `(26,2)`/`(27,2)` release).
- **HOW:** RED first (constant was 1, test asserted 2 → `left:1 right:2`), then bumped to GREEN. Recorded the final version triple in the main plan doc.
- **IMPACT:** Phase 3b is done. **Version triple: `(schema_version=28, extract_contract_version=2, EXTRACTION_CONTRACT_VERSION="2026-05-29.bridge-anchors-v2")`.** Miller must re-pin `JulieSchemaGate`/`MillerExtractContract` to `(28, 2)` at its M4 (one-line change; does not block Miller's current M1).

### Verification
- 47/47 `tests::external_extract` green (polyglot literals e2e + contract roundtrip + new lockstep guard).
- The one `cli` bucket failure (`test_bootstrap_standalone_handler_indexes_workspace`) is a PRE-EXISTING parallel-race flake on `/tmp/.julie` + `/private/tmp/.julie` pollution (still present, created today 20:23): passes in isolation, passes 45/45 on clean tree, passes 45/45 with this diff. The contract constant has no causal path into CLI workspace init. Same env-sensitivity class as the rebind/root_detection failures (task #33), NOT a Phase 3b regression.
