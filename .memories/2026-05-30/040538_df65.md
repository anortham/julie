---
id: checkpoint_df656be6
timestamp: 2026-05-30T04:05:38.492Z
tags:
  - miller-bridge
  - test-roles
  - elixir
  - exunit
  - task-54
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: c684255f
  files:
    - crates/julie-extractors/src/c/mod.rs
    - crates/julie-extractors/src/c/test_calls.rs
    - crates/julie-extractors/src/cpp/mod.rs
    - crates/julie-extractors/src/cpp/test_calls.rs
    - crates/julie-extractors/src/elixir/calls.rs
    - crates/julie-extractors/src/lua/core.rs
    - crates/julie-extractors/src/lua/mod.rs
    - crates/julie-extractors/src/lua/test_calls.rs
    - crates/julie-extractors/src/r/mod.rs
    - crates/julie-extractors/src/r/test_calls.rs
    - crates/julie-extractors/src/tests/c/mod.rs
    - crates/julie-extractors/src/tests/c/test_calls.rs
    - crates/julie-extractors/src/tests/cpp/mod.rs
    - crates/julie-extractors/src/tests/cpp/test_calls.rs
    - crates/julie-extractors/src/tests/elixir/mod.rs
    - crates/julie-extractors/src/tests/elixir/test_detection.rs
    - crates/julie-extractors/src/tests/lua/mod.rs
    - crates/julie-extractors/src/tests/lua/test_detection.rs
    - crates/julie-extractors/src/tests/r/mod.rs
    - crates/julie-extractors/src/tests/r/test_detection.rs
    - docs/plans/.test-role-integration-specs.scratch.md
    - docs/plans/2026-05-29-call-style-test-framework-breadth-ledger.md
summary: "#54 Elixir ExUnit gap-fill — DONE (4/4 green)"
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: "Elixir describe blocks now feed the role classifier as containers, and
  setup/setup_all hooks are materialized as lifecycle test symbols — closing
  ledger wave-2 item #3."
next: Await lead; ready for dart shared-core fold (#45) on request.
confidence: 5
---

## #54 Elixir ExUnit gap-fill — DONE (4/4 green)

Filled the 2 Elixir test-role coverage gaps the #53 ledger found. Bespoke (no shared-core convergence, per lead — describe is Namespace-kind). Uncommitted, lead batches.

### Changes (`crates/julie-extractors/src/elixir/calls.rs`)
- **`extract_describe`**: `metadata: None` → `Some({test_container: true})`. Describe was materializing as a Namespace with empty metadata; now carries the container flag for the role classifier.
- **New dispatch arm** `"setup" | "setup_all" => extract_setup(...)` + new `extract_setup` fn → Function named after callee, metadata `is_test` + `test_lifecycle`, signature `setup()`/`setup_all()`, returns `children_visited=false` (matches `extract_test`).

### Test (`crates/julie-extractors/src/tests/elixir/test_detection.rs`, registered in tests/elixir/mod.rs)
4 tests: describe→test_container; nested test stays is_test + parents to describe (regression guard); setup→is_test+test_lifecycle; setup_all→same.

### TDD evidence (proper RED→GREEN, 2 runs)
- RED (pre-impl): `cargo nextest run -p julie-extractors --lib tests::elixir::test_detection` → 1 passed, 3 failed for right reasons (describe "got Some({})"; setup/setup_all not materialized).
- GREEN (post-impl): same → 4 passed.

### Notes
- Test target compiled clean (batch-b's tests/lua + tests/r files had landed).
- `setup :atom` / `setup [:a,:b]` atom-ref forms not specially handled (only the dominant `do`-block hook form); flagged to lead.
- Pre-existing rust-analyzer non_snake_case warnings at calls.rs:120/184 are not mine.

### Status
- #54 complete. Earlier this session: #48 (swift/java/dart, parked uncommitted) and #53 (call-style breadth ledger doc) done.
- Awaiting lead; offered to do the dart/test_calls.rs fold onto shared build_test_call_symbol (#45) now since the core is final.
