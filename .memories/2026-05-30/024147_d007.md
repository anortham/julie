---
id: checkpoint_d00795be
timestamp: 2026-05-30T02:41:47.765Z
tags:
  - test-roles
  - tr-oop
  - task-48
  - swift
  - java
  - dart
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: d4af63c8
  files:
    - crates/julie-extractors/examples/testdetect_probe.rs
    - crates/julie-extractors/src/tests/c/mod.rs
    - crates/julie-extractors/src/tests/c/test_detection.rs
    - crates/julie-extractors/src/tests/cpp/mod.rs
    - crates/julie-extractors/src/tests/cpp/test_detection.rs
    - crates/julie-extractors/src/tests/zig/mod.rs
    - crates/julie-extractors/src/tests/zig/test_detection.rs
summary: "Task #48 (tr-oop): test detection for swift/java/dart — architecture
  mapped, starting impl"
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
next: Inspect java/classes.rs (superclass+interfaces+annotations) and
  dart/mod.rs (call visitor). Then implement swift base_types + toml first
  (RED→GREEN).
---

## Task #48 (tr-oop): test detection for swift/java/dart — architecture mapped, starting impl

**Goal:** Feed the P2a test-role classifier for swift/java/dart. I own disjoint extractor/test/TOML files; lead owns test_detection.rs + test_calls.rs + integration (#44/#45).

### Architecture (verified)
- `src/analysis/test_roles.rs` (P2a, DONE) = the consumer. `classify_test_role` reads:
  - `symbol.annotations[].annotation_key` → mapped via `TestRoleConfig` (from TOML `[annotation_classes.test]`: test_case/parameterized_test/fixture_setup/fixture_teardown/test_container)
  - `symbol.metadata["base_types"]` (or `superclasses` alias) JSON array → base-type container rule via `test_base_types` config (last-segment match)
  - `is_test` metadata flag → convention fallback
- TOML schema in `src/search/language_config.rs`: `TestAnnotationClasses` has all 6 fields incl. `test_base_types`. `build_test_role_configs()` maps them.
- `crates/julie-extractors/src/test_detection.rs` (lead-owned): `is_test_symbol` naming/path heuristics; detect_swift + detect_dart ALREADY exist.
- annotation_key normalization (base/annotations.rs): lowercased; swift `@Test`→"test", `@Suite`→"suite" (verified key_annotation).

### Gap to fill (the rollout)
- **NO extractor emits `base_types` metadata yet** (only python emits `superclasses`). Must add to swift + java class symbols.
- **swift.toml**: NO `[annotation_classes.test]` → add test_case=["test"], test_container=["suite"], test_base_types=["XCTestCase"].
- **java.toml**: HAS test annotations (test_case/parameterized/fixture_setup/teardown/test_container=nested) → ADD test_base_types=["TestCase"].
- **dart.toml**: NO test section → add @test annotation + call-style (test()/group()) via a dart dispatch hook reusing test_calls helpers. NOTE: test_calls.rs TEST_BLOCKS=["it","test"] has "test" but CONTAINER_BLOCKS=["describe","context","suite"] LACKS "group" → report to lead to add "group" + setUp/tearDown for dart.

### Per-extractor facts
- swift/types.rs extract_class: has `inheritance` (Option<String> comma-joined from extract_inheritance in signatures.rs) + `annotations` (extract_annotations). Need to also store base_types Vec in metadata.
- Still TODO to inspect: swift callables.rs (does it extract @Test on funcs?), java classes.rs (superclass/interfaces + annotations), dart mod.rs (call_expression visitor + string node kind).

### Test plan
Extractor-crate tests (tests/{swift,java,dart}/) assert extractor OUTPUT: base_types metadata, annotation keys, dart test-call symbols w/ is_test. End-to-end role classification golden re-bless is lead's (#44). RED→GREEN narrow, max 2 runs/lang (subagent discipline).
