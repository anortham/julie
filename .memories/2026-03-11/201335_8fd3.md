---
id: checkpoint_8fd3ef31
timestamp: 2026-03-11T20:13:35.080Z
tags:
  - tray
  - bundling
  - daemon
  - auto-restart
  - plan
git:
  branch: main
  commit: cca09c1
  files:
    - TODO.md
    - docs/superpowers/plans/2026-03-11-tray-bundling-and-daemon-autorestart.md
summary: "## Implementation Plan: Tray Bundling & Daemon Auto-Restart"
planId: sidecar-binary-distribution
type: checkpoint
impact: Unblocks self-contained tray distribution and eliminates dev restart dance
symbols:
  - find_julie_binary
  - binary_monitor
  - start_server
  - daemon_start
  - is_binary_newer
  - spawn_daemon
  - wait_for_health
next: Execute the plan — Chunk 1 (Tasks 1-5) and Chunk 2 (Tasks 6-8) are
  independent and can be parallelized
---

## Implementation Plan: Tray Bundling & Daemon Auto-Restart

Reviewed GPT's code review of the tray app, verified findings against actual code, then wrote a comprehensive implementation plan covering two features:

### Feature 1: Bundle server binary in tray app (Tasks 1-5)
- Tauri `externalBin` sidecar mechanism bundles `julie-server` inside DMG/installer
- Updated `find_julie_binary()` to discover sidecar binaries with target-triple suffix
- CI workflow updated so `build-tray` depends on `build`, downloads server artifact by exact name
- Dev helper script for local builds
- Also fixes GPT review bugs: wrong updater repo URL (`anthropics` → `anortham`), version drift, dashboard label

### Feature 2: Daemon auto-restart on binary change (Tasks 6-8)
- New `src/binary_monitor.rs` polls daemon's own binary mtime every 5s
- Shares `CancellationToken` with server — cancel triggers graceful shutdown
- `connect` bridge's existing reconnect logic automatically restarts with new binary
- `start_server` signature changed to accept external `CancellationToken` (requires `server_tests.rs` update)
- Health timeout increased from 5s to 15s

### Plan reviewed and fixed
- Two code-reviewer agents caught: download-artifact doesn't support globs, test double-unwrap bug, server_tests.rs breakage, env-var test race conditions, `&PathBuf` vs `&Path` Clippy lint, timing fragility in monitor test
- All issues fixed in the plan

**Plan saved to:** `docs/superpowers/plans/2026-03-11-tray-bundling-and-daemon-autorestart.md`
