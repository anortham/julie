---
id: checkpoint_a66d7bbd
timestamp: 2026-04-16T23:08:08.207Z
tags:
  - release
  - version-sync
  - docs
git:
  branch: main
  commit: 86c7c5ea
  files:
    - .claude/settings.local.json
    - AGENTS.md
    - CLAUDE.md
summary: Synced release metadata to v6.9.0
briefId: julie-world-class-systems-program
type: checkpoint
impact: Release-facing metadata is now aligned across Cargo, dashboard, site,
  and project instruction docs.
next: Build the release binary from `main` and dogfood the dashboard and daemon
  version reporting.
confidence: 5
---

## Synced release metadata to v6.9.0

### WHAT
- Updated `AGENTS.md` and `CLAUDE.md` footer status lines from `v6.8.0` to `v6.9.0`.
- Confirmed `Cargo.toml`, `Cargo.lock`, the dashboard, daemon startup log, and `docs/site/index.html` were already on `6.9.0`.

### WHY
- The user wants the release build and dashboard to show the new code path clearly.
- Repo metadata was stale even though the binary-facing version sources were already correct.

### HOW
- Verified version sources with ripgrep before editing.
- Left version-gate tests and historical notes alone because those `6.8.0` references are fixtures and release history, not current metadata.

### IMPACT
- `main` now has consistent visible release metadata for `v6.9.0`.
- Building a release from `main` will show `6.9.0` in daemon and dashboard, without stale footer text in project instructions.
