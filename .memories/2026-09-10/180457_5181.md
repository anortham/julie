---
id: checkpoint_518142b8
timestamp: 2026-09-10T18:04:57.471Z
tags:
  - machine-service
  - facts.sqlite
  - test-full
  - workspace
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: grok-shell-goldfish
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Ported force-refresh routing test onto facts.sqlite
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo xtask test full failed: force refresh should attribute codehealth
    snapshot to rebound session primary"
  - cargo nextest run --lib
    test_manage_workspace_refresh_force_uses_rebound_session_primary_root PASS
    0.045s
symbols:
  - test_manage_workspace_refresh_force_uses_rebound_session_primary_root
---

## Ported force-refresh routing test onto facts.sqlite

- **WHAT:** `test_manage_workspace_refresh_force_uses_rebound_session_primary_root` no longer asserts daemon codehealth snapshots.
- **WHY:** Force refresh no longer writes `codehealth_snapshots`. Full failed at that stale assertion.
- **HOW:** Pin `in_process_index_root` to the shared `indexes/<rebound_id>/` root. Assert `facts.sqlite` (not `db/` or `store/`). Open CheckoutStore and check `rebound_primary_marker`.
- **IMPACT:** Unblocks `cargo xtask test full` at tools-workspace-targeting.

RED: full suite panic at refresh_routing.rs:156 (no snapshot).
GREEN: `cargo nextest run --lib test_manage_workspace_refresh_force_uses_rebound_session_primary_root` PASS.

Do not stage `crates/julie-context/tests/no_upward_deps.rs`.
