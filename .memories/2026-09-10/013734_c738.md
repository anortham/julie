---
id: checkpoint_c73801ed
timestamp: 2026-09-10T01:37:34.547Z
tags:
  - machine-service
  - task-9
  - gates
  - durable-roots
git:
  branch: main
  commit: 8b07074b
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 9 gates: durable_roots red on a stray init lock file"
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: Task 9 of the machine-service phase 2 plan, worktree
  /home/murphy/source/julie/.worktrees/machine-service at 98c72a9d
evidence:
  - "cargo nextest run --lib tests::service::durable_roots: FAIL stray
    .symbols.db.init.lock"
  - "cargo nextest run --lib tests::service::budget: 2/2 pass"
  - "cargo nextest run -p xtask: 179/179 pass"
  - "sh scripts/complexity-words.sh main: exit 1, 17 hits"
  - "cargo xtask test fast: 19.15 s wall"
next: "Lead rules on the init lock file and the 17 script hits; then commit
  test(gates): durable roots test, complexity-words script, tier cleanup"
---

## Task 9 gates: durable_roots red on a stray init lock file

**WHAT:** Added `src/tests/service/durable_roots.rs`, `scripts/complexity-words.sh`, the `complexity-words` on-demand bucket, `durable_roots` in the `service` bucket, and extended the coordination-words budget test to `seed.rs` and `registry/status.rs`. Removed the dead `nl_embeddings_daemon_tests` command from `tools-search-unified`.

**WHY:** Design section 12 gates for Phase 2 (durable roots per checkout and per machine; no coordination words in new code).

**HOW:** Test drives the service `POST /api/manage_workspace index` on a temp workspace, then enumerates `$JULIE_HOME`, `indexes/`, `indexes/<id>/`, and `db/`.

**IMPACT / FINDING:** `durable_roots` is red: `db/` still holds `.symbols.db.init.lock`, written by `acquire_database_init_lock` in `crates/julie-core/src/database/mod.rs:320` (an `fs2` file lock, cross-process coordination the plan says Phase 2 deleted). The script exits 1 on the branch with 17 hits (4 moved code, 11 pre-existing name referenced, 2 new). Not committed; lead rules. `cargo xtask test fast` wall time 19.15 s.
