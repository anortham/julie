---
id: checkpoint_bf791cd9
timestamp: 2026-09-10T17:27:55.242Z
tags:
  - task-13
  - test-restore
  - machine-service
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: grok-shell-goldfish
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Restored live test modules dropped with symbols.db
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
impact: dev buckets can find tests again. Lead owns cargo xtask test dev.
evidence:
  - cargo check --tests -p julie --offline green
  - "cargo nextest required filters: 235 passed then dashboard projection test
    fixed"
  - "cargo nextest run -p xtask: 178 passed"
  - cargo build green
confidence: 4
---

## Restored live test modules dropped with symbols.db

Task 13 review: `pub mod` lines were dropped from `src/tests/mod.rs`, so `cargo xtask test dev` died with nextest exit 4.

- Restored live `pub mod` lines for files that still exist.
- Ported handler, search, dashboard, registry, workspace, integration tests onto CheckoutStore/snapshot/facts.sqlite.
- Deleted tests that only named deleted SymbolDatabase APIs (resolver, index_embedding_tests, structural_facts_text_test, relationship_text_test, bulk_storage_atomicity) and removed their bucket commands.
- Required filters now run tests (not exit 4). `cargo build` green. `cargo nextest run -p xtask` 178 passed.

