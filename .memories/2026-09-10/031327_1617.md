---
id: checkpoint_16175a01
timestamp: 2026-09-10T03:13:27.442Z
tags:
  - machine-service
  - phase-2
  - gate
  - verification
git:
  branch: main
  commit: 8b07074b
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Machine service phase 2 gate passed (Task 10)
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
next: Run finishing-a-development-branch; no push or PR authority granted.
---

## Machine service phase 2 gate passed (Task 10)

**WHAT:** Branch gate for phase 2 (one writer per checkout, disposable indexes) is green: dev, system, service-process, and full all pass at 46bb53da (51 buckets, 471 s). Finding at `docs/findings/2026-09-10-machine-service-phase2-gate.md`, ledger at `docs/plans/2026-09-09-machine-service-phase2-ledger.md`, docs rewritten to the one-writer model.

**WHY:** Design section 14 requires the section 5.4 deletion list empty and net negative lines. Rust lines (plan tokei command) went from 159,488 at a3ef3e5a to 145,367; broker client and embedding generations deferred to phase 4 by the recorded sequencing decision.

**HOW:** The full tier had never run against several tests that encoded deleted behavior (root swap, init lock race, boundary tripwires) or that already failed on main (rename tests calling the prepare-only tools-crate entry). The search-quality snapshot (schema 9) cannot be opened without migrations; a rebuilt one is ~300 MB (over GitHub's 100 MB limit), so it is now git-ignored and built on demand by `ensure_julie_fixture` in the search-quality bucket.

**IMPACT:** Phase 3 (`facts.sqlite` + derived indexes + read tools) can start from a clean gate. The snapshot decision is reversible (LFS or a smaller snapshot).
