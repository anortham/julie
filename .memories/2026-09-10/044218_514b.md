---
id: checkpoint_514b2404
timestamp: 2026-09-10T04:42:18.033Z
tags:
  - machine-service
  - phase-3
  - plan
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 3 plan written: facts, graph, snapshots, read tools"
briefId: machine-service-one-rust-service-per-machine-repla
type: decision
decision: Phase 3 replaces symbols.db with facts.sqlite + in-memory graph +
  snapshots in one plan of 14 tasks, with vectors pulled forward from phase 4.
alternatives:
  - "Split phase 3 into two plans (storage first, tools second): rejected
    because the storage plan alone deletes nothing and cannot pass the
    net-negative gate"
  - "Drop semantics for one phase and keep vectors in phase 4: rejected to avoid
    a semantics outage"
  - "Move storage to <checkout>/.julie now: rejected because the standalone CLI
    would be a second writer on the same file until phase 6"
---

## Phase 3 plan written: facts, graph, snapshots, read tools

**WHAT:** `docs/plans/2026-09-10-machine-service-phase3-plan.md`, 14 tasks. New `julie-facts` crate (blob-keyed `facts.sqlite`, one writer, reader, version check), graph and pure resolution in `julie-index`, `Snapshot` + `CheckoutStore` + `ToolContext::snapshot`, six tool ports in two parallel batches, vectors in facts with a brute-force scan, workspace commands and seed on facts, remaining readers, then deletion of `crates/julie-core/src/database/` and the projection/repair bookkeeping, then the section 12 gate.

**WHY:** Phase 2's sequencing decision moved the storage schema and the read tools into one phase so old storage is deleted in the same phase that replaces it.

**HOW:** Strangler order: new store is written beside the old from Task 3 to Task 13; each tool port deletes that tool's SQLite reads; Task 13 deletes the old store. Seven sequencing decisions need approval: vectors move into phase 3 (no semantics outage), storage stays under `$JULIE_HOME/indexes/<id>/` until the CLI moves to `/api` in phase 6, Julie tool names stay, `extract` writes the facts schema, web edges derived not stored, crate renames wait for phase 6, reference scores computed in the graph.

**IMPACT:** Awaiting user approval of the plan before execution.
