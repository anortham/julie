---
id: checkpoint_4426f9de
timestamp: 2026-09-10T15:02:25.384Z
tags:
  - task-11
  - facts-store
  - workspace
  - machine-service
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: grok-shell-goldfish
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 11: workspace commands, startup, and seed write the facts store"
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
symbols:
  - CheckoutStore::open
  - CheckoutStore::apply
  - open_or_recreate
  - seed_from_sibling
  - plan_primary_workspace_repair
  - StoreStatus
next: "Task 12: health/dashboard/edit/extract. Task 13: delete symbols.db."
confidence: 4
unknowns:
  - Health/search readiness still reads symbols.db
  - Stale-file embedding refresh parked
  - Reference refresh codehealth snapshot parked
---

## Task 11: workspace commands, startup, and seed write the facts store

### WHAT
Index, refresh, rebuild, open, status, startup catch-up, and sibling seed now write and open `indexes/<id>/store` (`facts.sqlite` + Tantivy) as the primary path. Full and incremental index are one function: scan -> `PathChange` list -> `CheckoutStore::apply` -> embedding job.

### WHY
Tasks 1-10 left tools reading `ToolContext::snapshot`, but index/startup still wrote `symbols.db`. The project-local store also predated the encoder table, so Julie CLI failed with `no such table: encoder`.

### HOW
- `open_or_recreate` deletes `store/` on any failed open (including missing encoder) and reopens.
- `runtime_factory` retries initialize after deleting `store/`.
- Seed ATTACHes the sibling facts DB read-only and copies blobs/fact rows by hash.
- Startup catch-up compares `paths` to disk hashes, not mtime/`symbols.db`.
- Status fields come from `StoreStatus`.
- Deleted unused `resolver/` sources and the old persist_batch atomic writes.

### IMPACT
Julie CLI `context`/`refs` work again after store recreate. Worker-ceiling tests pass (163 workspace + 13 workspace_init). Health/search readiness still reads `symbols.db` until Task 12.

### Evidence
- `cargo nextest run --lib tests::tools::workspace` — 163 passed
- `cargo nextest run --lib tests::core::workspace_init` — 13 passed
- `cargo build` green
- banned identifiers gone from owned dirs
- seed copies 6 blobs, extracts 2
