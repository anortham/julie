---
id: checkpoint_6e2ff486
timestamp: 2026-09-10T12:59:56.146Z
tags:
  - machine-service
  - phase3
  - fast_search
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 3 Task 4 reviewed: fast_search on the snapshot"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Phase 3 Task 4 reviewed: fast_search on the snapshot

**WHAT:** `crates/julie-tools/src/search/**` now takes one `handler.snapshot(&target)` at the top of `execute_with_trace`; symbol hydration reads the graph, file text reads `Snapshot::file_text`, source regions read the facts reader. `crates/julie-index/src/search/index/query/` gained `SearchView<'a>` (searcher + fields) so `SearchIndex` methods and snapshot callers share one query body. Tests in `src/tests/tools/search/**` run on `SnapshotFixture` via the new `src/tests/helpers/snapshot.rs`.

**WHY:** Plan Task 4 (tool ports off `SymbolDatabase`). Batch A ran Tasks 4, 5, 6 in parallel with lead commit.

**HOW / decisions:** SearchView refactor chosen over duplicated `_with_searcher` copies. Deleted as deleted behavior: `line_mode/missing_index.rs`, the db-without-tantivy probes. Four `backend_param_tests` KNN tests are `#[ignore]` until Task 10 fills `VectorSet` (Task 10 must un-ignore them). `file_search_preserves_hidden_directory_ranking_in_tool_output` stays on the real handler because `SnapshotFixture::from_tree` skips dot-prefixed paths. Dev gate after Tasks 1-3 passed (30 buckets, 58.5s warm) at 3e43fec4.

**IMPACT:** Tasks 5 and 6 (same batch) commit next; then the Batch A dev gate. Request logged: `Graph::symbol_by_row_id` accessor to replace the per-hit scan in `hydrate_symbols`.
