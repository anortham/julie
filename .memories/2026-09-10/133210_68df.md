---
id: checkpoint_68df424c
timestamp: 2026-09-10T13:32:10.131Z
tags:
  - machine-service
  - phase3
  - batch-b
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 3 Batch B reviewed: deep_dive, blast_radius, get_context on the
  snapshot"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Phase 3 Batch B reviewed: deep_dive, blast_radius, get_context on the snapshot

**WHAT:** Three parallel ports land as three lead commits after 4e4a1250. deep_dive reads graph neighbors plus one facts complexity query; blast_radius walks references_to/callers/WebRoute/SqlQuery edges with the same frontier limits and loses the canonical-revision seed (plan decision 3); get_context pivots through SearchView over the snapshot searcher and expands over graph edges; `hybrid_search_with_tagged_embedding` takes `&Snapshot`. Lead merged the duplicated reference-site helper into `navigation::sites` (now pub(crate)).

**WHY:** Plan Tasks 7-9, Batch B, parallel-lead-commit. All worker scopes rerun green in the shared tree (169 julie-tools tests, 38 root tests).

**HOW / decisions:**
- `hybrid_search_tests` and `m1_challenger_tests` in julie-tools are held with `#[cfg(any())]`; they assert DB generation and revision behavior. Task 10 (owns hybrid.rs and vectors) ports or deletes them.
- Task 10 must also lift: four `backend_param_tests` KNN tests, `formatting_tests::test_fast_refs_semantic_fallback_offloaded_to_spawn_blocking`, and the `debug_assert!(vectors().is_empty())` seam in `deep_dive/data/similarity.rs`.
- The snapshot path never applies `apply_important_patterns_boost` (SearchView has no language configs); consistent across all ported tools; Task 14 dogfood gate decides.
- `test_linkage`/`test_coverage` metadata is not produced by the checkout store (analysis ran on SQLite only); Task 11 or 12 must port that enrichment or the gate records it.
- Revision-seed mentions removed from JULIE_AGENT_INSTRUCTIONS.md, impact-analysis SKILL.md, catalog.rs; orphaned `fixtures/eval/blast_radius_mixed_traversal.toml` deleted.
- Dogfood finding: after `cargo xtask test dev` rebuilt target/debug/julie-server with the ported tools, standalone `symbols`/`refs` return nothing for files not re-written since Task 3, because the transitional store only holds double-written paths. A full reindex of the dogfood home fills it. Investigate before Task 10 dispatch.

**IMPACT:** Next: dev gate on Batch B, then Task 10 (vectors) serial.
