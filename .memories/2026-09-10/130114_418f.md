---
id: checkpoint_418f55dc
timestamp: 2026-09-10T13:01:14.787Z
tags:
  - machine-service
  - phase3
  - batch-a
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 3 Batch A reviewed: Tasks 5 and 6 (get_symbols, patterns,
  fast_refs, call_path)"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Phase 3 Batch A reviewed: Tasks 5 and 6 (get_symbols, patterns, fast_refs, call_path)

**WHAT:** Task 5 committed at dda9ae38 (get_symbols reads `graph.symbols_in_path`; patterns reads facts structural facts with post-fetch glob/metadata filters; tools-get-symbols bucket gains patterns, recalibrated 40s -> 10s). Task 6 commits next: `navigation/` is one `find_references(snapshot, ..)` over graph walks; `target_workspace.rs` deleted; `call_path` BFS over `callees`/`callers`; tools-call-path bucket gains web_navigation, 80s -> 10s.

**WHY:** Plan Tasks 5 and 6, Batch A, parallel-lead-commit.

**HOW / findings:**
- Three tests ignored by Task 6 expose graph gaps: qualified identifiers (`FastRefsTool::call_tool`, `crate::search::hybrid::f()`) tie on leaf name and the resolver drops the edge; no `SqlQuery` edge kind for web mode routine -> table. Lead routes both to the Task 2 implementer as fix round 1 (EdgeKind gains `SqlQuery`; resolver uses the identifier's qualifier from `code_context` / `receiver_type` and the path-module suffix to break ties).
- Three private `SymbolRow -> julie_core::Symbol` converters exist (search/text_search.rs, symbols/rows.rs, navigation/resolution.rs); lead merges them into one shared helper in julie-tools.
- Deferred minor: manifest_contract_tests.rs:87 text still calls get_symbols_target_filtering "ignored slow tests"; tools-dogfood-repo-index still lists the now-fast dogfood test; possible fast_refs gap (refs missed two callers of get_symbols_for_file in dogfood).

**IMPACT:** After the fix round and dedupe, the lead runs the Batch A gates (dev + tool buckets) and dispatches Batch B (deep_dive, blast_radius, get_context).
