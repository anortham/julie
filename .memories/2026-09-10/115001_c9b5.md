---
id: checkpoint_c9b59fe1
timestamp: 2026-09-10T11:50:01.342Z
tags:
  - machine-service
  - phase3
  - julie-facts
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 3 Task 1 reviewed and approved: julie-facts crate"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Phase 3 Task 1 reviewed and approved: julie-facts crate

**WHAT:** Worker commit 44bdbaaf added `crates/julie-facts` (schema, store, writer, reader, version) and the `core-facts` xtask bucket. Lead follow-up updates `src/tests/core/engine_version.rs` (drops the `+epoch=9` and consumer-marker asserts, asserts `+facts=`) and adds a `crates/julie-facts/` -> `core-facts` rule in `xtask/src/changed/mapping/crates.rs`.

**WHY:** The engine version string is now `extractors=<contract>+extractors-tag=v2.42.0+facts=1`; the old test named a banned word (epoch). The changed mapping had no rule for the new crate.

**HOW / decisions:**
- `core-facts.expected_seconds = 2` (plan said 5) because the dev tier budget was at 598s of 600. Accepted.
- Lead resolution for Task 3/4: `facts.sqlite` stores no file content (design 6.1 `blobs` has no content column, and no new table is allowed). Tool text and Tantivy source bodies read the file from the checkout by path and verify blake3 against `paths.blob_hash`.
- Literal classification uses `julie_extractors::classify_literals_by_carrier`; test roles ported into `julie_facts::rows::test_roles`.

**IMPACT:** Task 2 (graph) can start on `FactsReader` row types. `FactsReader<'a>` borrows the store connection; Task 3 opens a second store for a snapshot-owned reader if needed.
