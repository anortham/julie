---
id: checkpoint_2522e671
timestamp: 2026-09-10T00:04:18.196Z
tags:
  - machine-service
  - phase2
  - task-2
  - fix-round-1
  - julie-index
git:
  branch: main
  commit: 8b07074b
  worktree: /home/murphy/source/julie
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 2 fix round 1: delete concurrent rebuild race test"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Task 2 fix round 1: delete concurrent rebuild race test

**WHAT**
- Deleted `concurrent_rebuild_both_succeed` and its winner/loser doc comment from `crates/julie-index/src/tests/search/compat_marker_v4_test.rs`.
- Kept `detects_v3_marker_as_stale` and `accepts_v4_marker` (single-thread recreate-on-incompatible-marker tests).

**WHY**
The test proved the `fs2` rebuild flock that Task 2 removed. Under the machine-service design one handler per index root opens indexes behind the workspace mutation gate, so two concurrent rebuilds of one index cannot happen (design rule 6: no branch for an impossible state).

**HOW**
`fs2` was already dropped from `crates/julie-index/Cargo.toml` at 386f49d6; grep confirms no `fs2` use remains in julie-index. `cargo build` green; `cargo nextest run -p julie-index --lib tests::search::compat_marker_v4_test` 2/2.

**IMPACT**
The core-index bucket no longer fails on the deleted race.
