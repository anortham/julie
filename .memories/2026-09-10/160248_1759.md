---
id: checkpoint_1759825e
timestamp: 2026-09-10T16:02:48.636Z
tags:
  - task-12
  - machine-service
  - facts-store
git:
  branch: main
  commit: 63cefbcf
  worktree: /home/murphy/source/julie
actor:
  harness: grok-shell-goldfish
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Task 12 ports remaining SymbolDatabase readers onto the facts store
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
decision: Kept the durable edit journal because recover_edit still needs
  before/after bytes; paths blob hashes cannot resume or rollback an edit_id.
alternatives:
  - Delete the journal and serve recover_edit from facts hashes only (cannot
    rewrite file bytes)
  - Add a new in-memory last-write map (section 4 already has ADR-0004 edit
    locks; no new journal identifier)
impact: Edits, health, extract, and dashboard no longer read SymbolDatabase.
  Dual-write of tool_calls to symbols.db remains in handler/tool_metrics.rs
  until Task 13.
evidence:
  - cargo nextest run --lib
    test_rewrite_symbol_metrics_include_symbol_span_and_failure_kind PASS
  - cargo nextest run --lib
    test_rewrite_symbol_failed_apply_metrics_record_applied_false PASS
  - cargo nextest run --lib tests::tools::editing tests::tools::refactoring
    tests::external_extract tests::dashboard tests::health 224 passed
  - cargo nextest run -p julie-tools --lib tests::editing 34 passed
  - cargo build green
symbols:
  - find_symbol
  - RewriteSymbolTool::prepare_rewrite
  - SourceEditCoordinator::publish_applied_paths
  - build_data_plane
  - run_external_scan
  - CheckoutStore::apply
next: Commit owned files. Task 13 deletes crates/julie-core/src/database and
  remaining SymbolDatabase readers.
confidence: 4
---

## Task 12 ports remaining SymbolDatabase readers onto the facts store

- **WHAT:** Edit tools look up symbols on the snapshot graph and check freshness from `facts.paths()` blob hashes. After an applied edit, `CheckoutStore::apply` runs under `mutation_gate`. Health reports `StoreStatus`. Extract writes `facts.sqlite` through `FactsWriter`. Dashboard intelligence reads the graph. Metrics query walks the graph. Codehealth snapshots take counts. Publication and recovery modules are deleted.
- **WHY:** Tasks 1-11 already write the facts store. Rewrite/health still failed after a facts-only index because they opened `symbols.db`.
- **HOW:** Reused `resolution::find_symbols` + `to_symbol`. `SourceEditCoordinator::with_store` publishes written paths. Health data plane uses `store.status()` only. Extract `--out` (alias `--db`) opens `FactsStore`.
- **IMPACT:** The two rewrite metrics tests are green without writing `symbols.db`. Worker-ceiling editing/refactoring/extract/dashboard tests pass. Task 13 can delete the database module. `rg` still lists handler/indexing/pipeline/watcher files (Task 13).
