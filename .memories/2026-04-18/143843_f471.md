---
id: checkpoint_f471dec2
timestamp: 2026-04-18T14:38:43.311Z
tags:
  - docs-drift
  - cleanup
  - tool-surface
  - skills
git:
  branch: main
  commit: 8895f147
  files:
    - README.md
    - dashboard/static/app.css
    - dashboard/static/app.js
    - docs/site/index.html
    - src/tests/tools/metrics/mod.rs
    - src/tests/tools/metrics/operational_metrics_tests.rs
    - src/tools/metrics/mod.rs
    - src/tools/metrics/operational.rs
summary: Tool surface drift cleanup (post v7.0.0)
briefId: julie-world-class-systems-program
type: checkpoint
---

## Tool surface drift cleanup (post v7.0.0)

After the `feat(tools): tighten the agent tool surface` and `refactor(skills): reduce repo skill set` commits, downstream docs and dead code needed updating to match the new 10-tool surface and 5-skill repo set.

### What changed

**Dead code removal (build warnings → 0):**
- Deleted `src/tools/metrics/operational.rs` (5 functions: `format_bytes`, `percentile_95`, `format_uptime`, `format_session_from_metrics`, `format_history_output`)
- Deleted `src/tests/tools/metrics/operational_metrics_tests.rs`
- Removed module declarations from `src/tools/metrics/mod.rs` and `src/tests/tools/metrics/mod.rs`
- Confirmed via `fast_refs`: zero non-test prod consumers; orphaned by the `query_metrics` removal

**README.md drift fixes:**
- Anchor link `#tools-8` → `#tools-10`
- "33 programming languages" → "34"
- Header `## Tools (8)` → `## Tools (10)` (already done)
- Added `call_path` to Search & Navigation
- Replaced `edit_symbol` block with `rewrite_symbol` (correct operations: replace_full/body/signature, insert_before/after, add_doc)
- Removed `query_metrics` MCP tool section, replaced with dashboard pointer
- Updated `manage_workspace` to list actual operations (index, open, add, remove, list, refresh, stats, clean, health)
- Updated `rename_symbol` to mention `scope` parameter
- Skills section: 11 → 5 (or 4 distributed); removed `/architecture`, `/metrics`, `/call-trace`, `/logic-flow`, `/dependency-graph`, `/type-flow`
- Project Structure: added `editing/`, `metrics/`, fixed `navigation/` (now has fast_refs + call_path)

**gh-pages (docs/site/index.html) drift fixes:**
- Replaced `/architecture` and `/metrics` skill cards with `/explore-area` and `/impact-analysis` (the actual distributed skills)
- Updated "Also included" line to mention only `/web-research` (the special-case skill)
- "11 pre-built workflows" → "A focused set of pre-built workflows"

### Why

Tool surface changed in two recent commits but downstream user-facing docs (README, gh-pages) still listed removed tools/skills. README also had off-by-one errors ("33 languages", `#tools-8`). Dead code was producing 5 build warnings.

### Verified

- `cargo build`: 0 warnings (was 5)
- `cargo nextest run --lib tests::tools::metrics`: 22/22 passing
- CLAUDE.md / AGENTS.md: in sync, no stale tool refs
- No remaining `edit_symbol`, `query_metrics`, removed-skill references in user-facing docs

### Next

User wants to discuss dashboard improvement opportunities + reference workspaces concept. Reference workspaces verified to still exist (via DaemonDatabase pairing, not the legacy `WorkspaceType::Reference` enum which is now dead in prod). Dashboard correctly shows them.
