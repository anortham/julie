---
id: checkpoint_8f9219cf
timestamp: 2026-07-21T18:09:32.324Z
tags:
  - xtask
  - runner
  - prebuild
  - test-control-plane
  - task-5
git:
  branch: feat/test-control-plane
  commit: 46a836ab
  files:
    - xtask/src/changed.rs
    - xtask/src/cli.rs
    - xtask/src/main.rs
    - xtask/src/runner.rs
    - xtask/tests/changed_tests.rs
    - xtask/tests/runner_tests.rs
summary: "Task 5: Account prebuild time in run summary"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Cold wall = prebuild + warm buckets is visible in every xtask test summary.
evidence:
  - cargo nextest run -p xtask --lib
    runner_tests_summary_includes_prebuild_elapsed → PASS
  - cargo nextest run -p xtask --lib runner_tests_ → 5 passed
  - cargo nextest run -p xtask --test runner_tests --test runner_coverage_tests
    → 44 passed
symbols:
  - RunSummary
  - prebuild_test_binary
  - run_named_buckets
  - render_summary
  - runner_tests_summary_includes_prebuild_elapsed
next: Lead reviews and commits; docs task should note declared budget ≠ cold wall.
confidence: 5
---

## Task 5: Account prebuild time in run summary

### WHAT
Runner now records and prints `prebuild_elapsed` separately from warm bucket wall.

### WHY
Cold `fast`/`nano` runs looked "under budget" because `prebuild_test_binary` time was omitted from `total_elapsed`.

### HOW
- Added `RunSummary.prebuild_elapsed`
- `prebuild_test_binary` returns `Duration` (or `PrebuildError` with elapsed)
- `total_elapsed` stays warm-only; soft 1.5× overrun unchanged
- `render_summary` prints warm / PREBUILD / COLD WALL
- FakeExecutor unit test in `runner.rs` asserts struct + rendered output

### IMPACT
Honest cold vs warm reporting without claiming declared budget includes prebuild. No commit (parallel-lead-commit). Report at `.razorback/sdd/task-5-report.md`.
