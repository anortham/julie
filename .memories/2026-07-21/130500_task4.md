---
type: checkpoint
tags: [xtask, changed, OverBudget, scale, task-4]
symbols: [ChangedSelectionMode, select_changed_buckets, apply_changed_scale, TestCommand]
---

## Task 4: changed OverBudget + --scale

### WHAT
`ChangedSelectionMode::OverBudget` after mapped budget gate (60s via `declared_expected_seconds`); `--scale` → `unique(mapped ∪ dev)`; main exits non-zero without running on OverBudget.

### WHY
Do not silently replace over-budget mapped sets (e.g. dogfood) with bare `dev`.

### HOW
Post-filter only when fully mapped; FallbackToDev unchanged; scale union rationale explicit.

### IMPACT
Gate tests green (dogfood OverBudget, under-budget Buckets, scale union, CLI parse). No commit (parallel-lead-commit).
