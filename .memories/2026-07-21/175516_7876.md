---
id: checkpoint_7876077c
timestamp: 2026-07-21T17:55:16.002Z
tags:
  - xtask
  - test-control-plane
  - task-2
  - fast-tier
git:
  branch: feat/test-control-plane
  commit: 94bcb269
  files:
    - xtask/src/cli.rs
    - xtask/src/manifest.rs
    - xtask/src/runner.rs
    - xtask/test_tiers.toml
    - xtask/tests/changed_tests.rs
    - xtask/tests/inventory_tests.rs
    - xtask/tests/manifest_contract_tests.rs
    - xtask/tests/manifest_tests.rs
    - xtask/tests/runner_coverage_tests.rs
    - xtask/tests/runner_tests.rs
    - xtask/tests/support/manifest_contract_expected.rs
summary: "Task 2: fast tier + validate() budget checks"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: decision
context: Task 2 of test-control-plane plan on feat/test-control-plane worktree
decision: Provisional fast mirrors nano membership; core-fast.expected_seconds
  60→55 so fast/nano sum ≤60; production validate hard-fails missing/over-budget
  fast and over-budget dev
alternatives:
  - Provisional fast = [core-database] only (sum 5) — rejected because nano ⊆
    fast would not hold
  - Keep core-fast at 60 and allow nano to exceed fast budget temporarily —
    rejected; prefer plan option 2
impact: Unblocks Task 2; Task 6 owns warm recalibration of core-fast
  expected_seconds / fast membership
evidence:
  - cargo nextest run -p xtask -- manifest_tests_reject_missing_fast_tier
    manifest_tests_reject_over_budget_fast_tier
    manifest_tests_accept_valid_fast_tier
    manifest_tests_reject_over_budget_dev_tier → 4 passed
  - fast=60s nano=60s nano⊆fast true; dev=584s
symbols:
  - TestManifest::validate
  - TestManifest::from_str
  - TestManifest::tier_expected_seconds
next: Task 6 warm-timing recalibration of provisional core-fast=55
confidence: 5
---

## Task 2: fast tier + validate() budget checks

### WHAT
Added production `TestManifest::validate` budget gates for `fast` (required, ≤60s) and `dev` (if present, ≤600s). Checked-in `test_tiers.toml` now defines provisional `fast = ["core-database", "core-fast"]` with `core-fast.expected_seconds` lowered 60→55 so nano ⊆ fast stays ≤60.

### WHY
Control-plane needs a hard ≤60s `fast` tier and production validation (not cfg(test)-only) so bad manifests cannot load.

### HOW
- TDD via `from_str` fixtures: missing/over-budget/valid fast + over-budget dev
- Constants `FAST_TIER_MAX_EXPECTED_SECONDS=60`, `DEV_TIER_MAX_EXPECTED_SECONDS=600` at module scope
- Fixture updates across related xtask tests so `from_str` still parses

### IMPACT
Real `test_tiers.toml` loads; Task 6 must recalibrate provisional 55s timing.
