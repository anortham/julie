---
id: checkpoint_5bf6815e
timestamp: 2026-04-23T21:29:53.167Z
tags:
  - annotations
  - review
  - watcher
  - search-quality
git:
  branch: codex/annotation-normalization
  commit: 80f3e2c9
  files:
    - docs/plans/2026-04-19-tantivy-0.26-comparison-report.md
    - src/search/projection.rs
    - src/tests/integration/watcher_handlers.rs
    - src/tests/integration/workspace_isolation_smoke.rs
    - src/tests/tools/search_quality/helpers.rs
    - src/watcher/handlers.rs
    - src/watcher/runtime.rs
summary: Fixed Claude Review Watcher Finding
briefId: julie-world-class-systems-program
type: checkpoint
impact: The Claude-identified watcher bug is fixed and the changed gate is green.
evidence:
  - "cargo nextest run --lib
    test_incremental_indexing_preserves_tantivy_annotation_fields 2>&1 | tail
    -100: failed before fix, passed after fix"
  - "cargo nextest run --lib
    test_incremental_indexing_preserves_tantivy_file_content 2>&1 | tail -80:
    passed"
  - "cargo nextest run --lib annotation_search 2>&1 | tail -80: passed"
  - "cargo nextest run --lib workspace_isolation_smoke 2>&1 | tail -120: passed"
  - "cargo nextest run --lib test_content_single_identifier_snake_case 2>&1 |
    tail -100: passed"
  - "cargo xtask test changed 2>&1 | tail -120: selected tools-search,
    core-fast, integration, search-quality; all passed in 270.9s"
  - "cargo build 2>&1 | tail -80: passed"
next: Commit review fixes, summarize Claude review as one verified finding fixed
  plus reviewer cap notes.
---

## Fixed Claude Review Watcher Finding

- **WHAT**: Fixed incremental watcher Tantivy updates so annotation fields survive file edits, added a watcher regression for `@test` searches, serialized workspace isolation smoke tests with file locks, and opened dogfood fixture DB copies through `SymbolDatabase::new` so migrations run.
- **WHY**: Claude review found that the watcher used bare `add_symbol`, which wrote empty annotation fields and caused annotation search false negatives after edits. The changed gate also exposed fixture-state races and an old-fixture schema gap.
- **HOW**: Added `apply_uncommitted_documents_from_symbols` in `src/search/projection.rs`, reused it from watcher update and dirty retry paths, added `file_serial` to shared fixture smoke tests, and routed search-quality fixture DB setup through the production database constructor.
- **IMPACT**: Live edit paths now preserve annotation search behavior, the integration bucket avoids shared fixture collisions, and dogfood fixtures receive current schema setup before backfill.
