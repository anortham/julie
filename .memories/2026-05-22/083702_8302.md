---
id: checkpoint_83021f1a
timestamp: 2026-05-22T08:37:02.235Z
tags:
  - t9
  - fts-phase2
  - cleanup
  - unified-schema
  - reranker
git:
  branch: feature/fts-phase2-unified-schema
  commit: fa9187dc
  files:
    - src/dashboard/search_compare.rs
    - src/search/debug.rs
    - src/search/hybrid.rs
    - src/search/index.rs
    - src/search/mod.rs
    - src/search/projection.rs
    - src/search/projection/apply.rs
    - src/search/reranker.rs
    - src/search/scoring.rs
    - src/tests/integration/projection_repair.rs
    - src/tests/integration/watcher_handlers.rs
    - src/tests/tools/get_context_pipeline_relevance_tests.rs
    - src/tests/tools/get_context_pipeline_tests.rs
    - src/tests/tools/get_context_quality_tests.rs
    - src/tests/tools/get_context_task_inputs_tests.rs
    - src/tests/tools/hybrid_search_tests.rs
    - src/tests/tools/search/annotation_search_tests.rs
    - src/tests/tools/search/fast_search_regression_tests.rs
    - src/tests/tools/search/file_hits_test_intent_tests.rs
    - src/tests/tools/search/file_mode_index_tests.rs
    - src/tests/tools/search/line_mode_or_fallback_tests.rs
    - src/tests/tools/search/mod.rs
    - src/tests/tools/search/nl_path_prior_pipeline_tests.rs
    - src/tests/tools/search/nl_symbol_query_latency_tests.rs
    - src/tests/tools/search/projection_search_doc_test.rs
    - src/tests/tools/search/quality.rs
    - src/tests/tools/search/relationship_text_test.rs
    - src/tests/tools/search/reranker_ordering_tests.rs
    - src/tests/tools/search/reranker_tests.rs
    - src/tests/tools/search/tantivy_index_tests.rs
summary: T9 Cleanup Deletions — Complete
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - 24/24 named acceptance tests pass
  - nano tier green
  - "grep -rn SymbolDocument|FileDocument: 0 hits"
  - "grep -rn SearchTarget: 0 hits"
  - "grep -rn rerank_content_score|rerank_symbol_score: 0 hits"
  - "cargo check --lib --tests: clean"
  - "cargo check -p xtask: clean"
symbols:
  - rerank_unified
  - SearchDocument::for_symbol
  - text_search_impl
  - apply_reranker_to_content_results
  - apply_symbol_title_boost_to_file_results
  - search_unified
  - SearchTarget
next: "T10: xtask matrix runner cleanup. Then T11: docs/skills/agent
  instructions sweep. Then T12: Eros bakeoff acceptance gate."
---

## T9 Cleanup Deletions — Complete

### WHAT
Finished Task T9 of the FTS Phase 2 unified-schema rollout: eliminated the old DTO layer, per-target search pipeline, per-target rerankers, `SearchTarget` enum, and old `SearchIndex` methods.

### WHY
T9 acceptance criteria required:
- `cargo check --lib --tests` clean
- `cargo check -p xtask` clean
- All named tests pass
- Zero grep hits for `SymbolDocument|FileDocument`, `SearchTarget`, `rerank_content_score|rerank_symbol_score`

### HOW — Key changes made

**Deleted:**
- `src/tools/search/target.rs` (SearchTarget enum + parse/canonical_name)
- `pub mod target;` from `src/tools/search/mod.rs`
- `rerank_content_score` from `src/search/reranker.rs`
- Dead imports in `text_search.rs`

**Shims added (test-only, `#[cfg(test)]`):**
- `index.rs`: `apply_reranker_to_content_results`, `apply_symbol_title_boost_to_file_results` — thin wrappers using `compact_alnum_lc` + `titles_for_files` + `EXACT_TITLE_BOOST`. These keep `title_exact_boost_tests.rs` working without rewiring 11 tests.
- `text_search.rs`: `text_search_impl` — delegates to `unified_search_impl`, preserves old 9-arg API for 14 test callers across tantivy_integration_tests, primary_workspace_bug, dogfood helpers.
- `text_search.rs`: `definition_search_with_index_for_test` (already existed from previous session, no changes needed)

**Bug fixes discovered and fixed:**
1. `search_files` adapter in `index.rs` was missing `normalize_file_path()` as second arg to `classify_file_match()` — fixed.
2. `rerank_unified` lacked `QueryIntent::Test` and `QueryIntent::Symbol` intent boosts — added them (mirrors `score_symbol` logic from old per-target reranker). This was causing `definition_test_intent_uses_metadata_for_inline_test_helpers_before_centrality` to fail.
3. `SearchDocument::for_symbol` didn't check symbol `metadata.is_test` for inline test helpers in non-test files — fixed to use metadata override when `is_test=true` and path isn't a test path.

**Test redesign:**
- `sqlite_rescue_counts_rescued_hits_in_pre_trunc_total` → renamed `tantivy_indexed_qualified_name_found_by_partial_token` and updated to index in Tantivy (SQLite-only rescue path deleted in T9).

**xtask cleanup (`search_ablation.rs`):**
- Replaced `definition_search_with_index_for_ablation` + `search_files` calls with `index.search_unified()` filtered by `kind == "file"` or `kind != "file"`.
- Removed `embedding_provider` variable, `file_path_of_file_hit` function, `FileSearchResult` import, prefixed unused `db` param with `_`.

### IMPACT
- All 24 named acceptance tests pass
- All three grep checks return 0 lines
- Nano tier green
- `rerank_unified` now correctly handles test-intent and kind-intent queries (behavioral improvement, not just cleanup)
- `SearchDocument::for_symbol` now correctly classifies inline test helpers from metadata
