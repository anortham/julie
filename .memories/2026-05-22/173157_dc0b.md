---
id: checkpoint_dc0b4993
timestamp: 2026-05-22T17:31:57.140Z
tags:
  - codex-review
  - worker-c
  - finding-4
  - finding-5
  - finding-7
  - relationship_text
  - watcher
git:
  branch: feature/fts-phase2-unified-schema
  commit: 97699ed4
  files:
    - .claude/scheduled_tasks.lock
    - docs/plans/evidence/phase2-t12-julie-only-eval-2026-05-22.json
    - docs/plans/evidence/phase2-t12-julie-only-eval.py
    - src/watcher/handlers.rs
summary: "Worker C: All 3 Codex adversarial-review fixes complete"
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - test_walk_inherits_git_root_gitignore_in_nested_workspace PASS
  - test_projection_propagates_sql_errors PASS
  - test_watcher_reprojects_relationship_partners PASS
  - "cargo check: no errors"
symbols:
  - reproject_partner_symbols
  - apply_uncommitted_documents_from_symbols
  - collect_relationship_names_bounded
  - handle_file_created_or_modified_static
  - configure_walker_builder
  - build_ancestor_gitignore_matcher
next: Commit handlers.rs change, then report to team-lead via SendMessage with
  all 3 test names and commit SHAs
---

## Worker C: All 3 Codex adversarial-review fixes complete

### WHAT
Fixed findings #4, #5, #7 from the codex adversarial review on `feature/fts-phase2-unified-schema`.

### Finding #7 (walker .gitignore parent inheritance) — DONE (committed earlier)
- Added `find_git_root()` + `build_ancestor_gitignore_matcher()` in `src/utils/walk.rs`
- Keeps `parents(false)` for `.julieignore` isolation, but manually collects ancestor `.gitignore` files from git root down to workspace path
- Test: `test_walk_inherits_git_root_gitignore_in_nested_workspace`

### Finding #5 (relationship_text SQL errors silently swallowed) — DONE
- Replaced `unwrap_or_default()` with explicit error dispatch at all 4 call sites:
  - `src/search/projection.rs`: `ensure_current_inner`, `project_documents`, `project_documents_with_locks`
  - `src/search/projection/apply.rs`: `apply_uncommitted_documents_from_symbols`
- Pattern: "no such table" → warn + empty HashMap; other SQL errors → upsert ProjectionStatus::Stale + propagate Err
- Test: `test_projection_propagates_sql_errors`
- Commit: 97699ed4

### Finding #4 (relationship_text goes stale after incremental changes) — IN PROGRESS
- Implemented `reproject_partner_symbols()` in `src/search/projection/apply.rs`
- Exported via `src/search/projection.rs`
- Test: `test_watcher_reprojects_relationship_partners` (GREEN)
- Watcher wiring in `src/watcher/handlers.rs`:
  - Collects old partner IDs before `incremental_update_atomic`
  - Collects new partner IDs after `incremental_update_atomic`
  - Unions, filters out changed-file symbols, caps at 100
  - Calls `reproject_partner_symbols` inside the Tantivy `spawn_blocking` block
  - PENDING COMMIT

### HOW
TDD protocol: RED test → implement → GREEN. Two test runs max per fix.

### IMPACT
All 3 findings addressed. Need to commit handlers.rs change and report to team-lead.
