---
id: checkpoint_066e5038
timestamp: 2026-06-03T18:44:27.007Z
tags:
  - julie-rescue
  - crate-split
  - phase0
  - relink-cure
  - adr-0006
git:
  branch: julie-rescue
  commit: 485afa49
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - crates/julie-core/tests/no_upward_deps.rs
    - docs/plans/2026-06-03-julie-rescue-phase0-results.md
summary: Julie rescue Phase 0 — crate split DONE; Task 8 (relink proof) + Task
  10 (xtask fix) closing out
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Wait for Task 10 (xtask bucket fix) → re-run cargo xtask test changed/dev
  + system → finalize ledger → commit tripwire+ledger →
  finishing-a-development-branch.
---

## Julie rescue Phase 0 — crate split DONE; Task 8 (relink proof) + Task 10 (xtask fix) closing out

Branch `julie-rescue`, team `julie-rescue-phase0`. All 7 worker crate-split tasks + the bakeoff are committed. Closing the final gates.

### Committed since last checkpoint
- **Task 6 fix** `68a858d1` — completed the move+shim (worker had only copied; I caught the duplication in review, sent back, verified single-source).
- **Task 7 + ADR-0006 rework** `479ae77f` → `485afa49` — relocated ~108 DB tests into julie-core. CRITICAL discovery: the plan's separate `julie-test-support` crate as a dev-dep of julie-core formed a dependency CYCLE → julie-core compiled as two rlibs → builders returning julie-core types rejected by julie-core's own tests (the two-rlib problem). Worker diagnosed it correctly but worked around it locally (duplicated builders + left 4 files unmoved) instead of reporting. **User chose the fix** (AskUserQuestion): host helpers in feature-gated `julie_core::test_support`, make julie-test-support a THIN re-export, sever julie-core's dev-dep on it. Documented in **ADR-0006** (`0b64cce0`). Verified: 118/118 tests in julie-core's own binary, cycle broken (`cargo tree`), single-source `file_info_builder`, tempfile absent from production tree (resolver 2 + edition 2024).

### Task 8 (lead-owned) — IN PROGRESS, mostly done
- ✅ Relink-cure timing proof: edit julie-core DB test → relink **1.68s build / 3.41s wall**; edit top-crate test → monolith **9.77s build / 12.91s wall**; monolith NOT relinked for julie-core test edits. ~5.8× build, ~3.8× wall. Honest caveat: editing julie-core PRODUCTION code still relinks monolith (top crate deps julie-core lib); the cure is for the test-edit loop on the relocated slice.
- ✅ Dep-direction tripwire `crates/julie-core/tests/no_upward_deps.rs` (2 tests: source-ref scan comment-stripped + manifest cycle guard). Spike-verified (planted `crate::tools` → FAIL with file:line; removed → green).
- ✅ Ledger appended to `docs/plans/2026-06-03-julie-rescue-phase0-results.md` (UNCOMMITTED, with tripwire test — commit after gates green).
- �doing **`dev` gate** — caught a real regression: crate split orphaned xtask buckets (`core-database` + 3 `core-fast` commands point at now-empty `--lib tests::core::database*`, exit-4). → **Task 10** dispatched to crate-surgeon: repoint `core-database` → `cargo nextest run -p julie-core`, drop 3 core-fast orphans, lockstep `manifest_contract_expected.rs` + `changed.rs` path mappings; verify `cargo nextest run -p xtask` + `cargo xtask test bucket core-database`.
- ⏳ After Task 10: re-run `cargo xtask test changed`(→dev) + `cargo xtask test system`, finalize ledger gate rows, commit tripwire+ledger, then `razorback:finishing-a-development-branch`.

### Notes
- Worker (crate-surgeon) just had its OWN compaction and lost Task 10; re-dispatched self-contained. It tends to go idle + need nudges, and deviated on complex tasks (copy-not-move, local rlib workaround) — verify its work hard.
- Uncommitted working tree: ledger doc edit + `crates/julie-core/tests/` (tripwire). HEAD `485afa49`.
