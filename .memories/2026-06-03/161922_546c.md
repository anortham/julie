---
id: checkpoint_546c90a7
timestamp: 2026-06-03T16:19:22.623Z
tags:
  - julie-rescue
  - crate-split
  - phase0
  - database-in-leaf
  - bakeoff-won
git:
  branch: julie-rescue
  commit: 3ef1e2c0
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: Julie rescue Phase 0 — database in the leaf + bakeoff won; Task 6 dispatched
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Verify Task 6 (cargo tree no upward edge); dispatch Task 7 (relocate DB
  tests into julie-core); then Task 8 lead-owned relink-cure timing proof.
---

## Julie rescue Phase 0 — database in the leaf + bakeoff won; Task 6 dispatched

Big progress since the Task-3 checkpoint. Branch `julie-rescue`, team `julie-rescue-phase0`.

### Newly done & committed
- **Task 4** `1a60066c` — `to_relative_unix_style` + `strip_unc_prefix` + `relative_by_normalized_string` + cfg-gated `strip_normalized_prefix` → `julie_core::paths`. Whole call-chain moved together (worker correctly expanded scope for compile-completeness). Re-export shim in `utils/paths.rs`; `relative_within_workspace`/`display_path`/`to_absolute_native` + the `crate::tools::navigation::resolution` import stayed. 11/11 paths tests.
- **Task 9 (bakeoff)** `692d97c6` — one-shot 3-way ranking merged to branch. **Julie 0.904 top5 / 0.881 MRR; Miller 0.622 / 0.556; Eros unreliable** (lancedb not installed — documented setup artifact). Julie's lead is STRUCTURAL: file/path +84pp, test-awareness +100pp, doc-phrase +52pp. Miller's only win: symbol-intent 97% vs 78%. Report-only, no re-run. **Moat confirmed → rescue is right.** Deliverables: `scripts/bakeoff/miller_mcp_driver.py`, `docs/plans/2026-06-03-julie-rescue-phase0-results.md`, `docs/plans/bakeoff-raw-results.json`.
- **Task 5 (THE big one)** `84fb4691` + de-link follow-up `3ef1e2c0` — entire 37-file `database` module → `crates/julie-core/src/database/`. `src/lib.rs`: `pub use julie_core::database;`. Rewrites: 33 `crate::extractors[::base]`→`julie_extractors::`, `files.rs` paths→`crate::paths::`, `mod.rs` connpool→`crate::connection_pool::`, de-linked the `indexing_core` doc link. pub(crate)→pub promotions (SymbolDatabase fields, SymbolDatabaseConn, CanonicalWriteSet, AtomicPersistenceMetadata, bulk submods, etc.). Deps added to julie-core: serde/serde_json/blake3/md5/zerocopy/chrono. Worker caught a `#[cfg(test)]`-hides-from-dependent-crates gotcha on `is_autocommit_for_test`. Gates I verified: dep-direction sweep CLEAN (zero upward refs in code), `cargo build -p xtask` ✓, database 70/70, vector_storage 21/21.

### In flight
- **Task 6** (dispatched) — create `julie-test-support` crate: MOVE handler-free helpers (`helpers/db/rows.rs`+`db/tests.rs`, `tempdir.rs`, `cleanup.rs`, `open_test_connection`) into it, rewrite `crate::database`→`julie_core::database` / `crate::extractors`→`julie_extractors`, shim the top-crate helpers so ~20 consumer suites keep compiling. Dev-dependency of julie-core (cycle only legal as dev-dep). EXCLUDE workspace.rs/JulieTestFixture/mcp.rs (handler-coupled). Gate: `cargo tree -p julie-test-support` shows no julie/handler edge.

### Remaining
- Task 7 (relocate pure DB test slice into julie-core — gives DB tests their OWN test binary, the actual relink cure for that slice), Task 8 (timed relink-cure proof + dep-direction tripwire, lead-owned).

### Process note
crate-surgeon twice reported "done" prematurely (before commit / before running all assigned gates). Corrected it for Tasks 6–8: commit + run ALL gates BEFORE reporting. Re-export shim strategy still holds — every move = zero call-site edits. bakeoff-runner released to standby.
