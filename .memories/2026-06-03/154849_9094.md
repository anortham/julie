---
id: checkpoint_9094de98
timestamp: 2026-06-03T15:48:49.116Z
tags:
  - julie-rescue
  - crate-split
  - phase0
  - relink-tax
git:
  branch: julie-rescue
  commit: 2b52785c
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: "Julie rescue Phase 0 — crate split: Tasks 1–3 done, Task 4 dispatched"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Review Task 4 commit when crate-surgeon reports; dispatch Task 5
  (database→core); ping bakeoff-runner if still silent.
---

## Julie rescue Phase 0 — crate split: Tasks 1–3 done, Task 4 dispatched

Executing `docs/plans/2026-06-03-julie-rescue-phase0-plan.md` on branch `julie-rescue` via TeamCreate team `julie-rescue-phase0` (lead = team-lead, workers = crate-surgeon + bakeoff-runner). Goal: cure the relink tax by splitting a `julie-core` leaf crate out of the monolith so targeted tests stop relinking 126k LOC.

### Done & committed (serial crate chain, all via re-export shim → zero call-site edits)
- **Task 1** `0fd0ef52` — scaffold `crates/julie-core` + wire into workspace.
- **Task 2** `da5ccf31` — move `EmbeddingProvider` trait + DeviceInfo/EmbeddingRuntimeStatus/EmbeddingBackend to `julie_core::embeddings_contract`; shim at `src/embeddings/mod.rs:32`. Gates: `tests::core::embedding_provider` 25/25, `tests::core::embedding_sidecar_provider` 10/10. Severs the search→embeddings prod edge (`hybrid.rs:21` routes through shim).
- **Task 3** `2b52785c` — move `connection_pool` (PooledConn + WorkspaceConnectionPool + PoolStats) to `julie_core::connection_pool`; `src/daemon/connection_pool.rs` collapses to a 5-line re-export shim (preserves exact `crate::daemon::connection_pool::*` path). Verified: 3 files changed, ZERO call-site edits, no stray files. Gates: `connection_pool_test` 9/9, `symbol_db_pooled_test` 5/5. Severs database→daemon edge.

### In flight
- **Task 4** (dispatched to crate-surgeon) — move ONLY `to_relative_unix_style` + private siblings `strip_unc_prefix` + `relative_by_normalized_string` (`src/utils/paths.rs:190-241`) to `julie_core::paths`. CRITICAL: do NOT move the whole file — `utils/paths.rs:9` imports `crate::tools::navigation::resolution` (non-leaf, would drag tools into the leaf). Gate: `tests::core::paths`.
- **Task 9** (bakeoff-runner, isolated clone `/tmp/julie-bakeoff`) — one-shot 3-way retrieval ranking julie/miller/eros. `scripts/bakeoff/miller_mcp_driver.py` written (16.8KB). No report yet; legitimately long-running (julie release build + Miller run + eros corpus gen + scoring). Miller build present (Miller.Core.dll).

### Remaining
- Tasks 5 (database module → core), 6 (julie-test-support crate), 7 (relocate ~108 pure DB tests), 8 (relink-cure timing proof + dep-direction tripwire, lead-owned).

### Method holding
Re-export shim strategy validated 3× — every move so far = zero call-site edits because imports are 100% path-qualified. crate-surgeon uses Julie MCP tools (get_symbols/fast_refs/deep_dive) for orientation and reports the calls. Workers run ONLY narrow named tests (≤2 runs); lead owns regression gates.
