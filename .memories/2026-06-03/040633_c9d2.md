---
id: checkpoint_c9d205fc
timestamp: 2026-06-03T04:06:33.204Z
tags:
  - julie-extractors
  - migration
  - extractors
  - git-dependency
  - xtask
  - subagent-driven
git:
  branch: feat/julie-extractors-external-migration
  commit: f8d5f97c
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - src/daemon/app/helpers.rs
summary: julie-extractors v2.0.3 external migration — implementation phase (A–D)
  complete
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "Task A: engine_version RED→GREEN + real_world smoke + call_path rename PASS"
  - "Task B: cargo nextest run -p xtask 176/176"
  - "Task C: cargo nextest run -p xtask 165/165"
  - "Task D: real_world smoke PASS, CLAUDE≡AGENTS SYNC OK, sweep clean (only
    historical+frozen-data hits)"
next: "Task E: branch gate (cargo xtask test dev + dogfood) + prove reindex
  fires on engine-version mismatch + end-to-end extraction dogfood
  (Rust/TS/Python/Swift, Swift stdlib filtering). Then Codex pre-merge review,
  then finishing-a-development-branch."
---

## julie-extractors v2.0.3 external migration — implementation phase (A–D) complete

Migrated julie off its vendored in-tree `crates/julie-extractors` (v1.22) to consume the external `anortham/julie-extractors` library crate as a pinned git-dep, on branch `feat/julie-extractors-external-migration`.

- **Task A** (`d6b7e153`): Cargo.toml path-dep → `git tag = "v2.0.3"`; deleted in-tree crate (747 files); synced `SEMANTIC_INDEX_ENGINE_VERSION` to `extractors=2026-06-03.ecmascript-swift-shape-v3+schema=...` (RED→GREEN proven). Renamed synthetic `crates/julie-extractors/` paths in `call_path_tests.rs` → `crates/sample-lib/`. Build clean, all 4 nested git tree-sitter sub-deps resolved.
- **Task B** (`49628b93`): replaced `extractors`/`extractor-units`/`parser-upgrade` xtask buckets with `extractor-dep-integration`; root Cargo.toml/Cargo.lock now fall back to dev tier (cleaner than planned — finding #1 from Codex plan review). Updated changed_tests + manifest_contract self-tests. 176/176 xtask tests pass. Also fixed pre-existing site version drift (`1fb09bf2`, v7.13.1→7.13.2).
- **Task C** (`848e82ff`, Sonnet worker): removed `certify tree-sitter` command surface (5 impl files + dir + test + cli/main/lib wiring). 165/165 xtask tests pass.
- **Task D** (`f8d5f97c`, Sonnet worker): deleted `fixtures/extraction/` + 3 cert docs; redirected ~13 contributor docs to external repo. CLAUDE.md≡AGENTS.md. Keep-set + real_world smoke intact.

**IMPACT:** future language/extractor work now flows into julie via a single dependency re-pin + engine-version sync (which forces reindex). All 8 Codex plan-review findings were incorporated before execution.

**Pre-existing, deliberately untouched:** `src/daemon/app/helpers.rs` (unrelated local mod, kept unstaged); the frozen dogfood fixture-DB snapshot still contains `crates/julie-extractors` paths (ranking baseline, intentionally not rebuilt — Codex finding #5).
