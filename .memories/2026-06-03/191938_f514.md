---
id: checkpoint_f5147813
timestamp: 2026-06-03T19:19:38.512Z
tags:
  - julie-rescue
  - crate-split
  - phase0
  - relink-cure
  - adr-0006
  - gates-green
git:
  branch: julie-rescue
  commit: 3fcaa15d
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - crates/julie-core/tests/no_upward_deps.rs
    - docs/plans/2026-06-03-julie-rescue-phase0-results.md
summary: Julie rescue Phase 0 — crate split COMPLETE; both branch gates GREEN
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "cargo xtask test changed: 35 buckets passed in 1072.0s"
  - "cargo xtask test system: 7 buckets passed in 192.1s"
  - "relink cure: julie-core DB test edit 1.68s build / 3.41s wall vs monolith
    9.77s / 12.91s"
  - tripwire no_upward_deps 2/2 pass, FAILs on planted crate::tools spike
  - "julie-core own test binary: 118 DB tests pass"
next: Stage tripwire + ledger + checkpoint, commit, mark Task 8 complete, invoke
  razorback:finishing-a-development-branch.
---

## Julie rescue Phase 0 — crate split COMPLETE; both branch gates GREEN

Branch `julie-rescue`, team `julie-rescue-phase0`. All 10 tasks done. Closing out Task 8 (relink-cure proof + tripwire + ledger) and handing off to `finishing-a-development-branch`.

### What shipped
- **`julie-core` leaf crate** extracted from the monolith: embeddings-contract trait, connection pool, path helpers, the whole `database` module, and the `test_support` helpers. **118 pure DB-layer tests relocated into julie-core's own test binary** → editing one of those tests relinks only julie-core, not the 126k-LOC monolith.
- **ADR-0006** — test-support helpers live in a feature-gated `julie_core::test_support` module (NOT a separate cyclic dev-dep crate, which triggered the two-rlib type mismatch). `julie-test-support` reduced to a thin re-export. Cycle severed; `tempfile` optional + dev-dep, kept out of production by resolver 2.
- **Dep-direction tripwire** `crates/julie-core/tests/no_upward_deps.rs` (2 tests: comment-stripped source-ref scan + manifest cycle guard). Spike-verified.
- **Task 10** repointed xtask `core-database`/`core-fast` buckets at `cargo nextest run -p julie-core` after the relocation orphaned the old `--lib tests::core::database*` filters (exit-4). Lockstep `manifest_contract_expected.rs` + `changed.rs`.

### Gate results (HEAD 3fcaa15d)
- **Relink cure:** edit julie-core DB test → relink **1.68s build / 3.41s wall**; edit top-crate test → monolith **9.77s / 12.91s**. Monolith NOT relinked for julie-core test edits. ~5.8× build, ~3.8× wall.
- **`cargo xtask test changed`** (→ dev fallback, shared infra moved): **35 buckets passed in 1072.0s**, zero failures.
- **`cargo xtask test system`**: **7 buckets passed in 192.1s**, zero failures.

### Final commit (this checkpoint)
Staging only `crates/julie-core/tests/no_upward_deps.rs` + the finalized ledger in `docs/plans/2026-06-03-julie-rescue-phase0-results.md` + this checkpoint. Deliberately NOT staging `.codex/config.toml` or `.miller/` (local noise).
