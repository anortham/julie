---
id: checkpoint_8857e242
timestamp: 2026-05-04T00:24:24.039Z
tags:
  - dead-code-cleanup
  - lifecycle
  - testing
git:
  branch: codex/http-daemon-transport
  commit: 203dd6cd
  files:
    - docs/plans/2026-05-03-dead-code-audit-baseline.md
    - docs/plans/2026-05-03-dead-code-audit-cleanup.md
    - src/daemon/lifecycle.rs
    - src/tests/daemon/lifecycle.rs
    - src/tests/daemon/state.rs
summary: Removed lifecycle restart-pending test fossil
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo nextest run --lib
    test_controller_restart_pending_preserves_existing_shutdown_phase 2>&1 |
    tail -80: RED, then PASS after setup matched the stopping invariant"
  - "cargo nextest run --lib tests::daemon::lifecycle tests::daemon::state 2>&1
    | tail -120: PASS, 26 tests in 10.088s"
  - "cargo xtask test reliability 2>&1 | tail -160: PASS, 3 buckets in 55.3s"
  - "git diff --check: PASS"
next: "Commit this lifecycle fossil cleanup, then continue with the remaining
  dead-code cleanup candidates as separate focused passes: WorkspacePool
  constructor params/active_count or watcher ref helpers."
---

## Removed lifecycle restart-pending test fossil

### WHAT
- Removed `flag_restart_pending_for_restart`, which had become test-only after `DaemonLifecycleController::mark_restart_pending` took over runtime behavior.
- Replaced the remaining old helper-specific shutdown-phase check with `test_controller_restart_pending_preserves_existing_shutdown_phase`.
- Merged `store_phase` into the private lifecycle publish helper and narrowed state-file writer visibility to `pub(crate)`.
- Updated dead-code audit baseline and cleanup plan evidence.

### WHY
The dead-code audit showed the restart helper had no product callers. Keeping it only to satisfy state tests would preserve an obsolete lifecycle path while the controller is now the actual owner.

### HOW
- Verified the candidate with Julie refs, raw `rg`, and subagent review.
- Added/ran controller coverage before deleting the helper.
- Ran daemon lifecycle/state focused tests and the reliability gate required for daemon lifecycle cleanup.

### IMPACT
- Removes a real test fossil without weakening restart-pending coverage.
- Reduces lifecycle module public surface.
- Confirms adapter/IPC candidates are graph gaps and should stay until a deliberate IPC removal plan.
