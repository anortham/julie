---
id: checkpoint_f3ea22e7
timestamp: 2026-05-04T12:18:41.066Z
tags:
  - release-prep
  - version-bump
git:
  branch: main
  commit: 61c15f0f
  files:
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - docs/site/index.html
    - src/main.rs
    - xtask/test_tiers.toml
summary: Prepared v7.6.0 release bump
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo nextest run -p xtask docs_contract_tests_agent_docs_stay_in_sync:
    PASS"
  - "cargo check: PASS, checked julie v7.6.0"
  - "git tag -l v7.6.0: no existing tag"
  - "rg for 7.5.5/v7.5.5 in active release files: no matches"
next: Commit release prep, then tag the resulting commit as v7.6.0 when ready.
confidence: 5
---

## Prepared v7.6.0 release bump

### WHAT
- Bumped Julie from `7.5.5` to `7.6.0` in `Cargo.toml` and the `julie` package entry in `Cargo.lock`.
- Updated the gh-pages site footer in `docs/site/index.html` to `v7.6.0`.
- Updated mirrored harness status lines in `AGENTS.md` and `CLAUDE.md` to the 2026-05-04 `v7.6.0` release status.
- Also retained the audit cleanup that corrected stale IPC wording in `src/main.rs` and `xtask/test_tiers.toml`.

### WHY
The architecture series completed HTTP daemon transport, lifecycle ownership, workspace service split, projection readiness, dead-code cleanup, and test-runner improvements. This warrants the next minor release, `v7.6.0`, after the latest existing remote tag `v7.5.5`.

### HOW
- Verified latest local/remote tag was `v7.5.5` and `v7.6.0` did not already exist.
- Used direct patch edits for the active version surfaces.
- Verified no active release-facing files still referenced `7.5.5` or `v7.5.5`.
- Ran focused verification: docs sync contract and `cargo check`.

### IMPACT
The tree is prepared for a `v7.6.0` release tag after the release-prep commit. CI should derive plugin metadata from the tag as described in `AGENTS.md`.
