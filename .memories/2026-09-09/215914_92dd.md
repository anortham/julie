---
id: checkpoint_92dd658f
timestamp: 2026-09-09T21:59:14.816Z
tags:
  - service
  - tests
  - budget
  - process
  - phase1
git:
  branch: main
  commit: 8b07074b
  worktree: /home/murphy/source/julie
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 5: Multi-process bucket and line budget"
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
impact: Phase 1 Task 5 complete; passes all tests and xtask bucket gates
evidence:
  - cargo test --lib tests::service::budget passed
  - cargo test --lib tests::service::process passed
  - cargo xtask test bucket service passed in 13.2s
  - cargo xtask test bucket service-process passed in 12.6s
  - src/service/*.rs reduced from 715 to 501 lines (<= 600 budget)
confidence: 5
---

## Task 5: Multi-process bucket and line budget

### What
Added budget tests enforcing <= 600 lines across `src/service/*.rs` and zero banned coordination words (with 1 allowlisted `lock()` call in `status.rs`). Tightened formatting across `src/service/*.rs` reducing lines from 715 to 501. Added real multi-process integration tests for the service binary in `src/tests/service/process.rs`. Configured `service` and `service-process` buckets in `xtask/test_tiers.toml`.

### Why
Phase 1 of Machine Service requires strict boundaries: the service process model must stay compact (<= 600 lines) and avoid ad-hoc distributed coordination words. Real subprocesses verify shim startup, idle termination, stale record replacement, version mismatch exits (code 3), and stop verbs.

### How
- Created `src/tests/service/budget.rs` with `service_process_model_stays_under_600_lines` and `service_modules_contain_no_coordination_words`.
- Created `src/tests/service/process.rs` using `resolve_julie_binary()` and temporary `JULIE_HOME`.
- Tightened `client.rs`, `discovery.rs`, `http.rs`, `mod.rs`, `shim.rs`, and `status.rs` to 501 lines total.
- Registered buckets in `xtask/test_tiers.toml` with `expected_seconds = 15` and `commands = ["cargo build --bin julie-server", ...]` avoiding duplicate command conflicts and keeping dev tier at 598s (<= 600s).

### Impact
Enforces the 600-line budget and coordination-free discipline while providing automated regression testing across multi-process lifecycle workflows in xtask tiers.
