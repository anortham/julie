---
id: checkpoint_f3b47a48
timestamp: 2026-09-09T22:28:42.651Z
tags:
  - machine-service
  - phase1
  - host-gate
  - verification
git:
  branch: main
  commit: 8b07074b
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Phase 1 Three-Host Integration Gate & Architecture Documentation
briefId: revive-julie-for-a-fair-full-function-comparison-w
type: checkpoint
context: "Julie Machine Service Phase 1 Task 6: completed real three-host
  integration gate across Claude Code, Codex, and Cursor on both Streamable HTTP
  and stdio shim transports, updated architecture documentation, and validated
  entire dev tier gate."
decision: Verified stateless MCP 2026-07-28 and stdio shim operate with zero
  session state and zero Tasks extension required across all three target hosts.
impact: Machine service Phase 1 is verified on all 3 target hosts and ready for review.
evidence:
  - docs/findings/2026-09-09-machine-service-phase1-host-gate.md
  - cargo xtask test dev passes in 178.5s
  - cargo test --lib tests::service::budget passes (507 lines)
symbols:
  - ServiceApp
  - ServiceClient
  - McpAdapter
next: Pre-merge review with Codex as specified in execution handoff.
---

# Phase 1 Three-Host Integration Gate & Architecture Documentation

## WHAT
- Completed the three-host integration gate for Phase 1 across Claude Code, OpenAI Codex, and Cursor Agent.
- Verified both Streamable HTTP (`/mcp` with bearer token) and stdio shim (`julie-server` no-arg forwarding) on each host using the same 3 operations: `manage_workspace list`, `fast_search`, and `get_symbols`.
- Recorded all findings in `docs/findings/2026-09-09-machine-service-phase1-host-gate.md` with GATE PASSED verdict.
- Updated `docs/WORKSPACE_ARCHITECTURE.md`, `AGENTS.md`, `CLAUDE.md`, and `README.md` to reflect machine service and stdio shim.
- Synchronized `xtask/tests/manifest_contract_tests.rs` and `xtask/tests/support/manifest_contract_expected.rs` with `service` and `service-process` buckets.
- Ran full `cargo xtask test dev` suite: 29 buckets passed in 178.5s warm wall.

## WHY
Fulfill Task 6 requirements and prove that stateless MCP 2026-07-28 over HTTP and the byte-forwarding stdio shim work across real MCP clients without session state or Tasks extension.

## HOW
- Executed live MCP queries through Claude Code, Codex CLI (`codex exec`), and Cursor Agent (`agent --print`).
- Handled protocol negotiation details: MCP 2026-07-28 cache metadata (`ttlMs`, `cacheScope`) and `params.protocolVersion` mirroring in handshake headers.
- Verified line count budget strictly holds at 507 lines (limit: <= 600 lines) with zero banned coordination words.

## IMPACT
Phase 1 of Machine Service is 100% complete and verified with green dev tier test gate.
