---
id: checkpoint_778eb89e
timestamp: 2026-05-10T19:23:03.970Z
tags:
  - best-in-class-tree-sitter
  - phase-2
  - regression-bar
  - tdd
  - shape-contract
git:
  branch: best-in-class-treesitter
  commit: e6a9806c
  files:
    - docs/LANGUAGE_CERTIFICATION_REPORT.md
    - docs/plans/2026-05-10-best-in-class-tree-sitter-plan.md
summary: Phase 2 — Shared Extraction-Contract Regression Bar (DONE)
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo nextest run -p julie-extractors --lib
    structured_pending_entries_have_non_placeholder_fields: PASS"
  - "cargo nextest list --run-ignored=all
    capability_matrix_negative_cases_emit_no_wrong_edges: registered"
  - "cargo xtask test bucket extractors: 3/3 PASS"
symbols:
  - structured_pending_entries_have_non_placeholder_fields
  - capability_matrix_negative_cases_emit_no_wrong_edges
  - load_matrix
  - load_expected_fixture
next: Phase 3.1 SQL structured pending — hand-write extract_sql, migrate out of
  NO_PENDING_CAPABILITIES, add structured pending for cross-schema FK targets
confidence: 5
---

## Phase 2 — Shared Extraction-Contract Regression Bar (DONE)

Phase 2 is complete. Two tasks landed two regression-bar tests on top of Phase 1's schema validators.

### What changed

- **Task 2.1** (commit `6cf85795`): New `crates/julie-extractors/src/tests/pending_shape_contract.rs` module with `structured_pending_entries_have_non_placeholder_fields`. Asserts every `structured_pending_relationships[]` entry has non-placeholder values on `target.terminal_name`, `target.display_name`, `caller_scope_key` (when present), `pending.line_number > 0`, `pending.file_path`. Discovered the rubric §2.1 prose uses camelCase but the canonical extractor emits snake_case; test follows the actual emit shape. Widened visibility of capability_matrix helpers (`workspace_root`, `load_matrix`, `load_expected_fixture`, `CapabilityMatrix`, `CapabilityRow`, `FixtureRow`, `CapabilityFlags`) from private to `pub(crate)` so the new module reuses them.
- **Task 2.2** (commit `e6a9806c`): New `capability_matrix_negative_cases_emit_no_wrong_edges` test (annotated `#[ignore]`). Scans every language declaring `target_capabilities.relationships=true` and requires a `fixtures/extraction/<lang>/negative/` sub-directory. Phase 4d's Task 4d.ignore-flip removes the `#[ignore]` once per-language negatives land. Verified with `--run-ignored=ignored-only` that the test reports a real failure listing every relationship-claiming language without a negative fixture (correct Phase 4d-time signal).

### Phase 2 boundary gate

`cargo xtask test changed` again reports "no buckets matched" (the xtask diff mapper has no bucket trigger for the touched files). Substituted `cargo xtask test bucket extractors` (3/3 PASS) and regenerated `docs/LANGUAGE_CERTIFICATION_REPORT.md` so `certify tree-sitter --check` matches HEAD.

### Discovery

Two existing fixtures (vue/basic, qml/basic) emit structured_pending_relationships entries that do NOT actually have empty `terminal_name`/`display_name` — initial test failure was due to me writing camelCase field names that didn't match the snake_case JSON. The fixtures pass once the test reads the right fields. So Phase 2.1 is GREEN at HEAD as the plan predicted, but only after correcting a documentation/code mismatch in the rubric.

### Next

Phase 3 — Implementation Shape (SQL pending, JSON $ref, TOML). Three tasks:
- 3.1: SQL emit structured pending for cross-file FK targets (HARDEST — moves SQL out of NO_PENDING_CAPABILITIES, adds add_structured_pending_relationship to sql/mod.rs, hand-writes extract_sql in registry).
- 3.2: JSON emit relationships for `$ref`.
- 3.3: TOML emit relationships for Cargo deps + pyproject tool tables.

Phase 3 is a real implementation phase (not just validators), and 3.1 specifically is escalation-tier per the plan's model routing. Subagent delegation is available; I should consider dispatching workers for 3.2 and 3.3 in parallel.
