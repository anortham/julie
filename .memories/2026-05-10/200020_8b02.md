---
id: checkpoint_8b023236
timestamp: 2026-05-10T20:00:20.526Z
tags:
  - best-in-class-treesitter
  - phase-4-in-progress
  - loop-handoff
git:
  branch: best-in-class-treesitter
  commit: c774e194
summary: Best-in-class tree-sitter run progress (Phases 1-3 + 4a.rust complete)
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - c774e194 commit on best-in-class-treesitter
  - Phases 1+2+3+4a.rust verified green
symbols:
  - test_rust_emits_structured_pending_for_cross_module_call
next: Resume Phase 4a with vbnet (likely verify-only) or another low-effort
  language; gather signal on whether extractors emit pending or need real fixes.
---

## Best-in-class tree-sitter run progress (Phases 1-3 + 4a.rust complete)

### Branch / commits
`best-in-class-treesitter` @ c774e194 (latest commit). Linear sequence in this autonomous /loop run:
- Phase 1: capability matrix schema migration (typed evidence, planned_closure_task field, exception rules)
- Phase 2: shape contract regression bar (`pending_shape_contract` test scanning all fixtures)
- Phase 3.1: SQL structured pending for cross-schema FK targets (commit a4beac3a / 8120d04e)
- Phase 3.2: JSON Schema $ref relationships (commit 2e4c50c7) — local→concrete, external→pending, malformed→nothing
- Phase 3.3: TOML Cargo deps + pyproject tool tables (commit 216ecbc8) — file-name dispatch, dedup on tool name
- Phase 4a.rust: structured pending Imports + Calls for `use crate::other_module::Function;` (commit c774e194)

### Established recipe (Recipe A) for remaining Phase 4 languages
1. Add `fixtures/extraction/<lang>/cross_file/source.<ext>` exercising the per-language scenario from plan §4
2. Add `crates/julie-extractors/src/tests/<lang>/cross_file_pending.rs` with positive + negative tests
3. Run test → expect RED if extractor doesn't emit, GREEN if it already does
4. If RED: investigate the language's extractor relationship code; this is real implementation work
5. Register the fixture in capabilities.json
6. Run UPDATE_GOLDEN=1 to generate expected.json
7. Close the gap in capabilities.json (delete from capability_gaps; row's pending_relationships is already true)
8. Run `cargo xtask certify tree-sitter --out docs/LANGUAGE_CERTIFICATION_REPORT.md` then `cargo xtask test bucket extractors`
9. Commit per the plan

### What I learned about per-language state (vs plan's "already structured-full" claims)
- **Rust**: emits BOTH Imports (use stmt) and Calls (call site) pending — no fix needed, just fixture (commit c774e194)
- **C**: extern decl + call to extern does NOT emit pending today (verified locally; reverted). Real implementation needed in `crates/julie-extractors/src/c/relationships.rs` to detect calls to functions whose definition isn't in the local symbol map. The plan's claim "already structured-full" overstates the situation — the macro is wired but the relationship-extraction code doesn't dispatch the cross-file case.

### Next-iteration startup checklist
1. Read this checkpoint via goldfish recall.
2. `git log --oneline -10` to see commit history.
3. `jq '.languages[] | select(has("capability_gaps")) | select(.capability_gaps | map(.status == "open") | any) | .language' fixtures/extraction/capabilities.json` to enumerate remaining open gaps.
4. Tackle Phase 4a languages in this rough order, easiest first:
   - **Verify-only (likely fixture-only)**: rust ✓ done; check vbnet, dart, gdscript, java, csharp, scala, kotlin, php, swift, typescript, javascript, jsx, tsx
   - **Investigate / likely needs fix**: c (confirmed needs work), cpp, go, zig, python, ruby, elixir, lua, r, bash, powershell
5. For each language, follow the recipe above. The rust commit c774e194 is the worked example.

### Iteration discipline status
- 0 escalations open
- 0 budget violations
- Phase 1-3 ledger rows updated; Phase 4 ledger row template still all `_TBD_`
