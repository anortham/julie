---
id: checkpoint_ef07f5b4
timestamp: 2026-03-31T20:14:42.334Z
tags:
  - review
  - embeddings
  - sidecar
  - ort-removal
  - release-readiness
  - test-fixes
  - stale-comments
git:
  branch: main
  commit: 25f4d2f4
  files:
    - src/embeddings/mod.rs
    - src/tests/core/embedding_provider.rs
    - src/tests/core/sidecar_supervisor_tests.rs
    - src/tests/mod.rs
    - src/tests/tools/workspace/mod_tests.rs
summary: Sidecar standardization review complete, pre-release fixes applied
type: checkpoint
evidence:
  - "cargo xtask test dev --timeout-multiplier 4: 8/8 buckets passed in 216s"
  - "grep for ORT/fastembed references: only 3 stale comments found (now fixed)"
symbols:
  - EmbeddingBackend
  - EmbeddingProviderFactory
  - parse_provider_preference
  - detect_nvidia_cuda
  - test_python_version_parses_current_interpreter
  - test_is_indexed_flag_with_populated_database
next: Commit the 5 fixes, then tag as release (user needs to confirm version number)
---

## Sidecar standardization review complete, pre-release fixes applied

Reviewed the full 9-commit sidecar standardization change set (31 files, ~6k lines, net -2,688 lines). ORT backend fully removed, sidecar is now the sole embedding provider on all platforms.

### Review findings

- **Clean removals:** No stale `EmbeddingBackend::Ort` references in source. `ort_provider.rs`, `windows_directml.rs`, Windows embedding policy tests, ORT integration tests, and ORT dogfood tests all gone. Dependencies (`fastembed`, `ort`, `windows` crate) removed from Cargo.toml.
- **Factory simplified:** `Auto` resolves to `Sidecar`, period. `parse_provider_preference("ort")` returns helpful migration error.
- **CUDA detection solid:** `detect_nvidia_cuda()` via `nvidia-smi`, non-fatal torch swap with version pinning.
- **CI unified:** All three platforms use identical `cargo build` invocation.

### Fixes applied (5 total)

1. **3 stale comments** referencing ORT/fastembed in `mod.rs`, `embedding_provider.rs`, `tests/mod.rs`
2. **Python version test** (`test_python_version_parses_current_interpreter`) asserted >= 3.10 but macOS ships 3.9; relaxed to >= 3.6 since the test validates parsing, not PyTorch compatibility
3. **Workspace test** (`test_is_indexed_flag_with_populated_database`) expected "already indexed" message but catch-up indexing (commit 028f332a) changed behavior to incremental re-index; updated assertion to match

### Result

- All 8 dev tier buckets passing (216s)
- Ready for release pending commit of these fixes
