---
id: checkpoint_a94109cc
timestamp: 2026-05-08T14:20:05.916Z
tags:
  - release
  - v7.8.1
  - patch
  - tree-sitter
  - index-health
  - version-bump
git:
  branch: main
  commit: 069b9d76
  files:
    - .claude/settings.local.json
    - Cargo.lock
    - Cargo.toml
    - docs/release-notes/v7.8.1.md
    - docs/site/index.html
summary: Prepared v7.8.1 patch release
briefId: julie-world-class-systems-program
type: checkpoint
next: "User approved the tag-and-release sequence. Stage non-settings files,
  commit chore(release): prepare v7.8.1, push main, tag v7.8.1, push tag, then
  replace the GitHub release body with v7.8.1.md notes."
---

## Prepared v7.8.1 patch release

### What
- Bumped `Cargo.toml` 7.8.0 → 7.8.1; `Cargo.lock` re-resolved by the test run.
- Updated gh-pages site footer (`docs/site/index.html`) v7.8.0 → v7.8.1.
- Wrote `docs/release-notes/v7.8.1.md` covering TS-RF-001..008 + B1 ProjectionFailure invariant + B2 phantom-fd detail + B3 Vue determinism + L1 SQL CTE doc.
- Updated active brief to reflect program closure and current release-train question.

### Why
v7.8.0 shipped, but the post-release tree-sitter review surfaced 12 substantive findings (one HIGH severity: `ProjectionFailure` invariant silently cleared by unrelated retry success). Those fixes already landed on local main as `7738219d` and `069b9d76` but had not been pushed or tagged.

### How
- TaskCreate scaffold for the 4 release-prep steps; tracked progression in TaskUpdate.
- Confirmed only the root `Cargo.toml` carries the package version (xtask and julie-extractors are independently versioned).
- Verified site footer was the only `7.8.0` literal under `docs/site/`.
- Drew release-notes substance from the actual fix commits' bodies + the verification evidence they record.

### Verification
- `cargo xtask test dev` (batch gate before tag): 22/22 buckets PASS in 543.4s.
- Three buckets ran above their expected budget (xtask-runner, tools-navigation, tools-refactoring) but all green; not regressions.

### Impact
Version bump + release notes + brief update committed alongside the two pre-existing fix commits, all about to be pushed and tagged as v7.8.1.
