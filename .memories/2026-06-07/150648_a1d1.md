---
id: checkpoint_a1d155af
timestamp: 2026-06-07T15:06:48.921Z
tags:
  - julie
  - release
  - v7.14.0
  - plugin
  - verification
git:
  branch: main
  commit: c05184d0
  files:
    - .codex/config.toml
summary: Published Julie v7.14.0 replacement release
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Replacement release is published and verified; plugin users should
  receive 7.14.0 with the stale extracted binary fix.
evidence:
  - "Julie release workflow `27095777371`: completed success with all matrix
    builds and Create Release successful."
  - "`gh release view v7.14.0`: not draft, not prerelease, published
    2026-06-07T15:02:29Z, four platform assets uploaded."
  - "`gh release view` default/latest resolves to `v7.14.0`."
  - Downloaded `julie-v7.14.0-aarch64-apple-darwin.tar.gz`; archive contains
    `julie-server` and `julie-embedding-host`; `julie-server --version` prints
    `julie-server 7.14.0`.
  - "Downloaded release asset dashboard probe: rendered root page contains
    `7.14.0`."
  - "`julie-plugin` workflow `27096146986`: completed success; remote `main`
    commit `3b3b9e7ac17f0619860ac63dfbf97eff74308fd2` has message `feat: update
    to julie v7.14.0`."
  - Remote `julie-plugin` `package.json`, `.claude-plugin/plugin.json`, and
    `.claude-plugin/marketplace.json` all report `7.14.0`.
  - Remote `julie-plugin` archives are `julie-v7.14.0-*`; downloaded plugin
    macOS arm64 archive reports `julie-server 7.14.0`.
  - Remote `julie-plugin/hooks/run.cjs` still contains `.extracted-archive`,
    `extractedArchiveMatches`, and `writeExtractionMarker`.
next: Leave `.codex/config.toml` unstaged; it is local config churn unrelated to
  the release.
---

## Published Julie v7.14.0 replacement release

- **WHAT:** Bumped Julie to `v7.14.0`, pushed commit `c05184d0466e4b9cb4f8a499f86a2c73a136b98e`, pushed annotated tag `v7.14.0`, and verified the GitHub release plus `julie-plugin` update.
- **WHY:** `v7.13.3` was pulled/drafted after an OpenClaw indexing performance regression and a plugin launcher cache issue allowed an older extracted `julie-server 7.13.2` binary to keep running.
- **HOW:** Kept the indexing performance fixes in Julie, added release notes, updated version/docs to `7.14.0`, and separately fixed `julie-plugin` launcher extraction invalidation with a `.extracted-archive` marker so archive-version changes force re-extraction.
- **IMPACT:** `v7.14.0` is now the latest public Julie release, release assets report `julie-server 7.14.0`, the dashboard rendered from the downloaded release asset shows `7.14.0`, and `julie-plugin/main` is updated to `7.14.0` while retaining the launcher fix.
