---
id: checkpoint_91815d18
timestamp: 2026-06-05T03:35:39.884Z
tags:
  - feature
  - rescue-3c
  - T8
  - in-process
  - leader-election
  - F2
  - inode-coupling
  - serve-entry
  - run_in_process_server
  - initialize_with_index_root
  - workspace
  - leadership
  - rmcp
  - stdio
git:
  branch: julie-rescue-phase3c2
  commit: 730b5da2
  files:
    - .codex/config.toml
    - crates/julie-runtime/src/workspace/mod.rs
    - src/handler.rs
    - src/server_in_process.rs
    - src/tests/core/handler.rs
    - src/tests/core/handler/inprocess_ctor.rs
    - src/tests/core/handler/inprocess_serve.rs
    - src/tests/core/handler/leader_watcher.rs
    - src/tests/core/handler/loser_refuses.rs
summary: "T8: run_in_process_server + F2 storage/lock inode coupling"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "cargo check -p julie-runtime: clean"
  - "cargo check -p julie: clean"
  - "test_f2_inode_coupling_same_index_tree: PASS"
  - "test_inprocess_handler_f2_storage_under_index_root: PASS"
  - 81/81 tests::core::handler tests pass
symbols:
  - run_in_process_server
  - JulieWorkspace::initialize_with_index_root
  - JulieServerHandler::new_in_process
  - initialize_workspace_with_force
  - LeadershipState
  - DaemonLockGuard
  - DaemonPaths
  - in_process_index_root
next: Commit with conventional message, then report to lead
---

## T8: run_in_process_server + F2 storage/lock inode coupling

Implemented the in-process MCP serve entry for Julie Rescue Phase 3c.2, the highest-risk task carrying the codex F2 HARD GATE.

### Changes Made

**`crates/julie-runtime/src/workspace/mod.rs`**
- Added `JulieWorkspace::initialize_with_index_root(root, index_root)` — mirrors `WorkspacePool::init_workspace` struct literal exactly; sets `index_root_override = Some(index_root)` BEFORE db/search init so storage lands under daemon paths.

**`src/handler.rs`**
- Added `in_process_index_root: Option<PathBuf>` field (initialized to `None` in all 3 struct literal sites)
- Updated `new_in_process` to take 4th param `index_root: Option<PathBuf>`, stores as `handler.in_process_index_root`
- Added F2 branch to `initialize_workspace_with_force` non-pool path: when `in_process_index_root.is_some()`, calls `JulieWorkspace::initialize_with_index_root` instead of `detect_and_load`/`initialize`

**`src/server_in_process.rs`**
- Added `run_in_process_server(startup_hint)` — full serve entry:
  1. `DaemonPaths::try_new()` — respects $JULIE_HOME
  2. `generate_workspace_id` from path
  3. `create_dir_all(index_root)` BEFORE acquiring lock (lock lives inside it)
  4. `DaemonLockGuard::try_acquire(lock_path)` → `leader(guard)` or `follower()` (NOT `none()`)
  5. Bounded timeout on `acquire_in_process_embedding_provider` (5s default, `JULIE_INPROCESS_EMBED_WAIT_SECS`)
  6. `new_in_process(hint, embed, leadership, Some(index_root))`
  7. `handler.serve(rmcp::transport::stdio()).await?.waiting().await?`

**Test files (6 changed)**
- Updated all 9 `new_in_process` call sites (inprocess_ctor.rs ×5, leader_watcher.rs ×2, loser_refuses.rs ×2) to pass `None` as 4th param
- New `inprocess_serve.rs` with 2 F2 gate tests
- Registered in `handler.rs` module

### F2 Gate Invariant Proven

`test_f2_inode_coupling_same_index_tree`:
- Asserts `workspace_leader_lock`, `workspace_db_path`, `workspace_tantivy_path` all resolve under `~/.julie/indexes/{ws}/`
- Asserts second `try_acquire` on same lock path → `AlreadyHeld`

`test_inprocess_handler_f2_storage_under_index_root`:
- Builds handler via exact `run_in_process_server` sequence
- Initializes workspace, asserts db exists at daemon-shared path NOT project-local `.julie/`

### Test Evidence
- `cargo check -p julie-runtime` ✅
- `cargo check -p julie` ✅
- All 81 `tests::core::handler` tests pass ✅ (including all T4/T5/T7 tests with updated signatures)

