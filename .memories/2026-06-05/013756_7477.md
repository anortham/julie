---
id: checkpoint_74773875
timestamp: 2026-06-05T01:37:56.414Z
tags:
  - julie-rescue
  - phase3c
  - pr-3c1
  - pre-review
git:
  branch: julie-rescue-phase3c
  commit: 3e0d7f55
  files:
    - .codex/config.toml
summary: Julie Rescue PR 3c.1 — all 5 tasks done & green, branch-gate running
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Await branch-gate (bg b9hhlak1f) GREEN → codex pre-merge review →
  finishing-a-development-branch (PR for human merge).
---

## Julie Rescue PR 3c.1 — all 5 tasks done & green, branch-gate running

PR 3c.1 (leader-lock + hang-guard infra) implementation **complete**. All 5 tasks accepted by inline lead review + lead-verified GREEN at HEAD. Both workers (`core-dev`, `lock-dev`) **frozen**.

### Commits on `julie-rescue-phase3c` (off main)
- `d33e2980` plan
- `be2855a0` **T1** — `DaemonPaths::workspace_leader_lock` (julie-core); D7 finding: in-process replaces the *adapter* → shared `~/.julie/indexes`; derive index dir + lock from ONE `DaemonPaths`.
- `c2d35c27` **T2** — lifted `DaemonLockGuard` → `julie-core/src/workspace/leader_lock.rs`, re-export from `src/daemon/discovery.rs` (callers unchanged). Stale plan ref `app/helpers.rs:96` corrected.
- `9010a71a` **T13** — §11 dashboard persist-vs-drop table in the Phase 3 design doc. One new 3d write-site: `started_at_unix`→`registry.db.daemon_state`.
- `d5e9ad25` **T3** — `dispatch_with_deadline` (handler per-request `tokio::time::timeout`, default 120s env `JULIE_INPROCESS_REQUEST_TIMEOUT_SECS`, EXEMPT writers edit_file/rename_symbol/rewrite_symbol/manage_workspace).
- `d53fb00a` + `3e0d7f55` **T4** — `new_in_process(startup_hint, embedding_provider, leader)` + `LeadershipState` (now `src/leadership.rs`). Delegates to `new_deferred_daemon_startup_hint_with_project_log` (preserves source + per-project ProjectLog + writes-enabled). `is_leader()`, injected-provider-first `embedding_provider()`. Fix round added source-preservation test + `Arc::ptr_eq`.

### Verified GREEN at HEAD (lead)
julie-core leader_lock+paths 2/2; julie re-export `test_daemon_lock_try_acquire_fails_when_held`; handler subtree **37/37** (deadline 6 + inprocess_ctor 4 + existing).

### Next
Branch-gate `cargo xtask test dev` running (bg `b9hhlak1f`). On GREEN → goldfish pre-review checkpoint already done → **codex pre-merge review** (razorback:pre-merge-review) → finishing-a-development-branch (push + PR for HUMAN merge; never auto-merge). Then PRs 3c.2 (T5–T9) and 3c.3 (T10–T12).
