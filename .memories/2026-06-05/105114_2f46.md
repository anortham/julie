---
id: checkpoint_2f46120c
timestamp: 2026-06-05T10:51:14.317Z
tags:
  - julie-rescue
  - phase-3c
  - cutover
  - leader-election
  - hard-gate
git:
  branch: julie-rescue-phase3c3
  commit: fa780060
  files:
    - .codex/config.toml
    - src/tests/integration/in_process_boundary.rs
    - src/tests/integration/t11_kill_writer.rs
    - src/tests/mod.rs
summary: Phase 3c.3 T11 + T12 landed — cutover gates GREEN
briefId: julie-rescue-de-slop-packaging-keep-the-moat
evidence:
  - cargo nextest run --lib test_t11_kill_writer_hard_gate
    no_args_main_serves_in_process_not_adapter
    section7_dag_files_are_bypassed_not_deleted → 3 passed (0.286s)
  - t11_writer_subprocess no-op standalone → 1 passed (0.012s)
  - cargo test --lib --no-run → compiles clean (15.91s)
next: Run 3c.3 branch-gate (dev + system + reliability), then codex pre-merge
  review, then PR for human merge (flag manual dogfood smoke).
---

## Phase 3c.3 T11 + T12 landed — cutover gates GREEN

THE CUTOVER (T10) already committed at `fa780060`. This milestone adds the two cutover gates:

### T11 — kill-the-writer HARD GATE (`src/tests/integration/t11_kill_writer.rs`)
Two-process test proving leader-crash recovery across a real OS boundary:
- **Subprocess** (gated on `_JULIE_T11_WORKSPACE_DIR`): wins `{ws}/leader.lock`, commits an orphan symbol to canonical SQLite via `bulk_store_fresh_atomic` WITHOUT Tantivy projection (the crash gap), touches `READY`, blocks forever holding lock + live DB connection.
- **Parent**: spawns via `current_exe()` (NEXTEST* stripped → libtest mode), waits READY, asserts cross-process `AlreadyHeld` while leader alive, `SIGKILL`s it, asserts kernel lock-release + re-acquire, then `ensure_current_from_database` recovers the orphan symbol (canonical_revision=1, num_docs>=1, searchable).
- Part (d) (reader degradation) explicitly cited to the existing R1 cross-process reload test — not re-litigated.
- Fork-bomb guard: parent early-returns if `_JULIE_T11_WORKSPACE_DIR` set; subprocess no-ops in 0.011s when env unset (won't hang the suite).

### T12 — boundary tripwire (`src/tests/integration/in_process_boundary.rs`)
- Source-scans `main.rs`: `run_in_process_server` present and inside the `None =>` arm; `run_adapter`/`DaemonLauncher` absent from code (comments stripped).
- Asserts all 13 §7-DAG files still exist (adapter/**, bin/julie-adapter.rs, daemon HTTP transport, singleton/legacy/pid, search_compare, migration.rs) — bypassed, not deleted.
- Compile-time refs to `crate::adapter::run_adapter` + `crate::daemon::cli::start_daemon` make "still compiles" load-bearing.

Both modules registered in `src/tests/mod.rs`.
