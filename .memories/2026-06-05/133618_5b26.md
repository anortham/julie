---
id: checkpoint_5b26172e
timestamp: 2026-06-05T13:36:18.974Z
tags:
  - phase-3d1
  - refactor
  - adapter-deletion
  - http_client
  - julie-adapter
  - tripwire
  - in_process_boundary
  - transport-bucket
  - rescue
git:
  branch: julie-rescue-phase3d1
  commit: b7ae9fc2
  files:
    - .codex/config.toml
    - Cargo.toml
    - src/adapter/forwarder.rs
    - src/adapter/http_stdio.rs
    - src/adapter/launcher.rs
    - src/adapter/mod.rs
    - src/bin/julie-adapter.rs
    - src/daemon/http_client.rs
    - src/daemon/mod.rs
    - src/lib.rs
    - src/tests/adapter/http_stdio.rs
    - src/tests/adapter/launcher.rs
    - src/tests/adapter/mod.rs
    - src/tests/adapter/retry_resilience.rs
    - src/tests/daemon/app_test.rs
    - src/tests/integration/in_process_boundary.rs
    - src/tests/mod.rs
    - xtask/test_tiers.toml
summary: "Phase 3d.1 T2 complete: adapter + http_client deleted; T12 tripwire flipped"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Adapter module fully gone. julie-adapter bin target removed from
  manifest. T12 tripwire now guards 3d.2/3d.3 remaining bypassed daemon server
  files only.
symbols:
  - run_adapter
  - DaemonLauncher
  - DaemonReadiness
  - http_client_config_for_endpoint
  - _bypassed_entry_points_still_compile
  - section7_dag_files_are_bypassed_not_deleted
next: "Commit with: refactor(rescue-3d1): delete adapter + http_client +
  julie-adapter bin; flip T12 tripwire. Then team-lead runs branch-gate (T4)."
---

## Phase 3d.1 T2 complete: adapter + http_client deleted; T12 tripwire flipped

### What was done

Deleted the stdio↔daemon adapter module, the daemon HTTP client, the `julie-adapter` binary target, and all adapter test files. Flipped the T12 in-process boundary tripwire to reflect 3d.1 state.

**Files deleted (git rm):**
- `src/adapter/mod.rs`, `forwarder.rs`, `http_stdio.rs`, `launcher.rs`
- `src/bin/julie-adapter.rs`
- `src/daemon/http_client.rs`
- `src/tests/adapter/mod.rs`, `http_stdio.rs`, `launcher.rs`, `retry_resilience.rs`

**Files modified:**
- `src/lib.rs` — removed `pub mod adapter;`
- `src/daemon/mod.rs` — removed `pub mod http_client;`
- `Cargo.toml` — removed `[[bin]] name = "julie-adapter"` target
- `src/tests/mod.rs` — removed `pub mod adapter;` + comment block
- `src/tests/integration/in_process_boundary.rs` — updated to 3d.1: removed `crate::adapter::run_adapter` from `_bypassed_entry_points_still_compile`, removed adapter/http_client entries from `section7_files`, updated docstrings
- `xtask/test_tiers.toml` — removed `tests::adapter` command from `transport` bucket
- `src/tests/daemon/app_test.rs` — removed `DaemonLauncher`/`DaemonReadiness` stray import + the 6-line adapter readiness assertion in `test_daemon_app_serve_and_shutdown` (missed by fast_refs because `DaemonReadiness` wasn't indexed; `TransportEndpoint::probe_readiness()` already covers the same invariant)

### fast_refs findings (clean)
- `run_adapter`: only in tripwire (editing) + `julie-adapter.rs` (deleting). No kept files.
- `http_client_config_for_endpoint`: only in `adapter/http_stdio.rs` + `tests/adapter/http_stdio.rs` (both deleting). No kept files.
- `DaemonLauncher`: only in adapter files + `tests/adapter/launcher.rs` (all deleting). **BUT** also in `src/tests/daemon/app_test.rs:12` — missed because `DaemonReadiness` wasn't the queried symbol. Fixed inline.
- `main.rs`: comment-only mentions of "adapter", zero live code references. ✅
- `http_transport.rs`: imports only from `daemon::token_file`, `daemon::transport`, `paths`. No adapter imports. ✅

### Verification (all 4 passed)
- `cargo nextest run -p julie --no-run` → compiled clean
- `cargo build --bin julie-adapter` → error: no bin target named `julie-adapter` ✅
- `cargo nextest run -p julie --lib tests::integration::in_process_boundary` → 2/2 PASS
- `cargo nextest list -p julie --lib tests::adapter` → zero tests listed ✅
