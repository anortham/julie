---
id: checkpoint_1ee1025e
timestamp: 2026-06-05T01:28:49.045Z
tags:
  - phase3c
  - rescue
  - T4
  - leader-election
  - handler
git:
  branch: julie-rescue-phase3c
  commit: d5e9ad25
  files:
    - .codex/config.toml
    - src/handler.rs
    - src/tests/core/handler.rs
    - src/tests/core/handler/inprocess_ctor.rs
summary: T4 COMPLETE — new_in_process ctor + LeadershipState on JulieServerHandler
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - cargo nextest run -p julie tests::core::handler::inprocess_ctor — 3/3 PASS
  - cargo nextest run -p julie tests::core::handler::deadline — 6/6 PASS
  - cargo check -p julie — clean
symbols:
  - JulieServerHandler
  - LeadershipState
  - new_in_process
  - is_leader
  - embedding_provider
next: Commit T4. Then report to team-lead and await T5/T6 assignment.
---

## T4 COMPLETE — new_in_process ctor + LeadershipState on JulieServerHandler

### WHAT
- Added `LeadershipState` struct to `src/handler.rs` (pub, wraps `Option<DaemonLockGuard>`)
- Added two fields to `JulieServerHandler`: `leadership: Arc<LeadershipState>` + `injected_embedding_provider: Option<Arc<dyn EmbeddingProvider>>`
- Updated all 3 existing constructor struct literals with `LeadershipState::none()` and `None`
- Added `new_in_process(startup_hint, embedding_provider, leader)` constructor (delegates to deferred-startup path, then overrides the two new fields)
- Added `is_leader()` method on `JulieServerHandler`
- Modified `embedding_provider()` to check injected provider first, before embedding_service and per-workspace provider
- Wrote TDD test file `src/tests/core/handler/inprocess_ctor.rs` (3 tests)

### WHY
Phase 3c — in-process MCP server needs:
- Leader-election state on the handler (T2 DaemonLockGuard surfaced here)
- Injected embedding provider (one sidecar serves multiple sessions)
- Correct Cwd vs Cli deferred auto-index behavior (startup_hint.source preserved)

### HOW
- `LeadershipState` wraps `Option<DaemonLockGuard>` (not Clone); stored as `Arc<LeadershipState>` on the handler so `#[derive(Clone)]` on the handler works
- `new_in_process` delegates to `new_deferred_daemon_startup_hint_with_project_log` (which preserves startup_hint.source), then overwrites leadership + injected_embedding_provider
- `embedding_provider()` priority: injected → embedding_service → per-workspace provider
- `EmbeddingProvider` trait lives at `crates/julie-core/src/embeddings_contract.rs`, re-exported via `crate::embeddings::EmbeddingProvider`

### IMPACT
- T5/T8 can use `LeadershipState` + `is_leader()` + `new_in_process` directly
- All existing constructors unaffected (new fields default to `none()`/`None`)
- No regression on T3 deadline tests (6/6 still green)
