---
id: checkpoint_a1031a61
timestamp: 2026-06-05T05:49:49.994Z
tags:
  - julie-rescue
  - phase3c
  - codex-review
  - pre-merge
git:
  branch: julie-rescue-phase3c2
  commit: "04091e76"
  files:
    - .codex/config.toml
summary: Codex pre-merge review of PR 3c.2 — 3 findings triaged
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: decision
decision: Fix codex F-B (force-branch F2 hole) + F-C (T9 repair re-spawn) in
  3c.2; carry F-A (election timing) to 3c.3.
next: Implement F-C + F-B with TDD, re-run branch-gate, push + PR for human
  merge; carry F-A into 3c.3 plan.
---

## Codex pre-merge review of PR 3c.2 — 3 findings triaged

Branch `julie-rescue-phase3c2` @ HEAD `04091e76`, branch-gate GREEN (dev 37 / system 7 / reliability 3). Codex verdict: needs-attention, 3 findings — all in the **in-process path, which is DORMANT in 3c.2 production** (main.rs None arm still → `run_adapter`; cutover is T10 in 3c.3).

- **F-B (high 0.94) — force reindex bypasses `in_process_index_root`.** `initialize_workspace_with_force` non-pooled FORCE branch (handler.rs ~1619) calls `JulieWorkspace::initialize(target_path)` = project-local storage, while the non-force branch (1635) honors `in_process_index_root`. Verified real. Leaves leader.lock at `~/.julie/indexes/{ws}` but db/tantivy project-local → F2 broken on force. **Fix now** (completes the F2 invariant 3c.2/T8 owns).
- **F-C (medium 0.78) — per-read repair re-spawn on persistent failure.** F1 envelope (handler.rs ~2685) spawns a repair task per in-process read observing `deferred_auto_index_pending`. Mutex+swap dedups the happy path, but on persistent failure each queued task re-runs repair (flag reset true at 486). Verified real. **Fix now** (in-flight CAS guard, T9 hardening).
- **F-A (high 0.89) — leader election keyed before root reconciliation.** `run_in_process_server` keys lock+storage on `startup_hint.path` (server_in_process.rs 181-195); a Cwd-source handler later rebinds primary to client `list_roots` root (handler.rs 596) which can differ → binding-vs-storage key mismatch / split indexes across two leaders with different hints. Verified reachable AT CUTOVER. codex "SQLite/Tantivy corruption" wording overstated (different hints → different storage AND lock, no shared-file corruption). **Carry to 3c.3** (cutover-election-timing design; validated live by T11 kill-the-writer).

## Execution
Lead-inline TDD fixes (same-file handler.rs, sanctioned by pre-merge-review skill). F-C: add `deferred_auto_index_in_flight: Arc<AtomicBool>` + `try_claim_deferred_repair_slot()`/`release`. F-B: in-process force sub-branch clears `index_root/tantivy` only (preserve db + held leader.lock), re-init via `initialize_with_index_root`. Then re-gate + finish branch (push + PR, human merge).
