---
id: checkpoint_d6c0a38f
timestamp: 2026-06-05T01:35:22.672Z
tags:
  - phase3c
  - rescue
  - T4
  - fixup
  - leader-election
git:
  branch: julie-rescue-phase3c
  commit: d53fb00a
  files:
    - .codex/config.toml
    - src/handler.rs
    - src/leadership.rs
    - src/lib.rs
    - src/tests/core/handler/inprocess_ctor.rs
summary: T4 fixup — source-preservation test + ptr_eq + LeadershipState extraction
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - cargo nextest run -p julie tests::core::handler::inprocess_ctor — 4/4 PASS
  - cargo check -p julie — clean
symbols:
  - LeadershipState
  - test_new_in_process_preserves_startup_hint_source
next: Commit fixup. Report to team-lead. Await T5/T6.
---

## T4 fixup — source-preservation test + ptr_eq + LeadershipState extraction

### WHAT
Three targeted fixes per team-lead review of T4:

1. **Source-preservation test added** (`test_new_in_process_preserves_startup_hint_source`):
   - Proves `new_in_process` keeps `WorkspaceStartupSource::Cwd` intact (the deferral trigger that `new()` drops to `None`)
   - Also proves `WorkspaceStartupSource::Cli` round-trips
   - This is the load-bearing invariant for Phase 3c correctness

2. **`Arc::ptr_eq` assertion** on injected provider (was `is_some()` only — now proves same Arc instance)

3. **`LeadershipState` extracted** to `src/leadership.rs` (new module):
   - Added `pub mod leadership;` to `src/lib.rs` (alphabetical: after `language`, before `logging`)
   - `handler.rs` now imports via `use crate::leadership::LeadershipState;`
   - Test file import updated to `use crate::leadership::LeadershipState;`

### WHY
- Source test: nothing previously proved the Cwd source survives → `on_initialized` deferral would silently break if regression introduced
- ptr_eq: `is_some()` only asserts a provider exists, not that it's the injected one
- Extraction: handler.rs is ~2700 lines; `LeadershipState` is the seam T5/T8/T10 build on

### IMPACT
- 4/4 tests GREEN; no regression (cargo check -p julie clean)
- `crate::leadership::LeadershipState` is now the stable import path for downstream tasks
