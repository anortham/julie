---
id: checkpoint_60d95af2
timestamp: 2026-05-05T21:56:56.810Z
tags:
  - version-bump
  - release
  - dashboard
git:
  branch: main
  commit: 00fa891d
  files:
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - docs/site/index.html
    - src/startup.rs
    - src/tests/tools/workspace/mod_tests.rs
summary: Bumped Julie Patch Version
briefId: julie-world-class-systems-program
type: checkpoint
impact: Dashboard-visible version marker is ready for the fixed build.
evidence:
  - "`rg -n \"7\\\\.7\\\\.0|v7\\\\.7\\\\.0\" . --glob '!target/**' --glob
    '!.git/**' --glob '!.julie/**' --glob '!.memories/**'` returned no matches"
  - "`cargo metadata --locked --format-version 1 --no-deps` reports `julie`
    version `7.7.1`"
  - "`cargo nextest run -p xtask docs_contract_tests 2>&1 | tail -40` passed 10
    tests"
confidence: 5
---

## Bumped Julie Patch Version

- **WHAT:** Updated Julie's project version from `7.7.0` to `7.7.1` in `Cargo.toml`, `Cargo.lock`, the docs site footer, and the synchronized agent docs status footer.
- **WHY:** The new startup embedding fix needs an obvious version marker in the dashboard and project docs so live runs can be distinguished from the previous binary.
- **HOW:** Applied a metadata-only patch and verified Cargo locked metadata resolves the root package as `julie` `7.7.1`. Searched source-controlled files for stale `7.7.0` references outside `.memories`.
- **IMPACT:** New builds will report/display `7.7.1`, and the docs site footer matches the crate version.
