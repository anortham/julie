---
id: checkpoint_0f6d68cd
timestamp: 2026-05-05T18:37:56.390Z
tags:
  - tree-sitter
  - release-build
  - dogfood
  - main
  - merge
git:
  branch: main
  commit: "16182528"
summary: Merged Tree-Sitter Gap Closure and Dogfooded Release Binary
briefId: julie-world-class-systems-program
type: checkpoint
context: User asked to merge tree-sitter quality bar gap closure back to main,
  build the release binary, and dogfood it after restarting the session onto the
  latest binary.
impact: Release-binary dogfood is green for the merged tree-sitter gap closure
  path. The remaining strict quality-bar release claim still needs the full
  fixed-target gate set recorded at the exact final release SHA if we want to
  mark the document achieved.
evidence:
  - git merge --ff-only codex/tree-sitter-quality-bar-gaps succeeded on main
  - cargo build --release passed in 2m 47s
  - manage_workspace health detailed=true reported READY, daemon serving,
    projection current 3987/3987, 34810 symbols, 33167 relationships, 7009
    vectors
  - call_path extract_symbols_static extract_canonical found the one-hop
    production extraction edge
  - fast_refs extract_canonical found definition plus 25 visible references
  - manage_workspace refresh workspace_id=julie_528d4264 reported already
    up-to-date at canonical revision 3987
  - sqlite3 metadata query reported schema_version 24, semantic_index_engine
    2026-05-05.reference-identifier-v3, projection ready 3987/3987,
    symbol_vectors table present
  - cargo xtask test dogfood passed 2 buckets in 204.4s
symbols:
  - extract_symbols_static
  - extract_canonical
  - VueExtractor::extract_relationships
  - migration_010_add_symbol_vectors
next: Either run `cargo xtask test full` for final release-candidate evidence,
  or investigate and decide whether to harden the parallel standalone migration
  race before calling this release-ready.
confidence: 5
unknowns:
  - Parallel standalone CLI initialization can race around migration 010 and
    report `table symbol_vectors already exists`; sequential standalone search
    did not reproduce it.
---

## Merged Tree-Sitter Gap Closure and Dogfooded Release Binary

### WHAT
Merged `codex/tree-sitter-quality-bar-gaps` into `main` with a fast-forward to commit `16182528`, built the release binary, and dogfooded the restarted live MCP daemon.

- Committed branch work as `16182528 feat(extractors): close tree-sitter quality bar gaps`.
- Fast-forwarded `main` from `509ed6eb` to `16182528`.
- Built `/Users/murphy/source/julie/target/release/julie-server`.
- Verified the restarted live MCP daemon is serving the merged release binary.
- Ran live MCP dogfood checks for health, production extraction call path, refs, refresh, and SQLite metadata.
- Ran the `dogfood` xtask tier at the merged `main` commit.

### WHY
The previous implementation closed the code gaps but had not yet been merged or proven through the release binary and live MCP path. The quality bar explicitly requires dogfood after release rebuild and restart.

### HOW
Used a local fast-forward merge into clean `main`, then built release with Cargo. After the user restarted the session, used Julie MCP tools against the live daemon and shell SQLite probes for the persisted schema/projection metadata.

### IMPACT
`main` now contains the tree-sitter quality bar gap closure and the release binary is built. Live MCP dogfood passed for the required product surfaces tested so far. I also investigated the earlier standalone CLI failure and narrowed it to a parallel standalone initialization race: sequential standalone search works, while the earlier error occurred after launching two standalone searches concurrently against the same workspace.
