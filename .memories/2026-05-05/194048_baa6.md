---
id: checkpoint_baa63834
timestamp: 2026-05-05T19:40:48.771Z
tags:
  - release-dogfood
  - standalone-race
  - tree-sitter
  - sqlite
  - verification
git:
  branch: main
  commit: 4401746b
summary: Rebuilt Release Binary Dogfood After Standalone Race Fix
briefId: julie-world-class-systems-program
type: checkpoint
context: User rebuilt the release binary and restarted the MCP session after
  merging the standalone SQLite initialization race fix to main.
impact: Post-rebuild live MCP dogfood and release standalone race verification
  are green at main commit 4401746b.
evidence:
  - git status clean and HEAD 4401746b
  - target/release/julie-server reports 7.6.0 and live daemon processes use the
    release path
  - manage_workspace health detailed=true reported READY, projection current
    3988/3988, 34819 symbols, 33180 relationships, 7009 vectors
  - call_path extract_symbols_static extract_canonical found the one-hop
    production extraction edge
  - fast_refs SymbolDatabase::new found the new lock helper calls and regression
    test reference
  - fast_search symbol_database_new_serializes_concurrent_initialization found
    the new test definition
  - manage_workspace refresh workspace_id=julie_528d4264 reported already
    up-to-date at canonical revision 3988
  - sqlite metadata query reported schema_version 24, semantic_index_engine
    2026-05-05.reference-identifier-v3, projection ready 3988/3988,
    symbol_vectors table present
  - Five two-process standalone release-binary race attempts against tiny git
    workspaces completed with zero failures
  - cargo xtask test dogfood passed 2 buckets in 203.2s
symbols:
  - SymbolDatabase::new
  - acquire_database_init_lock
  - symbol_database_new_serializes_concurrent_initialization
  - extract_symbols_static
  - extract_canonical
next: Run `cargo xtask test full` and update the tree-sitter quality-bar ledger
  at 4401746b if we want to mark the fixed target as achieved.
confidence: 5
unknowns:
  - Full tier has not yet been rerun at 4401746b.
---

## Rebuilt Release Binary Dogfood After Standalone Race Fix

### WHAT
Dogfooded the rebuilt release binary at `4401746b` after the user restarted the MCP session.

- Confirmed live daemon processes are running `/Users/murphy/source/julie/target/release/julie-server`.
- Verified live MCP health is READY with projection current.
- Re-ran the release standalone two-process race that originally exposed the bug.
- Re-ran the dogfood xtask tier at the current main commit.

### WHY
The SQLite initialization race fix needed proof from the actual rebuilt release binary, not just the test binary or debug CLI. The tree-sitter quality-bar release claim also depends on live MCP restart dogfood and dogfood-tier evidence at the exact commit.

### HOW
Used Julie MCP tools for health, call graph, refs, search, refresh, and live indexed-state checks. Used disposable tiny git workspaces to run two concurrent `target/release/julie-server --standalone` searches against fresh indexes, which is the race shape that previously failed. Then ran `cargo xtask test dogfood`.

### IMPACT
The rebuilt release binary passed the live MCP dogfood checks and the standalone race no longer reproduces. Julie's release-candidate evidence is stronger; the remaining strict quality-bar release claim still needs the full tier and ledger update if we want the document to say achieved.
