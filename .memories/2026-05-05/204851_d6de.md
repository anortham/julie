---
id: checkpoint_d6deaf26
timestamp: 2026-05-05T20:48:51.775Z
tags:
  - release-notes
  - tree-sitter
  - vue
  - regex
git:
  branch: main
  commit: "06e50920"
summary: Corrected stale v7.7.0 release-note caveat
briefId: julie-world-class-systems-program
type: incident
context: User noticed v7.7.0 GitHub release notes still claimed Vue and Regex
  relationship support were open gaps even though the earlier gap-closure
  session had fixed them.
impact: Prevents public release notes from contradicting the actual v7.7.0
  implementation and the gap-closure session.
evidence:
  - gh release edit v7.7.0 updated release body
  - gh release view v7.7.0 confirmed updated notes
  - git show --stat 16182528 confirmed Vue/Regex relationship implementation and
    tests
symbols:
  - vue::relationships::extract_relationships
  - vue::relationships::extract_structured_pending_relationships
  - regex::relationships::extract_relationships
next: No code change needed. If we want the repository quality-bar ledger to
  reflect tag-level release evidence, update docs/TREE_SITTER_QUALITY_BAR.md
  separately.
confidence: 5
---

## Corrected stale v7.7.0 release-note caveat

### WHAT
Updated the GitHub release body for `v7.7.0` to remove the incorrect claim that Vue and Regex relationship support remain open gaps.

### WHY
The original release note caveat was stale. It reused the earlier `016cb56a` quality-bar checkpoint that marked Vue and Regex as open, but ignored the later `16182528 feat(extractors): close tree-sitter quality bar gaps` commit and checkpoint.

### HOW
Verified current evidence before editing the release:

- `docs/TREE_SITTER_QUALITY_BAR.md` now says the implementation gaps were closed and native relationship output exists for Vue SFCs and regex backreferences.
- `crates/julie-extractors/src/tests/vue/relationships.rs` covers template refs, script setup calls, and pending external component refs.
- `crates/julie-extractors/src/tests/regex/relationships.rs` covers named and numeric backreferences.
- `.memories/2026-05-05/175646_11e7.md` records the gap closure and narrow verification.

### IMPACT
The release notes now state that the previously open Vue and Regex relationship gaps were closed before the tag. The remaining caveat is only about claim hygiene around broad "best in class" wording, not missing Vue/Regex implementation.
