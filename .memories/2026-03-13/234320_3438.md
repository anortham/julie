---
id: checkpoint_3438f8c8
timestamp: 2026-03-13T23:43:20.816Z
tags:
  - semantic
  - embeddings
  - similarity
  - cross-language
  - polyglot
  - deep_dive
  - fast_refs
  - dogfooding
  - knn
  - vector-search
  - reference-workspace
  - onedrive-bug
git:
  branch: main
  commit: "676711e4"
  files:
    - src/search/similarity.rs
    - src/tools/navigation/fast_refs.rs
summary: "## Semantic enhancements — implemented, dogfooded, iterating on
  fast_refs fallback"
planId: sidecar-binary-distribution
type: checkpoint
evidence:
  - "deep_dive context depth: find_similar_symbols → build_similar (0.59),
    find_similar_by_query (0.58)"
  - "deep_dive UserDto: C# class ↔ TS interface at 0.60"
  - "fast_refs primary: search_similar_embeddings found 4 results"
  - "fast_refs reference: logs show 'KNN returned 0 results' — wiring correct,
    threshold or data issue"
symbols:
  - find_similar_symbols
  - find_similar_by_query
  - try_semantic_fallback
  - build_similar
  - format_semantic_fallback
  - find_workspace_root
next: Rebuild with KNN raw score diagnostics to determine if reference workspace
  scores are just below 0.3 threshold or if knn_search is returning empty. Need
  to stop Julie process first to unlock binary.
---

## Semantic enhancements — implemented, dogfooded, iterating on fast_refs fallback

### What
Built and validated semantic similarity enhancements across Julie's MCP tools:
- **deep_dive**: shows similar symbols at context depth (was full-only) — **working, validated**
- **fast_refs**: semantic fallback when zero refs found via `embed_query` on the fly — **partially working**
- **Shared similarity module** at `src/search/similarity.rs` with `find_similar_symbols` (stored embedding) and `find_similar_by_query` (query vector)
- **Distance threshold** (0.5 for symbol↔symbol, 0.3 for query↔symbol)

### Cross-language results confirmed
- C# `UserDto` ↔ TypeScript `UserDto` interface bridged at 0.60 similarity (LabHandbookV2)
- Python `EmbeddingManager` → Rust `EmbeddingProviderFactory` at 0.31 (Miller → Julie)
- `VectorStore` → `memory_vectors`, `vector_storage`, `vectors` — correct concept mapping

### Current investigation
**fast_refs semantic fallback for reference workspaces** — wiring is correct (DB opens, KNN executes) but returns 0 results. Logs confirm: `KNN returned 0 results`. Added raw score diagnostic logging to `find_similar_by_query` to see if scores are just below 0.3 threshold or if KNN itself is empty. Blocked on needing to rebuild (can't overwrite release binary while Julie is running).

### Bugs found and fixed during dogfooding
1. **Semantic fallback used stored embeddings** — impossible to produce results since symbol must exist in DB for stored lookup, but if it exists fast_refs already found it. Fixed: use `embed_query` on the fly
2. **workspace/mod.rs missing ~/.julie guard** — walking up from temp dirs found ~/.julie, treated home dir as workspace, triggered OneDrive mass download on Windows. Fixed: ported `is_global_julie_dir` check from paths.rs
3. **fast_refs skipped reference workspaces** — semantic fallback only searched primary workspace. Fixed: use `get_database_for_workspace` for correct DB

### Also in this session
- Released v5.0.4: context-aware error recovery hints across all tools
- Added reference workspaces: Miller (Python), LabHandbookV2 (.NET + Vue/TypeScript)
