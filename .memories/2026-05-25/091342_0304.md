---
id: checkpoint_0304a7ae
timestamp: 2026-05-25T09:13:42.859Z
tags:
  - cleanup
  - tests
  - analysis
  - test-quality
  - verification
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 845248be
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/mod.rs
    - src/tests/analysis/quality_body_analysis_tests.rs
    - src/tests/analysis/quality_pipeline_tests.rs
    - src/tests/analysis/test_quality_tests.rs
summary: Split Test Quality Tests Below Cap
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo nextest run --lib test_pipeline_integration_updates_metadata: pass"
  - "cargo nextest run --lib test_pipeline_integration_with_identifier_evidence:
    pass"
  - "cargo nextest run --lib test_comment_assertions_not_counted: pass"
  - "cargo xtask test bucket analysis: pass"
  - "cargo xtask test changed: pass, selected analysis only"
  - "Claude CLI review: approve with one documentation finding, corrected"
next: Commit the test-quality split, then re-review oversized test inventory and
  choose the next slice.
confidence: 5
---

## Split Test Quality Tests Below Cap

### WHAT
Split `src/tests/analysis/test_quality_tests.rs` into focused modules:

- Added `src/tests/analysis/quality_pipeline_tests.rs` for five `compute_test_quality_metrics` pipeline integration tests.
- Added `src/tests/analysis/quality_body_analysis_tests.rs` for body-analysis false-positive and edge-case tests.
- Registered both modules in `src/tests/analysis/mod.rs`.
- Reduced `test_quality_tests.rs` from 1416 lines to 981 lines, below the 1000-line test-file cap.
- Updated the cleanup plan and verification ledger.

### WHY
The remaining oversized test inventory identified `test_quality_tests.rs` as a low-risk split candidate. The initial pipeline extraction alone left the file at 1073 lines, so the body false-positive/edge cluster was also moved to finish the size-cap objective instead of leaving a partial split.

### HOW
Moved tests by behavior group and added a small local `TestQualityDb` setup helper in the pipeline module to remove repeated `TempDir`/`SymbolDatabase`/`LanguageConfigs`/file-row boilerplate. Fixed a compile failure by preserving the actual `SymbolBuilder::metadata` API: metadata helpers now deserialize JSON objects into `HashMap<String, serde_json::Value>`, matching the old inferred `serde_json::from_str` behavior. Renamed the new modules away from the ignored `test_*.rs` pattern.

### IMPACT
The analysis test suite remains behavior-equivalent and the split files are all under cap:

- `test_quality_tests.rs`: 981 lines
- `quality_pipeline_tests.rs`: 257 lines
- `quality_body_analysis_tests.rs`: 89 lines

Verification passed:

- `cargo fmt --check`
- `cargo check --all-targets`
- `cargo nextest run --lib test_pipeline_integration_updates_metadata`
- `cargo nextest run --lib test_pipeline_integration_with_identifier_evidence`
- `cargo nextest run --lib test_comment_assertions_not_counted`
- `cargo xtask test bucket analysis`
- `cargo xtask test changed` selected only `analysis` and passed
- Claude CLI review approved after one documentation line-count correction

Next cleanup target is re-reviewing the oversized inventory, with `src/tests/tools/deep_dive_tests.rs` as the next likely low-risk split.
