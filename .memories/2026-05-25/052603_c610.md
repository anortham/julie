---
id: checkpoint_c6102134
timestamp: 2026-05-25T05:26:03.412Z
tags:
  - cleanup
  - candidate-12
  - db-builders
  - reference-scores
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: d3117630
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated database type-usage centrality fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo nextest run --lib
    test_compute_reference_scores_includes_type_usage_identifiers: pass"
  - "cargo nextest run --lib
    test_compute_reference_scores_includes_constants_with_type_usage: pass"
  - "cargo nextest run --lib
    test_compute_reference_scores_qualified_name_identifiers: pass"
  - "cargo nextest run --lib
    test_compute_reference_scores_escapes_like_wildcards: pass"
  - "cargo nextest run --lib
    test_step1b_identifier_boost_excludes_test_file_symbols: pass"
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo xtask test changed: pass, selected core-database"
  - "git diff --check: pass"
  - "Claude CLI review: approve with no actionable findings"
next: Commit the slice, then continue with the next verified-safe `database.rs`
  raw setup cluster (likely path-sensitive relationship/header propagation) or
  re-review remaining raw inventory before editing.
---

## Migrated database type-usage centrality fixtures

### WHAT
- Migrated five `src/tests/core/database.rs` Step 1b type-usage reference-score fixtures from raw `files`/`symbols`/`identifiers` SQL to shared DB row builders and storage APIs.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` progress notes and verification ledger.
- Left the `kind='import'` fixture raw because `IdentifierKind` has no `Import` variant.

### WHY
- Continues Candidate 12 test harness cleanup by replacing repeated raw setup SQL with typed fixture builders while preserving behavior-focused assertions.
- Avoids changing import scoring semantics that the current builder API cannot represent.

### HOW
- Used `file_info_builder`, `symbol_builder`, and `identifier_builder` for type-usage fixtures covering GDScript type annotations, Zig type constants, qualified QML component refs, SQL wildcard safety, and test-file symbol exclusion.
- Verified locally and with a read-only explorer that `compute_reference_scores` Step 1b reads only identifier `name`, `kind`, and `file_path`; builder byte defaults are inert for this scoring path.
- Ran a fresh Claude review against the diff; it approved with no actionable findings.

### IMPACT
- Reduces raw setup SQL in the large core database test file without broadening production behavior or changing scoring contracts.
- Documents the remaining raw import-kind exception so future migration slices do not accidentally collapse import weight from `2.0` to type-usage weight or no match.
