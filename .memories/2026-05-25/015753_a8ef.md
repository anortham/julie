---
id: checkpoint_a8ef684c
timestamp: 2026-05-25T01:57:53.748Z
tags:
  - cleanup
  - candidate-12
  - test-fixtures
  - test-linkage
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 1cb8e4ad
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/linkage_edge_cases_tests.rs
summary: Migrated stale-cleanup linkage fixture
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo nextest run --lib
    test_compute_linkage_clears_stale_symbol_and_parent_linkage: pass after
    cargo fmt"
  - "cargo fmt --check: pass after cargo fmt"
  - "cargo check --all-targets: pass"
  - "cargo xtask test bucket analysis: pass"
  - "cargo xtask test changed: pass, selected analysis and ignored plan doc
    after final rerun"
  - Julie blast_radius on linkage_edge_cases_tests.rs reviewed as
    analysis/test-only impact
  - "Claude CLI strict schema review: approve with no findings"
symbols:
  - test_compute_linkage_clears_stale_symbol_and_parent_linkage
next: Commit this slice, then migrate the filter/raw SQL cluster in
  `linkage_edge_cases_tests.rs`.
---

## Migrated stale-cleanup linkage fixture

### WHAT
- Replaced the raw SQL setup cluster in `test_compute_linkage_clears_stale_symbol_and_parent_linkage` with shared `symbol_builder` / `relationship_builder` plus storage APIs.
- Updated the cleanup plan progress, remaining-work note, and verification ledger.

### WHY
- Continues Candidate 12 by removing duplicated fixture SQL while keeping behavior-specific mutation SQL visible.

### HOW
- Preserved parent, child, and test symbol spans, parent relationship, metadata, and schema-default confidence.
- Preserved the `rel_1` call relationship via `relationship_builder` with file path and line number.
- Left `DELETE FROM relationships WHERE id = 'rel_1'` explicit because that deletion is the behavior trigger under test.
- Applied rustfmt after `cargo fmt --check` reported formatting-only relationship builder drift.

### IMPACT
- The stale-cleanup setup cluster is builder-backed.
- Remaining raw setup clusters in `linkage_edge_cases_tests.rs` are filters, parent aggregation, and best-confidence cases.
