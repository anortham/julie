---
id: checkpoint_a9902986
timestamp: 2026-05-25T04:28:58.654Z
tags:
  - cleanup
  - candidate-12
  - workspace
  - tests
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: "10225e70"
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/tools/workspace/index_embedding_tests.rs
summary: Migrated workspace embedding fixture setup
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib tests::tools::workspace::index_embedding_tests
    passed before and after cargo fmt
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: tools-workspace, 9 commands, 29.5s"
  - git diff --check passed
  - Claude CLI approved with no findings
next: Commit this slice, then migrate the verified-safe `metrics/query_tests.rs`
  raw symbol setup.
confidence: 5
---

## Migrated workspace embedding fixture setup

### WHAT
- Replaced raw file/symbol setup SQL in `src/tests/tools/workspace/index_embedding_tests.rs` with shared `file_info_builder`, `symbol_builder`, and `set_symbol_reference_scores` helpers.
- Updated the cleanup plan with progress notes, verification ledger rows, and Claude review evidence.

### WHY
- Candidate 12 is removing duplicated raw test fixture setup where shared builders can express the same row shape.
- This slice removed another raw `INSERT OR IGNORE INTO files` / `INSERT INTO symbols` cluster while preserving embedding-pipeline behavior.

### HOW
- Preserved old same-file `INSERT OR IGNORE` behavior in the shared helper by checking `get_file_hash` before `store_file_info`, avoiding `INSERT OR REPLACE` cascade deletion.
- Preserved `reference_score = 1.0` explicitly with `set_symbol_reference_scores` after `store_symbols`.
- Left the batch-cancel loop unguarded because each generated file path is unique.

### IMPACT
- `workspace/index_embedding_tests.rs` no longer has raw file/symbol setup SQL for this fixture path.
- Remaining Candidate 12 migration order now starts with `metrics/query_tests.rs`.
- Verification: targeted workspace embedding tests, formatting, typecheck, changed bucket, whitespace check, and fresh Claude review all passed.
