---
id: checkpoint_c7911712
timestamp: 2026-05-25T06:41:06.690Z
tags:
  - cleanup
  - candidate-12
  - test-fixtures
  - analysis
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 6c7bf164
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/change_risk_tests.rs
    - src/tests/analysis/linkage_edge_cases_tests.rs
    - src/tests/analysis/test_linkage_tests.rs
    - src/tests/core/database_row_mapping.rs
summary: Candidate 12 Analysis And Row-Mapping Helper Cleanup
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
context: After committing hybrid fixture migration, read-only re-review found a
  few remaining builder-safe raw file/FK setup helpers and an oversized hybrid
  test file. This checkpoint covers the small raw-helper cleanup slice before
  the structural split.
impact: Reduces remaining raw setup SQL and clarifies the raw-vs-builder
  boundary. Establishes `hybrid_search_tests.rs` split as the next priority from
  current re-review findings.
evidence:
  - cargo nextest run --lib test_compute_change_risk_scores passed
  - cargo nextest run --lib test_linked_tests_capped_at_five passed
  - cargo nextest run --lib test_name_match_ambiguity_guard_is_language_scoped
    passed
  - cargo nextest run --lib test_class_inherits_method_linkage passed
  - Six linkage_edge_cases exact tests passed
  - Three database_row_mapping malformed-row tests passed
  - rg raw setup scan across changed files now reports only malformed
    row-mapping inserts
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: dev fallback, 34 buckets in 515.6s"
  - git diff --check passed
  - Claude CLI review approved with no code changes recommended after retrying
    with fallback model
symbols:
  - store_file_info_if_missing
  - symbol_builder
  - insert_file
  - insert_symbol
next: Commit this slice, then start the `hybrid_search_tests.rs`
  module-directory split with exact tests from the structural explorer report.
---

## Candidate 12 Analysis And Row-Mapping Helper Cleanup

### WHAT
- Migrated remaining analysis raw file-row helpers in `change_risk_tests.rs`, `test_linkage_tests.rs`, and `linkage_edge_cases_tests.rs` from `INSERT OR IGNORE INTO files` to `store_file_info_if_missing` + `file_info_builder`.
- Migrated valid FK setup rows in `database_row_mapping.rs` to shared file/symbol builders.
- Kept malformed `kind`/`visibility` rows raw in `database_row_mapping.rs` because those invalid strings are the behavior under test.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with re-review findings, ledger rows, Claude review result, and next priority: split oversized `hybrid_search_tests.rs`.

### WHY
- Raw setup inventory showed these rows were builder-safe and not schema/default assertions.
- The shared `store_file_info_if_missing` helper now captures the prior `INSERT OR IGNORE` behavior without repeated raw SQL.
- `database_row_mapping.rs` had mixed raw rows: support rows could move to builders, but malformed rows must remain raw.

### HOW
- Reused `file_info_builder` with explicit `.symbol_count(0).line_count(0)` to match raw file schema defaults where tests previously omitted those columns.
- Reused `symbol_builder` for valid `database_row_mapping` FK symbols, preserving span/byte fields and explicit `confidence=1.0` to match the schema default.
- Ran exact tests for change-risk, linkage, linkage edge cases, and malformed row mapping.
- Ran `cargo fmt --check`, `cargo check --all-targets`, `cargo xtask test changed`, and `git diff --check`.
- Retried Claude CLI review after a 529 overload; fallback run approved with no code changes recommended.

### IMPACT
- The targeted analysis files no longer contain raw setup inserts for file rows.
- `database_row_mapping.rs` now uses typed helpers for valid FK setup while still testing malformed raw rows directly.
- Remaining raw setup is mostly intentional or low-value, making the next cleanup slice the structural `hybrid_search_tests.rs` split rather than more tiny raw-row migrations.
