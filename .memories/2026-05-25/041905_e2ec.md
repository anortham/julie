---
id: checkpoint_e2eca303
timestamp: 2026-05-25T04:19:05.794Z
tags:
  - cleanup
  - tests
  - db-builders
  - vector-storage
  - review
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: d6822f72
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/vector_storage.rs
summary: Re-reviewed Candidate 12 and migrated vector storage fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Keeps the cleanup loop moving while preventing repeated rediscovery of
  intentional raw SQL categories.
evidence:
  - Claude re-review classified remaining raw SQL inventory and plan direction
  - "cargo nextest run --lib test_delete_embeddings_for_file: failed before
    get_file_hash guard, passed after fix"
  - "cargo nextest run --lib tests::core::vector_storage: final 21 tests passed"
  - cargo fmt
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo xtask test changed: core-fast passed in 16.9s; final rerun after
    review ledger doc edit passed in 16.5s"
  - "Claude review of vector storage slice: approve with no findings; verified
    cascade-delete root cause, get_file_hash guard, benign builder default
    deltas, ledger accuracy, and doc-comment caveat"
  - git diff --check
next: "Commit this slice, then start the next verified candidate:
  workspace/index_embedding_tests.rs."
---

## Re-reviewed Candidate 12 and migrated vector storage fixtures

- **WHAT:** Ran a focused Claude re-review of remaining raw SQL fixture inventory, updated the cleanup plan with explicit stay-raw categories and next migration order, and migrated `src/tests/core/vector_storage.rs` symbol/file setup to shared builders.
- **WHY:** The cleanup loop requires re-review after completed slices, and raw SQL should only remain when positively justified. `vector_storage.rs` was the lowest-risk next migration candidate.
- **HOW:** Used `file_info_builder` and `symbol_builder` in `insert_test_symbol_with_lang`. During verification, `test_delete_embeddings_for_file` exposed a semantic regression: `store_file_info` uses `INSERT OR REPLACE`, which cascades symbol deletion for an existing file row. Fixed by checking `get_file_hash(file_path).is_none()` before storing file info, preserving the old `INSERT OR IGNORE` behavior.
- **IMPACT:** Candidate 12 plan now distinguishes intentional raw SQL from remaining migrations. `vector_storage.rs` no longer has raw file/symbol setup SQL, and the affected core-fast gate is green.
