---
id: checkpoint_7426640d
timestamp: 2026-05-25T04:58:20.779Z
tags:
  - cleanup
  - candidate-12
  - database
  - relationships
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: c0f36bf3
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated outgoing-only centrality fixture
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib test_compute_reference_scores_zero_for_no_incoming
    passed
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: core-database, 1 command, 1.6s"
  - git diff --check passed
  - Claude CLI approved with no findings
next: Commit this outgoing-only batch, then continue with the next safe
  database.rs propagation cluster.
confidence: 5
---

## Migrated outgoing-only centrality fixture

### WHAT
- Replaced raw symbol and relationship setup SQL in `test_compute_reference_scores_zero_for_no_incoming` with shared builders.
- Updated the cleanup plan with progress, verification, and Claude review evidence.

### WHY
- The test is a safe relationship-only centrality fixture: it asserts `compute_reference_scores` gives an outgoing-only sender zero score and an incoming-call receiver score of 3.0.

### HOW
- Used `symbol_builder` for sender/receiver function rows with exact ids, names, Rust language, `test.rs` path, span, byte range, and explicit `.confidence(1.0)`.
- Used `relationship_builder` with `RelationshipKind::Calls` and `.line_number(0)` to preserve the raw schema default for omitted line number.
- Kept score assertion SQL unchanged.

### IMPACT
- Removed another raw symbol/relationship setup cluster from `database.rs`.
- Verification: exact test, formatting, typecheck, changed core-database bucket, diff whitespace check, and Claude review passed.
