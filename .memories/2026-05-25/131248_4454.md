---
id: checkpoint_4454a5ff
timestamp: 2026-05-25T13:12:48.429Z
tags:
  - cleanup
  - tests
  - dashboard
  - candidate-12
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: dfa22f7d
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/dashboard/integration.rs
    - src/tests/dashboard/integration/metrics.rs
    - src/tests/dashboard/integration/signals.rs
    - src/tests/dashboard/integration/static_search.rs
    - src/tests/dashboard/integration/status.rs
summary: Split dashboard integration tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Reduces dashboard test-file size while preserving dashboard behavior
  coverage and narrow changed-file verification.
evidence:
  - cargo check --all-targets passed
  - "cargo nextest run --lib tests::dashboard::integration -- --skip
    search_quality passed: 22 run, 22 passed"
  - cargo xtask test bucket dashboard passed in 12.5s
  - cargo xtask test changed selected only dashboard and passed in 11.6s
  - cargo fmt --check passed
  - git diff --check passed
  - git check-ignore -v src/tests/dashboard/integration/*.rs || true returned no
    ignored files
  - "wc -l shows root/children: 401/317/158/231/222"
  - Remaining over-cap scan no longer includes dashboard/integration.rs
  - Claude CLI review approved with no defects
next: Commit this split, then evaluate the daemon database workspace-status tail
  split suggested by the read-only explorer.
confidence: 5
---

## Split dashboard integration tests

### WHAT
- Split `src/tests/dashboard/integration.rs` from 1,320 lines into a 401-line parent plus four child modules under `src/tests/dashboard/integration/`.
- Child modules cover signals pages, metrics/pages, static/search/project routes, and status health rendering.
- Updated the cleanup plan with progress, remaining over-cap files, and verification ledger rows.

### WHY
- Candidate 12 is reducing oversized test files while keeping test behavior and changed-file verification narrow.
- Dashboard integration had clear helper ownership: all shared setup helpers stayed in the parent, and the existing dashboard changed-file route already covers child paths.

### HOW
- Kept imports and shared state/fixture helpers in the parent module.
- Moved test bodies mechanically with `use super::*;` in each child.
- Left `src/tests/dashboard/mod.rs` unchanged because the root `integration.rs` module remains in place.
- Used existing `src/tests/dashboard/` changed-file routing, so no xtask mapper change was required.

### IMPACT
- `dashboard/integration.rs` is no longer over the 1000-line test-file cap.
- All 22 original async tests remain discoverable and pass under the original module filter.
- `cargo xtask test changed` stays narrow for this split: dashboard bucket only.
