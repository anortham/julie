---
id: checkpoint_1a1aedb8
timestamp: 2026-05-25T05:04:07.004Z
tags:
  - cleanup
  - candidate-12
  - database
  - relationships
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: c0e3b6be
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated interface and base propagation fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib
    test_compute_reference_scores_propagates_interface_centrality_to_implementations
    passed
  - cargo nextest run --lib
    test_compute_reference_scores_propagates_base_class_centrality passed
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: core-database, 1 command, 1.5s"
  - git diff --check passed
  - Claude CLI approved with no findings
next: Commit this propagation batch, then continue with constructor or
  type-usage identifier database.rs clusters.
confidence: 5
---

## Migrated interface and base propagation fixtures

### WHAT
- Replaced raw symbol/relationship SQL in `test_compute_reference_scores_propagates_interface_centrality_to_implementations` and `test_compute_reference_scores_propagates_base_class_centrality` with shared builders.
- Updated the cleanup plan with verification and Claude review evidence.

### WHY
- These C# centrality tests are builder-safe propagation fixtures: they assert interface implementation and base-class inheritance centrality behavior, not raw SQL defaults.

### HOW
- Used `symbol_builder` for C# class/interface symbols with exact ids, names, language, path, span, bytes, and explicit confidence.
- Used `relationship_builder` for implements/uses/extends/calls rows, with `line_number(0)` to preserve the raw relationship schema default.
- Kept SQL assertions for computed `reference_score` values unchanged.

### IMPACT
- Removed two more raw symbol/relationship setup clusters from `database.rs`.
- Verification: both exact propagation tests, formatting, typecheck, changed core-database bucket, diff whitespace check, and Claude review passed.
