---
id: checkpoint_6c4d3b3f
timestamp: 2026-05-25T00:02:11.387Z
tags:
  - cleanup
  - daemon-db
  - workspace-registry
  - candidate-11
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 3480b5b3
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/daemon/database.rs
    - src/daemon/database/workspaces.rs
summary: Split daemon DB workspace registry storage
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib test_upsert_and_get_workspace
  - cargo nextest run --lib test_increment_decrement_session_count
  - cargo nextest run --lib test_reset_all_session_counts
  - cargo nextest run --lib test_update_workspace_stats
  - cargo nextest run --lib test_update_vector_count
  - cargo nextest run --lib test_insert_and_list_cleanup_events
  - cargo nextest run --lib test_cleanup_event_log_is_capped_at_fifty_rows
  - cargo nextest run --lib test_delete_workspace_with_root_path
  - cargo nextest run --lib test_upsert_workspace_path_conflict_updates_status
  - cargo nextest run --lib test_upsert_workspace_allows_upgrade_to_ready
  - cargo nextest run --lib
    test_normalize_workspace_paths_fixes_slashes_and_status
  - cargo nextest run --lib
    test_normalize_workspace_paths_skips_pending_without_symbols
  - cargo nextest run --lib test_normalize_restores_ready_on_all_platforms
  - cargo nextest run --lib test_migrate_workspace_ids_updates_all_tables
  - cargo nextest run --lib
    test_migrate_workspace_ids_merges_when_target_workspace_exists
  - cargo nextest run --lib tests::daemon::database -- --skip search_quality
  - cargo fmt --check
  - cargo check --all-targets
  - cargo xtask test bucket daemon
  - cargo xtask test changed
  - fresh Claude review approved with no findings
next: Commit the workspace registry split, then reassess remaining Candidate 12
  linkage/quality edge clusters and Candidate 8 contract prerequisite.
---

## Split daemon DB workspace registry storage

### WHAT
- Moved workspace CRUD/stat/session/cleanup-event storage from `src/daemon/database.rs` to new `src/daemon/database/workspaces.rs`.
- Re-exported `WorkspaceRow` and `WorkspaceCleanupEventRow` from `daemon::database` to preserve caller import paths.
- Left `normalize_workspace_paths` and `migrate_workspace_ids` in the root module because they are startup/migration repair paths.
- Updated cleanup plan progress, remaining-candidate notes, and verification ledger.

### WHY
- Completes the safe daemon DB table-family split and brings `src/daemon/database.rs` below the 500-line implementation target.

### HOW
- Preserved method signatures, SQL strings, ordering, `COALESCE` behavior, session-count clamp, and cleanup-event cap behavior.
- Used the same child-module private-access pattern as codehealth/tool_calls/search_compare.
- Ran Julie blast-radius and a fresh Claude review after affected gates.

### IMPACT
- `src/daemon/database.rs` is now 296 lines.
- New `src/daemon/database/workspaces.rs` is 287 lines.
- Daemon DB root now mainly owns open/table_exists/startup repair/test connection/workspace-ID migration and delegates table-family persistence to submodules.
