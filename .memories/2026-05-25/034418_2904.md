---
id: checkpoint_2904ace9
timestamp: 2026-05-25T03:44:18.292Z
tags:
  - cleanup
  - tests
  - db-builders
  - deep-dive
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 343cdac5
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/tools/deep_dive_regression_tests.rs
summary: Migrated deep-dive regression identifier helper
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Reduces raw SQL fixture duplication in deep-dive regression tests while
  preserving qualified-method fallback coverage.
evidence:
  - cargo nextest run --lib
    test_deep_dive_regression_qualified_method_identifier_fallback_avoids_bare_name_noise
  - cargo fmt
  - cargo nextest run --lib
    test_deep_dive_regression_qualified_method_identifier_fallback_avoids_bare_name_noise
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo xtask test changed: tools-deep-dive passed in 1.9s; final rerun after
    plan review row passed in 1.9s"
  - 'rg -n "INSERT INTO identifiers"
    src/tests/tools/deep_dive_regression_tests.rs: no matches'
  - "claude -p review: approve with no findings; verified row-shape parity,
    IdentifierKind::Call serialization, optional containing-symbol preservation,
    call-site coverage, and ledger accuracy"
  - git diff --check
next: Commit this slice, then evaluate the larger deep_dive_tests identifier
  helper separately.
---

## Migrated deep-dive regression identifier helper

- **WHAT:** Replaced the raw `INSERT INTO identifiers` helper in `src/tests/tools/deep_dive_regression_tests.rs` with `identifier_builder` plus `bulk_store_identifiers`, and updated its two call sites to use `IdentifierKind::Call` and `&mut db`.
- **WHY:** Candidate 12 is removing ad hoc test fixture SQL where shared DB builders can preserve row semantics. This helper only supports the qualified-method identifier fallback regression.
- **HOW:** Preserved the original identifier ID format, names, call kind, Rust language default, fixed line/column/byte spans, optional containing symbol IDs, no target/code_context, and confidence. Claude reviewed the row shape and approved with no findings.
- **IMPACT:** `deep_dive_regression_tests.rs` no longer has raw identifier setup SQL; the larger duplicate helper in `deep_dive_tests.rs` remains a separate candidate.
