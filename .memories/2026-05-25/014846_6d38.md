---
id: checkpoint_6d38a7e5
timestamp: 2026-05-25T01:48:46.904Z
tags:
  - cleanup
  - candidate-12
  - test-fixtures
  - test-linkage
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 3ab99e83
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/linkage_edge_cases_tests.rs
summary: Migrated tied-score linkage fixture
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo nextest run --lib
    test_name_match_fallback_is_deterministic_on_tied_scores: pass after cargo
    fmt"
  - "cargo fmt --check: pass after cargo fmt"
  - "cargo check --all-targets: pass"
  - "cargo xtask test bucket analysis: pass"
  - "cargo xtask test changed: pass, selected analysis and ignored plan doc
    after final rerun"
  - Julie blast_radius on linkage_edge_cases_tests.rs reviewed as
    analysis/test-only impact
  - "Claude CLI strict schema review: approve with no findings"
symbols:
  - test_name_match_fallback_is_deterministic_on_tied_scores
next: Commit this slice, then migrate stale-cleanup setup in
  `linkage_edge_cases_tests.rs` one reviewed cluster at a time.
---

## Migrated tied-score linkage fixture

### WHAT
- Replaced the raw SQL setup cluster in `test_name_match_fallback_is_deterministic_on_tied_scores` with shared `symbol_builder` / `identifier_builder` and database storage APIs.
- Updated the cleanup plan progress, remaining-work note, and verification ledger.

### WHY
- Continues Candidate 12 by moving linkage fixture setup away from duplicated SQL while keeping assertion and mutation SQL explicit where it describes the tested behavior.

### HOW
- Preserved C# method symbols, spans, visibility, test metadata, schema-default confidence, and identifier containing-symbol shape.
- Left `target_symbol_id` as NULL for the identifier fallback path.
- Did not carry decorative `reference_score` values because `compute_test_linkage` does not read that column.
- Applied rustfmt after `cargo fmt --check` found only builder-block formatting drift.

### IMPACT
- The tied-score fallback raw setup cluster is builder-backed.
- Remaining `linkage_edge_cases_tests` raw SQL clusters are stale cleanup, filters, parent aggregation, and best confidence.
