---
id: checkpoint_1ee5d820
timestamp: 2026-05-25T04:47:29.421Z
tags:
  - cleanup
  - candidate-12
  - database
  - tests
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: b9424797
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated database get-reference-score fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib test_get_reference_scores_batch passed
  - cargo nextest run --lib test_get_reference_scores_large_batch passed
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: core-database, 1 command, 1.5s"
  - git diff --check passed
  - Claude CLI approved with no findings
  - Read-only subagent classified remaining database.rs raw setup
next: Commit this slice, then migrate the next safe database.rs
  relationship-only cluster in a small exact-test batch.
confidence: 5
---

## Migrated database get-reference-score fixtures

### WHAT
- Replaced the two explicit raw `reference_score` symbol insert loops in `src/tests/core/database.rs` with `symbol_builder` plus `set_symbol_reference_scores`.
- Updated the cleanup plan with progress notes, verification evidence, Claude review, and subagent per-cluster classification of remaining database raw setup.

### WHY
- These two tests exercise `get_reference_scores` read behavior with known stored scores, so builders can express the row shape while the helper explicitly restores the centrality values.
- Schema-default and compute-score tests intentionally exercise different database behavior and were left untouched.

### HOW
- Made the DB mutable only in the two migrated tests because `store_symbols` requires `&mut self`.
- Preserved function kind, Rust language, `test.rs` file path, span `(1,0)-(10,1)`, byte range `0..100`, and confidence default via `.confidence(1.0)`.
- Used `set_symbol_reference_scores` after `store_symbols` for exact score values, including the 1500-symbol large batch.
- Recorded subagent classification: additional relationship/type-usage/path-sensitive clusters are builder-safe in future small batches; default-score and import-identifier rows stay raw.

### IMPACT
- The explicit raw `INSERT INTO symbols ... reference_score` setup in the two get-reference-score tests is gone.
- Remaining `database.rs` cleanup is better classified and can proceed in exact-test batches.
- Verification: both exact tests, formatting, typecheck, changed core-database bucket, diff whitespace check, and Claude review passed.
