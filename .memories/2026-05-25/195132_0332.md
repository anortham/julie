---
id: checkpoint_0332423b
timestamp: 2026-05-25T19:51:32.541Z
tags:
  - refactor
  - cleanup
  - code-review
  - architecture
  - adr
  - typed-errors
  - workspace-paths
  - prepared-edit
  - edit-locks
  - source-aware-language
  - test-harness
  - multi-agent-review
git:
  branch: main
  commit: 4297e6f8
  files:
    - docs/adr/ADR-0001-strict-workspace-path-resolver-contract.md
    - docs/adr/ADR-0002-typed-mcp-boundary-error-classifier.md
    - docs/adr/ADR-0003-prepared-once-edit-rewrite-invariant.md
    - docs/adr/ADR-0004-per-path-edit-lock-invariant.md
    - docs/adr/ADR-0005-source-aware-language-detection-precedence.md
    - docs/adr/README.md
    - src/handler/tools/edit_file.rs
    - src/handler/tools/manage_workspace.rs
    - src/handler/tools/rewrite_symbol.rs
    - src/handler/tools/spillover_get.rs
summary: Pre-push code review and architecture cleanup landed
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: "Branch went from compile-broken (lost tests in last commit) to fully
  green dev tier (37 buckets, 773.7s). Caller obligations dropped substantially:
  path normalization, status compares, error mapping, test-exclusion, prepared
  work, and language detection are each one shared module now. Five ADRs make
  the load-bearing invariants discoverable for future agents. Phase 2 (splitting
  handler.rs and search/index.rs) is now mechanically achievable."
evidence:
  - "cargo xtask test changed (fell back to dev+system+dogfood): 37 buckets
    passed, 0 failures, 773.7s"
  - "Pushed 99 commits: f92bead5..4297e6f8 to origin/main"
  - All four review agents reported clean or with documented findings; all four
    fix agents reported complete
  - Audited all 31 split commits — only database.rs dropped tests (2 tests,
    recovered)
symbols:
  - resolve_workspace_file_input
  - WorkspaceResolutionFailureKind
  - classify_tool_failure
  - PreparedEdit
  - PreparedRewrite
  - EDIT_LOCKS
  - EditingTransaction
  - MultiFileTransaction
  - is_test_symbol_result
  - detect_language_for_source
  - ManageWorkspaceRequest
  - WorkspaceTarget
next: Wait for adr-followthrough-fixer to apply classify_tool_failure to
  edit_file/rewrite_symbol/manage_workspace/spillover_get error paths, then
  commit + push.
confidence: 5
---

## Pre-push code review and architecture cleanup landed

Marathon multi-agent review of 94 cleanup commits sitting on `main`, then a follow-on round of fixes that came out of the review, then ADRs codifying the load-bearing invariants. One small wrapper fix still in flight.

### What landed
- **Compile break recovered.** Commit `3f59dfb5` had declared `mod test_deweighting;` in `src/tests/core/database.rs` but never created the file, dropping two tests in the database.rs split. Audited all 31 split commits — database.rs was the only one that dropped tests. Recovered to `src/tests/core/database/deweighting.rs` (intentionally not `test_*.rs` because `.gitignore` line 43 would have skipped it on push).
- **W3:** `resolve_workspace_file_input` now returns `Result`; outside-workspace input surfaces as `WorkspaceResolutionFailureKind::FileOutsideWorkspace`. Three callers (`symbols/primary`, `symbols/target_workspace`, `refactoring/rename`) propagate via `?`. No raw-input fallback.
- **W1+W2:** New `src/handler/tools/error.rs::classify_tool_failure(name, &anyhow::Error) -> McpError` is the single MCP-boundary classifier. Applied across 8 wrappers. `metrics_workspace_binding_for_workspace_param(Option<&str>)` replaced with `metrics_workspace_binding_for_target(&WorkspaceTarget)`; `fast_refs`, `fast_search`, `get_context` now resolve workspace once via `*_with_target` variants.
- **S1 + editing nits:** `search_annotation_symbols` routes through `is_test_symbol_result`; `PreparedRewrite` caches `diff` + `changed_bytes`; `edit_file::call_prepared` short-circuits no-op writes.
- **5 misc nits:** codehealth mutex normalized, `OPERATIONS` table dedups manage_workspace parse + help, empty `.h` warn silenced, `select_best_candidate` doc-pinned test-only, handler test helpers extracted to `src/tests/core/handler/common.rs`.

### Pushed
99 commits to `origin/main` (`f92bead5..4297e6f8`). No release tag — user said not yet.

### ADRs written
5 ADRs in new `docs/adr/` directory codifying the load-bearing invariants:
- **ADR-0001** Strict workspace path resolver contract
- **ADR-0002** Typed MCP-boundary error classifier
- **ADR-0003** Prepared-once edit/rewrite invariant
- **ADR-0004** Per-path edit lock invariant (scoped to user source files; daemon/config/marker writes out of scope)
- **ADR-0005** Source-aware language detection precedence (distinguishes indexing gate from language attribution)

### Audit found drift in two ADRs
- **ADR-0002 drift:** 4 more wrappers (`edit_file`, `rewrite_symbol`, `manage_workspace`, `spillover_get`) still hand-roll `McpError::internal_error`. Fix worker dispatched; awaiting result.
- **ADR-0004 and ADR-0005 wording:** tightened scope inline — file writes for daemon state / config / markers are explicitly out of scope; extension-only language lookup is correct for indexing gates, only attribution requires source-aware.

### Architecture grade
Pre-cleanup: **C+**. Post-cleanup: **B / B+**. Wide modules remain (handler 2509 LOC, search/index 2249 LOC) but the policy seams around them are typed and consolidated, which is the precondition for splitting them next.

### Working tree state
Clean post-push. One fix worker still running; user wants commit+push when it lands.
