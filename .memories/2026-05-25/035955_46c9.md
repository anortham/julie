---
id: checkpoint_46c98c9a
timestamp: 2026-05-25T03:59:55.245Z
tags:
  - cleanup
  - tests
  - db-builders
  - core-database
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 53fdddff
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated core identifier query fixture
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Reduces raw SQL fixture duplication in a core database query test while
  preserving filtering coverage.
evidence:
  - cargo nextest run --lib
    test_get_identifiers_by_names_kinds_excluding_containers_filters_rows
  - cargo fmt
  - cargo nextest run --lib
    test_get_identifiers_by_names_kinds_excluding_containers_filters_rows
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo xtask test changed: core-database passed in 1.5s; final rerun after
    plan review row passed in 1.5s"
  - "Claude review: approve with no findings; verified symbol and identifier row
    parity, valid_symbol_ids=None, no import variant, and ledger scope accuracy"
  - git diff --check
next: Commit this slice, then continue evaluating remaining raw SQL fixtures;
  keep import-kind fixtures explicit unless a typed representation exists.
---

## Migrated core identifier query fixture

- **WHAT:** Replaced raw symbol and identifier inserts in `test_get_identifiers_by_names_kinds_excluding_containers_filters_rows` with `symbol_builder`, `identifier_builder`, `store_symbols`, and `bulk_store_identifiers`.
- **WHY:** Candidate 12 is removing raw SQL fixture setup where builders preserve row semantics. This query fixture only used supported function/call/type-usage/member-access rows.
- **HOW:** Preserved symbol IDs/names/kind/language/path/span/bytes and identifier IDs/name/kinds/path/span/bytes/containing/target/confidence defaults. Explicitly left import-kind raw SQL elsewhere because `IdentifierKind` has no `Import` variant.
- **IMPACT:** One core database query fixture is now builder-backed without changing the query behavior under test.
