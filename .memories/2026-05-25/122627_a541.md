---
id: checkpoint_a54162fd
timestamp: 2026-05-25T12:26:27.350Z
tags:
  - cleanup
  - tests
  - dashboard
  - candidate-12
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 4ea02906
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/dashboard/intelligence.rs
summary: Migrated dashboard intelligence fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Dashboard intelligence fixture setup is less schema-coupled while
  preserving tested behavior.
evidence:
  - cargo fmt --check passed
  - git diff --check passed
  - cargo check --all-targets passed earlier for this slice
  - cargo xtask test changed selected dashboard and passed earlier for this slice
  - "cargo nextest run --lib tests::dashboard::intelligence passed: 25 run, 25
    passed"
  - Claude CLI review approved with no blocking findings
next: Commit this dashboard fixture migration, then pause active goal per user
  request.
confidence: 5
---

## Migrated dashboard intelligence fixtures

### WHAT
- Replaced ordinary raw `symbols` and `relationships` SQL setup in `src/tests/dashboard/intelligence.rs` with shared DB builder/storage helpers.
- Left only the two intentional raw `files` inserts that exercise `NULL line_count` and empty-language aggregate behavior.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with Candidate 12 progress, verification ledger evidence, and Claude review disposition.

### WHY
- The cleanup plan identified raw fixture SQL as schema-coupled test setup that should use existing test fixture builders when the row semantics are ordinary.
- Dashboard intelligence tests were low-risk: helper migration should preserve query behavior without changing production code.

### HOW
- Added `store_function_symbol` around `symbol_builder` + `store_symbols` + `set_symbol_reference_scores`.
- Added `store_call_relationship` around `relationship_builder` + `store_relationships`.
- Migrated setup rows for `get_top_symbols_by_centrality`, `get_file_hotspots`, and `get_aggregate_stats` tests.
- Addressed Claude CLI review nits by documenting inert span/byte ranges and making the plan progress entry more specific.

### IMPACT
- Reduces direct coupling between dashboard tests and the exact `symbols`/`relationships` table column layout.
- Preserves intentional raw SQL coverage for file metadata edge cases.
- Leaves the next larger cleanup slice as the `tools/workspace/global_targeting.rs` test split.
