---
id: checkpoint_969e3ec3
timestamp: 2026-05-25T08:56:16.213Z
tags:
  - cleanup
  - dead-code
  - verification
  - search
  - daemon
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: b1d3ce09
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/daemon/app.rs
    - src/daemon/app/handle.rs
    - src/daemon/mcp_session.rs
    - src/search/index.rs
    - src/search/scoring.rs
    - src/tests/tools/search/line_output_tests.rs
    - src/tests/tools/search/mod.rs
    - src/tools/mod.rs
    - src/tools/search/line_mode.rs
    - src/tools/search/line_output.rs
    - src/tools/search/mod.rs
    - src/tools/shared.rs
summary: Removed Verified Dead Code Cluster
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo nextest run --lib stage_counts_zero_on_happy_path: pass"
  - "cargo nextest run --lib
    every_line_match_satisfies_the_caller_filters_primary: pass"
  - "cargo nextest run --lib
    test_reranker_source_language_phrase_bonus_requires_non_docs_role: pass"
  - "cargo xtask test changed: pass, 35 buckets in 652.5s"
  - "Claude CLI review: approve with no findings"
next: Commit this dead-code cleanup slice, then split the test-quality pipeline
  integration tests.
confidence: 5
---

## Removed Verified Dead Code Cluster

### WHAT
Removed the small Candidate 13 dead-code cluster after verification and review:

- Deleted dead `line_mode_search` wrapper.
- Deleted now-unreachable line output formatter/module/test and the unused `LineModeSearchResult::workspace_label` field exposed by removing the wrapper.
- Removed unused `KNOWN_CODE_EXTENSIONS` and its re-export.
- Removed stale `#[allow(dead_code)]` attributes from live daemon fields/helpers.
- Wired `src/search/index.rs` to use the existing `is_source_language` helper and removed its stale allow attribute.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with completion notes, Claude review evidence, verification ledger rows, and the next `test_quality_tests` pipeline split recommendation.

### WHY
The cleanup plan identified dead code and test-only/private code that survived earlier churn. Removing it reduces false dead-code noise and prevents stale private formatting tests from preserving unreachable behavior.

### HOW
Used Julie reference checks and compile feedback to separate truly unused code from live daemon helpers. After removing `line_mode_search`, `cargo check --all-targets` exposed the line-output branch as production-dead, so the formatter/module/test and associated result field were removed in the same slice. A fresh Claude CLI review verified no production callers or stale references remained.

### IMPACT
The working tree now has a smaller line-mode surface and cleaner dead-code signal. Verification passed:

- `cargo fmt --check`
- `cargo check --all-targets`
- `cargo nextest run --lib stage_counts_zero_on_happy_path`
- `cargo nextest run --lib every_line_match_satisfies_the_caller_filters_primary`
- `cargo nextest run --lib test_reranker_source_language_phrase_bonus_requires_non_docs_role`
- `cargo xtask test changed` (35 buckets, 652.5s, including daemon and search-quality)
- Claude CLI review: approve with no findings

Next cleanup slice is the low-risk `src/tests/analysis/test_quality_tests.rs` pipeline integration split.
