---
id: checkpoint_66a2a310
timestamp: 2026-05-25T01:24:09.214Z
tags:
  - cleanup
  - candidate-12
  - test-fixtures
  - test-linkage
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 2e0cf493
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/test_linkage_tests.rs
summary: Migrated linkage dedup fixture
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo nextest run --lib test_deduplication_across_strategies: pass"
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo xtask test bucket analysis: pass"
  - "cargo xtask test changed: pass, selected analysis only and ignored plan doc"
  - Julie blast_radius on src/tests/analysis/test_linkage_tests.rs reviewed as
    analysis/test-only impact
  - "Claude CLI strict schema review: approve with no findings"
symbols:
  - test_deduplication_across_strategies
next: Commit this slice, then continue with the next remaining
  `test_linkage_tests` raw SQL cluster. Because the file is at 976 lines, split
  the file before or during larger upcoming migrations if they would cross the
  cap.
---

## Migrated linkage dedup fixture

### WHAT
- Replaced the raw `INSERT INTO identifiers` in `test_deduplication_across_strategies` with the shared `identifier_builder` plus `bulk_store_identifiers`.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with the completed slice, corrected remaining raw SQL list, and verification ledger rows.

### WHY
- Continues Candidate 12 cleanup by replacing duplicated SQL row setup with the shared test DB row builders while preserving visible assertion SQL.

### HOW
- Preserved the duplicate `test_1` to `prod_1` identifier edge using `.line(12)`, `.column(0, 20)`, `.containing_symbol_id("test_1")`, and `.target_symbol_id("prod_1")`.
- Kept output assertions unchanged and left larger raw SQL clusters for separate review.
- Fresh Claude review approved the diff after a strict no-tool rerun.

### IMPACT
- One more `test_linkage_tests` setup cluster is builder-backed.
- `test_linkage_tests.rs` is now 976 lines, still under the 1000-line test-file cap but close; upcoming slices likely need a behavior split.
