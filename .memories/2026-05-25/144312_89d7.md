---
id: checkpoint_89d7fd0c
timestamp: 2026-05-25T14:43:12.712Z
tags:
  - cleanup
  - tests
  - handler
  - candidate-12
  - xtask-routing
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 0224bd72
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/handler.rs
    - src/tests/core/handler/editing_metrics.rs
    - src/tests/core/handler/metrics_recording.rs
    - src/tests/core/handler/path_helpers.rs
    - src/tests/core/handler/public_surface.rs
    - src/tests/core/handler/startup_checkpoint.rs
    - src/tests/core/handler/workspace_binding_metrics.rs
    - xtask/src/changed.rs
    - xtask/tests/changed_tests.rs
summary: Split core handler tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Removes another Candidate 12 over-cap test file and preserves narrow
  affected-change routing for split handler test modules.
evidence:
  - cargo fmt
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo nextest run -p xtask
    changed_tests_route_handler_split_modules_to_core_fast_bucket: RED before
    route fix, GREEN after"
  - "cargo nextest run --lib tests::core::handler::workspace_binding_metrics --
    --skip search_quality: 3 passed"
  - "cargo nextest run --lib tests::core::handler -- --skip search_quality: 53
    passed"
  - "cargo xtask test changed: xtask-runner and core-fast passed in 32.2s"
  - "git check-ignore -v src/tests/core/handler/*.rs || true: no ignored
    children"
  - "wc -l: 130/496/374/44/250/95/415"
  - over-cap scan now only lists core/database.rs, core/workspace_init.rs,
    integration/stale_index_detection.rs
  - "git diff --check: clean"
  - Claude CLI review approved; one plan count correction from 27 to 28 handler
    tests was fixed
next: "Stop after committing per user request. Next remaining files when
  resumed: core/database.rs, core/workspace_init.rs,
  integration/stale_index_detection.rs."
---

## Split core handler tests

### WHAT
- Split oversized `src/tests/core/handler.rs` into a 130-line parent helper/module file plus six behavior-focused child modules under `src/tests/core/handler/`:
  - `public_surface.rs`
  - `metrics_recording.rs`
  - `editing_metrics.rs`
  - `startup_checkpoint.rs`
  - `path_helpers.rs`
  - `workspace_binding_metrics.rs`
- Added `xtask` changed-file routing for `src/tests/core/handler/` child paths to `core-fast` and added a route regression test.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with progress, verification, Claude review, and current over-cap inventory.

### WHY
- Candidate 12 is reducing oversized test files while preserving test discovery and narrow changed-file routing.
- `core/handler.rs` was above the 1000-line test-file cap and would have forced broad fallback if split child paths were not routed.

### HOW
- Parent keeps shared imports/helpers and declares child modules.
- Child modules use `use super::*` so existing parent helper/import visibility is preserved.
- Wrote route test first; it failed RED with handler child paths falling back to dev, then passed after adding `src/tests/core/handler/` to `core-fast` routing and adding `core-fast` to the xtask test fixture manifest.
- Fixed split generation after `cargo check --all-targets` exposed dropped test attributes; rebuilt chunks from HEAD including comments, test attributes, and cfg attributes.

### IMPACT
- `src/tests/core/handler.rs` is now 130 lines; children are 496/374/44/250/95/415 lines.
- Remaining over-cap test files are `src/tests/core/database.rs`, `src/tests/core/workspace_init.rs`, and `src/tests/integration/stale_index_detection.rs`.
- No production code changed.
