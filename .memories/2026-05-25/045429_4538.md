---
id: checkpoint_4538f01b
timestamp: 2026-05-25T04:54:29.936Z
tags:
  - cleanup
  - candidate-12
  - database
  - relationships
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 7ac3068c
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/core/database.rs
summary: Migrated first database relationship-only centrality fixtures
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo nextest run --lib test_compute_reference_scores_weighted passed
  - cargo nextest run --lib test_compute_reference_scores_excludes_self_refs
    passed
  - cargo fmt --check passed
  - cargo check --all-targets passed
  - "cargo xtask test changed passed: core-database, 1 command, 1.5s"
  - git diff --check passed
  - Claude CLI approved with no findings
next: Commit this relationship-only batch, then continue with the next safe
  database.rs cluster such as outgoing-only zero score or interface/base
  propagation.
confidence: 5
---

## Migrated first database relationship-only centrality fixtures

### WHAT
- Replaced raw symbol and relationship setup SQL in `test_compute_reference_scores_weighted` and `test_compute_reference_scores_excludes_self_refs` with shared builders.
- Updated the cleanup plan with progress notes, verification rows, and Claude review evidence.

### WHY
- Per-cluster review classified these as safe relationship-only centrality fixtures.
- The tests assert `compute_reference_scores` weighting and self-reference exclusion, not raw SQL defaults.

### HOW
- Used `symbol_builder` for function symbols with exact id/name/path/language/span/byte shape and explicit `.confidence(1.0)`.
- Used `relationship_builder` with `RelationshipKind::{Calls, Imports, Uses}` and `.line_number(0)` to match the raw relationship schema default for omitted `line_number`.
- Kept assertion SQL untouched so behavior is still verified through stored reference scores.

### IMPACT
- Removed two more raw symbol/relationship setup clusters from `database.rs`.
- Verification: exact weighted and self-ref tests, formatting, typecheck, changed core-database bucket, diff whitespace check, and Claude review passed.
