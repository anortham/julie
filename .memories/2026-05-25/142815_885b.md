---
id: checkpoint_885b4500
timestamp: 2026-05-25T14:28:15.369Z
tags:
  - cleanup
  - tests
  - watcher-handlers
  - candidate-12
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: b7f3a917
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/integration/watcher_handlers.rs
    - src/tests/integration/watcher_handlers/repair_projection.rs
summary: Split watcher handler repair/projection tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Removes one more Candidate 12 over-cap test file while preserving
  watcher-handler test discovery and integration routing.
evidence:
  - cargo fmt
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo nextest run --lib
    tests::integration::watcher_handlers::repair_projection -- --skip
    search_quality: 6 passed"
  - "cargo nextest run --lib tests::integration::watcher_handlers -- --skip
    search_quality: 15 passed, one existing leaky-test note"
  - "cargo xtask test changed: selected integration and passed in 83.3s"
  - "git check-ignore -v
    src/tests/integration/watcher_handlers/repair_projection.rs || true: no
    ignored child"
  - "wc -l: 935/480"
  - over-cap scan no longer includes integration/watcher_handlers.rs
  - "git diff --check: clean"
  - Claude CLI review approved with no findings
next: "Continue with the remaining over-cap files. Scout recommendation: split
  `core/handler.rs` next and add xtask route prefix coverage for
  `src/tests/core/handler/` child paths."
---

## Split watcher handler repair/projection tests

### WHAT
- Split oversized `src/tests/integration/watcher_handlers.rs` by moving the repair/projection/hash-match tail tests into `src/tests/integration/watcher_handlers/repair_projection.rs`.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with progress, current over-cap inventory, verification evidence, Claude review evidence, and next-slice scout notes.
- Collected subagent scout recommendations for the remaining `core/database.rs`, `core/handler.rs`, `core/workspace_init.rs`, and `integration/stale_index_detection.rs` slices.

### WHY
- Candidate 12 is reducing oversized test files without behavior changes.
- `watcher_handlers.rs` was still above the 1000-line test-file cap and had a clean tail cluster that could be moved mechanically.

### HOW
- Preserved parent imports and added `mod repair_projection;`.
- Moved the tail tests with `use super::*` in the child module so existing helper/import visibility is preserved.
- Existing `src/tests/integration/` changed-file routing covers the child path, so no xtask routing change was needed.

### IMPACT
- `watcher_handlers.rs` is now 935 lines and the child is 480 lines.
- Remaining over-cap test files are `src/tests/core/database.rs`, `src/tests/core/handler.rs`, `src/tests/core/workspace_init.rs`, and `src/tests/integration/stale_index_detection.rs`.
- No production code changed.
