---
id: checkpoint_653f0205
timestamp: 2026-05-25T05:42:45.796Z
tags:
  - cleanup
  - candidate-12
  - plan
  - raw-sql-inventory
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: c6df051c
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
summary: Classified remaining database raw setup after embedding fixture migration
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - 'rg -n "INSERT(?: OR [A-Z]+)? INTO
    (files|symbols|relationships|identifiers)" src/tests/core/database.rs:
    remaining locations classified'
  - "cargo xtask test changed after doc-only update: no code/test buckets
    matched; plan doc ignored"
  - "git diff --check: pass"
next: Commit the plan classification update, then continue with the next
  Candidate 12 target such as `hybrid_search_tests.rs` doc-comment-preserving
  builder support.
---

## Classified remaining database raw setup after embedding fixture migration

### WHAT
- Updated the cleanup plan's remaining-candidates note after rescanning `src/tests/core/database.rs` raw setup inserts.
- Recorded that the remaining `database.rs` raw setup inserts are intentional stay-raw clusters: legacy migration coverage, ignored raw-concurrency stress writers, reference-score default coverage, and import-kind identifier scoring.

### WHY
- Prevents future cleanup passes from treating verified stay-raw SQL as unreviewed cleanup debt.
- Keeps Candidate 12 direction accurate after the safe database fixture migrations landed.

### HOW
- Used `rg` to enumerate remaining raw `INSERT INTO files|symbols|relationships|identifiers` locations.
- Inspected representative surrounding code for legacy schema migration, ignored concurrency stress writers, schema default coverage, and import-kind scoring.

### IMPACT
- The next cleanup slice should move outside `database.rs` unless intentionally adding builder support for a currently unsupported row shape such as import identifiers or doc comments.
