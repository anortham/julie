---
id: checkpoint_b0cd487a
timestamp: 2026-05-25T10:09:37.905Z
tags:
  - cleanup
  - tests
  - rewrite-symbol
  - editing
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: "47325681"
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/tools/editing/rewrite_symbol_tests.rs
    - src/tests/tools/editing/rewrite_symbol_tests/stateful.rs
summary: Split Rewrite Symbol Stateful Tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
context: Continuing cleanup plan after committing the blast-radius split. The
  next reviewed oversized test target was rewrite_symbol_tests.rs, with clear
  stateful/prepared-rewrite seams and existing tools-editing routing.
evidence:
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo nextest run --lib
    test_rewrite_symbol_uses_current_primary_db_after_rebind: pass"
  - "cargo nextest run --lib
    test_rewrite_symbol_keeps_primary_binding_snapshot_across_swap_window: pass"
  - "cargo nextest run --lib
    test_prepared_rewrite_drives_metrics_and_dry_run_output: pass"
  - "cargo nextest run --lib
    test_prepared_rewrite_rejects_changed_target_before_commit: pass"
  - "cargo nextest run --lib test_rewrite_symbol_replace_body_via_live_parse:
    pass"
  - "cargo nextest run --lib test_rewrite_symbol_rejects_stale_index: pass"
  - "cargo nextest run --lib
    test_rewrite_symbol_file_path_filter_uses_suffix_not_substring: pass"
  - "cargo xtask test bucket tools-editing: pass"
  - "cargo xtask test changed: pass before and after review docs, selected only
    tools-editing and ignored plan doc"
  - "git check-ignore -v src/tests/tools/editing/rewrite_symbol_tests/*.rs ||
    true: no ignored paths"
  - "git diff --check: pass"
  - "Claude CLI review: approved with no required findings; verified 27 tests
    preserved exactly once, module discovery, helper visibility, imports,
    routing, and plan accuracy"
next: Commit the rewrite-symbol split, then re-review remaining oversized
  inventory and dead-code plan-later items before selecting another cleanup
  slice.
confidence: 5
---

## Split Rewrite Symbol Stateful Tests

### WHAT
- Split `src/tests/tools/editing/rewrite_symbol_tests.rs` into:
  - 838-line root module retaining ordinary live-parse/error/no-op/dry-run/file-path tests.
  - 350-line `src/tests/tools/editing/rewrite_symbol_tests/stateful.rs` containing primary rebind, swap-window, and prepared-rewrite tests.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with completed work, refreshed oversized-test inventory, and ledger/review rows.

### WHY
- The original file was 1186 lines, above the project test-file target.
- The stateful and prepared-rewrite tests share higher-coupling handler/workspace state and form a clean child module while the root keeps ordinary rewrite behavior coverage.

### HOW
- Kept shared helpers in the root module and loaded the child with `mod stateful;`.
- Child uses `use super::*` for parent-private helpers and imports `poll`/`Arc` locally after removing those imports from the root.
- Existing `src/tests/tools/editing/` changed-file routing keeps affected verification in `tools-editing` with no xtask change.

### IMPACT
- `rewrite_symbol_tests.rs` is now below the 1000-line test cap.
- All 27 tests are preserved exactly once.
- The remaining oversized test inventory no longer has an obvious low-risk tool-only split; next cycle needs fresh structure review before choosing another slice.
