---
id: checkpoint_9579776b
timestamp: 2026-05-25T09:55:29.269Z
tags:
  - cleanup
  - tests
  - blast-radius
  - verification-routing
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: a444ac14
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/tools/blast_radius_determinism_tests.rs
    - src/tests/tools/blast_radius_determinism_tests/relationship_walk.rs
    - src/tests/tools/blast_radius_determinism_tests/rendering.rs
    - src/tests/tools/blast_radius_determinism_tests/seed_resolution.rs
    - src/tests/tools/blast_radius_determinism_tests/walk_semantics.rs
summary: Split Blast Radius Determinism Tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
context: Continuing cleanup plan after committing the deep-dive test split.
  Refreshed oversized-test inventory identified
  blast_radius_determinism_tests.rs as a small, low-risk tool-focused split
  candidate with existing routing coverage.
evidence:
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo nextest run --lib
    test_file_path_seeds_filter_noisy_structural_symbols_but_symbol_ids_are_exa\
    ct: pass"
  - "cargo nextest run --lib
    test_rank_impacts_prioritizes_and_labels_extends_relationships: pass"
  - "cargo nextest run --lib
    test_walk_impacts_identifier_edges_choose_strongest_kind_without_replacing_\
    relationships: pass before and after rename to walk_semantics"
  - "cargo nextest run --lib test_blast_radius_surfaces_identifier_only_callers:
    pass"
  - "cargo xtask test bucket tools-blast-spillover: pass before and after rename"
  - "cargo xtask test changed: pass before and after rename, selected only
    tools-blast-spillover and ignored plan doc"
  - "git check-ignore -v src/tests/tools/blast_radius_determinism_tests/*.rs ||
    true: no ignored paths"
  - "git diff --check: pass"
  - "Read-only subagent review: approved candidate and grouping"
  - "Claude CLI review: approved with no required findings; non-blocking notes
    addressed or recorded"
next: Commit the blast-radius split, then split
  `tools/editing/rewrite_symbol_tests.rs` using the reviewed
  stateful/prepared-rewrite grouping unless the next re-review finds a
  higher-value cleanup slice.
confidence: 5
---

## Split Blast Radius Determinism Tests

### WHAT
- Split `src/tests/tools/blast_radius_determinism_tests.rs` into a 247-line root helper/module file plus child modules:
  - `seed_resolution.rs`
  - `relationship_walk.rs`
  - `walk_semantics.rs`
  - `rendering.rs`
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with the completed slice, refreshed oversized-test inventory, next target, and ledger rows.

### WHY
- The original file was 1086 lines, above the project test-file target.
- The test groups had clear seams: seed resolution, relationship/ranking, walk/budget semantics, and output rendering.

### HOW
- Kept shared builders, handler setup, output extraction, and seed fixture in the parent module.
- Moved tests into child modules using `use super::*`, preserving parent-private helper access.
- Kept files under the existing `src/tests/tools/blast_radius...` prefix so changed-file routing continues to select `tools-blast-spillover` without xtask changes.
- Addressed Claude's non-blocking grouping note by renaming `identifier_walk.rs` to `walk_semantics.rs`.

### IMPACT
- All blast-radius determinism split files are below the 1000-line test cap; largest child is 385 lines.
- `cargo xtask test changed` selects only `tools-blast-spillover` for this slice and ignores the plan doc.
- Next lower-risk oversized test candidate is `src/tests/tools/editing/rewrite_symbol_tests.rs`, with a reviewed stateful/prepared-rewrite grouping.
