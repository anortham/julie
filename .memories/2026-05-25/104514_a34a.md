---
id: checkpoint_a34adce5
timestamp: 2026-05-25T10:45:14.958Z
tags:
  - cleanup-plan
  - test-split
  - workspace-discovery
  - verification
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: a9663f27
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/tools/workspace/discovery.rs
    - src/tests/tools/workspace/discovery/daemon_home.rs
    - src/tests/tools/workspace/discovery/file_discovery.rs
    - src/tests/tools/workspace/discovery/file_filtering.rs
    - src/tests/tools/workspace/discovery/julieignore_generation.rs
    - src/tests/tools/workspace/discovery/vendor_patterns.rs
summary: Split Workspace Discovery Tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: "Removes `src/tests/tools/workspace/discovery.rs` from the remaining
  oversized test inventory and records the next likely cleanup choices:
  `mod_tests/part5.rs` split or smaller Candidate 13 remove-now slices."
evidence:
  - "`cargo fmt --check` passed."
  - "`cargo check --all-targets` passed after restoring the original helper
    field set."
  - Representative exact tests passed across all five child modules.
  - "`cargo nextest run --lib tests::tools::workspace::discovery -- --skip
    search_quality` ran 40 tests and passed after restoring the original
    empty-file helper body."
  - "`cargo xtask test bucket tools-workspace` passed."
  - Final `cargo xtask test changed` selected only `tools-workspace` and passed
    in 30.0s, ignoring the plan doc.
  - "`git check-ignore -v src/tests/tools/workspace/discovery/*.rs || true`
    returned no ignored files."
  - Fresh Claude CLI review approved with no required findings; its
    helper-content note was addressed.
next: Commit this slice. Then choose between the low-risk
  `src/tests/tools/workspace/mod_tests/part5.rs` split and Candidate 13
  remove-now items (`handler.workspace_root`,
  `primary_database_and_search_index`, extractor placeholders).
confidence: 5
---

## Split Workspace Discovery Tests

### WHAT
Split `src/tests/tools/workspace/discovery.rs` from an oversized test file into a 53-line root helper/module file plus five child modules:
- `discovery/vendor_patterns.rs`
- `discovery/julieignore_generation.rs`
- `discovery/file_discovery.rs`
- `discovery/file_filtering.rs`
- `discovery/daemon_home.rs`

Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with the completed split, refreshed oversized inventory, subagent re-rank, Candidate 13 dead-code re-review results, verification ledger row, and Claude review row.

### WHY
Candidate 12 cleanup continues reducing oversized test files while preserving behavior and narrow changed-file routing. `workspace/discovery.rs` had clean top-level test families and existing `src/tests/tools/workspace/` routing to `tools-workspace`.

### HOW
Moved test bodies by behavior family and left the original shared `ManageWorkspaceTool` and temp-workspace helpers in the root. Verification caught and fixed an initially guessed `ManageWorkspaceTool` helper body. Claude's non-blocking note also caught that the split helper had changed file contents from empty to `"test content"`; the final split restores the original empty-file helper body.

### IMPACT
`src/tests/tools/workspace/discovery.rs` is below the 1000-line test cap. All 40 discovery tests still run under `tests::tools::workspace::discovery`, child files avoid the ignored `test_*.rs` pattern, and `cargo xtask test changed` remains narrow on `tools-workspace`.
