---
id: checkpoint_cbcbed54
timestamp: 2026-05-25T13:26:59.495Z
tags:
  - cleanup
  - candidate-12
  - test-split
  - daemon
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: 2ce68141
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/daemon/database.rs
    - src/tests/daemon/database/tests/workspace_status.rs
summary: Split daemon database workspace-status tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo fmt
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo nextest run --lib tests::daemon::database::tests::workspace_status --
    --skip search_quality: 5 passed"
  - "cargo nextest run --lib tests::daemon::database -- --skip search_quality:
    34 passed"
  - "cargo xtask test bucket daemon: pass in 61.4s"
  - "cargo xtask test changed: selected daemon only; pass in 61.5s"
  - "Claude CLI review: approve with no defects"
next: "Commit `test(cleanup): split daemon database workspace status tests`,
  then continue with the next recommended slice: split
  `src/tests/daemon/http_transport.rs` restart-pending test and add child-path
  transport routing."
confidence: 5
---

## Split daemon database workspace-status tests

### WHAT
Moved the workspace-status/path-normalization tail tests from `src/tests/daemon/database.rs` into `src/tests/daemon/database/tests/workspace_status.rs`.

- Parent test file is now 934 lines.
- New child module is 143 lines.
- Updated `docs/plans/2026-05-24-codebase-cleanup-plan.md` with progress, over-cap inventory, next-slice recommendation, and verification ledger rows.

### WHY
`src/tests/daemon/database.rs` was still above the 1000-line test-file cap after earlier daemon DB production splits. The moved tests form a coherent tail cluster around workspace status preservation and path normalization, all sharing the existing private `create_test_db()` helper.

### HOW
Kept the existing private `mod tests` shape and added `mod workspace_status;` inside it. The child module uses `use super::*;`, so it sees `create_test_db()` without adding a new helper or changing production code.

### IMPACT
Removes `daemon/database.rs` from the remaining over-cap test inventory. Existing `src/tests/daemon/` changed-file routing keeps the slice in the daemon bucket. Fresh Claude review approved the split with no defects and confirmed test preservation, module pathing, helper visibility, gitignore safety, routing evidence, and plan/ledger accuracy.
