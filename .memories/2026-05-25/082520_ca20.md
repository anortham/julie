---
id: checkpoint_ca201bbe
timestamp: 2026-05-25T08:25:20.510Z
tags:
  - cleanup-plan
  - watcher
  - test-extraction
  - xtask-routing
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: edcc9595
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/integration/watcher_filtering.rs
    - src/tests/mod.rs
    - src/watcher/filtering.rs
    - xtask/src/changed.rs
    - xtask/test_tiers.toml
    - xtask/tests/support/manifest_contract_expected.rs
summary: Extracted watcher filtering tests
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo nextest run --lib tests::integration::watcher_filtering -- --skip
    search_quality: 13 passed"
  - "cargo nextest run -p xtask
    changed_tests_route_watcher_filtering_to_core_fast_bucket
    manifest_contract_tests_checked_in_manifest_uses_exact_bucket_specs: 2
    passed"
  - "cargo xtask test bucket core-fast: pass in 16.9s"
  - "cargo xtask test bucket xtask-runner: pass in 10.6s"
  - "cargo xtask test changed: dev fallback because src/tests/mod.rs changed; 34
    buckets passed in 502.0s"
  - "git diff --check: pass"
  - "Claude CLI review: approve with no findings"
next: "Commit `test(cleanup): extract watcher filtering tests`, then remove the
  remaining small verified dead-code items before splitting
  `src/tests/analysis/test_quality_tests.rs`."
confidence: 5
---

## Extracted watcher filtering tests

### WHAT
Moved the inline `#[cfg(test)] mod tests` block from `src/watcher/filtering.rs` into `src/tests/integration/watcher_filtering.rs` and registered the module in `src/tests/mod.rs`.

Also removed the stale commented `test_git_context` module line from `src/tests/mod.rs` and updated `core-fast` runner routing/manifests to point at the moved watcher filtering tests.

### WHY
`src/watcher/filtering.rs` was over the 500-line implementation-file target mostly because it embedded 13 tests. The project standard says tests belong under `src/tests/`.

### HOW
Preserved all 13 test names/bodies/attributes, imported the public watcher filtering functions from the new integration test module, then fixed two runner-root-cause failures exposed by `cargo xtask test changed`: stale `core-fast` command and stale checked manifest expectation.

### IMPACT
Production watcher filtering code is now 240 lines. The moved tests still run in `core-fast`, and changed-file routing maps `src/tests/integration/watcher_filtering.rs` to `core-fast` instead of broad integration.
