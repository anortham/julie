---
id: checkpoint_c2ac1d5c
timestamp: 2026-05-25T01:37:32.331Z
tags:
  - cleanup
  - candidate-12
  - test-organization
  - test-linkage
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: f607ca94
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/analysis/linkage_edge_cases_tests.rs
    - src/tests/analysis/mod.rs
    - src/tests/analysis/test_linkage_tests.rs
summary: Split linkage edge-case tests below file cap
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - "cargo nextest run --lib
    test_name_match_fallback_is_deterministic_on_tied_scores: pass"
  - "cargo nextest run --lib
    test_compute_linkage_clears_stale_symbol_and_parent_linkage: pass"
  - "cargo nextest run --lib
    test_scorable_filter_excludes_fixture_includes_test_case_and_legacy: pass"
  - "cargo nextest run --lib test_best_confidence_present_in_linkage_output:
    pass"
  - "cargo nextest run --lib test_parent_aggregation_includes_best_confidence:
    pass"
  - "cargo nextest run --lib test_best_confidence_defaults_when_metadata_absent:
    pass"
  - "cargo fmt --check: pass"
  - "cargo check --all-targets: pass"
  - "cargo xtask test bucket analysis: pass"
  - "cargo xtask test changed: pass, selected analysis and ignored plan doc
    after final rerun"
  - Julie get_symbols sees new file and moved tests
  - Julie blast_radius reviewed as analysis/test-only impact
  - "Claude CLI strict schema review: approve with no findings"
symbols:
  - test_name_match_fallback_is_deterministic_on_tied_scores
  - test_compute_linkage_clears_stale_symbol_and_parent_linkage
  - test_scorable_filter_excludes_fixture_includes_test_case_and_legacy
  - test_best_confidence_present_in_linkage_output
  - test_parent_aggregation_includes_best_confidence
  - test_best_confidence_defaults_when_metadata_absent
next: Commit this split, then migrate the tied-score fallback raw SQL cluster in
  `linkage_edge_cases_tests.rs` using shared builders.
---

## Split linkage edge-case tests below file cap

### WHAT
- Moved the late linkage edge-case tests from `src/tests/analysis/test_linkage_tests.rs` into `src/tests/analysis/linkage_edge_cases_tests.rs`.
- Registered the new module in `src/tests/analysis/mod.rs`.
- Updated the cleanup plan progress, remaining-work note, and verification ledger.

### WHY
- `test_linkage_tests.rs` reached 976 lines after the previous fixture migrations. The next raw SQL migrations would likely cross the 1000-line test-file guideline, so the behavior split was needed before continuing.

### HOW
- Kept early/basic linkage tests in `test_linkage_tests.rs`.
- Moved the larger raw-SQL edge cases into a git-visible file name that avoids the repo's ignored `test_*.rs` pattern.
- Preserved test bodies and assertions; only comment punctuation was normalized to ASCII during the move.

### IMPACT
- `test_linkage_tests.rs` is now 584 lines.
- `linkage_edge_cases_tests.rs` is 407 lines.
- Remaining raw SQL cleanup can continue inside the smaller edge-case file one reviewed cluster at a time.
