---
id: checkpoint_a80bdd16
timestamp: 2026-05-25T13:36:52.636Z
tags:
  - cleanup
  - candidate-12
  - test-split
  - transport
git:
  branch: cleanup/codebase-cleanup-2026-05-24
  commit: df46c781
  files:
    - docs/plans/2026-05-24-codebase-cleanup-plan.md
    - src/tests/daemon/http_transport.rs
    - src/tests/daemon/http_transport/tests/restart_pending.rs
    - xtask/src/changed.rs
    - xtask/tests/changed_tests.rs
summary: Split HTTP transport restart-pending test
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - cargo fmt
  - cargo fmt --check
  - cargo check --all-targets
  - "cargo nextest run -p xtask
    changed_tests_routes_http_transport_paths_to_transport_bucket: RED before
    mapper fix, GREEN after"
  - "cargo nextest run --lib
    test_http_julie_session_rejects_new_sessions_after_restart_pending_with_act\
    ive -- --skip search_quality: pass"
  - "cargo nextest run --lib tests::daemon::http_transport -- --skip
    search_quality: 18 passed"
  - "cargo xtask test bucket transport: pass in 3.4s"
  - "cargo xtask test changed: selected xtask-runner + transport; pass in 20.1s"
  - "Claude CLI review: approve with no defects"
next: "Commit `test(cleanup): split http transport restart pending test`, then
  proceed with the scouted `src/tests/integration/watcher.rs` event-queue
  split."
confidence: 5
---

## Split HTTP transport restart-pending test

### WHAT
Moved the restart-pending HTTP admission test from `src/tests/daemon/http_transport.rs` into `src/tests/daemon/http_transport/tests/restart_pending.rs`.

- Parent test file is now 895 lines.
- New child module is 110 lines.
- Added `src/tests/daemon/http_transport/` to the xtask changed-file transport route.
- Extended `changed_tests_routes_http_transport_paths_to_transport_bucket` to cover the split child path.
- Updated the cleanup plan progress, remaining over-cap inventory, and verification ledger.

### WHY
`src/tests/daemon/http_transport.rs` was still just over the 1000-line test-file cap. The restart-pending admission test was a coherent tail test and moving it required a route fix because child paths previously fell through to the broad daemon bucket instead of transport.

### HOW
Kept the existing private `mod tests` shape and added `mod restart_pending;` inside it. The child module uses `use super::*;` to reuse the existing fixture helpers. The xtask route test was run RED first, showing `transport, daemon`; after adding the transport prefix, it passed and `cargo xtask test changed` selected only `xtask-runner` plus `transport`.

### IMPACT
Removes `daemon/http_transport.rs` from the remaining over-cap inventory. Future edits under `src/tests/daemon/http_transport/` stay in the transport bucket. Fresh Claude review approved the split with no defects and verified test preservation, module pathing, helper visibility, gitignore safety, route coverage, line counts, inventory, and plan/ledger accuracy.
