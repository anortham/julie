---
id: checkpoint_576816b2
timestamp: 2026-03-15T15:47:19.712Z
tags:
  - plan
  - review
  - validation
  - code-health-intelligence
  - bugfixes
git:
  branch: main
  commit: ebf36acc
  files:
    - Cargo.lock
    - TODO.md
    - docs/plans/2026-03-15-code-health-bugfixes.md
summary: "## Validated GPT Review of Code Health Intelligence & Created Fix Plan"
planId: sidecar-binary-distribution
type: checkpoint
evidence:
  - Bug 6 confirmed via dogfooding — deep_dive(detect_go) showed Change Risk
    twice in output
  - All 7 bugs traced to specific lines in source code
  - GPT's 'collapsed buckets' claim disproven — (test_id, ident_name) grouping
    is correct
symbols:
  - format_change_risk_info
  - format_header
  - line_mode_search
  - filter_test_symbols
  - definition_search_with_index
  - fetch_pivot_batch_data
  - build_pivot_entries
  - run_pipeline
  - compute_test_coverage
  - detect_go
  - build_test_refs
next: "Execute the plan: start with Chunk 1 (Tasks 1-3) for quick output quality
  wins"
---

## Validated GPT Review of Code Health Intelligence & Created Fix Plan

### What
Systematically validated all findings from GPT's deep review of the Code Health Intelligence work (Bugs, Tech Debt, Enhancements in TODO.md). Created a comprehensive 9-task implementation plan.

### Validation Results
**7/7 code bugs confirmed:**
- Bug 3: `line_mode_search` hardcodes `exclude_tests: false`, ignores param
- Bug 4: Test coverage name-match fallback has no language filter (partially confirmed — "collapsed buckets" claim was wrong)
- Bug 5: `get_context` drops risk/security labels in `SignatureOnly` mode — `full_symbols` is empty HashMap
- Bug 6: `format_header` + every kind-specific formatter both call `format_change_risk_info` — double print
- Bug 7: `run_pipeline` returns boxed `═══` no-results format, ignoring `format` param
- Bug 8: `truncate(limit)` before `filter_test_symbols` in both hybrid and keyword paths
- Bug 9: `incoming_total` includes all relationship types + identifier fallback, labeled as "callers"

**Tech debt confirmed:** Go Fuzz/Example detection, test location cap/dedup, language filter for coverage

### Plan Structure
4 chunks, 9 tasks in `docs/plans/2026-03-15-code-health-bugfixes.md`:
- Chunk 1: Output formatting fixes (Bugs 6, 8, 7)
- Chunk 2: Metadata accuracy (Bugs 5, 9)
- Chunk 3: Search parameter threading (Bug 3)
- Chunk 4: Data quality (Bug 4, Go detection, test location noise)

### Impact
Ready to execute — all fixes are localized, no schema changes needed. Most are 1-5 line changes with TDD tests.
