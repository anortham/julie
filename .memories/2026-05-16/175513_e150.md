---
id: checkpoint_e1503bc2
timestamp: 2026-05-16T17:55:13.791Z
tags:
  - daemon-split
  - A2.2c
  - A2.3
  - connection-pool
  - pooled-database
  - concurrent-readers
  - regression-test
  - structural-blocker
  - checkpoint
git:
  branch: feat/daemon-split-and-reranker
  commit: d929bd94
  files:
    - src/health/checker.rs
    - src/tests/daemon/symbol_db_pooled_test.rs
    - src/tools/deep_dive/mod.rs
    - src/tools/get_context/pipeline.rs
    - src/tools/impact/mod.rs
    - src/tools/navigation/target_workspace.rs
    - src/tools/search/mod.rs
    - src/tools/search/text_search.rs
    - src/tools/symbols/target_workspace.rs
summary: A2.2c sweep complete + A2.3 regression net landed
briefId: julie-world-class-systems-program
type: checkpoint
next: "Commit the changes. Then start Task #4 (A2.2c-followup): decide whether
  to migrate primary_workspace_snapshot/primary_database to pool-aware first, or
  surgically migrate each blocked site via temporary helpers. Also surface the
  manifest_contract_tests stale assertion as a separate cleanup (bump daemon
  expected from 12→60 in xtask/tests/manifest_contract_tests.rs)."
confidence: 4
---

## A2.2c sweep complete + A2.3 regression net landed

Vertical-slice migration (find_references_in_target_workspace) + sweep of cleanly-migratable production read sites + A2.3 concurrent-reader regression test.

### Migrated to `get_pooled_database_for_workspace` (7 production read sites)

- `src/tools/navigation/target_workspace.rs:24` — find_references_in_target_workspace (vertical slice)
- `src/tools/symbols/target_workspace.rs` — get_symbols_from_target_workspace (2 inline lock blocks collapsed)
- `src/tools/deep_dive/mod.rs:96` — Target branch in call_tool
- `src/tools/get_context/pipeline.rs:209` — Target branch
- `src/tools/impact/mod.rs:113` — Target branch (blast_radius)
- `src/tools/search/text_search.rs:93` — Target branch
- `src/health/checker.rs:173` — check_workspace_store_readiness (dropped the "busy = assume present" heuristic; not meaningful under pool semantics)

### Reverted to legacy (kept on Arc<Mutex<>>)

- 5 readiness probes in `src/tools/search/mod.rs` (lines 291, 315, 393, 539, 552). Pool requires workspace_pool membership; these probes just do file-level existence checks to choose the right error message. Pool path broke `test_fast_search_definitions_reports_index_requirement_for_reference_without_tantivy` (target workspace registered in daemon_db but not pool-loaded). Re-applied legacy method with comment explaining intent.

### Deferred (structural blockers) — Task #4 follow-up

These 5+ sites can't migrate cleanly in isolation because they share Arc<Mutex<>> type with the Primary branch (via `primary_workspace_snapshot()`, `primary_database()`, or helper signatures):

- `src/tools/navigation/fast_refs.rs` (try_semantic_fallback, resolve_source_names) — take `primary_db: Option<Arc<Mutex<>>>` from caller
- `src/tools/search/line_mode.rs:582` — `run_line_mode_with_scope_rescue` takes Arc<Mutex<>>
- `src/tools/editing/rewrite_symbol.rs:576` — returns `WorkspaceEditTarget` struct
- `src/tools/navigation/call_path.rs:363` — returns `WorkspaceQueryTarget` struct
- `src/health/checker.rs:108` — stores in `PrimaryWorkspaceState::database`
- 8 test sites — assertions against legacy API; legacy method retained with deprecation note

Migrating these requires `primary_workspace_snapshot`/`primary_database` to be pool-aware (changes a struct shared with ~10 callers including startup and workspace/indexing).

### A2.3: Concurrent reader regression net

`src/tests/daemon/symbol_db_pooled_test.rs::test_pooled_symbol_database_supports_concurrent_readers` — 4 tasks each acquire pool conn → wrap in SymbolDatabase → read → block at tokio::sync::Barrier(4). A single global mutex would deadlock at the barrier (N-1 tasks couldn't acquire). Verified passes in 34ms. Catches future regressions that re-introduce serialization.

### Verification

- `cargo check`: clean
- `cargo nextest run --lib test_find_references_in_target_workspace_parity`: PASS (392ms)
- `cargo nextest run --lib test_find_references_in_target_workspace_accepts_limit_and_kind`: PASS (177ms)
- `cargo nextest run --lib test_pooled_symbol_database_supports_concurrent_readers`: PASS (34ms)
- `cargo xtask test changed`: 18 buckets PASS in 228.5s
- `cargo xtask test bucket daemon`: PASS in 61.2s

### Pre-existing failure surfaced (NOT my changes)

`xtask::manifest_contract_tests_checked_in_manifest_uses_exact_bucket_specs` fails on HEAD before my changes: contract test hardcodes `daemon` bucket at 12s, manifest has 60s (correct — the bucket genuinely runs ~61s). Stale assertion in the test, unrelated to pool migration.
