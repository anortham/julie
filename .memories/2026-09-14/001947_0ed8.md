---
id: checkpoint_0ed8faad
timestamp: 2026-09-14T00:19:47.128Z
tags:
  - release
  - v8
  - qualification
  - fixture
  - full-gate
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: The final v8 full-gate retry reached 2,305/2,306 and failed the valid
  synthetic release archive because its fake server wrote an empty service.json.
impact: Removes the last known Linux branch-gate failure without weakening the
  production verifier.
evidence:
  - "Exact RED:
    release_qualification_rejects_invalid_archives_and_partial_public_assets
    failed with empty service.json"
  - "Exact GREEN: cargo nextest run -p xtask
    release_qualification_rejects_invalid_archives_and_partial_public_assets, 1
    passed"
  - cargo fmt --check passed; cargo clippy --workspace --all-targets passed with
    warnings
next: Commit the fixture correction, then request the explicit owner decision
  required for the third full run.
---

## WHAT

Fixed the release-qualification fixture to satisfy the service discovery contract.

- The valid and async-cleanup fake servers now write `{"pid":1}` before returning initialization instructions.
- Production archive-verifier code remains unchanged.
- The original exact xtask test now passes.

## WHY

The fixture still used an empty discovery record from before packaged lifecycle verification began reading the service PID. A temporary verifier-poll hypothesis was rejected after the exact gate proved the record stayed empty forever.

## HOW

Miller inspection of `release_qualification_rejects_invalid_archives_and_partial_public_assets` showed both fake server variants deliberately truncated `service.json` and never wrote JSON. The correction changes only those fixture script strings while preserving stop/removal and checksum behavior.

## IMPACT

The final release gate can proceed after explicit owner approval for a third broad run. The first broad run failed from exhausted `/tmp` quota; its three exact IDs pass after removing only completed Plan 3/4 scratch. The second broad run exposed this fixture defect.
