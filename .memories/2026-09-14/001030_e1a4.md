---
id: checkpoint_e1a4db6e
timestamp: 2026-09-14T00:10:30.955Z
tags:
  - revival
  - plan-4
  - release
  - v8
  - macos-waiver
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: decision
decision: Proceed with the v8 tag and release without local macOS
  prequalification; the release workflow must still pass both Mac archive jobs
  before creating the GitHub release.
alternatives:
  - Wait for Rocinante SSH access; rejected by the owner's direct release
    instruction.
  - Call macOS qualified; rejected because no local install/upgrade evidence
    exists.
impact: Unblocks the owner-authorized v8 release while keeping missing local Mac
  evidence honest.
evidence:
  - "native_qualification_workflow_is_nonpublishing_and_exercises_windows_lock:
    1 passed"
  - "release_workflow_qualifies_archives_and_awaits_the_pinned_plugin_workflow:
    1 passed"
  - "plugin node --test hooks/*.test.cjs: 30 passed"
next: Commit the waiver/release notes, run final source gates, publish the
  immutable plugin source, then push and tag the exact verified Julie SHA.
---

## WHAT

Recorded the owner's direct `tag it and release it` instruction as the macOS pre-release disposition and reconciled the v8 release notes.

- Plan 4 local release scope is complete.
- Local macOS arm64/Intel install and upgrade remain explicit post-release gaps.
- The release workflow still builds, packages, and verifies the actual Mac archives before its dependent release job.
- Removed the stale release-note claim that partial vector backfill does not resume.

## WHY

Rocinante's verified host key was not enough to authenticate, so local Mac qualification could not run. The owner explicitly chose release rather than waiting.

## HOW

The plan, qualification report, and release notes now distinguish owner waiver from a pass. Two focused xtask release-contract tests passed 1/1 each. Current Plan 1 restart tests support removal of the stale backfill caveat.

## IMPACT

The release branch can now freeze and run final Linux/Windows gates. A Mac workflow failure will prevent GitHub release creation. Plan 5C/5D and optional client adoption remain post-release work.
