---
id: checkpoint_3a94c0ab
timestamp: 2026-09-14T15:50:35.491Z
tags:
  - release
  - v8.0.0
  - plugin
  - workflow
  - source-pin
git:
  branch: main
  commit: 4ea54324
  files:
    - .github/workflows/release.yml
    - xtask/tests/toolchain_contract_tests.rs
actor:
  harness: codex-mcp-client
  user: alann
  git_user: Alan Northam
  git_email: alnorth@iu.edu
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
next: Commit this Julie workflow/test correction, verify both repos and
  worktrees clean, push the plugin source branch/tag and Julie main, then
  dispatch and verify the v8.0.0 plugin publication.
---

## WHAT

Updated Julie's `.github/workflows/release.yml` to pin the corrected plugin publication source commit `d427fec9df777b045d4562a4e56e3f14f0a7a260` and removed nonexistent `mcp_codex.json` from final plugin snapshot verification. Added focused assertions to `xtask/tests/toolchain_contract_tests.rs` covering the exact required MCP files and forbidding the phantom filename.

## WHY

The v8 release's plugin publication first exposed a missing manual-dispatch token fallback, then passed all 30 plugin tests but failed because both release workflows required `mcp_codex.json`. Repository history and the deployment design show only root `mcp.json` (Codex/Agent Plugins) and `mcp_config.json` (Antigravity); the third filename never existed.

## HOW

A Luna worker wrote the Julie contract assertion first and observed the exact test fail on the old workflow. After the corrected plugin source commit was produced in its isolated repo worktree, the worker updated only the source SHA pin and file list; the same exact test passed 1/1. `cargo fmt --all -- --check` and `git diff --check` also pass.

## IMPACT

Future Julie releases call the repaired, immutable plugin workflow source and validate the actual plugin design. Once both clean commits are pushed, the new plugin source tag can publish v8.0.0 without a temporary repository secret.
