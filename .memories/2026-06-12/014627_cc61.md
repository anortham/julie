---
id: checkpoint_cc61d823
timestamp: 2026-06-12T01:46:27.613Z
tags:
  - dependency-upgrade
  - julie-extractors
  - rescue
git:
  branch: main
  commit: ce0e515e
  files:
    - .codex/config.toml
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/Cargo.toml
    - crates/julie-index/Cargo.toml
    - crates/julie-pipeline/Cargo.toml
    - crates/julie-runtime/Cargo.toml
    - crates/julie-runtime/src/watcher/handlers.rs
    - crates/julie-tools/Cargo.toml
    - src/tools/workspace/indexing/engine_version.rs
summary: Upgraded julie-extractors v2.1.0 → v2.4.0
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Optionally consume structural_facts/complexity_metrics domains in julie;
  continue test-economy bucket splits and DaemonDatabase→RegistryDatabase rename
  per 2026-06-06 status doc.
---

## Upgraded julie-extractors v2.1.0 → v2.4.0

**WHAT:** Re-pinned the `julie-extractors` git dependency from v2.1.0 to v2.4.0 across all six manifests (root `Cargo.toml` + julie-core, julie-index, julie-pipeline, julie-runtime, julie-tools) and synced `SEMANTIC_INDEX_ENGINE_VERSION` to the new extraction contract.

**WHY:** Upstream shipped three releases past our pin, including a breaking v3 artifact contract (v2.2.0), a broad language data-quality release (v2.3.0), and git-exact ignore-rule semantics (v2.4.0).

**HOW:**
- New `EXTRACTION_CONTRACT_VERSION`: `2026-06-03.ecmascript-swift-shape-v3.source-regions-v1.structural-facts-v1.complexity-metrics-v1` — engine-version literal updated in `src/tools/workspace/indexing/engine_version.rs`, which forces a stored-index reindex for users on upgrade.
- One API break: `ExtractionResults` gained `structural_facts` and `complexity_metrics` fields; fixed the `ExtractionMode::TextOnly` construction site in `crates/julie-runtime/src/watcher/handlers.rs` with empty vecs.
- Julie does not yet consume the new structural-facts / complexity-metrics domains — they flow through as unused fields for now.

**VERIFICATION:**
- `cargo check --workspace --all-targets` clean
- engine-version lockstep tests pass (2/2)
- `cargo xtask test bucket extractor-dep-integration` PASS
- `cargo xtask test changed` buckets all green (core-index hit the 60s timing budget on cold compile — known timeout-flake; warm re-run of `cargo nextest run -p julie-index` 377/377 PASS)
- `cargo xtask test dev` 27/27 buckets PASS in 365s

**IMPACT:** Julie tracks the current extractor contract again; new releases pick up the v2.2–v2.4 extraction quality and ignore-precedence fixes. Also confirmed during this session: julie-plugin origin/main is at v7.14.1 with run.cjs launching julie-server directly (the status-doc release blocker was already resolved; local clone was just stale). Remaining rescue debt: DaemonDatabase naming (57 files), remaining slow `full`-only bucket splits.
