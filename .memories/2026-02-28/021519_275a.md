---
id: checkpoint_275a7f63
timestamp: 2026-02-28T02:15:19.119Z
git:
  branch: main
  commit: 41b247b
  files:
    - .memories/2026-02-28/021418_456f.md
    - .memories/2026-02-28/021501_396f.md
summary: "## Embeddings Fix Deployed + Hybrid Search Diagnostics Added"
planId: fix-embedding-performance-incremental-pipeline-mps
type: incident
evidence: >
  
  - Embeddings logs: "Embedding provider initialized: BGE-small-en-v1.5 (CPU,
  384d)" + "19,489 symbols embedded"

  - Git status shows `.memories/` files + search logging changes

  - `deep_dive` results show semantic similarity scores working at tool level

  - No hybrid search logs in output despite running queries with
  `search_target="definitions"`
symbols: >
  
  - `text_search_impl()` - main search entry point
  (src/tools/search/text_search.rs:279)

  - `is_nl_like_query()` - NL detection gate
  (src/tools/search/text_search.rs:323)

  - `hybrid_search()` - semantic merge implementation
  (src/tools/search/hybrid.rs:1)

  - `fallback_backend_after_init_failure()` - fixed in factory.rs
next: Stage & commit logging changes, rebuild, retest with actual logs to see if
  hybrid path fires or where it bails out
unknowns: |
  
  - Is NL detection correctly identifying natural language queries?
  - Is `embedding_provider` actually being passed into `text_search_impl`?
  - Is the hybrid gate even reached, or does something earlier bail out?
---

## Embeddings Fix Deployed + Hybrid Search Diagnostics Added

Fixed asymmetric fallback logic in embedding backend selection. ORT recovered after rebuild; 19,489 symbols embedded via ORT CPU. Added observability logging to hybrid search path to diagnose semantic search activation.

### Timeline
1. **Previous session:** Identified fallback asymmetry — ORT→Candle path missing in `fallback_backend_after_init_failure()`
2. **This session:** Added reverse fallback case + test, rebuilt, restarted
3. **Result:** ORT initialized successfully (transient failure before), embeddings fully generated
4. **New problem:** Can't confirm hybrid search is actually firing — no logs

### Current State
- **Embeddings:** ✅ 19,489 symbols embedded successfully via ORT (384d, BGE-small-en-v1.5)
- **Fallback fix:** ✅ Merged (symmetric ORT↔Candle fallback + test)
- **Observability:** ⚠️ Added `info!` logging to hybrid search path:
  - `text_search.rs:286` - hybrid mode entry with query
  - `hybrid.rs` - semantic result counts pre-RRF
  - `hybrid.rs` - upgraded semantic failure to `warn!`
- **Problem:** Logging changes written to files but NOT staged → rebuild compiled old code → no new logs visible

### Root Cause
Git status shows untracked/unstaged changes weren't picked up by cargo rebuild. Binary still runs old code.

### Files Modified (unstaged)
- `src/tools/search/text_search.rs` - added hybrid entry log
- `src/tools/search/hybrid.rs` - added merge stats + warning upgrade

### Next Steps
1. Stage and commit the logging changes
2. Rebuild + restart Julie
3. Re-run semantic search test queries
4. Check logs to confirm hybrid path is/isn't firing
5. If not firing, diagnose: NL detection? embedding_provider path? gate logic?
