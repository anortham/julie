---
id: checkpoint_96cdc7ea
timestamp: 2026-02-28T14:51:35.185Z
tags:
  - embeddings
  - semantic-search
  - hybrid-search
  - debugging
git:
  branch: main
  commit: 026debc
  files:
    - .memories/2026-02-28/144847_3d7e.md
    - .memories/2026-02-28/145101_282f.md
summary: "## Embeddings Fallback & Hybrid Search Debug Session"
planId: fix-embedding-performance-incremental-pipeline-mps
type: incident
context: Hybrid search not activating even with correct query parameters.
  Embeddings generated successfully but semantic search path appears inactive.
symbols:
  - text_search_impl
  - is_nl_like_query
  - embedding_provider
---

## Embeddings Fallback & Hybrid Search Debug Session

### What We've Done
- Fixed asymmetric backend fallback: ORT→Candle recovery now works (commit d6db2ed)
- Added info/warn logging to hybrid search pipeline for observability
- Rebuilt and restarted Julie with new code
- Embeddings successfully generated: 19,489 vectors via ORT CPU

### What We're Testing
Semantic search in `fast_search` tool. Hybrid search should activate when:
1. `search_target="definitions"`
2. Query is natural language (not identifier-like)
3. `embedding_provider` is Some (confirmed initialized)

### Current Problem
Hybrid search logging isn't appearing in logs even with correct query parameters. Examples tested:
- "gracefully handle failures when backend is unavailable" 
- "how to split camelCase words efficiently"

All return "Relaxed search" (keyword-only fallback) with no hybrid logs.

### Diagnostic Findings
- ✅ Embeddings ARE generated (19,489 vectors in database)
- ✅ Fallback code IS compiled in binary
- ✅ Logging strings ARE in compiled binary (verified with strings grep)
- ❌ Hybrid search path logging NOT appearing in runtime logs
- ❓ Unknown: Whether `text_search_impl` is even being called

### Next Step
Need to add diagnostic log at entry of `text_search_impl` to verify function is being called at all. If it is, we can trace why hybrid gate is failing. If it isn't, the problem is earlier in the call chain.

### Code Status
- Changes committed and pushed to working directory
- Binary rebuilt and fresh
- Logs are at `.julie/logs/julie.log.2026-02-28`
- Both `scoring::is_nl_like_query` functions confirmed present and correct

### Investigation Path
1. Add info log at text_search_impl entry to confirm it's called
2. If yes: trace the hybrid gate conditions (embedding_provider, is_nl_like_query, search_target check)
3. If no: problem is in MCP tool routing, not search logic

Files involved: `src/tools/search/text_search.rs` (main search), `src/search/hybrid.rs` (merge logic)
