---
id: checkpoint_454f8fa6
timestamp: 2026-05-29T16:47:39.307Z
tags:
  - planning
  - miller-bridge
  - extraction
  - code-review
  - schema-migration
git:
  branch: main
  commit: 6cbaff06
  files:
    - docs/plans/2026-05-29-extraction-enrichments-for-miller-bridge.md
summary: Reviewed + hardened the Miller-bridge extraction-enrichments plan
briefId: fts-phase-2-unified-schema-implementation
type: decision
next: "Plan is execution-ready. Open empirical question: DI-gate bloat prototype
  (type_arguments row count vs identifiers on newtonsoft_json + a TS repo) —
  gated on building Phase 2's reader. Phase 1 can start immediately under
  contract=1."
confidence: 4
---

## Reviewed + hardened the Miller-bridge extraction-enrichments plan

Reviewed `docs/plans/2026-05-29-extraction-enrichments-for-miller-bridge.md` (3 gaps julie must emit for Miller's deterministic cross-language bridge resolver: C# type-level annotations, ordered/nested generic type-args, string literals) via an 11-agent verify+critique workflow, then rewrote it in place addressing every finding.

- **Architecture validated:** the 3 core data-model decisions (separate `type_arguments` self-ref table, separate `literals` table, resolve-lazily) are sound and not over-fit. Most factual claims (version constants, migration mechanics, Miller consumer justification) verified accurate against source.
- **5 load-bearing corrections folded in** as a new "Cross-cutting correctness rules" section:
  1. `ON DELETE CASCADE` is inert on the write path — all bulk writes run `PRAGMA foreign_keys = OFF` (`ForeignKeyGuard`, atomic.rs:91/224/279); cleanup must be explicit DELETE in **three** sites (delete_file_rows_tx, delete_all_indexed_rows_tx, delete_workspace_data). Added `file_path` column to type_arguments to kill the delete-ordering hazard.
  2. Two persistence entry points, not one — live path `pipeline.rs:249/:301` (the plan only named `persistence.rs`); silent data loss if half-threaded. Fix: `CanonicalWriteSet` param-object refactor (Phase 2 step 0) so omissions are compile errors.
  3. `EXTRACT_CONTRACT_VERSION` bump moves to the FINAL batch commit (Miller's gate is exact-equality, rejects intermediate (26,2)/(27,2)).
  4. Drift dial is `engine_version.rs:16` literal, NOT `capabilities.json`. (Caught + fixed my own bad `concat!` prescription — it accepts only literals, not the const; const_format not a dep.)
  5. `ExternalExtractCounts`/`InsertCounts` are hardcoded 5-table structs needing extension.
- **Other fixes:** Phase 1 add fields/events/destructor (destructor "grammar can't" claim refuted); DI-gate bloat-budget prototype before removal; LiteralKind demoted to read-time hint + carrier reuses AnnotationMarker.carrier; Python AST-path caveat; signature-uncapped correction; re-index cleanup tests at the right harness (incremental_update_atomic.rs).
