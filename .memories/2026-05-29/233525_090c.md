---
id: checkpoint_090ce24b
timestamp: 2026-05-29T23:35:25.124Z
tags:
  - miller-bridge
  - literals
  - phase-3a
  - rebind-bug
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: 350c2c1f
summary: Phase 3a (Miller bridge literals infra) complete + committed
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - 24 literal-named tests pass
  - migrations module 25 tests pass
  - external_extract cli+operations integration tests pass
  - rebind failure reproduces on main with tmp_11fe14a5
next: "Phase 3b breadth (#32): per-language capture arms + [literal_carriers]
  TOML across all applicable languages, ledger to 100%. Then final commit bumps
  EXTRACT_CONTRACT_VERSION 1->2."
---

## Phase 3a (Miller bridge literals infra) complete + committed

Committed `350c2c1f` on `feat/miller-bridge-extraction-enrichments`: the full carrier-gated string-literal persistence pipeline.

- **Schema/persistence:** migration 028 + `literals` table (schema 27→28); `insert_literals_tx`; threaded through `CanonicalWriteSet`/`InsertCounts`/`ExtractedBatch`/both atomic entry points; cleanup wired on all 3 delete paths (`delete_file_rows_tx`, `delete_all_indexed_rows_tx`, `delete_workspace_data`) — explicit DELETEs because FKs are off during bulk writes so CASCADE never fires.
- **Gate:** `classify_literals_by_carrier` + `[literal_carriers]` TOML (TS+C# reference legs). Gate placed inside `extract_files_for_indexing_with_records` (covers live pipeline AND CLI-extract path Miller reads) + watcher — NOT pipeline.rs (CLI/watcher bypass it). Evidence-driven deviation from plan to avoid the multi-path classify trap.
- **Reader contract:** `literals_total` on counts/report/operations. EXTRACT_CONTRACT_VERSION stays 1 — bumps 1→2 only in the FINAL batch commit (after 3b), in lockstep with schema 28.

## Discovery: pre-existing rebind bug (off-path, tracked as task #33)

`cargo xtask test changed` fell back to dev; 20/21 buckets green. Only `tools-workspace-targeting` failed — 4 tests in `rebind_index.rs`. **Proven pre-existing on `main`** (stash + checkout-main reproduced identically), NOT caused by literals. Root cause: after `index(path=B)`, `current_workspace_id()` returns `tmp_11fe14a5` = `generate_workspace_id("/private/tmp")` (SHA256 first-8 confirmed). The rebind resolves the primary binding to the SYSTEM TEMP ROOT instead of the target workspace. Environment-dependent.
