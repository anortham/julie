---
id: checkpoint_aef27a5e
timestamp: 2026-05-29T17:14:03.715Z
tags:
  - miller-bridge
  - phase-1
  - csharp
  - annotations
  - extraction
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: adff0b69
  files:
    - crates/julie-extractors/src/csharp/fields.rs
    - crates/julie-extractors/src/csharp/members.rs
    - crates/julie-extractors/src/csharp/types.rs
    - crates/julie-extractors/src/lib.rs
    - crates/julie-extractors/src/tests/csharp/annotations.rs
    - crates/julie-extractors/src/tests/csharp/mod.rs
    - crates/julie-extractors/tests/downstream_smoke.rs
    - src/tests/core/annotation_storage.rs
    - src/tools/workspace/indexing/engine_version.rs
summary: Phase 1 complete — C# type/member attributes → `symbol_annotations`
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - 7 tests::csharp::annotations PASS
  - downstream_smoke integration PASS
  - store_symbols_persists_annotations_for_class_and_field_kinds PASS
  - "cargo xtask test changed: 36 buckets PASS in 560.4s"
symbols:
  - csharp::types::extract_class
  - csharp::fields::extract_fields
  - csharp::members::extract_property
  - helpers::extract_annotations
  - EXTRACTION_CONTRACT_VERSION
next: Commit Phase 1 on feat/miller-bridge-extraction-enrichments, then start
  Phase 2 (type_arguments table + CanonicalWriteSet param-object refactor as
  step 0).
confidence: 5
---

## Phase 1 complete — C# type/member attributes → `symbol_annotations`

Miller-bridge extraction enrichments, Phase 1 of 3. Wired C# attribute extraction into the symbol kinds that previously dropped them, so Miller's deterministic cross-language bridge resolver can read `[Table(...)]`/`[Column(...)]`-style annotations from `julie-server extract` output.

- **WHAT:** Added `helpers::extract_annotations(base, &node)` calls + `annotations` into `SymbolOptions` for C# class/interface/struct/enum/enum-member/record (`types.rs`), property/delegate/destructor (`members.rs`), and field/event (`fields.rs`, annotations live on the shared `field_declaration`/`event_field_declaration` node and are cloned per declarator). `extract_method`/`extract_constructor` already had it.
- **WHY:** Phase 1 of the plan at `docs/plans/2026-05-29-extraction-enrichments-for-miller-bridge.md`. Type-level and member attributes were silently dropped; bridge resolver needs them.
- **HOW (TDD):** 7 RED tests in `crates/julie-extractors/src/tests/csharp/annotations.rs` (parameterized type kinds + enum-member/property/field/event/delegate/destructor), all failed for the right reason (symbol found, annotations empty), then GREEN after wiring. Persistence layer is kind-agnostic (no schema change needed) — guarded by new `store_symbols_persists_annotations_for_class_and_field_kinds` roundtrip test in `src/tests/core/annotation_storage.rs`.
- **Drift dial:** bumped `EXTRACTION_CONTRACT_VERSION` to `2026-05-29.bridge-anchors-v2` (lib.rs), mirrored in `engine_version.rs` literal + `downstream_smoke.rs` assertion. The reader-facing `EXTRACT_CONTRACT_VERSION` (Miller's contract) bumps in the FINAL commit after all 3 phases, per plan.
- **IMPACT:** unblocks Phase 2 (type_arguments table) and Phase 3 (literals table). Engine-version bump correctly forces stale-index re-index (verified by `test_incremental_indexing_forces_reindex_when_index_engine_version_is_stale`).

### Verification
- 7 `tests::csharp::annotations` extractor tests: PASS (post version-bump)
- `downstream_smoke` integration test: PASS (now pins `bridge-anchors-v2`)
- storage roundtrip + 2 engine_version tests: PASS
- `cargo xtask test changed` → fell back to broad 36-bucket set (shared infra moved): all 36 PASS (560.4s)
