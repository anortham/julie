---
id: checkpoint_a9ccdd3b
timestamp: 2026-05-29T17:42:42.708Z
tags:
  - miller-bridge
  - phase-2
  - step-1
  - migration-027
  - type_arguments
  - schema
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: 30b27fd3
  files:
    - src/database/migrations.rs
    - src/database/schema.rs
    - src/tests/core/database/migrations.rs
summary: Phase 2 Step 1 complete — migration 027 + `type_arguments` table
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
evidence:
  - 4 migration 027 tests RED->GREEN
  - 58 external_extract + migrations tests PASS
  - core-database bucket PASS 1.7s
  - no live schema==26 assertion exists (grep confirmed)
symbols:
  - LATEST_SCHEMA_VERSION
  - migration_027_add_type_arguments
  - create_type_arguments_table
next: "Phase 2 Step 2: extractor model (recursive TypeArgument struct in
  base/types.rs) + core helper extract_type_arguments + per-language readers (C#
  member_type_relationships/di_relationships, TS relationships). C#/TS readers
  are parallelizable. Then Step 3 (write-path/cleanup/counts). Run cargo xtask
  test dev at end of Phase 2 batch."
confidence: 5
---

## Phase 2 Step 1 complete — migration 027 + `type_arguments` table

Pure new-table migration creating the empty `type_arguments` table (the storage for ordered/nested generic use-site arguments). No write path or extractor capture yet — those are Steps 2 & 3.

- **WHAT:** Bumped `LATEST_SCHEMA_VERSION` 26→27 (migrations.rs:16). Added `apply_migration` arm `27 => migration_027_add_type_arguments()`, `record_migration` description arm, and `migration_027_add_type_arguments()` (mirrors migration_020/026 — delegates to the schema.rs creator, no redundant table_exists guard since CREATE TABLE IF NOT EXISTS is idempotent). Added `create_type_arguments_table()` (`pub(crate)`) in schema.rs, wired into `initialize_schema()` after `create_identifiers_table()` (dependency order — FK to identifiers).
- **DDL:** self-referential (`parent_arg_id` → type_arguments.id for nesting), `identifier_id` → identifiers.id (use-site key), `ordinal` (order preservation), `type_name`, `target_symbol_id` (NULL at extract), own `file_path` (flat cleanup, Rule 1), `language`, `last_indexed`. 4 indexes: idx_type_args_{identifier,parent,name,file}.
- **HOW (TDD):** 4 RED tests in src/tests/core/database/migrations.rs — fresh-at-LATEST has table+4 indexes; v26-upgrade-preserving-data; idempotent re-open; fresh-vs-migrated PRAGMA table_info + index-set equality (with non-empty guard against trivially-equal empty schemas). All failed for the right reason (missing table), then GREEN.
- **TEST INFRA FIX:** initial hand-rolled partial v26 parent tables broke `initialize_schema`'s index creation (missing `symbols.semantic_group`). Rewrote `build_v26_database_with_rows` to build a full real-schema DB via the API + canonical write path, then downgrade (DROP type_arguments + DELETE schema_version >= 27) — robust, parent tables keep all real columns.

### Verification
- 4 migration 027 tests: RED→GREEN
- The three hardcoded `26`s in external_extract/cli.rs are arbitrary serialization-fixture data (not live schema reads) — confirmed they don't break; grep confirmed NO test asserts live get_schema_version==26
- tests::external_extract (all ops) + tests::core::database::migrations: 58 PASS
- core-database bucket: PASS (1.7s)
- Contract: schema_version→27, EXTRACT_CONTRACT_VERSION stays 1 (bumps in final batch commit), EXTRACTION_CONTRACT_VERSION already bumped in Phase 1
