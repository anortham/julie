---
id: checkpoint_fa3af668
timestamp: 2026-05-29T22:27:36.552Z
tags:
  - miller-bridge
  - phase-3
  - literals
  - extraction
  - typescript
git:
  branch: feat/miller-bridge-extraction-enrichments
  commit: a1567c40
  files:
    - crates/julie-extractors/src/base/extractor.rs
    - crates/julie-extractors/src/base/mod.rs
    - crates/julie-extractors/src/base/types.rs
    - crates/julie-extractors/src/tests/typescript/literals.rs
    - crates/julie-extractors/src/tests/typescript/mod.rs
    - crates/julie-extractors/src/typescript/identifiers.rs
summary: "Phase 3 (Miller bridge): Literal infra + TS reference-leg capture (3a step 1)"
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
next: Commit TS literal-capture unit, then build C# reference leg (verify
  verbatim/raw/interpolated string grammar nodes)
---

## Phase 3 (Miller bridge): Literal infra + TS reference-leg capture (3a step 1)

Phase 2 (generic type-args, all 18 langs + construct parity) is DONE + committed (3 commits: 77d61e96 feat, 0f4769b8 qml fix, 5b91e598 docs). Phase 3 redesigned to **carrier-gated breadth** (owner: "breadth mandate always wins") + committed (a1567c40).

**This checkpoint — started Phase 3 sub-phase 3a (infra + TS/C# reference legs), TS leg green:**
- **Architecture (locked):** extractors capture string-literal call-args **config-free** (`Literal{literal_text, carrier=callee, arg_position, kind:Other}`); a single shared `classify_literals_by_carrier()` pass in `src/` (at `pipeline.rs:52-57`, mirrors `classify_symbols_by_role`) gates on per-language `[literal_carriers]` TOML — match sets kind, no-match DROPS (the bloat gate). Verified dataflow via Explore agent.
- **Built (uncommitted):** `Literal` + `LiteralKind{Url,Sql,Route,Other}` in `base/types.rs`; `BaseExtractor.literals` + `record_literal` + `get_literals` + `decode_string_literal` (handles fragments + `{}` for interpolation/substitution + delimiter-strip fallback `strip_string_delimiters`); exported from `base/mod.rs`. TS capture arm in `typescript/identifiers.rs` (`record_call_arg_literals` + `callee_text`: identifier→name, member_expression→`object.property` join for `axios.get`).
- **5 TS tests green** (`tests::typescript::literals`): fetch→/api/users, template `${id}`→`{}`, axios.get dotted carrier, arg_position over full list, multi-arg carrier-agnostic. `cargo check -p julie-extractors --all-targets`: 0 warnings.

**Next:** (1) commit this TS unit; (2) C# reference leg (verbatim/raw/interpolated string node kinds — VERIFY grammar); (3) thread `literals` through ExtractionResults+registry+ExtractedBatch.all_literals; (4) `LiteralCarriersConfig` + `[literal_carriers]` TOML + `build_literal_carrier_configs` + `classify_literals_by_carrier` (gate); (5) migration 028/schema28 + `insert_literals_tx` + CanonicalWriteSet + 3 cleanup sites + counts; (6) breadth via team; (7) EXTRACT_CONTRACT_VERSION 1→2 in FINAL commit.
