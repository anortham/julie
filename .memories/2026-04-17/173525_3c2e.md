---
id: checkpoint_3c2ea4b0
timestamp: 2026-04-17T17:35:25.544Z
tags:
  - feature
  - vbnet
  - visual-basic
  - tree-sitter
  - extractor
  - language-support
  - pr-merge
  - release
  - v7.0
  - parity-review
  - grammar-dep
  - contribution
git:
  branch: main
  commit: fb85f0f4
summary: "Merged PR #8 (VB.NET extractor) + doc bump, ready to push for v7.0"
briefId: julie-world-class-systems-program
type: checkpoint
impact: "Julie ships language #34 (VB.NET). Luis's contribution preserved via
  `--author` attribution; PR #8 will auto-close on push. Positions the week's
  cumulative work (test velocity + daemon hardening + VB.NET) for a v7.0 tag."
evidence:
  - 54/54 VB.NET extractor tests pass (53ms)
  - cargo check --workspace clean (17.68s)
  - factory_consistency + registry_matches unit tests pass with count 35→36
  - 6/10 dev-tier buckets PASS; tools-misc timeout is pre-existing (not
    VB.NET-related)
  - Cargo.lock pinned to grammar rev 25dca4ac
symbols:
  - VbNetExtractor
  - CSharpExtractor
  - tree-sitter-vb-dotnet
  - crates/julie-extractors/src/vbnet/mod.rs
  - crates/julie-extractors/Cargo.toml
  - docs/site/index.html
  - registry.rs::registry_matches_supported_language_count
  - factory.rs::test_factory_rejects_unknown_language
next: "Await user sign-off to `git push origin main`. After push: (1) verify PR
  #8 auto-closes, (2) decide 7.0 vs 6.10 version bump, (3) return to tools-misc
  bucket investigation as remaining pre-tag polish item, (4) optionally file
  followup issue for VB.NET field/property/ctor-param Uses relationships."
confidence: 5
---

## Merged PR #8 (VB.NET extractor) + doc bump, ready to push for v7.0

Luis Carreras's VB.NET extractor PR landed locally as 3 commits on `main`. Everything compiles, narrow tests pass, docs/site/index.html updated to 34 languages. Awaiting user sign-off to `git push origin main` (will auto-close PR #8 via the `(#8)` trailer).

### Local commits (unpushed)
- `4495172c feat(vbnet): add VB.NET language extractor (#8)` — squash of Luis's 7 PR commits, authored as `Luis Carreras <luis.carreras@outlook.com>`
- `bfc2024e fix(vbnet): use git dep for tree-sitter-vb-dotnet grammar` — swapped local-path dep to `git = "https://github.com/LuisCarreras/tree-sitter-vb-dotnet", rev = "25dca4ac1456c691e2381a2d76151ef432aefc9e"` pattern (matches how razor/qmljs/powershell are declared)
- `fb85f0f4 docs(vbnet): update language count to 34 across docs + gh-pages site` — bumped every live `33` reference, added VB.NET badge (`#945db7`) to Core category on gh-pages, synced AGENTS.md via pre-commit hook

### Grammar provenance
- Fork: `github.com/LuisCarreras/tree-sitter-vb-dotnet` (MIT, forked from `jamie8johnson/tree-sitter-vb-dotnet`, CodeAnt-AI sibling)
- Pinned rev `25dca4ac` (2026-04-12 — LINQ + operators + 246 test fixtures expansion)
- Uses modern `tree-sitter-language` API → compatible with our locked tree-sitter 0.25

### Verification
- 54/54 VB.NET extractor tests pass in 53ms
- factory_consistency + registry_matches count assertions pass (35 → 36)
- `cargo check --workspace` clean in 17.68s
- 6/10 dev-tier buckets PASS; `tools-misc` hit pre-existing 120s budget (known v6.10 polish item, NOT a VB.NET regression — was already flagged before this PR landed)

### Parity gaps vs C# extractor (non-blockers, real)
VB.NET is on par for symbol kinds, type inference, call/member-access identifiers, core relationships, data routing. Missing three **Uses** relationship flavors that C# has: field-type, property-type, constructor-parameter-type. Also no `TypeUsage` identifier extraction — but C# doesn't have this either (same gap both languages). These affect centrality scoring for VB.NET types used in declarations vs. method calls. Same class of fix applied to GDScript/Zig in `LANGUAGE_VERIFICATION_RESULTS.md`. Correct place to track these is post-7.0 via the LANGUAGE_VERIFICATION workflow.

### Release framing
User leaning toward v7.0 (not v6.10) given the cumulative week: daemon lifecycle hardening, test velocity rescue (dev ~4x faster), release plumbing, and now language #34. No semver breakage (MCP tool contract unchanged), so 7.0 is a communication choice not a requirement.

### Tools-misc follow-up still pending
From pre-VB.NET work: `tools-misc` bucket at ~115s wall-clock, suspected another hidden dogfood-style test. User previously asked whether to investigate before tagging; was paused to address Luis's PR first. Still open after push.
