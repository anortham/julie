---
id: checkpoint_0305d9f3
timestamp: 2026-05-07T14:13:58.242Z
tags:
  - pr-18
  - extractor-audit
  - fix-batch
  - codex-review
  - pre-merge
git:
  branch: codex/tree-sitter-extractor-audit-remediation
  commit: 6cad896e
  files:
    - crates/julie-extractors/src/base/types.rs
    - crates/julie-extractors/src/go/relationships.rs
    - crates/julie-extractors/src/sql/relationships.rs
    - crates/julie-extractors/src/tests/json/mod.rs
    - crates/julie-extractors/src/tests/python/imports.rs
    - crates/julie-extractors/src/tests/sql/relationships.rs
    - fixtures/extraction/capabilities.json
    - src/tests/tools/search/mod.rs
    - src/tests/tools/search/tantivy_kind_conversion_tests.rs
    - src/tools/search/text_search.rs
summary: "PR #18 fix batch + Codex adversarial follow-ups landed"
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo xtask test changed: 9/9 buckets PASS in 127.1s"
  - "Narrow tests: test_enum_member_rescue_stringification_round_trips PASS"
  - Codex Finding 2 fix verified at text_search.rs:399
symbols:
  - tantivy_symbol_to_symbol
  - SymbolKind::try_from_string
  - extract_foreign_key_relationship
  - extract_embedding_relationships
next: "Stage + commit + push fix batch to PR #18, then dispatch I5-I8 fix team"
confidence: 5
---

## PR #18 fix batch + Codex adversarial follow-ups landed

Pre-merge fix layer for the tree-sitter extractor audit remediation PR. Two-phase work: (1) in-team review surfaced 2 Critical + 8 Important issues, (2) targeted fix batch dispatched against C1, C2, I1–I4, then Codex adversarial validation caught a defect the team review missed.

### What changed

- **C1** SQL synthetic FK IDs (`external_{table}`) — suppressed at extraction; structured pending-relationship promotion deferred and recorded as a capability gap. `crates/julie-extractors/src/sql/relationships.rs` + 2 regression tests.
- **C2** SymbolKind panic at Tantivy boundary — swapped `SymbolKind::from_string` for `try_from_string` with `tracing::warn!` + `Variable` fallback. `src/tools/search/text_search.rs:539`.
- **I1** JSON tautology test — replaced `assert!(symbols.len() >= 0)` with exact array-as-root assertions (count==2, names==["name","name"], kind==Variable).
- **I2** Python multi-import placeholder — real `test_python_plain_import_statement_emits_every_binding`.
- **I3** Go embedding stub `extract_embedding_relationships` — removed; verified no callers via fast_refs.
- **I4** SQL capability_gaps entry added; `required_closure` updated post-Codex to call out FK-pending-promotion as deferred work (Path A on Codex Finding 1).
- **Codex Finding 2** EnumMember silent downgrade — `text_search.rs:399` was `format!("{:?}", s.kind).to_lowercase()` producing `"enummember"`, which `try_from_string` (snake_case keys) silently mapped to `Variable`. Switched to `s.kind.to_string()` (Display impl) and added `test_enum_member_rescue_stringification_round_trips`.

### Why this matters

The team review caught the panic at the Tantivy call site (line 539) but didn't trace upstream to where the kind string was generated (line 399). Codex's data-flow trace caught the Debug-vs-Display mismatch. Real defect; merits the extra review pass.

### Verification

- Narrow tests: 3 named tests passed (`test_enum_member_rescue_stringification_round_trips`, `test_tantivy_symbol_to_symbol_unknown_kind_does_not_panic`, `test_tantivy_symbol_to_symbol_known_kind_round_trips`).
- Lead-side regression sweep: `cargo xtask test changed` 9/9 buckets PASS in 127.1s at HEAD post-fix.

### Next

- Commit + push fix batch as one conventional commit on PR #18 branch.
- Spawn another fix-batch team for I5–I8 Important items (Vue parses-once test, data-format relationships test, Razor fragile fallback, sum-test capability matrix) before merge.
