---
id: checkpoint_2224a4c5
timestamp: 2026-05-07T02:50:26.794Z
tags:
  - task-17
  - parser-failure
  - workspace-indexing
  - tree-sitter-audit
git:
  branch: codex/tree-sitter-extractor-audit-remediation
  commit: fff66664
  files:
    - docs/plans/2026-05-06-tree-sitter-extractor-audit-remediation.md
    - src/tests/tools/workspace/processor.rs
    - src/tools/workspace/indexing/processor.rs
summary: Completed Task 17 degraded parser failure contract
briefId: julie-world-class-systems-program
type: checkpoint
impact: Task 17 plan acceptance is complete and ready to commit.
evidence:
  - "cargo nextest run --lib
    test_process_file_with_parser_keeps_file_info_for_degraded_parse_result:
    PASS"
  - "cargo nextest run -p julie-extractors --lib
    test_extract_canonical_parse_none_returns_degraded_result_with_diagnostic:
    PASS"
  - "cargo nextest run --lib
    test_workspace_index_records_parse_diagnostics_for_recovered_file: PASS"
  - "cargo xtask test changed: PASS, tools-workspace and workspace-init buckets
    passed in 39.0s"
  - "cargo xtask test dev: PASS, 22 buckets passed in 363.4s"
  - "cargo nextest run --lib tests::integration::indexing_pipeline -- --skip
    search_quality: PASS, 7 tests passed"
next: Commit Task 17, then run final branch finishing workflow if no remaining
  plan tasks exist.
confidence: 5
---

## Completed Task 17 degraded parser failure contract

### WHAT
- Added a crate-private `process_file_with_parser_using` path in `src/tools/workspace/indexing/processor.rs` so tests can inject a degraded `ExtractionResults` while production still uses canonical extraction.
- Added `test_process_file_with_parser_keeps_file_info_for_degraded_parse_result` in `src/tests/tools/workspace/processor.rs`.
- Updated the plan status, Task 17 completion notes, and verification ledger.

### WHY
Task 17 required proof that a total parser failure does not make indexing lose file identity. The core extractor pipeline already returned degraded parse diagnostics for `parser.parse(...) == None`, but the caller/indexing path lacked a deterministic zero-symbol file identity regression.

### HOW
- Kept `process_file_with_parser` behavior the same for production and routed it through a private helper that accepts an extractor closure.
- Used the test helper to return an empty `ExtractionResults` with one parse diagnostic.
- Asserted the indexer still returns workspace-relative `FileInfo`, `symbol_count == 0`, stored content, and exact diagnostics, then wrote the zero-symbol file row through `bulk_store_fresh_atomic` and read diagnostics back.

### IMPACT
Task 17 acceptance is covered without relying on huge files, timeouts, or a real tree-sitter parser failure. The branch now has explicit protection against losing file rows when extraction degrades to diagnostics-only output.
