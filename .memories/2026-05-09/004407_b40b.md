---
id: checkpoint_b40b590b
timestamp: 2026-05-09T00:44:07.746Z
tags:
  - feature
  - shipped
  - xtask
  - dev-link
  - dev-restart
  - dev-workflow
  - maintainer
  - stale-binary
  - daemon-lifecycle
  - plugin-cache
  - v7.8.2
git:
  branch: main
  commit: 09d51312
summary: Committed dev-link/dev-restart for maintainer dev loop (`09d51312`)
briefId: julie-world-class-systems-program
type: checkpoint
symbols:
  - xtask::dev_workflow::run_dev_link
  - xtask::dev_workflow::run_dev_restart
  - xtask::cli::DevLinkCommand
  - julie::daemon::lifecycle::stop_daemon
next: Push 09d51312 to origin/main when user confirms; daemon-drain-timeout
  follow-up still open in TODO.md
confidence: 5
---

## Committed dev-link/dev-restart for maintainer dev loop (`09d51312`)

Landed on `main` as `feat(xtask): add dev-link and dev-restart for maintainer dev loop`. Not pushed yet — user wanted a checkpoint first.

- **Session arc:** pre-merge review of v7.8.2 (4 commits, 70 files, no Critical/Important findings) → tag/push v7.8.2 → diagnosed daemon disconnects (10s drain timeout force-kills active MCP sessions when stale-binary auto-restart fires mid-session) → designed and implemented dev-link/dev-restart to make `cargo build --release` non-disruptive → user re-installed the v7.8.2 plugin from GitHub → relinked the new cache → resolved `~/source/julie-plugin` git divergence (the local doc commit was already in remote at `4d914fd`).
- **Architecture decision (maintainer-only scope):** initial brainstorm included broader dev-tooling for everyone; user clarified scope was just for them. Final design: dev-link only touches the Claude Code plugin cache (Codex CLI and OpenCode already point their MCP `command` at `target/release/julie-server` directly per their config conventions). One symlink, one graceful daemon stop — that's the whole loop.
- **What's left in TODO.md** (not addressed in this commit): drain-timeout-too-short on daemon force-kill (10s → 60–120s), adapter resilience to retry HTTP for ~30s before dropping the stdio MCP session. These are still real defense-in-depth items even now that the new dev workflow makes them rare.
- **Verified end-to-end on this machine:** `cargo nextest run -p xtask --lib` 19/19 green; `cargo check --workspace` clean; live `dev-link` linked both v7.8.1 and v7.8.2 plugin cache binaries; idempotent re-run reports `already-linked`; `dev-restart` against stopped daemon correctly reports "not running".
