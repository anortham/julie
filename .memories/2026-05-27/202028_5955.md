---
id: checkpoint_5955a420
timestamp: 2026-05-27T20:20:28.886Z
tags:
  - session-close
  - daemon
  - lifecycle
  - cold-start-race
  - phantom-daemon
  - adapter
  - discovery-json
  - kernel-singleton
  - codex-review
  - deferred-work
git:
  branch: main
  commit: 1c45455a
summary: Session close — cold-start daemon race fix committed as 1c45455a
briefId: fts-phase-2-unified-schema-implementation
type: checkpoint
impact: Production fix committed for the user-visible phantom-daemon symptom.
  Two earlier checkpoints capture the technical detail; this one marks the
  session boundary and lists what's deferred so a future session can pick up
  Codex's recommended sequence without re-deriving it.
evidence:
  - Commit 1c45455a on main, 8 files, 500 insertions, 4 deletions
  - 30/30 launcher tests pass
  - 12/12 daemon::app_test tests pass
  - "cargo xtask test changed: transport + lifecycle + daemon buckets all green
    at 135.0s"
symbols:
  - publish_starting_discovery
  - DaemonLauncher::daemon_readiness
  - run_daemon
next: "If continuing on the daemon lifecycle area: QW3 (remove PidFile fallback)
  is the smallest next step — now safe because early discovery makes it
  redundant. Otherwise the user may push 1c45455a and move on."
confidence: 5
---

## Session close — cold-start daemon race fix committed as 1c45455a

Closed out the daemon cold-start phantom-process bug after two rounds of Codex
review. Earlier checkpoints (`checkpoint_f181e869` for initial fix,
`checkpoint_fe0ed881` for the revert+phase-guard rewrite) cover the work in
detail. This is the final session-state marker.

### What landed in 1c45455a

- `publish_starting_discovery` helper writes `discovery.json` with
  `phase="starting"` immediately after listener bind in `run_daemon`, before
  `DaemonApp::new` runs slow migrations. Closes the cold-start race window
  from multi-second to microseconds.
- `daemon_readiness` short-circuits `phase="starting"` to `Starting` before
  the endpoint probe — prevents adapters opening sessions while the HTTP
  layer hasn't started routing.
- 4 new tests (2 launcher, 2 app); 30/30 launcher + 12/12 app pass; xtask
  changed buckets green.

### Codex collaboration arc

1. First Codex pass (`gpt-5.5 high`, second-opinion mode) verified 6/8 bugs
   from initial assessment, refined #5 and #6, found 2 new findings (stop/status
   can't see new daemon during slow start; phase-vs-discovery race during
   mark_restart_pending).
2. Implemented lock probe + early publish ("do both" per user).
3. Second Codex pass (adversarial review with `--output-schema`) caught 3
   real correctness bugs in the lock probe. All confirmed by reading the
   cited lines.
4. Reverted lock probe; kept early publish; added phase guard.
5. Committed.

### Deferred work (Codex's recommended sequence, items 3-5)

User did not select any of these for this session:
- QW3: Remove legacy `PidFile` fallback from `daemon_readiness` — now redundant
- QW4: `julie daemon list` CLI diagnostic (`src/daemon/cli.rs` only has start/stop/status)
- QW5: Background the safe migrations (vector-count backfill only; workspace-ID
  must precede WorkspacePool per `app/helpers.rs:79-81`)
- Cross-harness `run.cjs` extract/preflight lock (assessment bug #6 partial)
- Larger lifecycle simplification (2026-05-15 plan is partly stale)

### Not pushed

Commit is local. User to decide whether to push to main or build on it.
