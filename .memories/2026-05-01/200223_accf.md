---
id: checkpoint_accfaaa2
timestamp: 2026-05-01T20:02:23.228Z
tags:
  - fast-search
  - tests
  - root-rebinding
git:
  branch: codex/fast-search-metrics-followup
  commit: 4936942a
  files:
    - src/tests/tools/search/line_mode.rs
summary: Updated Root-Rebinding Search Test
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "`cargo nextest run --lib
    test_fast_search_primary_wrapper_resolves_roots_before_searching` passed: 1
    test, 0.144s"
next: Commit the test update, then rerun `cargo xtask test dev` and `cargo xtask
  test dogfood`.
confidence: 4
---

## Updated Root-Rebinding Search Test

- **WHAT:** Widened `test_fast_search_primary_wrapper_resolves_roots_before_searching` to accept the new `0 content matches` response shape.
- **WHY:** The test verifies workspace-root rebinding, not the exact zero-hit prose. Target hints changed the empty content response from the old `No lines found matching` text to a content-count header plus guidance.
- **HOW:** Added `response_text.contains("0 content matches for")` as an accepted normal roots-bound response while preserving the final `current_workspace_id` assertion.
- **IMPACT:** The `dev` failure is resolved without weakening the root-rebinding invariant.
