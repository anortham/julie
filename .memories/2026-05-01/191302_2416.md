---
id: checkpoint_241638bc
timestamp: 2026-05-01T19:13:02.506Z
tags:
  - search
  - task-4
  - regression-tests
git:
  branch: codex/fast-search-metrics-followup
  commit: b1e279f9
  files:
    - src/tests/tools/search/file_pattern_tests.rs
    - src/tests/tools/search/line_match_strategy_tests.rs
    - src/tools/search/query.rs
summary: Task 4 punctuation regression coverage
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - cargo nextest run --lib
    test_punctuation_heavy_queries_match_separator_variants_via_tokens 2>&1 |
    tail -10
  - cargo nextest run --lib
    test_punctuation_heavy_queries_match_literal_substrings 2>&1 | tail -10
symbols:
  - line_matches_literal
  - line_matches_tokenized_phrase_without_punctuation
  - token_sequence_contains_subsequence
  - test_punctuation_heavy_queries_match_literal_substrings
  - test_punctuation_heavy_queries_match_separator_variants_via_tokens
next: Stage only the owned files, commit with a conventional message, and report
  the unrelated worktree edit in file_pattern_tests.rs as untouched.
confidence: 5
---

## Task 4 punctuation regression coverage

Added regression tests for punctuation-heavy search queries in `line_match_strategy_tests.rs` and taught the shared line verifier to normalize separator-heavy literals with existing tokenizer helpers.

- **WHAT:** Added coverage for exact literal matches and separator-variant matches for `OS.has_feature`, `ArgAction::SetTrue`, `target_symbol_id.unwrap_or`, and `tool_name(&self)`.
- **WHY:** Metrics showed remaining `line_match_miss` churn on symbol-like queries, and the current verifier rejected some separator variants that should still match.
- **HOW:** Kept the literal substring path intact, then added a punctuation-normalized fallback in `src/tools/search/query.rs` that tokenizes both sides, strips punctuation-only tokens, and checks ordered subsequence matches.
- **IMPACT:** Exact literal behavior is preserved, separator-variant code shapes now match without Rust-specific heuristics, and the exact worker tests for both literal and tokenized cases pass.
