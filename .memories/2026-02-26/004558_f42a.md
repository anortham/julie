---
id: checkpoint_f42afbd1
timestamp: 2026-02-26T00:45:58.585Z
tags:
  - get-context
  - tooling
  - agent-ux
  - evaluation
git:
  branch: main
  commit: 91ada0a
summary: "## Evaluated new `get_context` tool quality for agent workflows"
planId: fix-reference-workspace-support-for-rename-symbol-
type: learning
impact: Assessment is now grounded in both code review and runtime behavior; can
  be reused as input to a focused improvement plan.
symbols:
  - src/tools/get_context/pipeline.rs::run_pipeline
  - src/tools/get_context/pipeline.rs::get_pivot_relationship_names
  - src/tools/get_context/scoring.rs::select_pivots
  - src/tools/get_context/formatting.rs::format_context
confidence: 4
---

## Evaluated new `get_context` tool quality for agent workflows

### WHAT
- Reviewed implementation across `src/tools/get_context/{pipeline,scoring,allocation,formatting}.rs`
- Reviewed test coverage in `src/tests/tools/get_context_tests.rs`, `src/tests/tools/get_context_formatting_tests.rs`, and `src/tests/tools/get_context_scoring_tests.rs`
- Dogfooded the tool with real queries (including one successful targeted query and two poor-relevance natural-language queries)
- Ran targeted tests: `cargo test --lib tests::tools::get_context` (all passing)

### WHY
- User asked for an assessment of project/tool usefulness to coding agents and likely problems/opportunities.
- Needed concrete evidence from code + behavior rather than speculation.

### HOW
- Used Julie tools (`fast_search`, `read`, `get_context`) to inspect implementation and output quality.
- Validated behavior on representative queries and checked test health.
- Identified quality signals (centrality-aware ranking, graph expansion, token budgeting) and rough edges (relevance drift, duplicate caller names, N+1 queries, formatting overhead).

### IMPACT
- Produced evidence-backed recommendations for improving agent UX and retrieval quality.
- Captured likely high-ROI improvements: deduping relationship names, richer pivot retrieval (content+symbol hybrid), and performance fixes for per-pivot DB fetches.

