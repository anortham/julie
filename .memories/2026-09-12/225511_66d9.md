---
id: checkpoint_66d97ce4
timestamp: 2026-09-12T22:55:11.003Z
tags:
  - revival
  - plan-4
  - upgrades
  - service-restart
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo nextest run -p xtask --lib
    dev_link_creates_server_and_sidecar_override_for_versioned_layout: 1 passed"
  - "cargo nextest run -p xtask --lib
    dev_restart_reports_shared_service_restart: 1 passed"
  - "cargo nextest run -p julie --lib
    restart_stops_mismatched_service_and_starts_current_binary: 1 passed"
  - "cargo nextest run -p julie --lib
    version_mismatch_exits_3_with_the_exact_message: 1 passed"
  - "cargo nextest run -p julie --lib
    version_mismatch_error_formats_and_maps_to_exit_code_3: 1 passed"
symbols:
  - ConnectError::fmt
  - run_dev_link
  - split_binary_names
  - discover_plugin_bin_dirs
  - run_dev_restart
  - restart_stops_mismatched_service_and_starts_current_binary
next: Commit the accepted Julie Plan 4B changes, then continue to Plan 4C
  cold/offline semantic readiness.
confidence: 5
unknowns:
  - Native Windows executable-lock qualification remains pending until Plan 4E.
---

## WHAT

Completed and lead-reviewed Julie's Plan 4B support for version-safe upgrades. `dev-link` now creates a paired server/semantic-sidecar maintainer override for both direct and completed versioned plugin layouts. Version-mismatch errors name both versions, quote the exact current executable's `service restart` command, and tell operators to restart old harness clients. Process coverage proves explicit restart replaces the old PID, waits for exit, starts the current binary, and leaves a stoppable new service.

## WHY

Windows can keep a running executable locked, so ordinary upgrades must install elsewhere and rely on explicit shared-service restart. Maintainer overrides also need both binaries or semantics can silently diverge.

## HOW

Terra added exact TDD coverage for paired dev-link discovery, corrected the retired in-process `dev-restart` text, and characterized the existing authenticated shutdown/wait/restart implementation through the real debug process fixture. Lead review strengthened PID/exit assertions and actionable, quoted mismatch guidance. No lifecycle implementation change was needed.

## IMPACT

The Rust side of Plan 4B is ready for a local commit. Local Linux restart and maintainer flows are covered; the real Windows locked-executable qualification remains deferred to Plan 4E as requested.
