---
id: checkpoint_8ee8453d
timestamp: 2026-09-12T18:43:29.734Z
tags:
  - plan-3
  - runtime
  - observability
git:
  branch: fix/revival-runtime
  commit: bf517c2f
  files:
    - src/request_engine/runtime_factory.rs
    - src/service/http.rs
    - src/service/status.rs
    - src/tests/service/http_api.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 3A runtime-count observation
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "RED: `cargo nextest run --lib
    status_with_header_or_query_token_reports_version_and_uptime 2>&1 | tail
    -10` (assertion absent before wiring)."
  - "GREEN: same exact command passed: 1 test passed, 1331 skipped."
symbols:
  - RuntimeFactory.loaded_runtime_count
  - StatusDocument
  - service::http::status
next: Commit only this instrumentation and checkpoint, then run the Linux-only
  isolated baseline workload.
---

## Plan 3A runtime-count observation

### WHAT

Added `RuntimeFactory::loaded_runtime_count()` and surfaced it as the read-only `loaded_runtime_count` field on authenticated `/status`. Extended the existing status API contract test.

### WHY

Plan 3A requires a directly observed live runtime count, but the prior `/status` document exposed only RSS and checkout watcher status.

### HOW

The accessor counts the existing bound-runtime map plus the optional unbound runtime without changing initialization, teardown, retention, configuration, or ownership. A focused status test was written red first and then passed after the field was wired.

### IMPACT

The next slice can measure the isolated service baseline with direct runtime-count evidence. This instrumentation commit is the source identity for that baseline.
