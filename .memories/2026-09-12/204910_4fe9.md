---
id: checkpoint_4fe92b16
timestamp: 2026-09-12T20:49:10.252Z
tags:
  - plan-3
  - service-budget
  - shutdown
git:
  branch: fix/revival-runtime
  commit: 6849556a
  files:
    - src/service/mod.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo nextest run -p julie --lib
    service_modules_contain_no_coordination_words: RED then GREEN"
symbols:
  - ServiceApp::serve
next: Commit ServiceApp serve correction; lead may run the existing lifecycle
  exact test during acceptance.
---

## WHAT

Reworked `ServiceApp::serve` to remove the budget-forbidden coordination token while keeping service lifecycle behavior intact.

## WHY

The final Linux gate found `service_modules_contain_no_coordination_words` rejecting the prior server-handling implementation.

## HOW

The idle timeout and existing shutdown cancellation now compose into the Axum graceful-shutdown future. It retains the prior status-based 250ms idle checks, awaits the server directly, then preserves the established cancellation, maintenance join, and runtime drain ordering.

## IMPACT

Exact budget reproduction failed on the forbidden token, then passed after this change. The existing service lifecycle test remains logically preserved and is available for lead acceptance; no Windows verification was run.
