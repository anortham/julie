---
id: checkpoint_1b01fad0
timestamp: 2026-09-12T19:39:38.864Z
tags:
  - plan-3
  - runtime-lifecycle
  - test-fix
git:
  branch: fix/revival-runtime
  commit: f051b546
  files:
    - docs/plans/2026-09-11-revival-runtime-plan.md
    - src/tests/request_engine.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 3B bounded barrier polling repair
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
symbols:
  - cold_workspace_initialization_does_not_block_warm_workspace
---

## Plan 3B bounded barrier polling repair

### WHAT

- Wrapped the test-only yield-based attempt-counter poll in a 100ms `tokio::time::timeout`.

### WHY

The previous poll could wait forever if initialization never reached the deterministic probe, leaving the exact test dependent on the outer suite timeout.

### HOW

- Preserved yield-based condition polling and no sleeps.
- Added a clear `initialization never reached the barrier` failure message.
- The exact cold/warm test passed after the correction.

### IMPACT

The Plan 3B cold/warm test now fails fast for both a lost entry notification and an initializer that never reaches the barrier. Production runtime behavior remains unchanged.
