---
id: checkpoint_63c86927
timestamp: 2026-09-12T20:45:39.448Z
tags:
  - plan-3
  - runtime-lifecycle
  - measurement
git:
  branch: main
  commit: bf517c2f
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 3C after-measurement
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
impact: Post-change measurement is complete and supports the final Linux branch gate.
evidence:
  - release build from c3096cf3
  - "3/3 replay: 60 alternating opens + 15 searches + 12 statuses HTTP 200"
  - all three owned service processes stopped and reaped
next: Commit the documentation evidence, then let the lead run the final Linux
  gate; defer Windows to post-merge validation.
unknowns:
  - Public status has no empty-slot count or per-slot byte exporter.
---

## Plan 3C after-measurement

### WHAT
- Built the release binary from `c3096cf3` and ran the documented isolated Linux warm/cold replay three times.
- Updated `docs/findings/revival-runtime-baseline.md` with baseline and post-change identities, complete measurements, HTTP counts, retention-policy observability, and cleanup proof.
- Updated Plan 3 status, 3C measurement acceptance, and the live verification ledger.

### WHY
- The lifecycle changes require the same workload after implementation to prove resource bounds and expose any warm-query regression.

### HOW
- Used a fresh `JULIE_HOME`, copied `fixtures/seed/a` into a fresh warm checkout each repetition, kept semantics off, and used the Plan 3 worktree as cold.
- Ran fresh/warm/cold/20-open stages; issued five concurrent warm lexical searches during cold initialization; collected service-PID RSS, FDs, status counts, HTTP outcomes, and timings.
- Stopped and reaped each owned service before the next repetition.

### IMPACT
- Peak after-20-open usage was 797179904 bytes RSS and 49 FDs, below the 933927322-byte and 58-FD budgets. Warm p95 was at most 18.104 ms versus the 12-second baseline interference.
- Public status reports loaded runtime/watcher counts but not empty-slot or per-slot bytes; the matched replay stayed below the 60-second expiry, ending with three loaded runtimes and two watchers without per-open growth.
- No Windows verification was run; it remains final post-merge work.
