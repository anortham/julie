---
id: checkpoint_646e10ec
timestamp: 2026-09-12T20:35:38.206Z
tags:
  - plan-3
  - runtime-lifecycle
  - service-shutdown
git:
  branch: main
  commit: bf517c2f
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo nextest run -p julie --lib
    service_shutdown_joins_maintenance_watchers_and_embedding_writers: PASS (1
    test)"
symbols:
  - ServiceApp::serve
  - RuntimeFactory::shutdown_all
next: Commit Task 7 files after final worktree-state check; lead owns acceptance
  and final gate.
---

## WHAT

Implemented Plan 3C service-owned runtime lifecycle work in the `fix/revival-runtime` worktree: a cancellation-backed maintenance task periodically retires idle slots, and service shutdown joins maintenance before draining every loaded and unbound runtime through `teardown_loaded_workspace`.

## WHY

Idle retirement must be owned by the service rather than request waiters, and shutdown must join watchers and embedding writers before releasing runtime slots.

## HOW

`ServiceApp::serve` starts maintenance from its existing shutdown token, converts the graceful Axum server into a future before pinning, waits for graceful stop, cancels and joins maintenance, then calls `RuntimeFactory::shutdown_all`. The service-control test creates a real watched workspace and a notify-gated registered writer, proves shutdown takes the writer then blocks until release, and verifies watcher/runtime release.

## IMPACT

Exact Linux test `service_shutdown_joins_maintenance_watchers_and_embedding_writers` passed. Windows remains deferred to final post-merge validation; broad Linux gate remains lead-owned.
