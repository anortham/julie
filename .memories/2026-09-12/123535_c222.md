---
id: checkpoint_c2226884
timestamp: 2026-09-12T12:35:35.906Z
tags:
  - plan1
  - verification
  - native-recovery
git:
  branch: fix/revival-correctness
  commit: c2b47cb6
  files:
    - src/tests/integration/native_semantic_lifecycle.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
next: Commit fixture correction, rerun full, finish isolated live probe diagnosis.
---

## WHAT
Corrected the enabled native semantic recovery fixture to publish its raw SQLite vector writes into the existing checkout snapshot.

## WHY
The first full gate at c2b47cb6 stopped at native_semantics_becomes_ready_without_client_restart: the fixture had updated facts but left the request snapshot without an encoder. Production writes publish vectors; the test now follows that contract without recreating its runtime or restarting the client.

## HOW
Reacquire the cached runtime/store and call publish_vectors after the fixture insert. The exact regression now passes (1 selected); cargo check -p julie --tests passes. Formatting and clippy had passed at c2b47cb6, with warnings. Full and live gates remain pending; the first live probe timed out awaiting partial coverage and is being diagnosed separately.

## IMPACT
Ready for the next clean-commit integration gate. No production behavior was weakened and no publication is authorized.
