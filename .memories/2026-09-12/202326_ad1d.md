---
id: checkpoint_ad1dac46
timestamp: 2026-09-12T20:23:26.286Z
tags:
  - plan-3
  - runtime-retirement
git:
  branch: main
  commit: bf517c2f
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Same-key retirement test deadlock resolved
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo nextest run -p julie --lib
    runtime_reacquire_waits_for_teardown_without_duplicate_writer: PASS
    (1.065s)"
next: Lead can integrate this correction; no broad or Windows verification was run.
---

## Same-key retirement test deadlock resolved

**WHAT:** Removed the pre-release global watcher-count assertion from the runtime retirement barrier test.

**WHY:** Retirement intentionally holds the target slot write lock while paused. `loaded_watcher_count()` reads every slot and waited for that target lock before the test released the probe.

**HOW:** The test now uses its target-acquisition oneshot and unrelated-slot assertion while retirement is paused; it retains the post-release watcher count. The fixture count policy remains three so age expiration selects only the aged target.

**IMPACT:** The exact same-key test passed in 1.065 seconds without removing the duplicate-writer assertion.
