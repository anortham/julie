---
id: checkpoint_644fde15
timestamp: 2026-09-12T20:55:13.422Z
tags:
  - plan-3c
  - embedding
  - test-followup
git:
  branch: fix/revival-runtime
  commit: 572affc8
  files:
    - docs/plans/2026-09-11-revival-runtime-plan.md
    - src/tests/tools/workspace/mod_tests/part7.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Cooperative force-reindex test double
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "`cargo nextest run -p julie --lib
    test_force_reindex_cancels_embedding_task_when_explicit_path_resolves_to_pr\
    imary_root` passed (1 test, 0.046s)."
symbols:
  - test_force_reindex_cancels_embedding_task_when_explicit_path_resolves_to_primary_root
next: Lead may resume the pending branch gate from this commit.
---

## Cooperative force-reindex test double

### WHAT

Updated `test_force_reindex_cancels_embedding_task_when_explicit_path_resolves_to_primary_root` so its inserted embedding-task double exits after observing the cloned cancellation flag.

### WHY

Plan 3C changed force cancellation from outer-task abort to cooperative cancel-and-join. The old `std::future::pending()` double could never cooperate, so the broad gate hung even though production behavior was correct.

### HOW

The spawned test task uses a five-second bounded deadline and `tokio::task::yield_now()` while checking `AtomicBool` with Acquire ordering. It exits when force reindex sets the flag, preserving the assertions for primary-ID resolution and stale registry removal.

### IMPACT

The test now validates the production quiescence contract rather than requiring abort behavior. Exact verification passed; no broad or Windows rerun was performed in this worker.
