---
id: checkpoint_fff664f3
timestamp: 2026-09-12T15:00:42.883Z
tags:
  - plan2
  - search
  - filters
  - regression
git:
  branch: fix/revival-retrieval
  commit: 04eec815
  files:
    - crates/julie-index/src/search/scoring.rs
    - src/tests/tools/search/backend_param_tests.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
next: Commit classifier correction and rerun dev/dogfood; then2C plus evaluator
  builds.
---

## WHAT
Fixed shared test-file classification so a filename stem ending _test is recognized regardless of extension. Existing content-filter regression now prints returned hits on failure.

## WHY
Lead dev04eec815 failed with864pass1fail: explicit exclude_tests leaked routing_test.rs line hits because the shared classifier only recognized Go/C/C++ _test extensions. Cause of the earlier isolated passing observation remains unproven; the reproduced output establishes this product defect.

## HOW
Active matrix Sol reused existing classifier and removed the hardcoded extension list. Exact product RED identified leaked Rust rows; cargo check on index/tools/root passed and exact GREEN1/1 passed. Lead reviewed narrow shared fix. New Sol spawn/followup currently returns agent type unavailable; existing active matrix/paging Sol workers remain usable.

## IMPACT
Ready for corrected dev/dogfood gate.2C discovery ready;2D follows. Frozen matrix inputs remain ready8178/8178, paired evaluator builds await gate.
