---
id: checkpoint_98768391
timestamp: 2026-09-12T22:24:08.414Z
tags:
  - revival
  - plan-4
  - guidance
  - workspace-routing
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
impact: Plan 4A is accepted and ready for local Julie/plugin commits.
evidence:
  - "cargo nextest run -p julie --lib
    manage_workspace_health_accepts_workspace_id_and_rejects_unbound_call_with_\
    guidance: 1 passed"
  - "cargo nextest run -p julie --lib
    mcp_catalog_requires_a_non_null_workspace_for_scoped_tools: 1 passed"
  - "cargo nextest run -p xtask --test docs_contract_tests
    docs_contract_tests_agent_instructions_fit_the_server_instruction_budget: 1
    passed"
  - "cargo nextest run -p xtask --test docs_contract_tests
    docs_contract_tests_deployment_guidance_matches_runtime_contract: 1 passed"
  - "node --test hooks/*.test.cjs: 22 passed"
  - "cargo xtask sync-plugin --plugin-root
    /home/murphy/.config/razorback/worktrees/julie-plugin/revival-deployment
    --dry-run: 0 pending skill/instruction changes"
symbols:
  - ManageWorkspaceRequest::try_from
  - DecodedTool::is_unbound
  - mcp_catalog_requires_a_non_null_workspace_for_scoped_tools
next: Commit the accepted Plan 4A changes and begin Plan 4B version-safe upgrade
  implementation.
confidence: 5
---

## WHAT

Completed and lead-reviewed Plan 4A across the isolated Julie and julie-plugin worktrees. Guidance now puts explicit workspace routing in the first 512 instruction characters, every affected skill call uses the correct selector, health requires `workspace_id` with an actionable open command, and source/plugin hook payloads agree. README and operations now report the pinned extractor's 37 user-facing languages and distinguish JSX/TSX aliases plus the qmldir container.

## WHY

Fresh no-hook clients need enough initialization guidance to route their first scoped call correctly, and plugin/session guidance must not drift from runtime schemas. The prior health examples omitted the required selector, several skill examples omitted `workspace`, and stale tool/language/skill counts made deployment guidance unreliable.

## HOW

Terra implemented the serial 4A slice with exact RED/GREEN tests. Lead review corrected selector examples, plugin skill inventory, `source_regions` wording, and the extractor count after inspecting pinned julie-extractors v2.42.0 commit `3868ed8`. `cargo xtask sync-plugin --dry-run` reports zero pending source/plugin mirror changes, and the full plugin Node gate passes 22/22.

## IMPACT

Plan 4A is ready for local commits. New clients receive self-contained routing guidance; management failures name the exact recovery call; generated schemas retain nine scoped tools; and documentation reflects the actual pinned language inventory. Plan 4B can now implement immutable versioned upgrades without carrying guidance drift.
