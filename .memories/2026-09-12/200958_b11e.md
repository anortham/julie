---
id: checkpoint_b11e4473
timestamp: 2026-09-12T20:09:58.426Z
tags:
  - plan-3
  - runtime-retirement
git:
  branch: main
  commit: bf517c2f
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 3C retirement core
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "cargo nextest run -p julie --lib
    runtime_cache_never_evicts_active_request_or_background_writer: passed"
symbols:
  - RuntimeFactory::retire_idle_runtimes
  - JulieServerHandler::teardown_loaded_workspace
next: Lead should run the two pending exact tests once, then integrate and run
  the branch gate.
unknowns:
  - The same-key barrier test was interrupted before completion; no further
    worker retry is allowed by the task run budget.
---

## Plan 3C retirement core

**WHAT:** Added slot-owned idle runtime retirement, handler teardown ordering, deterministic retirement tests, and ledger/report evidence in the revival-runtime worktree.

**WHY:** Idle checkout runtimes must release watchers and embedding work without allowing duplicate same-key writers.

**HOW:** Kept the fixed 8/60s policy local to RuntimeFactory; empty slots persist; a slot write lock spans teardown; workspace moves out of the handler lock before watcher stop and shared embedding cancel-and-join. Active request Arc ownership and registered embedding tasks refresh activity.

**IMPACT:** The active-request/background-writer test passed. The oldest-idle and same-key barrier exact tests remain for the lead after the worker run budget; the latter was simplified to warm the unrelated key before the barrier.
