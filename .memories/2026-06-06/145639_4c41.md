---
id: checkpoint_4c41df27
timestamp: 2026-06-06T14:56:39.576Z
tags:
  - test
  - xtask
  - bucket-splitting
  - julie-rescue
  - test-economy
  - workspace-tools
git:
  branch: main
  commit: 8968692c
  files:
    - .codex/config.toml
    - docs/plans/2026-06-06-julie-rescue-current-status.md
    - docs/plans/2026-06-06-julie-test-economy-plan.md
    - xtask/src/changed.rs
    - xtask/src/manifest.rs
    - xtask/test_tiers.toml
    - xtask/tests/changed_tests.rs
    - xtask/tests/manifest_contract_tests.rs
    - xtask/tests/support/manifest_contract_expected.rs
summary: Split the workspace test bucket
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Broad workspace coverage is now split into more precise buckets, and
  durable docs/brief reflect that MCP tool consolidation is out of scope for the
  current rescue path.
evidence:
  - "RED: `cargo nextest run -p xtask
    changed_tests_route_workspace_split_modules_to_narrow_buckets` failed before
    implementation because discovery tests still routed to `tools-workspace`."
  - "GREEN: `cargo nextest run -p xtask` passed 169/169 tests in 1.840s after
    the split."
  - "Scoped gate: `XTASK_CHANGED_PATHS=... cargo xtask test changed` selected
    `xtask-runner` and passed in 2.5s."
  - "Bucket checks: discovery passed in 1.1s, indexing passed in 282.9s,
    management passed in 4.1s."
symbols:
  - xtask::changed::buckets_for_path
  - xtask::changed::select_changed_buckets
  - xtask/test_tiers.toml
next: Commit the workspace bucket split, then continue with `tools-search-line`
  splitting as the next rescue slice.
---

## Split the workspace test bucket

Completed the first broad-bucket split in the Julie rescue test-economy path.

- **WHAT:** Replaced the old broad `tools-workspace` bucket with `tools-workspace-discovery`, `tools-workspace-indexing`, and `tools-workspace-management`; left `tools-workspace-targeting` separate.
- **WHY:** The rescue goal is to keep Julie working while removing complexity and fixing the old expensive test loop. Splitting broad buckets makes changed-path verification cheaper and more explainable without deleting coverage.
- **HOW:** Updated `xtask/test_tiers.toml`, changed-path routing, manifest contract fixtures, and the rescue/test-economy docs. Added a routing regression test that failed before the split and passes after implementation.
- **IMPACT:** The next rescue slice can move to `tools-search-line`; MCP tool consolidation is explicitly out of the active rescue path unless the user reopens it.
