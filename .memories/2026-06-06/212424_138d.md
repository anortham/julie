---
id: checkpoint_138ddbff
timestamp: 2026-06-06T21:24:24.781Z
tags:
  - julie-rescue
  - release-v7.13.3
  - deep-review
  - findings-fixed
  - recovery-marker-removal
  - spindump-gate-flake
git:
  branch: main
  commit: 71f5f25c
summary: v7.13.3 deep release review + findings fixed (committed `71f5f25c`)
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "cargo check --workspace: clean (9 crates, julie 7.13.3)"
  - "cargo nextest tests::daemon::session + in-process registration: green"
  - "cargo nextest tests::cli_tools_tests + tests::cli_execution_tests: 72
    passed"
  - "cargo nextest tests::tools::get_symbols_target_workspace: 4 passed 34.2s
    (isolated, under 40s cap)"
  - "cargo xtask test changed->dev: 17 buckets passed; tools-get-symbols timed
    out under spindump load only"
next: "User decision: push plugin launcher fix (4165fcb FF to julie-plugin
  origin/main) now vs at tag time. Then run a clean full gate on a quiet box
  (restart session to clear hung adapter / spindump) before tagging v7.13.3."
---

## v7.13.3 deep release review + findings fixed (committed `71f5f25c`)

Ran an 8-dimension multi-agent release-readiness review of the v7.13.3 post-rescue candidate, then fixed every actionable julie-repo finding.

**Review verdict:** 0 code blockers in the julie repo. 40 findings, all high/critical refuted on adversarial verify. The only real gate is cross-repo (plugin launcher).

**Fixes committed (`71f5f25c`, net -229 LOC):**
- **rescue-1**: deleted orphaned recovery-marker write path + dead session-drain machinery (`drain_with_markers`, `append_recovery_marker`, `write_atomic`, `DrainOutcome`, `drain_sessions`, `is_idle`, `session_notify`, `SessionTracker.notify` field). Kept live dashboard read surface. In-process recovery = startup reconcile, not drain markers. Traced full blast radius; `active_count`/`current_workspace_counts` stay (live dashboard).
- **reg-1**: in-process registry open now best-effort (warn+None, not abort).
- **tear-1/2/3**: scrubbed user-facing daemon vocab (FILEWATCHER.md, JULIE_AGENT_INSTRUCTIONS.md, dead-code-audit skill).
- **rel-1/disclosure/risk-1/2**: added Known Issues/Caveats + Operational Knobs to v7.13.3 notes (verified env-var names in source).

**Verification:** `cargo check --workspace` clean; focused tests green for every changed area (daemon/session, in-process registration, CLI contract — caught+fixed one real regression where my doc edit broke an agent-instructions assertion; get_symbols target-workspace 4/4 in 34.2s).

**Full gate NOT clean-passable on this box:** root-caused to a root-owned `spindump` pinning a core (sampling the hung legacy-adapter MCP server this session still runs). Three gate attempts each flaked on a DIFFERENT bucket's wall-clock cap — every executed test passed. Not killable without sudo.
