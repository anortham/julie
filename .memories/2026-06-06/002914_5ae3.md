---
id: checkpoint_5ae33d5c
timestamp: 2026-06-06T00:29:14.360Z
tags:
  - julie-rescue
  - phase-3d
  - daemon-teardown
  - merged
  - milestone
git:
  branch: main
  commit: 6469b0a0
  files:
    - .claude/scheduled_tasks.lock
    - .codex/config.toml
summary: Phase 3d.2b-ii MERGED — daemon HTTP server-core + pools deleted
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "Branch gate (per-crate superset cargo nextest run -p julie --no-fail-fast
    -E 'not test(search_quality)'): 1452/1452 passed, 0 failed, 113 skipped
    (995.6s) @ d8a872c1 — first fully-GREEN superset of the 3d teardown"
  - "codex pre-merge (gpt-5.5, escalation): 2 findings, both verified
    PRE-EXISTING in-process behavior from the 3c.3 cutover (not regressions) →
    flagged + tracked to 3d.3"
  - main HEAD 6469b0a0; daemon/app.rs + http_transport.rs + workspace_pool.rs
    confirmed gone on main
next: "Phase 3d.3 — daemon.db→registry.db (+started_at_unix), standalone
  registry.db-reader dashboard (Option B), delete src/migration.rs, rewire
  search_matrix::run_baseline_async off the bail! stub, excise discovery.json
  readers + delete pid.rs, finalize in-transition CLAUDE.md/AGENTS.md daemon
  prose, and address the two 3d.2b-ii codex items (F1 in-process remove/cleanup
  safety = product-intent call; F2 cross-workspace metrics target-DB
  attribution). Per plan §3d.3. Cross-repo before next release: julie-plugin
  run.cjs + update-binaries.yml still name julie-adapter → move to
  julie-server."
---

## Phase 3d.2b-ii MERGED — daemon HTTP server-core + pools deleted

**WHAT:** Shipped and merged PR #37 (merge `6469b0a0` → main, 2026-06-06), the atomic server-core kill of the Julie daemon teardown.

- Deleted the daemon HTTP runtime + `WorkspacePool`/`WatcherPool` in ONE commit (`d8a872c1`): `daemon/app/**`, `{http_transport,transport,mcp_session,token_file,singleton,fd_limit,shutdown_event,workspace_pool,watcher_pool}.rs` + tests (~−16.5k LOC, 120 files).
- De-typed handler / dashboard(+5 routes) / health / test-helper off the pools (app.rs was the sole live pool threader → indivisible). Kept SessionTracker/LifecyclePhase/ShutdownCause + drain_sessions + RecoveryMarker family (→3d.3).
- **Production fix folded in:** the de-pool severed the shared `~/.julie/indexes/` root WorkspacePool supplied; `workspace_index_dir_for` now consults `in_process_index_root` first so rebound/secondary/reference workspaces keep the shared anchor (stdio None unchanged).

**WHY:** The 3c.3 cutover left the daemon runtime + pool types as dead code wired into kept paths. 3d.2b-ii removes them.

**HOW:** TeamCreate Sonnet team (Workers A–D + B1/B2/B3 fix batch) for the deletion; lead owned the atomic compile + branch gate + codex review. 8 pool-backed daemon multi-workspace tests `#[ignore]`d with a 3d.3 reason (owner decision).

**IMPACT:** PHASE 3d.2 COMPLETE (3d.2a #35 + 3d.2b-i #36 + 3d.2b-ii #37 all merged). Daemon entry, write-path, HTTP server core, and pools are all gone. Kept daemon surface = registry/connection-pool/embedding-service/discovery/pid + recovery family, all targeted by 3d.3.
