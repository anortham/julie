---
id: checkpoint_95b5c201
timestamp: 2026-06-06T21:53:36.983Z
tags:
  - julie-rescue
  - release-v7.13.3
  - handoff
  - awaiting-decision
  - julie-plugin
  - single-server-launcher
  - full-gate-blocked
git:
  branch: main
  commit: 71f5f25c
summary: "v7.13.3 release: findings committed, awaiting 2 user decisions"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: "Await user: (a) approve plugin FF push to julie-plugin origin/main; (b)
  restart session for a clean full gate. Then tag v7.13.3 (with explicit
  approval only)."
---

## v7.13.3 release: findings committed, awaiting 2 user decisions

Stopping point after the deep release review + fixes (full detail in prior checkpoint `checkpoint_138ddbff`). Working tree clean, all findings fixed and committed at **`71f5f25c`** on `main` (now 10 commits ahead of `origin/main`). Nothing pushed/tagged/released.

### Release is gated on two user decisions (not code)

**1. Plugin launcher fix → land on `julie-plugin` `origin/main`** (the real cross-repo gate; without it, tagging julie ships a plugin that launches the deleted `julie-adapter`).
- Clean fast-forward: `fix/single-server-launcher` (`4165fcb`) = `origin/main` (`03f5c7a`) + 1 commit touching only `hooks/run.cjs` + `run.test.cjs`.
- Command (needs user approval — remote-main push):
  `git -C /Users/murphy/source/julie-plugin push origin fix/single-server-launcher:main`
- Trigger confirmed: julie `release.yml:224` dispatches "Update Plugin", which orphan-pushes from plugin `origin/main` without touching run.cjs → fix MUST be there before the tag.
- plugin-3 (skills 4→6) is a NON-issue: the 2 extra (`dead-code-audit`, `search-debug`) are julie-DEV skills, correctly not shipped.

**2. Clean full gate** — blocked by a root-owned `spindump` pinning a core (sampling the hung legacy-adapter MCP server this session runs). Code is verified sound via focused/dev-partial evidence. To get gold-standard proof: restart Claude Code session (clears hung adapter + spindump), then `cargo xtask test full` on the quiet box.
