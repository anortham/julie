---
id: checkpoint_0e9d5cff
timestamp: 2026-06-06T16:33:58.042Z
tags:
  - julie
  - rescue
  - dashboard
  - manage_workspace
  - release-prep
  - verification
git:
  branch: main
  commit: 4111fa9d
  files:
    - .codex/config.toml
    - README.md
    - docs/site/index.html
    - src/cli_tools/commands.rs
    - src/cli_tools/subcommands.rs
    - src/dashboard/mod.rs
    - src/dashboard/standalone.rs
    - src/handler.rs
    - src/main.rs
    - src/tests/cli_execution_tests.rs
    - src/tests/core/handler/deadline.rs
    - src/tests/dashboard/mod.rs
    - src/tests/dashboard/standalone.rs
    - src/tests/tools/workspace/manage_workspace_request.rs
    - src/tools/workspace/commands/dashboard.rs
    - src/tools/workspace/commands/mod.rs
summary: Added in-session dashboard launch and release-facing docs
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Release prep can proceed with the in-process runtime docs and in-session
  dashboard launch represented in source and tests.
evidence:
  - "cargo check: passed"
  - "cargo xtask test changed: 32 buckets passed in 782.8s"
  - "cargo nextest run -p xtask: 170 passed"
symbols:
  - dashboard::standalone::launch_dashboard
  - dashboard::standalone::serve_dashboard_forever
  - ManageWorkspaceOperation::Dashboard
  - ManageWorkspaceTool::handle_dashboard_command
next: Commit the Julie changes, then inspect and update
  /Users/murphy/source/julie-plugin for release readiness.
---

## Added in-session dashboard launch and release-facing docs

Julie now exposes the standalone dashboard through `manage_workspace(operation="dashboard")` while keeping the shell path `julie-server dashboard` intact.

- **WHAT:** Added shared dashboard launcher code, wired `manage_workspace` to launch the dashboard in-session, and updated README/site docs for both the new dashboard operation and the removed `julie-adapter` / `julie-daemon` runtime.
- **WHY:** Agents should be able to open the dashboard from an MCP session after the adapter/daemon teardown, and release docs must tell old users to point configs at `julie-server` directly.
- **HOW:** Moved the dashboard startup logic out of `src/main.rs` into `src/dashboard/standalone.rs`; added `src/tools/workspace/commands/dashboard.rs`; extended the manage_workspace parser and docs; guarded one-shot CLI wrappers so dashboard launch does not immediately die with the CLI process.
- **IMPACT:** Prepares Julie for release with an in-session dashboard entry point and clear migration guidance for older split-daemon installs.

Verification before commit:
- `cargo check` passed.
- Focused tests passed for manage_workspace parser, dashboard launcher, deadline classification, and CLI dashboard guards.
- Buckets passed: `cli`, `dashboard`, `core-fast`, `tools-workspace-discovery`, `tools-workspace-indexing`, `tools-workspace-management`, `tools-workspace-targeting`, `workspace-init`, direct `cargo nextest run -p xtask`.
- `cargo xtask test changed` passed 32 buckets in 782.8s. Slow buckets remain visible: `tools-workspace-indexing` 290.7s, `tools-get-symbols` 68.5s, `xtask-runner` 50.1s.
