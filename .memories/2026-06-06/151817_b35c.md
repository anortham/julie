---
id: checkpoint_b35c6693
timestamp: 2026-06-06T15:18:17.618Z
tags:
  - test
  - xtask
  - bucket-splitting
  - julie-rescue
  - test-economy
  - search-line
git:
  branch: main
  commit: 4bfa2361
  files:
    - .codex/config.toml
    - docs/plans/2026-06-06-julie-rescue-current-status.md
    - docs/plans/2026-06-06-julie-test-economy-plan.md
    - xtask/src/changed.rs
    - xtask/src/manifest.rs
    - xtask/test_tiers.toml
    - xtask/tests/changed_tests.rs
    - xtask/tests/manifest_contract_tests.rs
    - xtask/tests/support/manifest_contract_expected.rs
summary: Split the search line-mode test bucket
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Search line-mode coverage is split into actionable buckets without
  changing Julie's MCP tool surface.
evidence:
  - "RED: `cargo nextest run -p xtask
    changed_tests_route_line_mode_split_modules_to_narrow_buckets` failed before
    implementation because `line_mode/basic.rs` still routed to
    `tools-search-line`."
  - "GREEN: targeted xtask routing/manifest tests passed 10/10."
  - "Buckets: `tools-search-line-core` passed in 31.6s,
    `tools-search-line-filters` passed in 140.2s, `tools-search-line-primary`
    passed in 64.9s."
  - "Package gate: `cargo nextest run -p xtask` passed 170/170 in 1.969s."
  - "Scoped gate: `XTASK_CHANGED_PATHS=... cargo xtask test changed` selected
    `xtask-runner` and passed in 2.3s."
symbols:
  - xtask::changed::search_test_buckets_for_path
  - xtask::changed::sort_bucket_names
  - xtask/test_tiers.toml
next: Commit the line-mode bucket split, then continue with `tools-editing`
  splitting as the next rescue slice.
---

## Split the search line-mode test bucket

Completed the next broad-bucket split in the Julie rescue test-economy path.

- **WHAT:** Replaced old `tools-search-line` with `tools-search-line-core`, `tools-search-line-filters`, and `tools-search-line-primary`.
- **WHY:** Line-mode changes previously required one 250s serialized bucket. The split keeps full release coverage while making changed-path routing more precise.
- **HOW:** Added a RED routing test for line-mode split modules, updated `xtask/test_tiers.toml`, changed-path routing, manifest contract fixtures, rescue docs, and the active Goldfish brief.
- **IMPACT:** `dev` remains 27 buckets / 589s expected. `full` is now 48 buckets / 2534s expected. The next recommended rescue slice is `tools-editing`.

## Dogfood Finding

Julie MCP health is still degraded in this live session: after `manage_workspace refresh --force` and `manage_workspace index --force`, health still reported SQLite-only readiness and missing Tantivy projection. `get_context`/`get_symbols` worked, but `fast_search` continued to report that Tantivy search requires refresh. This was not fixed in this xtask-only slice.
