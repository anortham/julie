---
id: checkpoint_3da80caa
timestamp: 2026-06-06T02:57:13.083Z
tags:
  - julie
  - phase3d3
  - task7
  - xtask
  - search-matrix
git:
  branch: julie-rescue-phase3d3
  commit: 8fcfa893
  files:
    - crates/julie-core/src/paths.rs
    - dashboard/templates/partials/project_detail.html
    - dashboard/templates/partials/project_row.html
    - dashboard/templates/partials/search_compare_results.html
    - dashboard/templates/partials/signals_summary.html
    - dashboard/templates/projects.html
    - dashboard/templates/search.html
    - dashboard/templates/search_analysis.html
    - dashboard/templates/search_compare.html
    - docs/plans/2026-06-05-julie-phase3d-delete-daemon-adapter.md
    - src/cli.rs
    - src/daemon/database.rs
    - src/daemon/database/migrations.rs
    - src/daemon/database/search_compare.rs
    - src/daemon/discovery.rs
    - src/daemon/mod.rs
    - src/daemon/pid.rs
    - src/daemon/shutdown.rs
    - src/dashboard/mod.rs
    - src/dashboard/routes/events.rs
    - src/dashboard/routes/mod.rs
    - src/dashboard/routes/projects.rs
    - src/dashboard/routes/projects_actions.rs
    - src/dashboard/routes/search.rs
    - src/dashboard/routes/search_compare.rs
    - src/dashboard/routes/search_session.rs
    - src/dashboard/search_compare.rs
    - src/handler.rs
    - src/handler/tool_metrics.rs
    - src/handler/tools/fast_refs.rs
summary: Restored Julie rescue 3d.3 Task 7 search-matrix baseline
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "RED: cargo nextest run -p xtask
    search_matrix_contract_tests_baseline_no_longer_errors_on_removed_workspace\
    _pool failed with the WorkspacePool removed stub message."
  - "GREEN: cargo nextest run -p xtask
    search_matrix_contract_tests_baseline_no_longer_errors_on_removed_workspace\
    _pool -> 1/1 passed."
  - cargo nextest run -p xtask search_matrix_contract_tests -> 12/12 passed.
  - cargo build -p xtask -> passed.
  - git diff --check -> clean.
symbols:
  - run_baseline_async
  - run_search_matrix_baseline_with_home
  - execute_baseline_case
  - SearchMatrixBaselineReport
next: Continue with 3d.3 Task 8 final docs/guidance rewrite and then
  branch-level verification.
confidence: 4
---

## Restored Julie rescue 3d.3 Task 7 search-matrix baseline

### WHAT
- Replaced the `run_baseline_async` stub that bailed after `WorkspacePool` deletion.
- Implemented a registry-driven baseline loop in `xtask/src/search_matrix.rs`.
- Added a contract test proving baseline no longer errors on the removed `WorkspacePool` path and writes a skipped-repo report for missing roots.
- Recorded Task 7 verification evidence in the phase 3d plan.

### WHY
- 3d.2b deleted `WorkspacePool`, temporarily breaking `cargo xtask search-matrix baseline` with an intentional stub.
- 3d.3 needs the baseline runner to work from `registry.db` and global per-workspace index dirs.

### HOW
- Load profile repos from the corpus, resolve roots, match registry rows by normalized repo root, skip missing/not-ready/missing-index repos with explicit reasons.
- For ready registered repos, initialize a one-workspace `JulieWorkspace` from `DaemonPaths::workspace_index_dir(workspace_id)`, create a `JulieServerHandler`, and execute eligible cases through `execute_search`.
- Preserve baseline report fields: hits, trace diagnostics, top hits, latency, summary flags, and ablation label.

### IMPACT
- `search-matrix baseline --ablation none` no longer fails just because the pool was deleted.
- Missing repos now produce skipped-repo entries rather than aborting the whole run.
