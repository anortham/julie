---
id: checkpoint_189a6b0c
timestamp: 2026-06-06T02:52:32.065Z
tags:
  - julie
  - phase3d3
  - task6
  - cleanup
  - workspace-registry
git:
  branch: julie-rescue-phase3d3
  commit: 8fcfa893
  files:
    - crates/julie-core/src/paths.rs
    - dashboard/templates/partials/project_detail.html
    - dashboard/templates/partials/project_row.html
    - dashboard/templates/partials/search_compare_results.html
    - dashboard/templates/partials/signals_summary.html
    - dashboard/templates/projects.html
    - dashboard/templates/search.html
    - dashboard/templates/search_analysis.html
    - dashboard/templates/search_compare.html
    - docs/plans/2026-06-05-julie-phase3d-delete-daemon-adapter.md
    - src/cli.rs
    - src/daemon/database.rs
    - src/daemon/database/migrations.rs
    - src/daemon/database/search_compare.rs
    - src/daemon/discovery.rs
    - src/daemon/mod.rs
    - src/daemon/pid.rs
    - src/daemon/shutdown.rs
    - src/dashboard/mod.rs
    - src/dashboard/routes/events.rs
    - src/dashboard/routes/mod.rs
    - src/dashboard/routes/projects.rs
    - src/dashboard/routes/projects_actions.rs
    - src/dashboard/routes/search.rs
    - src/dashboard/routes/search_compare.rs
    - src/dashboard/routes/search_session.rs
    - src/dashboard/search_compare.rs
    - src/handler.rs
    - src/handler/tool_metrics.rs
    - src/handler/tools/fast_refs.rs
summary: Completed Julie rescue 3d.3 Task 6 cleanup safety
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - "RED: cargo nextest run -p julie --lib
    test_remove_current_primary_workspace_is_blocked_in_process failed because
    remove returned Workspace Removed Successfully and deleted the live primary
    index."
  - "GREEN: cargo nextest run -p julie --lib
    test_remove_current_primary_workspace_is_blocked_in_process -> 1/1 passed."
  - cargo nextest run -p julie --lib
    test_remove_workspace_uses_global_index_dir_shape -> 1/1 passed.
  - cargo nextest run -p julie --lib
    tests::tools::workspace::global_targeting::global_remove -> 2/2 passed.
  - cargo nextest run -p julie --lib
    tests::tools::workspace::global_targeting::list_stats -> 5/5 passed.
  - cargo nextest run -p julie --lib
    tests::tools::workspace::global_targeting::deferred_sessions -> 5/5 passed.
  - cargo nextest run -p julie --no-run -> passed.
  - git diff --check -> clean.
symbols:
  - WorkspaceCleanupActivity
  - cleanup_activity_for_handler
  - delete_workspace_if_allowed
  - prune_missing_workspaces
  - ManageWorkspaceTool::handle_remove_command
next: "Continue with 3d.3 Task 7: restore search-matrix baseline without the old
  WorkspacePool path."
confidence: 5
---

## Completed Julie rescue 3d.3 Task 6 cleanup safety

### WHAT
- Replaced stateless `WorkspaceCleanupActivity::new()` usage in registry commands with an activity built from the handler's live workspace set.
- The live set includes the current primary workspace id plus session-attached workspace ids.
- Manual delete and auto-prune now block live in-process workspaces with `workspace is active in this in-process session`.
- Added a RED/GREEN test proving `manage_workspace remove` blocks the current primary and preserves registry/index data.
- Recorded Task 6 verification evidence in the phase 3d plan.

### WHY
- The old cleanup activity assumed no active refs after pool deletion, so `manage_workspace remove` could delete the current in-process primary workspace.
- 3d.3 requires cleanup to be safe without the daemon pools by deriving activity from the live handler/session state.

### HOW
- Added `live_workspace_ids` to `WorkspaceCleanupActivity`.
- Made `cleanup_activity_for_handler(handler)` async and populated it from `handler.current_workspace_id()` and `handler.session_attached_workspace_ids().await`.
- Updated register/remove, list/cleanup, and open call sites to await the helper.
- Left dashboard project-state display using an empty cleanup activity because it has no handler session and is read-only.

### IMPACT
- Current-primary removal is blocked and leaves the registry row and index dir intact.
- Inactive target removal still succeeds.
- Deferred-session list/register/remove tests still pass.
