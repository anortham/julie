---
id: checkpoint_598117e8
timestamp: 2026-06-06T02:08:56.932Z
tags:
  - julie
  - phase-3d3
  - registry
  - tdd
git:
  branch: julie-rescue-phase3d3
  commit: 8fcfa893
  files:
    - crates/julie-core/src/paths.rs
    - docs/plans/2026-06-05-julie-phase3d-delete-daemon-adapter.md
    - src/daemon/database.rs
    - src/daemon/database/migrations.rs
    - src/tests/daemon/database.rs
    - src/tests/daemon/paths.rs
    - xtask/src/search_matrix.rs
summary: Completed Julie Rescue 3d.3 Task 1 registry slice
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - cargo nextest run -p julie --lib tests::daemon::paths -> 30/30 passed
  - cargo nextest run -p julie --lib tests::daemon::database -> 34/34 passed
  - cargo nextest run -p julie --no-run -> Finished test profile
  - cargo build -p xtask -> Finished dev profile
  - git diff --check -> clean
symbols:
  - DaemonPaths::registry_db
  - DaemonPaths::daemon_db
  - DaemonDatabase::open
  - migration_006_add_daemon_state
next: "Continue 3d.3 Task 2: delete discovery JSON readers and pid-file code
  with a boundary test first."
confidence: 4
---

## Completed Julie Rescue 3d.3 Task 1 registry slice

### WHAT
- Added `DaemonPaths::registry_db()` and moved the live search-matrix path callers from `daemon_db()` to `registry_db()`.
- Kept `daemon_db()` as a temporary deprecated shim for the rest of 3d.3.
- Added registry migration 006 with `daemon_state.started_at_unix`.
- Added compatibility handling so opening `registry.db` migrates an existing `daemon.db` file when the new file does not exist.
- Expanded the 3d.3 plan into a detailed task breakdown and recorded Task 1 verification rows.

### WHY
3d.3 starts by turning the old daemon-named central database into the process-independent `registry.db`, which dashboard, cleanup, metrics, and search-matrix follow-up work will depend on.

### HOW
- Drove the slice with RED tests for the missing path helper, missing `daemon_state`, and missing legacy `daemon.db` migration.
- Implemented a minimal compatibility layer in `DaemonDatabase::open()` that moves `daemon.db` and SQLite sidecars to `registry.db` before opening.
- Updated migration logging and cross-crate `xtask` path usage to the new registry path.

### IMPACT
- Existing users with `~/.julie/daemon.db` keep their registry rows after the rename.
- New production path usage goes through `registry_db()` where touched.
- Remaining 3d.3 work can proceed to discovery/pid deletion, migration module deletion, dashboard read-only conversion, metrics source-of-truth cleanup, in-process cleanup safety, search-matrix baseline restore, and final docs.
