---
id: checkpoint_fafae744
timestamp: 2026-06-06T22:53:40.969Z
tags:
  - julie-rescue
  - release-v7.13.3
  - published
  - julie-plugin
  - single-server-launcher
  - release-evidence
git:
  branch: main
  commit: 71f5f25c
summary: Published Julie v7.13.3
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: v7.13.3 is published and julie-plugin distribution is updated to the
  single-server launcher.
evidence:
  - "Julie release: https://github.com/anortham/julie/releases/tag/v7.13.3"
  - "Julie release workflow:
    https://github.com/anortham/julie/actions/runs/27075591175"
  - "Plugin update workflow:
    https://github.com/anortham/julie-plugin/actions/runs/27076037742"
next: Do not push further release changes unless a patch follow-up is needed.
  Consider cleaning or separately reconciling the dirty
  `/Users/murphy/source/julie-plugin` checkout if future plugin work should use
  it again.
---

## Published Julie v7.13.3

Released the v7.13.3 single-server rescue release and verified both Julie and julie-plugin publication paths.

- **WHAT:** Pushed Julie `main` to `71f5f25c`, created/pushed annotated tag `v7.13.3`, published the GitHub release, and let the release workflow dispatch the plugin update workflow.
- **WHY:** v7.13.3 is the rescue release that removes the split daemon/adapter runtime, launches `julie-server` directly, and preserves the retrieval moat while simplifying packaging/runtime behavior.
- **HOW:** Verified locally first, pushed the prepared `julie-plugin` direct-launcher fix to plugin `main`, pushed Julie `main`, tagged `v7.13.3`, monitored GitHub workflows, verified release assets and plugin archive contents, and updated GitHub release notes with final publication evidence.
- **IMPACT:** v7.13.3 is public with four platform assets; `julie-plugin` main is updated to v7.13.3 with direct `julie-server` launcher and matching archives.

## Verification Evidence

- `node --test hooks/*.test.cjs` in `/Users/murphy/source/julie-plugin-single-server`: 15 tests passed.
- `git diff --check HEAD` passed in Julie and plugin launcher worktrees.
- `cargo xtask test full` at `71f5f25c`: 48 buckets passed in 1533.9s.
- `cargo build --release --bin julie-server --bin julie-embedding-host` at `71f5f25c`: passed in 1m57s.
- `target/release/julie-server --version`: `julie-server 7.13.3`.
- Julie Release workflow `27075591175`: success in 21m25s.
- GitHub release `v7.13.3`: public, non-draft, non-prerelease, 4 assets uploaded.
- Published Linux release asset digest verified as `sha256:69f3d2afc33bd6538f6ec3576d62954029aa787d66b643dce5c328cea7c1b006`; archive contains `julie-server` and `julie-embedding-host`.
- Plugin Update workflow `27076037742`: success; plugin `origin/main` force-updated to `fdedd01` with v7.13.3 manifests and archives.
- Plugin Linux archive digest matches the published release asset and contains the same two binaries.

## Notes

- Julie repo is synced with `origin/main`; only pre-existing untracked Goldfish memory files remain.
- The real `/Users/murphy/source/julie-plugin` checkout was intentionally left untouched because it has local dirty/diverged work. Verification used `/Users/murphy/source/julie-plugin-single-server` plus direct `git show origin/main:...` checks.
