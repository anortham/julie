---
id: checkpoint_5208bd81
timestamp: 2026-03-18T21:05:08.710Z
tags:
  - launch
  - brainstorm
  - spec
  - plan
  - python-centrality
  - swift
  - kotlin
  - dart
  - documentation
  - skills
  - verification
git:
  branch: main
  commit: 1ee937dd
  files:
    - .claude/settings.local.json
summary: "## Launch Readiness Brainstorm → Spec → Plan Complete"
planId: scala-and-elixir-language-support
type: checkpoint
symbols:
  - score_candidate
  - select_best_candidate
  - compute_reference_scores
  - is_test_path
  - extract_class
  - recover_dart3_modifier_class
next: Execute the plan. Start with Task 1 (Python centrality theft) and Task 13
  (README quick fixes) in parallel. Tasks 2-4 (extractor fixes) can run
  concurrently but limit to 2 at a time due to shared crate compile lock.
---

## Launch Readiness Brainstorm → Spec → Plan Complete

### What
Completed full brainstorming cycle for Julie's public launch readiness:
- Design spec written, reviewed (2 passes), and approved
- Implementation plan written, reviewed, and committed
- 17 tasks across 3 phases

### Why
Julie is functionally ready for public release but has 4 language bugs that hurt credibility (Python centrality theft being the worst), stale documentation (README says 31 languages/7 tools), no skills documentation, and incomplete verification matrix.

### How
- **Brainstorming skill** → clarified scope: skip plugin packaging, focus on fixes + docs + skill validation
- **Approach B (Fixes-First)** chosen: fix bugs before docs so docs reference final state
- **Spec review** caught 4 important issues: missing known limitations section, GH Pages tool count, Python fix complexity, JSONL resolution
- **Plan review** caught 3 critical issues: wrong `is_test_path` path (`src/utils/` doesn't exist, correct: `crate::search::scoring`), `score_candidate` is private (tests must use `select_best_candidate`), extractor tests live in `crates/julie-extractors/src/tests/` not `src/tests/extractors/`

### Key Discoveries
- Dart 3 recovery code **already exists** (`recover_dart3_modifier_class`) — Task 4 is debugging, not greenfield
- `compute_reference_scores` Step 4 already de-weights test files by 90%, but 213×0.1=21.3 still beats real Flask's 1.4 — fix must be in the **resolver**, not centrality
- Kotlin has ERROR node recovery in `visit_node` (lines 132-148) — need to debug why it fails for JsonReader specifically

### Impact
- Spec: `docs/superpowers/specs/2026-03-18-launch-readiness-design.md`
- Plan: `docs/superpowers/plans/2026-03-18-launch-readiness.md`
- Ready for execution via subagent-driven development
