---
id: checkpoint_844f4a56
timestamp: 2026-05-03T13:25:26.941Z
tags:
  - testing-contract
  - xtask
  - specialist-buckets
git:
  branch: codex/testing-contract-dogfood
  commit: e88df9f4
  files:
    - xtask/src/changed.rs
    - xtask/test_tiers.toml
    - xtask/tests/changed_tests.rs
    - xtask/tests/manifest_contract_tests.rs
summary: Added Specialist Architecture Test Buckets
briefId: julie-world-class-systems-program
type: checkpoint
impact: Reduces need to run broad dev/full gates for architecture work while
  keeping the specialist gates explicit and reviewable.
evidence:
  - "`cargo nextest run -p xtask --test changed_tests` passed 18 tests."
  - "`cargo nextest run -p xtask --test manifest_contract_tests` passed 3 tests."
  - "`cargo xtask test list` showed projection, transport, lifecycle, and
    workspace-runtime in system/full tiers."
next: Commit Task 3, then implement the verification ledger docs contract.
---

## Added Specialist Architecture Test Buckets

### WHAT

Added projection, transport, lifecycle, and workspace-runtime xtask buckets plus changed-file routing for the architecture cleanup plans.

### WHY

The next Julie cleanup plans need precise lead-owned gates instead of falling back to `dev` or unrelated broad buckets whenever daemon, projection, transport, or workspace runtime files move.

### HOW

- Added specialist buckets to `xtask/test_tiers.toml` with scope metadata and notes naming the existing filters backing each bucket.
- Added specialist buckets to `system` and `full` tiers while leaving `dev` unchanged.
- Routed projection state, projection health, indexing pipeline, transport, daemon lifecycle, workspace runtime, watcher, and registry command paths to the new buckets.
- Added contract tests for direct examples and lead-added tests for actual upcoming-plan paths: projection pipeline, future HTTP transport, daemon `mod.rs`, and workspace registry commands.
- Verified `cargo xtask test list` shows the new buckets and their tier membership.

### IMPACT

Task 3 of the testing-contract plan is complete. Changed-file routing can now select smaller architecture gates that match the work planned for projection, lifecycle, workspace split, and HTTP transport changes.
