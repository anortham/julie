---
id: checkpoint_8235d17d
timestamp: 2026-05-03T19:49:07.540Z
tags:
  - dead-code-audit
  - workspace-pool
  - cleanup
git:
  branch: codex/dead-code-audit-cleanup
  commit: 84b83c06
  files:
    - docs/plans/2026-05-03-dead-code-audit-baseline.md
    - docs/plans/2026-05-03-dead-code-tool-readiness.md
    - src/daemon/ipc_session.rs
    - src/daemon/workspace_pool.rs
    - src/daemon/workspace_session_attachment.rs
    - src/dashboard/routes/projects_actions.rs
    - src/dashboard/routes/search.rs
    - src/dashboard/search_compare.rs
    - src/handler.rs
    - src/tests/daemon/workspace_pool.rs
summary: Removed WorkspacePool Indexed Flag Fossils
briefId: julie-world-class-systems-program
type: checkpoint
impact: WorkspacePool owns less stale state, dashboard and IPC detach no longer
  perform a no-op sync, and the dead-code tool readiness doc explicitly
  recommends CLI/report first instead of an MCP tool.
evidence:
  - "cargo nextest run --lib test_get_returns_some_after_init: PASS"
  - "cargo nextest run --lib
    test_projects_open_action_warms_workspace_without_leaking_session_count:
    PASS"
  - "cargo nextest run --lib
    test_handle_ipc_session_cleanup_disconnects_startup_and_rebound_primary:
    PASS"
  - "cargo nextest run --lib tests::daemon::workspace_pool: PASS, 16 tests"
  - "cargo xtask test changed: PASS, 22 buckets in 375.6s before docs/fmt
    additions"
  - "cargo nextest run --lib tests::daemon::workspace_pool
    test_handle_ipc_session_cleanup_disconnects_startup_and_rebound_primary
    test_projects_open_action_warms_workspace_without_leaking_session_count:
    PASS, 18 tests, one transient LEAK marker"
  - "cargo nextest run --lib test_session_count_not_incremented_on_init_failure:
    PASS"
next: Commit the cleanup batch, then run final branch gates before merging.
confidence: 4
---

## Removed WorkspacePool Indexed Flag Fossils

- **WHAT:** Captured the dead-code audit baseline and removed the stale `WorkspacePool` in-memory indexed flag cluster.
- **WHY:** After the workspace service split, product code no longer read `WorkspaceEntry.indexed`; only tests and sync helpers kept it alive.
- **HOW:** Verified candidates with Julie inventory, `fast_refs`, content search, `deep_dive`, and blast-radius checks. Deleted `WorkspaceEntry.indexed`, `WorkspacePool::is_indexed`, `WorkspacePool::mark_indexed`, `WorkspacePool::sync_indexed_from_db`, the sync-only attachment variant, dashboard sync calls, and fossil tests.
- **IMPACT:** Shrinks workspace runtime state and removes a misleading synchronization path before the HTTP transport work.
