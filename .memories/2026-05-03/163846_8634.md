---
id: checkpoint_8634e9ac
timestamp: 2026-05-03T16:38:46.881Z
tags:
  - xtask
  - testing
  - gate-reliability
git:
  branch: codex/test-runtime-gate-reliability
  commit: acd7a9c9
  files:
    - xtask/src/cli.rs
    - xtask/src/manifest.rs
    - xtask/src/runner.rs
    - xtask/tests/manifest_tests.rs
    - xtask/tests/runner_tests.rs
summary: Added first xtask runner reliability fixes
briefId: julie-world-class-systems-program
type: checkpoint
impact: Completes the first runner/manifest hardening slice before bucket splitting.
evidence:
  - "cargo nextest run -p xtask runner_tests_cli_rejects_unknown_bucket_name:
    RED then passed"
  - "cargo nextest run -p xtask
    runner_tests_bucket_output_includes_command_timings: RED then passed"
  - "cargo nextest run -p xtask
    manifest_tests_reject_duplicate_commands_in_same_bucket: RED then passed"
  - "cargo nextest run -p xtask
    runner_tests_cli_contract_supports_tiers_list_and_bucket: passed"
  - "cargo nextest run -p xtask
    runner_tests_run_bucket_returns_structured_result: passed"
  - "cargo nextest run -p xtask
    runner_tests_failed_bucket_writes_captured_output_between_markers: passed"
  - "cargo nextest run -p xtask
    runner_tests_timed_out_bucket_writes_captured_output_between_markers:
    passed"
  - "cargo nextest run -p xtask manifest_tests_reject_duplicate_bucket_commands:
    passed"
  - "git diff --check: passed"
next: Commit this slice, then implement split buckets and changed routing.
confidence: 4
---

## Added first xtask runner reliability fixes

- **WHAT:** Hardened explicit bucket validation, added per-command timing output, and closed duplicate manifest command validation for same-bucket duplicates.
- **WHY:** The test runtime rescue plan needs `xtask` to expose which command is consuming time and reject obvious duplicate command work before broad gates waste time.
- **HOW:** Followed TDD with exact xtask tests only. Added RED/GREEN tests for unknown bucket validation, command timing lines, and same-bucket duplicate manifest commands. Reviewed the manifest worker's earlier duplicate-command commit and tightened the same-bucket edge case.
- **IMPACT:** `cargo xtask test bucket <name>` now fails earlier for bad bucket names, bucket output can show command timing, and exact duplicate command strings are rejected even inside one bucket.
