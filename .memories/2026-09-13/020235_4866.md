---
id: checkpoint_4866311e
timestamp: 2026-09-13T02:02:35.682Z
tags:
  - revival
  - plan-5
  - qualification
  - contract
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: implementation
next: Commit the Plan 5A owned docs and generated memory artifact locally, then
  let Sol review the frozen contract before starting Plan 5B.
---

## WHAT
Frozen Plan 5A's local experiment contract in the `fix/revival-qualification` worktree: six held-out retrieval rows, a 12-task core agent manifest, a contract finding, and the Plan 5 status/ledger. The existing 23 retrieval `.search` rows remain unchanged as the regression set.

## WHY
Plan 5 must define workloads, expected outcomes, fairness rules, and verdicts before any Julie/Miller/raw-tools comparison. The contract must be source-evidenced and cannot be tuned from product results.

## HOW
Used the existing Julie debug CLI to inspect the plan, manifest, runner validation contract, scorecard, and README. Pinned fixture heads were checked against all ten manifest commits; expected held-out sites were derived from `git show <pinned-commit>:<path>`, not product output. Added no runner/product code and ran no candidate experiment.

## IMPACT
The next authorized step is Plan 5B: teach the existing runner to enforce declared alternatives, exact-site/body/paging evidence, scoped workspace calls, and readiness/identity rules. Current worker verification passed: both JSON files parse, `run_matrix.py --validate --cases ...` reports `52 rows valid`, and `git diff --check` is clean. No external model spend, rebuild, public/user checkout modification, CI dispatch, push, tag, release, or publication occurred.
