---
id: checkpoint_c96553de
timestamp: 2026-09-13T14:10:59.319Z
tags:
  - revival
  - plan-4
  - handoff
  - blocked
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Revival brief reconciled after final native run
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - Active brief updated after run 34760490971 completed failure
  - Plan 4 evidence commit 79a948f8
  - Deployment branch HEAD 79a948f8 before brief synchronization
next: Commit the updated brief and this checkpoint locally, then perform final
  worktree-state verification and report the authorization blocker.
---

## Revival brief reconciled after final native run

### WHAT

The active Plans 4 and 5 brief now records the terminal failure of run `34760490971`, the local fixes at `ce574773` and `3115c6c8`, and evidence commit `79a948f8`.

### WHY

The prior brief still called the hosted run in progress and treated `055645e` as the final candidate. Product code changed after that full gate, so future work must not reuse the old candidate identity.

### HOW

The brief separates macOS auto/lexical fallback from the Windows HTTP 429, names skipped lifecycle and upload rows, and preserves every authorization boundary.

### IMPACT

The next session starts from the correct blocked state: new frozen-tree full gate plus explicit native-run authorization are needed before Plan 5C can begin.
