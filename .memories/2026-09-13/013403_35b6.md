---
id: checkpoint_35b6a356
timestamp: 2026-09-13T01:34:03.479Z
tags:
  - plan-4
  - packaging
  - manifest-schema
  - plugin-install
  - tdd
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Added launcher identity fields to release manifests
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - Packaging exact test RED then PASS for root schema/target
  - Archive qualification exact test RED then PASS for invalid launcher fields
  - "bash, Python, cargo fmt, and diff checks: passed"
next: Commit, repackage Linux without rebuilding the binary, and rerun the
  disposable plugin installation.
---

## Added launcher identity fields to release manifests

- **WHAT:** Added top-level `schema_version: 2` and `rust_target` to the release wrapper manifest, copied from and validated against the embedded sidecar manifest. The archive verifier now enforces the same identity contract.
- **WHY:** The real disposable plugin launcher rejected the canonical manifest because those fields were nested only under `source`, while version validation requires them at the root.
- **HOW:** Added RED coverage for missing/invalid launcher fields, then passed the target into the package wrapper generator and checked root/source equality in verification.
- **IMPACT:** The package manifest now matches all launcher prerequisites observed so far. The Linux archive must be repackaged and the real fresh install rerun.
