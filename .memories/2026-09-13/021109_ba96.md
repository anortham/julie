---
id: checkpoint_ba969970
timestamp: 2026-09-13T02:11:09.615Z
tags:
  - revival
  - plan-5
  - qualification
  - contract
  - provenance-correction
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: implementation
next: Copy the generated checkpoint into the revival-qualification worktree,
  commit only the cases provenance correction and checkpoint locally, then
  report the correction SHA to Sol.
---

## WHAT
Corrected the remaining Plan 5A provenance gap in the `fix/revival-qualification` worktree. The held-out jq same-line-reference row now records explicit pinned-commit source evidence for both accepted sites: `src/jv_aux.c:479` and `src/jv_aux.c:484`.

## WHY
The row accepted line 484 but previously cited only line 479. Every accepted alternative must be source-auditable before any candidate product executes.

## HOW
Changed only `docs/eval/head-to-head/cases.json` and updated the ignored Task 5A report. The accepted sites themselves remain unchanged. No runner or product code was touched.

## IMPACT
The frozen contract now has complete provenance for both accepted jq sites. Verification passed: JSON parse, `run_matrix.py --validate --cases ...` (`52 rows valid`), and `git diff --check`. No experiment, rebuild, external model call, public/user checkout edit, CI, push, tag, release, or publication occurred.
