---
id: checkpoint_1a624f09
timestamp: 2026-09-13T11:50:21.638Z
tags:
  - revival
  - plan-4
  - github-actions
  - yaml
  - qualification
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Native workflow parse failure diagnosed and corrected
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: Nonpublishing native qualification run 34755354063 failed before
  scheduling any jobs.
impact: Prevents a second no-job workflow failure and strengthens local workflow
  validation.
evidence:
  - "GitHub Actions run 34755354063: completed failure, zero jobs"
  - "actionlint 1.7.7: YAML error at line 92 before fix"
  - Exact workflow contract RED then GREEN, run 35996266
  - actionlint syntax passes after fix
next: Commit the YAML/test correction locally; hold the remote retry until the
  paging regression is fixed and the frozen branch gate passes.
---

## Native workflow parse failure diagnosed and corrected

### WHAT

The first pushed qualification workflow produced a zero-job GitHub Actions failure. The local workflow has been corrected so its embedded Python remains inside the YAML literal block.

### WHY

The heredoc body began at column zero, making the workflow invalid YAML at line 92. The earlier string-contract test did not parse the workflow and therefore missed the defect.

### HOW

`actionlint` reproduced the exact parser error locally. A narrow regression assertion was added first and failed against the bad indentation; the heredoc was then indented correctly and the exact test passed. `actionlint` now parses the workflow; its only remaining label warning is stale relative to GitHub's documented `macos-15-intel` runner.

### IMPACT

No native runner job or release action ran on the failed attempt. The corrected branch is ready for the single qualification retry after the newly discovered paging fix and branch gate are complete.
