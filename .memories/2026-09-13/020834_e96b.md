---
id: checkpoint_e96bb8f6
timestamp: 2026-09-13T02:08:34.226Z
tags:
  - revival
  - plan-5
  - qualification
  - contract
  - review-correction
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: implementation
next: Copy this generated checkpoint into the revival-qualification worktree,
  commit only the Plan 5A owned correction files plus the checkpoint, and return
  the correction SHA to Sol.
---

## WHAT
Corrected the Plan 5A frozen contract in the `fix/revival-qualification` worktree after lead review. The agent-task manifest now pins `terra_worker` / `gpt-5.6-terra`, gives every core task one deterministic product order, and includes exact source-text seed patches. The Flask held-out documentation case now declares two source-backed accepted paths.

## WHY
The original contract described counterbalancing and seeded faults but did not make them mechanically auditable. This correction freezes all six Julie/Miller/raw-tools permutations exactly twice, source-backed alternative acceptance, and reproducible disposable-copy mutations before any experiment.

## HOW
Kept the existing runner/product code untouched. Added `product_order` to the 12 core tasks, reused the six permutations twice, and added `seed_patch` objects with exact `path`, `before`, and `after` text for every seeded task. Read Flask's pinned `docs/api.rst` directly to source `Sessions` at line 53. Updated the contract and worker report.

## IMPACT
Plan 5A now has a deterministic and reproducible evaluation contract. Verification passed: both JSON files parse, `run_matrix.py --validate --cases ...` reports `52 rows valid`, invariant script reports `12 core tasks; 6 orders x2; 4 exact seed patches; accepted_paths frozen`, and `git diff --check` is clean. No experiments, rebuild, external model calls, public/user repository edits, CI, push, tag, release, or publication occurred.
