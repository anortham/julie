---
id: checkpoint_210be617
timestamp: 2026-09-13T11:44:03.858Z
tags:
  - revival
  - plan-4
  - native-qualification
  - ci
  - tdd
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 4 native qualification workflow ready to commit
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
impact: Unblocks nonpublishing macOS and Windows qualification from an isolated branch.
evidence:
  - Exact workflow contract RED observed
  - Exact workflow contract GREEN passed, run ID 23ae67ab
  - Worker reports only .github/workflows/native-qualification.yml and
    xtask/tests/toolchain_contract_tests.rs dirty in fix/revival-deployment
next: Commit the two scoped files locally, review the diff, push HEAD to
  qualification/revival-deployment, and monitor the artifact-only workflow.
---

## Plan 4 native qualification workflow ready to commit

### WHAT

The Plan 4 worktree now has a nonpublishing native qualification matrix covering macOS arm64, macOS Intel, and Windows x64. It accepts pushes only under `qualification/**` plus manual dispatch, packages and verifies each native archive, probes the packaged runtime lifecycle in isolated homes, and preserves the Windows executable-lock test.

### WHY

The prior artifact-only workflow covered Windows only and could not run from an unmerged branch through `workflow_dispatch`. The remaining native qualification rows require actual GitHub-hosted macOS and Windows execution without tagging or releasing.

### HOW

A narrow workflow contract test was added first and failed against the Windows-only workflow. The workflow was then generalized to a three-target matrix with branch-push triggering and artifact upload only. The exact contract test is green.

### IMPACT

This enables the minimum authorized remote branch push to collect native evidence without publication. No release, tag, merge, or live-profile installation is involved.
