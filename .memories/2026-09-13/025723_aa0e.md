---
id: checkpoint_aa0ef880
timestamp: 2026-09-13T02:57:23.124Z
tags:
  - plan-5
  - qualification
  - runner-contract
  - tdd
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 5B runtime-status and artifact-path correction
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - "RED: eval_accepts_current_embedding_child_status_shape failed against
    retired status/dims lookup."
  - "GREEN: cargo nextest run --lib
    eval_accepts_current_embedding_child_status_shape passed."
  - "RED: eval_validates_only_supplied_evaluation_artifact_paths failed against
    ignored supplied overrides."
  - "GREEN: cargo nextest run --lib
    eval_validates_only_supplied_evaluation_artifact_paths passed."
  - All 10 exact eval_contract tests passed serially.
  - run_matrix.py --self-check passed; 52-row --validate passed; py_compile,
    cargo fmt --check, and git diff --check passed.
next: Commit the scoped Plan 5B correction locally, then report 5C's
  integrated-candidate blocker.
---

## Plan 5B runtime-status and artifact-path correction

### WHAT
- Added `eval_accepts_current_embedding_child_status_shape`, covering the service's current `embedding_child` contract: `state`, `model_id`, `dimensions`, and `executable_sha256`; retired `status` and `dims` fields are rejected.
- Added `eval_validates_only_supplied_evaluation_artifact_paths`, proving an unrelated corpus `docs/eval/` is allowed while supplied cases, sibling agent-task, and output paths inside the corpus fail with their actual paths.
- Changed artifact validation errors to identify the supplied artifact path instead of always naming `docs/eval`.

### WHY
The previous commit `6df08baeed18c35c369dfd0b8f2383c79b16aac1` corrected production behavior but lacked direct regression proof for the actual service status JSON and override-path isolation semantics.

### HOW
Temporarily reversed only the two 6df production hunks, added the two tests, observed each fail for the absent behavior, then restored the current service-field contract and supplied-artifact validation. Existing exact `eval_contract` tests were rerun serially after the shared helper change.

### IMPACT
Plan 5B runner correctness now has local evidence for the real semantic readiness shape and isolated evaluation artifacts. 5C remains blocked on the integrated Plans 1–4 release candidate; no external model, product, service, or user-repository experiment ran.
