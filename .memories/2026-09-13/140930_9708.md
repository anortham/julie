---
id: checkpoint_9708fc87
timestamp: 2026-09-13T14:09:30.843Z
tags:
  - revival
  - plan-4
  - native-qualification
  - blocked
  - evidence
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Final authorized native run recorded
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: The owner-authorized final native matrix run 34760490971 completed with
  failure on all three targets after archive verification.
evidence:
  - Run 34760490971 completed failure at head SHA
    8c84e683977b3538e22aebd7f7cf94b2c807edad
  - All three Build candidate archive and Verify archive steps passed
  - macOS arm64 and Intel semantic responses were auto/lexical fallback
  - Windows model preparation failed with HTTP 429 before search
  - git diff --check passes for the two Plan 4 evidence files
next: Commit the two evidence docs plus this checkpoint locally, update the
  active brief, and stop at the authorization boundary.
---

## Final authorized native run recorded

### WHAT

Plan 4 evidence now records terminal run `34760490971` at pushed qualification SHA `8c84e683`. Every native target built and archive-verified, but none completed the remaining qualification steps.

### WHY

The plan must distinguish proven package construction from failed semantics, skipped lifecycle checks, and unretained artifacts. It must also stop Plan 5C until Plans 1-4 have an integrated qualified candidate.

### HOW

The macOS arm64 and Intel logs show search responses with `mode=auto`, `backend=lexical`, and `backend_fallback=true`. Windows stopped earlier when online model preparation received HTTP 429 from Hugging Face. Lifecycle, Windows lock, and upload steps were skipped on all jobs. Local fixes `ce574773` and `3115c6c8` have exact local verification, but no further hosted run is authorized.

### IMPACT

The previous `055645e` full-gated candidate is now historical because product code changed. Native qualification needs a new frozen-tree full gate and separate authorization for another hosted run. Plan 5C remains blocked; no result is fabricated.
