---
id: checkpoint_b600c60e
timestamp: 2026-09-13T14:17:07.726Z
tags:
  - revival
  - plan-4
  - full-gate
  - docs-contract
  - tdd
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Qualification source identity restored after full-gate failure
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: The new frozen-tree full gate failed its dev tier because terminal
  evidence edits dropped two source-identity labels required by the native
  qualification contract test.
evidence:
  - "Initial cargo xtask test full: failed 1/5 commands at
    native_qualification_workflow_is_nonpublishing_and_exercises_windows_lock"
  - "cargo nextest run -p xtask
    native_qualification_workflow_is_nonpublishing_and_exercises_windows_lock: 1
    passed"
  - "git diff --check: exit 0"
next: Commit the one docs fix plus checkpoint locally, then run the single
  allowed full-gate retry.
---

## Qualification source identity restored after full-gate failure

### WHAT

The candidate record again names `Archive binary source SHA` and `Package wrapper and verifier source SHA`, using the exact historical source identities `055645e02ebf261355c7db6659a9fe1b5159a791` and `a90720f010283270123c9679729fbf8f4fd5004f`.

### WHY

The terminal evidence rewrite preserved checksums but removed two explicit provenance rows. `cargo xtask test full` stopped in the dev tier at `native_qualification_workflow_is_nonpublishing_and_exercises_windows_lock`.

### HOW

The lead classified the failure as caused by the current docs diff. The worker restored only the two required rows. The exact failing test then passed 1/1 and `git diff --check` passed.

### IMPACT

The docs again satisfy the executable provenance contract. One full-gate retry remains and will run after this local commit.
