---
id: checkpoint_17d655f4
timestamp: 2026-09-13T16:16:37.535Z
tags:
  - revival
  - plan-4
  - windows
  - ntfs
  - service-stop
  - tdd
git:
  branch: main
  commit: d9aaba2d
  files:
    - .codex/config.toml
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Windows lock probe waits for discovery removal
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: After closing the shim first, local win-test still observed
  service.json immediately after service stop; the same path was absent on a
  read-only check seconds later.
evidence:
  - "win-test after cleanup reorder: immediate discovery-file assertion failed"
  - "read-only follow-up: exact service.json path was absent"
  - "Focused xtask contract RED then GREEN: 1 passed"
  - "git diff --check: exit 0"
symbols:
  - test-windows-executable-lock.ps1 finally
  - native_qualification_workflow_is_nonpublishing_and_exercises_windows_lock
next: Commit the two-file polling fix plus checkpoint, sync to win-test, and
  rerun the packaged lock test.
---

## Windows lock probe waits for discovery removal

### WHAT

The Windows executable-lock probe now polls `service.json` every 100ms for up to five seconds after a successful service stop before declaring cleanup failure.

### WHY

Windows exposed a short removal delay. The immediate assertion failed even though the exact discovery path disappeared moments later and no service remained.

### HOW

A focused contract first failed because the bounded deadline was absent. The minimal fix adds a condition-based wait and retains the original final assertion. It does not mask a persistent file: the probe still fails after five seconds.

### IMPACT

The NTFS qualification distinguishes transient file removal latency from a leaked service record. Lead will sync and rerun the exact packaged lock probe locally.
