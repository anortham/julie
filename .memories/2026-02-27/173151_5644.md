---
id: checkpoint_564461aa
timestamp: 2026-02-27T17:31:51.178Z
tags:
  - task-6
  - embeddings
  - ort
  - directml
  - candle
  - feature-gating
  - tdd
git:
  branch: main
  commit: 6719b0d
  files:
    - .memories/2026-02-27/152305_e9d8.md
    - .memories/2026-02-27/154714_e9be.md
    - Cargo.lock
    - TODO.md
    - docs/plans/2026-02-27-candle-mps-and-directml-design.md
    - docs/plans/2026-02-27-candle-mps-and-directml-implementation.md
summary: "## Task 6 completed: finalized Cargo feature wiring for ORT + Candle"
planId: embeddings-hardening-and-backend-foundation
type: checkpoint
impact: DirectML feature wiring is now explicit and validated; requested matrix
  and targeted dependency tests are green.
evidence:
  - "RED: cargo tree -e features --features embeddings-ort -i ort (before) -> no
    directml feature in ort tree"
  - 'GREEN: cargo tree -e features --features embeddings-ort -i ort (after) ->
    ort feature "directml" present'
  - cargo check --features embeddings-ort -> PASS
  - cargo check --features embeddings-candle -> PASS
  - cargo check --features "embeddings-ort embeddings-candle" -> PASS
  - cargo test --lib embedding_deps -> PASS
  - cargo test --lib embedding_deps --features embeddings-candle -> PASS
symbols:
  - Cargo.toml:[features].embeddings-ort
  - Cargo.toml:[dependencies].ort
  - src/tests/core/embedding_deps.rs::test_fastembed_single_and_batch_embedding
  - src/tests/core/embedding_deps.rs::test_windows_ort_policy_mentions_directml
  - src/tests/core/embedding_deps.rs::test_candle_feature_can_be_enabled
confidence: 5
---

## Task 6 completed: finalized Cargo feature wiring for ORT + Candle

### WHAT
- Updated `Cargo.toml` feature wiring so `embeddings-ort` now activates:
  - `dep:fastembed`
  - `dep:ort`
  - `ort/directml`
- Added explicit optional `ort` dependency in `Cargo.toml`.
- Updated `src/tests/core/embedding_deps.rs` to better reflect feature gating:
  - gated fastembed smoke test behind `#[cfg(feature = "embeddings-ort")]`
  - added Candle feature flag smoke test behind `#[cfg(feature = "embeddings-candle")]`
  - added ORT Windows policy assertion for DirectML+CPU ordering.

### WHY
- ORT DirectML registration requires enabling an ORT feature (`directml` or `load-dynamic`), and previous feature graph did not include `directml` under Julie’s ORT feature path.
- Dependency smoke tests should compile under different backend feature combinations without unconditionally referencing ORT-only crates.

### HOW
- Followed RED/GREEN style verification:
  - RED evidence before change: `cargo tree -e features --features embeddings-ort -i ort` showed no `ort feature "directml"`.
  - GREEN after change: same command shows `ort feature "directml"` enabled via `julie feature "embeddings-ort"`.
- Ran requested feature matrix checks and embedding_deps tests; all succeeded.

### IMPACT
- ORT builds now explicitly include DirectML support wiring needed for Windows execution-provider policy.
- Feature/dependency gating is cleaner for ORT-vs-Candle test code paths.
- Default behavior remains ORT-enabled (`default = ["embeddings-ort"]`).
