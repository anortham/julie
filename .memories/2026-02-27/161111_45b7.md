---
id: checkpoint_45b770cc
timestamp: 2026-02-27T16:11:11.713Z
tags:
  - embeddings
  - backend-resolver
  - tdd
  - auto-policy
  - candle
  - ort
git:
  branch: main
  commit: 6719b0d
  files:
    - .memories/2026-02-27/152305_e9d8.md
    - .memories/2026-02-27/154714_e9be.md
    - Cargo.lock
    - TODO.md
    - docs/plans/2026-02-27-candle-mps-and-directml-design.md
    - docs/plans/2026-02-27-candle-mps-and-directml-implementation.md
summary: "## Implemented Task 2 backend resolver with TDD"
planId: embeddings-hardening-and-backend-foundation
type: checkpoint
impact: Task 2 policy is now deterministic and test-covered; backend selection
  decisions are centralized and produce clear availability errors instead of
  implicit fallback assumptions.
symbols:
  - BackendResolverCapabilities
  - resolve_backend_preference
  - EmbeddingProviderFactory::create
  - JulieWorkspace::initialize_embedding_provider
confidence: 5
---

## Implemented Task 2 backend resolver with TDD

- Added deterministic backend resolver logic in `src/embeddings/factory.rs`.
- Added capability model (`BackendResolverCapabilities`) and pure helper (`resolve_backend_preference`) to resolve `auto|ort|candle` against platform + backend availability.
- Wired factory creation through resolver so `auto` now resolves via explicit policy before provider construction.
- Updated workspace embedding init path to use the same resolver and emit unresolved runtime status when resolution fails.
- Added focused resolver tests in `src/tests/core/embedding_provider.rs` for:
  - auto -> Candle on Apple Silicon (`macos` + `aarch64`)
  - auto -> ORT elsewhere
  - explicit override behavior
  - clear errors for unavailable resolved backend and unavailable explicit backend

### TDD evidence
- First test run failed at compile-time due to missing resolver symbols (`E0432` unresolved imports).
- Implemented minimal resolver + exports + wiring.
- Re-ran targeted resolver tests; all new resolver tests now pass.
