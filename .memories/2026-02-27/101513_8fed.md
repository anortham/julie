---
id: checkpoint_8fed97c5
timestamp: 2026-02-27T10:15:13.052Z
tags:
  - semantic
  - bug-fix
  - hybrid-search
  - reference-workspace
  - get-context
  - fast-search
git:
  branch: main
  commit: 96bf51d
  files:
    - .memories/2026-02-27/100524_7844.md
    - Cargo.lock
    - src/tools/get_context/pipeline.rs
    - src/tools/search/text_search.rs
summary: "## Fixed 3 Semantic Pipeline Bugs"
planId: dogfood-bug-fixes-fast-search-hybrid-rust-extracto
type: checkpoint
impact: All three semantic pipeline bugs fixed. Primary workspace NL searches
  now get centrality-boosted hybrid results. Reference workspace get_context and
  fast_search now use embeddings. Unblocks dogfood verification.
symbols:
  - text_search_impl
  - run_pipeline
  - hybrid_search
  - apply_centrality_boost
  - promote_exact_name_matches
  - embedding_provider
  - ref_embedding_provider
next: "Rebuild release and live-test with dogfood queries: fast_search('how are
  search results ranked', definitions), get_context('retry logic',
  workspace=coa), fast_search('caching layer', definitions, workspace=coa)"
confidence: 4
---

## Fixed 3 Semantic Pipeline Bugs

### Bug 1: Hybrid path missing post-processing (text_search.rs:263-289)
- **Problem:** `hybrid_search()` → `Vec<Symbol>` skipped centrality boost, exact-name promotion, and truncation
- **Fix:** Added `apply_centrality_boost()`, `promote_exact_name_matches()`, and `truncate(limit)` after hybrid_search returns SymbolSearchResults, before converting to Vec<Symbol>

### Bug 2: get_context reference workspace passes None for embeddings (pipeline.rs:457)
- **Problem:** `run_pipeline(..., None)` for reference workspaces — 5K+ vectors completely ignored
- **Fix:** Clone `workspace.embedding_provider` (cheap Arc clone) and pass as `embedding_provider.as_deref()`

### Bug 3: fast_search reference workspace skips hybrid search (text_search.rs:88-176)
- **Problem:** Reference workspace definition path used keyword-only search, no NL detection
- **Fix:** Added `is_nl_like_query` check + `ref_embedding_provider` threading into spawn_blocking closure; NL queries now route through `hybrid_search()` with full post-processing

### Verification
- Clean compile, 763 tests pass, 0 failures
