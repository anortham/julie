---
id: checkpoint_2e8071aa
timestamp: 2026-02-27T17:34:23.577Z
tags:
  - review
  - task-6
  - embeddings
  - candle
  - directml
  - feature-matrix
git:
  branch: main
  commit: 6719b0d
  files:
    - .memories/2026-02-27/152305_e9d8.md
    - .memories/2026-02-27/154714_e9be.md
    - Cargo.lock
    - TODO.md
    - docs/plans/2026-02-27-candle-mps-and-directml-design.md
    - docs/plans/2026-02-27-candle-mps-and-directml-implementation.md
summary: "## Task 6 compliance review executed with fresh verification"
planId: embeddings-hardening-and-backend-foundation
type: checkpoint
impact: Produced requirement-by-requirement compliance assessment with fresh
  command evidence; identified a candle-only test-build gating gap outside
  embedding_deps.
evidence:
  - cargo tree -e features --features embeddings-ort -i ort (shows ort feature
    directml)
  - cargo check (default) PASS
  - cargo check --features embeddings-ort PASS
  - cargo check --no-default-features --features embeddings-candle PASS
  - cargo check --features "embeddings-ort embeddings-candle" PASS
  - cargo test --lib embedding_deps PASS
  - cargo test --lib embedding_deps --features embeddings-candle PASS
  - cargo test --lib embedding_deps --no-default-features --features
    embeddings-candle FAIL (unresolved OrtEmbeddingProvider imports in unrelated
    tests)
symbols:
  - Cargo.toml:[features].embeddings-ort
  - src/embeddings/mod.rs
  - src/embeddings/factory.rs
  - src/embeddings/ort_provider.rs
  - src/tests/core/embedding_deps.rs
  - src/workspace/mod.rs
next: If desired, fix candle-only lib-test compile gating by feature-guarding
  ORT imports/usages in remaining test modules (e.g., search_quality and
  integration embedding tests).
confidence: 4
---

## Task 6 compliance review executed with fresh verification

### WHAT
- Reviewed Task 6-related implementation and test wiring in:
  - `Cargo.toml`
  - `src/embeddings/mod.rs`
  - `src/embeddings/factory.rs`
  - `src/embeddings/ort_provider.rs`
  - `src/workspace/mod.rs`
  - `src/tests/core/embedding_deps.rs`
- Ran requested feature/build matrix checks and embedding dependency tests.

### WHY
- User requested strict spec-compliance audit for Task 6 focused on TDD evidence, feature gating hygiene, DirectML enablement, defaults, and test updates.

### HOW
- Validated feature graph for ORT via `cargo tree` and confirmed `ort/directml` activation under `embeddings-ort`.
- Ran matrix checks:
  - default build
  - ORT build
  - candle-only build (`--no-default-features --features embeddings-candle`)
  - ORT + Candle build
- Ran relevant `embedding_deps` tests in default and candle-enabled modes.
- Probed stricter candle-only `embedding_deps` test run and captured unresolved ORT-import errors in unrelated test modules.

### IMPACT
- Confirmed core Task 6 wiring appears correct for DirectML and default ORT behavior.
- Found a real gating cleanliness gap for candle-only **lib test** builds due ungated ORT imports in other test files, which is relevant risk for feature-isolation claims.
