---
id: checkpoint_7844407a
timestamp: 2026-02-27T10:05:24.461Z
tags:
  - semantic
  - quality-audit
  - dogfood
  - bugs
  - hybrid-search
  - reference-workspace
git:
  branch: main
  commit: 96bf51d
  files:
    - Cargo.lock
summary: "## Semantic Pipeline Quality Audit — Complete"
planId: dogfood-bug-fixes-fast-search-hybrid-rust-extracto
type: incident
context: User asked to test semantic pipeline quality across primary and
  reference workspaces to verify it's living up to the reasons it was added
impact: "Identified 3 bugs: hybrid path missing post-processing, reference
  workspace embeddings wasted, docs/plans pollution. deep_dive and get_context
  technical queries are the successful integration points."
evidence:
  - deep_dive CodeTokenizer → TokenizerConfig (0.48), LanguageConfig (0.39)
  - get_context 'how are search results ranked' → calculate_score,
    apply_important_patterns_boost, calculate_search_confidence (all relevant)
  - get_context 'find symbols similar with vectors' → find_symbols_by_name,
    store_symbols (keyword noise, not knn_search)
  - fast_search 'how are search results ranked' definitions → 8/10 docs/plans,
    2/10 code
  - fast_search ref workspace 'caching layer' → 1 result from prompt file
  - "Confirmed: text_search.rs:265 passes None for ref workspace,
    pipeline.rs:465 passes None"
symbols:
  - text_search_impl
  - run_pipeline
  - hybrid_search
  - is_nl_like_query
  - apply_centrality_boost
  - promote_exact_name_matches
  - EmbeddingProvider
next: Fix fast_search hybrid post-processing gap (add centrality + name
  promotion). Wire embedding provider for reference workspaces. Consider
  filtering docs/plans from definition search.
---

## Semantic Pipeline Quality Audit — Complete

Live-tested all semantic integration points across 3 workspaces (primary julie, ref miller, ref coa).

### Quality Scorecard
- **deep_dive semantic similarity: A** — `CodeTokenizer` → `TokenizerConfig` (0.48), perfect
- **get_context hybrid (primary, technical NL): A** — "how are results ranked" → 3 scoring functions
- **get_context hybrid (primary, vague NL): C-** — "find similar symbols with vectors" → `find_symbols_by_name` (keyword noise)
- **fast_search hybrid (primary, NL defs): C-** — missing centrality + name promotion post-processing
- **Reference workspace semantic: F** — 5,327 vectors indexed but never queried (None passed)
- **fast_search content mode: N/A** — no semantic by design

### Bugs Found
1. **Hybrid path missing post-processing** in `text_search.rs:273-281` — skips `apply_centrality_boost` and `promote_exact_name_matches`
2. **Reference workspace embeddings wasted** — `get_context/pipeline.rs:465` passes `None`, `text_search.rs:87-94` uses direct `search_symbols`
3. **Docs/plans pollute NL definition search** — markdown headers indexed as Module symbols compete with code
