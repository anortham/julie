---
id: checkpoint_67b517db
timestamp: 2026-05-17T12:16:23.773Z
tags:
  - pre-merge-review
  - daemon-split
  - reranker
  - review-fixes
  - ready-to-merge
git:
  branch: feat/daemon-split-and-reranker
  commit: 97aa8400
  files:
    - Cargo.lock
    - Cargo.toml
    - src/adapter/http_stdio.rs
    - src/adapter/launcher.rs
    - src/adapter/mod.rs
    - src/daemon/app.rs
    - src/daemon/app/handle.rs
    - src/daemon/app/helpers.rs
    - src/daemon/app/runtime.rs
    - src/daemon/cli.rs
    - src/daemon/connection_pool.rs
    - src/daemon/discovery.rs
    - src/daemon/http_transport.rs
    - src/daemon/legacy_migration.rs
    - src/daemon/lifecycle.rs
    - src/daemon/mcp_session.rs
    - src/daemon/mod.rs
    - src/daemon/shutdown.rs
    - src/daemon/token_file.rs
    - src/daemon/watcher_pool.rs
    - src/daemon/workspace_pool.rs
    - src/database/mod.rs
    - src/handler.rs
    - src/health/data_plane.rs
    - src/paths.rs
    - src/search/hybrid.rs
    - src/search/index.rs
    - src/search/query_parse.rs
    - src/search/reranker.rs
    - src/search/scoring.rs
summary: "Pre-merge review fixes complete: daemon-split + reranker"
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo xtask test changed: 32/32 PASS in 495s"
  - "cargo xtask test dev: 32/32 PASS in 477s"
  - "cargo xtask test dogfood: 2/2 PASS in 252s"
  - "reranker_ordering_tests: 8/8 PASS"
  - "reranker_tests: 18/18 PASS"
  - "c3_enriched_schema_tests: 21/21 PASS"
next: Commit changes; merge feat/daemon-split-and-reranker → main locally; user
  builds release and dogfoods.
confidence: 5
---

## Pre-merge review fixes complete: daemon-split + reranker

Addressed all 11 findings from second pre-merge review (after Codex's adversarial pass). User said "we're not here to look for the easy way out" — every finding fixed, none deferred.

### Daemon (4 fixes)
- **C1**: `text_search.rs:148` primary fast_search now uses pooled DB
- **C2**: `concurrent_mcp.rs` deadlock detector now spawns FastSearchTool (5 tasks, was 4)
- **I2**: `DaemonHandle::Drop` removes token file for symmetry with graceful shutdown
- **I4**: Pool rollback-failure explicitly logs capacity shrink

### Reranker (7 fixes)
- **C1**: `SymbolSearchResult` carries `role/test_role/capability_flags`; KNN re-derives. ~20 test construction sites updated.
- **C2**: RRF base rescaled 200x in hybrid path so BM25/RRF live in same range when reranker boosts layer on. `rrf_merge` contract unchanged.
- **C3**: `reranker_ordering_tests.rs` adds 8 "X must rank above Y" tests covering vendor demotion, intent kind, I4 downgrade, test intent, phrase boost, base-score retention.
- **I1**: `VENDOR_PENALTY = GENERATED_PENALTY = 130`. Math justified: vendor exact full-stack 210, source partial 100, penalty 130 flips ordering while leaving vendor exact +80 net vs no-match.
- **I2**: `score_symbol` pre-lowercases title/path/body once. Eliminates ~40k String allocs/query on default-on with large definition over-fetch.
- **I3**: Split into `rerank_symbol_score` + `rerank_content_score`. Asymmetry now type-visible.
- **I4**: Two-pass intent downgrade in `rerank()` AND mirrored in `text_search.rs::effective_intent_query` for in-place rescorer.

### Verification
- `cargo xtask test changed`: 32 buckets PASS (495s)
- `cargo xtask test dev`: 32 buckets PASS (477s)
- `cargo xtask test dogfood`: 2 buckets PASS (252s, search_quality regression guard green)
- `reranker_ordering_tests`: 8/8 PASS
- `reranker_tests`: 18/18 PASS (arithmetic regression guard)
- `c3_enriched_schema_tests`: 21/21 PASS

### Still needs PR description reconciliation
Daemon I1/I3 are description-only — the "3 deferred sites" list and "workspace.db probe" claims don't match the code. Code is correct.

### Next
Commit, merge to main locally, dogfood the build.
