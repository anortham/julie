---
id: checkpoint_7f78d4e9
timestamp: 2026-05-06T12:12:08.938Z
tags:
  - release
  - v7.7.3
  - extractors
  - tree-sitter
git:
  branch: main
  commit: fa5e3000
  files:
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - docs/site/index.html
summary: Release v7.7.3 - Tree-sitter Review Findings Addressed
briefId: julie-world-class-systems-program
type: checkpoint
impact: Closes the 18-item tree-sitter review punch list. Prepares v7.7.3
  release with extractor quality improvements, language-agnostic scoring fixes,
  and three structural cleanups (file-size limits, helper consolidation,
  capability-matrix invariant extension).
evidence:
  - api_surface 2/2 pass
  - capability_matrix 5/5 pass
  - css 3/3 pass
  - markdown 37/37 pass
  - vue 44/44 pass
  - namespace 9/9 pass
  - cargo build OK on v7.7.3
next: Wait for `cargo xtask test dev` to pass, then commit version bump, tag
  v7.7.3, push, and write substantive release notes.
---

## Release v7.7.3 - Tree-sitter Review Findings Addressed

Bumped version to 7.7.3 after addressing all 18 findings from the tree-sitter extractor review (committed in fa5e3000 as `fix(extractors): address tree-sitter review findings`).

### What Changed

**Critical fixes (all verified):**
- **Vue parity**: `registry.rs` now derives `pending_relationships` from structured list. `api_surface.rs` parity test extended with Vue case. `expected.json` updated.
- **Cross-language scoring leak**: `namespace.rs` gates `std`/`core`/`alloc` resolution behind a Rust language check on both file path and candidate language.
- **CSS animation regex**: narrowed to `animation-name` only (no shorthand). Added `parse_animation_names` for comma-separated list and `strip_css_comments` for block-comment skipping.

**Important fixes:**
- Markdown self-reference filter (`source.id == target.id` early return)
- YAML anchor exact equality match via `yaml_anchor` metadata
- Markdown duplicate-slug HashMap → first-wins via `or_insert` (deterministic)
- `language_spec.rs` 550-line violation fixed by splitting into `mod.rs` + `specs.rs`
- `mod_tests.rs` 3298-line aggregate split into per-language `tests/<lang>/{mod,cross_file_relationships}.rs`
- QML synthetic IDs replaced with real symbol lookups via `find_property_target`
- `rust_reexports.rs` workspace anchoring tightened so `vendor/` and `examples/` no longer match
- `capability_matrix_requires_relationship_fixture_evidence` now calls `assert_fixture_pending_parity` per fixture

**Minor fixes:**
- Debug `println!` removed from bash/zig cross_file tests
- Rust `//!` and `/*!` doc comments now recognized via `RustInnerLine`/`RustInnerBlock`
- Vue `node_text` adds bounds check + `String::from_utf8_lossy`
- Macro-generated `parse_diagnostics` now consistently indented
- Lua `LuaBlock` ordered before `LuaDoubleDash` for discriminator-correctness
- `containing_symbol_at_line` consolidated as `pub(crate)` in `base/mod.rs`

### Verification

- All targeted regression tests pass: `api_surface` (2/2), `capability_matrix` (5/5), CSS relationships (3/3 — including `css_relationships_do_not_link_animation_shorthand_var_function_name`), markdown (37/37 — including `markdown_relationships_do_not_emit_self_reference_edges` and `markdown_relationships_duplicate_slugs_use_first_heading_deterministically`), vue (44/44), namespace resolver (9/9 — including `test_resolve_structured_batch_does_not_apply_rust_std_namespace_to_typescript`).
- `cargo build` against v7.7.3 succeeds.
- `cargo xtask test dev` running as final batch gate before commit.

### Files Touched

- `Cargo.toml`: 7.7.2 → 7.7.3
- `Cargo.lock`: refreshed via `cargo build`
- `docs/site/index.html:805`: footer version
- `CLAUDE.md:461`, `AGENTS.md:461`: status line + last-updated date

### Next

Commit, tag `v7.7.3`, push branch + tag, replace generic GitHub release notes with substantive content covering the extractor quality fixes and verification evidence.
