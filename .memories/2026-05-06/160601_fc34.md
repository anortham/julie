---
id: checkpoint_fc34877d
timestamp: 2026-05-06T16:06:01.328Z
tags:
  - tree-sitter-audit
  - ruby
  - rspec
  - extractors
git:
  branch: codex/tree-sitter-extractor-audit-remediation
  commit: adf3f73d
  files:
    - crates/julie-extractors/src/ruby/calls.rs
    - crates/julie-extractors/src/tests/ruby/mod.rs
    - crates/julie-extractors/src/tests/ruby/rspec.rs
summary: Added Ruby RSpec block extraction
briefId: julie-world-class-systems-program
type: checkpoint
evidence:
  - "cargo nextest run -p julie-extractors --lib
    test_ruby_rspec_blocks_are_marked_as_tests 2>&1 | tail -60: passed"
  - "cargo nextest run -p julie-extractors --lib
    test_ruby_attr_accessor_emits_all_property_symbols_with_class_parent 2>&1 |
    tail -30: passed"
  - "cargo nextest run -p julie-extractors --lib
    test_cross_file_inheritance_emits_structured_pending_relationship 2>&1 |
    tail -30: passed"
  - "cargo nextest run -p julie-extractors --lib
    test_cross_file_include_and_extend_emit_structured_pending_relationships
    2>&1 | tail -30: passed"
  - "cargo xtask test changed 2>&1 | tail -100: passed extractor bucket"
symbols:
  - extract_call
  - extract_rspec_block
  - extract_first_rspec_argument
next: Commit the RSpec addition, then run the batch-level dev gate before moving
  to the next remediation lane.
confidence: 5
---

## Added Ruby RSpec block extraction

- **WHAT:** Ruby extraction now emits symbols for RSpec-style `describe` and `context` containers plus `it` and `specify` examples, with `test_container` and `is_test` metadata.
- **WHY:** The approved Ruby task included RSpec block detection; previously these call-style tests were not symbols at all, so test metadata could never be attached.
- **HOW:** Added Ruby-specific special-call handling that mirrors the JS/TS call-style test extraction pattern while preserving Ruby parent nesting through the existing traversal.
- **IMPACT:** RSpec examples are visible as test symbols, nested under their containers, without changing ordinary Ruby `def test_*` detection.
