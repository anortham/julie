---
id: checkpoint_ea91316b
timestamp: 2026-03-06T20:39:04.163Z
tags: release, v3.9.0, code-review, bug-fix, workspace-path, agent-instructions,
  embeddings
git:
  branch: main
  commit: a3e9f46
summary: "# v3.9.0 Release — Code Review Follow-up Fixes"
planId: sidecar-binary-distribution
type: checkpoint
impact: Workspace path resolution, agent instructions, and embedding noise all
  fixed. Clean release.
symbols: resolve_workspace_path, load_agent_instructions, handle_index_command,
  initialize_embedding_provider
---

# v3.9.0 Release — Code Review Follow-up Fixes

## What
Fixed three issues from GPT code review follow-up, bumped to v3.9.0, tagged and pushed.

### P1: Workspace path resolution
- **`resolve_workspace_path`** now accepts `handler_root: Option<&Path>` — priority: explicit path > handler root > env var > cwd
- **`handle_index_command`** simplified from 20-line inline fallback to 5 lines using `handler.workspace_root` directly
- Auto-indexing via `--workspace` CLI flag now works end-to-end

### P2a: Agent instructions embedded at compile time
- `load_agent_instructions()` uses `include_str!("../JULIE_AGENT_INSTRUCTIONS.md")` instead of reading from workspace root
- Instructions always available regardless of which repo Julie indexes

### P2b: Embedding disable support
- `JULIE_EMBEDDING_PROVIDER=none|disabled|off` skips initialization entirely
- Eliminates sidecar/HuggingFace noise in CI, tests, and offline environments
- Early return in `initialize_embedding_provider` before any backend resolution

### P3: Version drift cleanup
- README: v1.5.0 → v3.9.0 artifact references
- CLAUDE.md: v3.7.0 → v3.9.0
- Removed stale `network_models` cfg_attr in tracing tests

## Impact
- 910 tests passing, 0 failures
- Tagged v3.9.0, pushed to origin
