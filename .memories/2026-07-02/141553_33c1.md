---
id: checkpoint_33c1b996
timestamp: 2026-07-02T14:15:53.093Z
tags:
  - todo-closeout
  - test-fix
  - doctest
  - edit-file
  - workspace-tests
git:
  branch: main
  commit: d225b6a5
  files:
    - .codex/config.toml
    - crates/julie-core/src/string_similarity.rs
    - crates/julie-index/src/search/reranker.rs
    - crates/julie-tools/src/editing/edit_file.rs
    - src/tests/integration/concurrent_mcp.rs
    - src/tests/tools/editing/edit_file_tests.rs
    - src/tests/tools/workspace/global_targeting/global_remove.rs
    - src/utils/paths.rs
summary: Fixed TODO red tests and doctest failures
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Closes the TODO red-test item and hardens several parallel test
  isolation/doc examples.
evidence:
  - cargo test --lib tests::tools::editing::edit_file_tests::test_edit_file --
    --test-threads=16 passed
  - cargo test --lib
    tests::integration::concurrent_mcp::tests::test_concurrent_mcp_requests_do_not_wedge
    -- --exact passed
  - cargo test -p julie-core --doc passed
  - cargo test -p julie-index --doc passed
  - cargo test --lib tests::tools::workspace::global_targeting::global_remove --
    --test-threads=16 passed
  - cargo test --workspace passed after final cleanup
symbols:
  - edit_file::set_before_commit_hook_for_test
  - edit_file::run_before_commit_hook_for_test
  - test_concurrent_mcp_requests_do_not_wedge
  - test_remove_workspace_uses_global_index_dir_shape
  - test_remove_current_primary_workspace_is_blocked_in_process
confidence: 5
unknowns:
  - No remaining known test failures from this task.
---

## Fixed TODO red tests and doctest failures

### WHAT
- Removed the temporary `TODO.md` red-test entry by fixing the failures it described.
- Fixed the `to_absolute_native` doctest so it compiles as a real example.
- Scoped the `edit_file` test-only before-commit hook by expected path so unrelated parallel edit-file tests cannot consume it.
- Increased the concurrent MCP watcher observation timeout from 10s to 30s for the sentinel/indexing proof-of-life waits.
- Fixed doctest imports in `julie-core` string similarity examples and marked the `julie-index` reranker scoring formula as `text`.
- Added `home_env` serial groups to global-remove workspace tests that mutate `HOME`.

### WHY
`cargo test --workspace` exposed the TODO failures plus additional doctest and isolation failures. The edit-file failures were caused by a global one-shot test hook being consumed by the wrong parallel test. The concurrent MCP failure was a too-tight watcher observation timeout under full-suite load. The global-remove failure was a HOME environment race with other tests.

### HOW
- Path-scoped the test hook in `crates/julie-tools/src/editing/edit_file.rs` and updated the commit-hook test to register the canonical target path.
- Kept the concurrent workload timeout unchanged but gave watcher observation up to 30s.
- Converted non-Rust doctest examples/formula blocks to valid doctest-safe forms.
- Serialized HOME-mutating global-remove tests with the existing `home_env` group.
- Reverted accidental broad `cargo fmt` churn and kept only the intended files.

### IMPACT
`cargo test --workspace` now passes against the final tree. The original TODO item is closed and `TODO.md` is no longer modified.
