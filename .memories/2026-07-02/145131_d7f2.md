---
id: checkpoint_d7f2b8bf
timestamp: 2026-07-02T14:51:31.129Z
tags:
  - release
  - v7.15.3
  - test-stability
git:
  branch: main
  commit: d225b6a5
  files:
    - .codex/config.toml
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/src/string_similarity.rs
    - crates/julie-index/src/search/reranker.rs
    - crates/julie-tools/src/editing/edit_file.rs
    - docs/release-notes/v7.15.3.md
    - docs/site/index.html
    - src/tests/integration/concurrent_mcp.rs
    - src/tests/tools/editing/edit_file_tests.rs
    - src/tests/tools/workspace/global_targeting/global_remove.rs
    - src/utils/paths.rs
summary: Prepared Julie v7.15.3 release
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
context: Preparing Julie v7.15.3 patch release after fixing recent TODO.md test
  failures and stabilizing the workspace test suite.
evidence:
  - cargo test --lib tests::tools::editing::edit_file_tests::test_edit_file --
    --test-threads=16 passed
  - cargo test --lib
    tests::integration::concurrent_mcp::tests::test_concurrent_mcp_requests_do_not_wedge
    -- --exact passed
  - cargo test -p julie-core --doc passed
  - cargo test -p julie-index --doc passed
  - cargo test --lib tests::tools::workspace::global_targeting::global_remove --
    --test-threads=16 passed
  - cargo check passed for julie v7.15.3
  - "cargo xtask test changed passed: 37 buckets in 924.7s"
  - cargo test --workspace passed on final v7.15.3 tree
  - cargo build --release --bin julie-server --bin julie-embedding-host passed
  - target/release/julie-server --version -> julie-server 7.15.3
unknowns:
  - The shared MCP registry/index drift is local-state related and not fixed in
    this patch release; it should be tracked separately if it recurs.
---

## Prepared Julie v7.15.3 release

### WHAT
- Bumped Julie from `7.15.2` to `7.15.3` in `Cargo.toml`, `Cargo.lock`, and the docs site footer.
- Added `docs/release-notes/v7.15.3.md` with user-facing notes for the test-suite stabilization patch.
- Kept the release scope to the TODO/test fixes: doctest import corrections, reranker prose doctest suppression, edit-file hook scoping, concurrent MCP watcher observation timeout, and HOME-env test serialization.

### WHY
- The recent TODO audit found real red tests at HEAD, and the user asked to fix however many existed, then bump a new patch release.
- Patch release is needed to ship a green workspace test suite and keep release/version surfaces synchronized.

### HOW
- Used Julie MCP for release-surface edits where possible, after finding the shared MCP search index had local registry/index drift.
- Verified the final `v7.15.3` tree with narrow tests, `cargo check`, `cargo xtask test changed`, full `cargo test --workspace`, release build, and `julie-server --version`.
- Preserved unrelated local dirty files such as `.codex/config.toml` and older untracked memory folders.

### IMPACT
- `v7.15.3` is ready to commit, tag, push, and verify through GitHub release assets plus plugin distribution.
- A local dogfood issue was observed: the shared Julie registry listed `julie_528d4264` as ready while `/Users/murphy/.julie/indexes/julie_528d4264` was missing, causing MCP search refresh to fail with a Tantivy lockfile ENOENT. Standalone/project-local indexing remained usable.
