---
id: checkpoint_d89bfaf2
timestamp: 2026-04-19T18:15:17.418Z
tags:
  - dogfood
  - call_path
  - rewrite_symbol
  - hardening
  - verification
git:
  branch: main
  commit: 8a24a359
  files:
    - .gitignore
    - README.md
summary: Dogfood confirms call_path + rewrite_symbol hardening works
briefId: julie-world-class-systems-program
type: checkpoint
symbols:
  - CallPathTool::call_tool
  - resolve_endpoints
  - rewrite_symbol
  - relationship_priority
next: File the two gaps as issues if you want them addressed; otherwise merge is
  clean.
confidence: 5
---

## Dogfood confirms call_path + rewrite_symbol hardening works

Exercised both tools live on Julie's own codebase after merging `feature/call-path-rewrite-symbol-hardening`.

### Verified fixes
- **call_path ambiguity**: lists every candidate with file:line range, suggests disambiguation params
- **call_path disambiguation**: `from_file_path`/`to_file_path` accepted, BFS runs on narrowed endpoint
- **call_path real paths**: `call_tool` -> `resolve_endpoints` hops=1 edge=call
- **rewrite_symbol no-op**: "Symbol X is already in the requested state" replaces the old header-only empty diff
- **rewrite_symbol span visibility**: dry-run preview includes `--- Old content ---` block (task 3)
- **rewrite_symbol file_path suffix semantics (42209291)**: `file_path="navigation/call_path.rs"` matched the full path

### Remaining gaps (not regressions)
1. Same-file name collision has no disambiguation path. `from_text` at mcp_compat.rs:19 (trait decl) and mcp_compat.rs:23 (impl) — error says "provide file_path" but both are in the same file. Could detect this and suggest qualified name or line range.
2. `TextContentCompat::from_text` qualified name still matches both trait decl and impl. Plan's task 8 fixed this for call_path only; rewrite_symbol didn't get the same qualified-name resolver. Root cause per the plan: extractor doesn't link impl-method parent_id to struct/trait name.

### Impact
Hardening is shippable. Two rough edges to file as follow-ups (NOT work-now items — the tool contract is respected, just with imperfect ergonomics in edge cases).
