---
id: checkpoint_673cc3bd
timestamp: 2026-07-19T00:51:52.193Z
tags:
  - dependency-upgrade
  - julie-extractors
  - v2.16.0
  - engine-version
  - reindex
  - tdd
git:
  branch: codex/julie-extractors-consumer-upgrade
  commit: 89c29362
  files:
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/Cargo.toml
    - crates/julie-index/Cargo.toml
    - crates/julie-pipeline/Cargo.toml
    - crates/julie-runtime/Cargo.toml
    - crates/julie-tools/Cargo.toml
    - src/tests/core/engine_version.rs
    - src/tools/workspace/indexing/engine_version.rs
    - xtask/tests/extractor_dependency_contract_tests.rs
summary: Pinned Julie's extractor consumer to v2.16.0
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
impact: Unblocks all later v2.16 consumer-upgrade tasks against a single
  verified dependency source and guarantees a reindex when the new Julie
  enrichments land.
evidence:
  - cargo check — PASS
  - cargo nextest run -p xtask extractor_dependency_release_is_v2_16_0 — RED on
    v2.14.0, then GREEN
  - cargo nextest run --lib
    test_semantic_index_engine_version_includes_extraction_contract — PASS
  - cargo tree -p julie-extractors --depth 0 — v2.16.0 at c8324f84
symbols:
  - SEMANTIC_INDEX_ENGINE_VERSION
  - test_semantic_index_engine_version_includes_extraction_contract
  - extractor_dependency_release_is_v2_16_0
next: Implement the normalized extraction-data seam and typed canonical
  persistence against the now-pinned v2.16 API.
confidence: 5
---

## Pinned Julie's extractor consumer to v2.16.0

The complete consumer-upgrade branch needed one exact upstream source and an engine stamp that forces a one-time reindex when Julie begins consuming the new extractor domains.

- **What:** Updated all six direct `julie-extractors` dependencies from `v2.14.0` to `v2.16.0` and refreshed `Cargo.lock` to upstream commit `c8324f84`.
- **How:** Added an xtask contract test covering every consumer manifest, kept tree-sitter at `0.26.11`, and appended `consumer-enrichments-v1` to `SEMANTIC_INDEX_ENGINE_VERSION` while retaining the exact upstream extraction contract.
- **TDD:** The new contract failed on `v2.14.0` before manifest edits and passed after the pin; the exact engine-version test and `cargo check` also pass.
- **Impact:** Establishes the exact v2.16 API and lockfile foundation required by the remaining typed persistence and tool-surface tasks.
