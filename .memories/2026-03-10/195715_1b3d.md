---
id: checkpoint_1b3d068f
timestamp: 2026-03-10T19:57:15.950Z
tags:
  - code-review
  - bug-fixes
  - review-findings
  - subagent-driven
  - v4.1.4
git:
  branch: fix/todo-review-findings
  commit: 90a53a0
  files:
    - .claude/settings.json
    - TODO.md
    - docs/plans/2026-03-10-todo-review-fixes.md
summary: "## Completed 8/13 GPT review findings (v3.9.1→v4.1.4)"
planId: sidecar-binary-distribution
type: checkpoint
impact: 8 verified bugs fixed with 35 new regression tests. Reference workspace
  search now has feature parity with primary. Daemon restarts no longer leave
  projects idle. Connect bridge errors are matchable. Federated search is fair
  across projects.
evidence:
  - 1288 fast-tier tests pass, 0 failures
  - 8 clean commits on fix/todo-review-findings branch
  - All acceptance criteria met per subagent reports
next: Merge branch fix/todo-review-findings. Resume Windows items on Windows
  machine. Consider CORS hardening design.
confidence: 5
---

## Completed 8/13 GPT review findings (v3.9.1→v4.1.4)

GPT ran a code review across the v3.9.1→v4.1.4 delta and wrote findings into TODO.md. Claude verified 12/13 findings valid, then fixed 8 non-Windows items via subagent-driven development.

### Fixes (8 commits, ~35 new tests, 1288 fast-tier pass)

1. **Ref-workspace fast_refs parity** — added `limit`, `reference_kind`, identifier-based refs (Strategy 3) to reference workspace path. 10 tests.
2. **Auto-queue indexing on startup** — daemon sends `IndexRequest` for `Registered`/`Stale` projects after `load_registered_projects()`. 4 tests.
3. **Connect bridge request ID** — `write_jsonrpc_error` now passes original JSON-RPC `id` instead of hardcoded `null`.
4. **Federated refs alphabetical starvation** — global limit sorts by confidence before truncating, extracted `apply_global_ref_limit()`. 5 tests.
5. **Line-mode workspace re-resolution** — `line_mode_search` accepts `WorkspaceTarget` directly, removes redundant `WorkspaceRegistryService`.
6. **Registry persistence failures** — `register_project`/`deregister_project` propagate file write errors. 2 tests.
7. **UI asset 404** — missing `.js`/`.css`/etc return 404; SPA fallback only for navigation routes. 8 tests.
8. **PID-file atomic locking** — `fs2::try_lock_exclusive()` eliminates TOCTOU race in `daemon_start`. 5 tests.

### Deferred

- 3 Windows-only items (stop_service self-kill, uninstall active exe, UNC display_path) → Windows session
- CORS + unauthed endpoints → design discussion
- Embedding provider cross-workspace → dismissed (stale post-Tantivy)
