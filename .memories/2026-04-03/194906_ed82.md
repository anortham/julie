---
id: checkpoint_ed82c9d3
timestamp: 2026-04-03T19:49:06.619Z
tags:
  - bug-fix
  - embeddings
  - indexing
  - watcher
  - hybrid-search
  - batch-complete
git:
  branch: main
  commit: f92dbc15
  files:
    - TODO.md
    - docs/superpowers/plans/2026-04-03-indexing-embedding-bugfixes.md
summary: All 6 Indexing/Embedding Pipeline Fixes Complete
type: checkpoint
symbols:
  - handle_index_command
  - matches_filter
  - spawn_workspace_embedding
  - IncrementalIndexer
  - dispatch_file_event
  - initialize_embedding_provider
  - SharedEmbeddingProvider
---

## All 6 Indexing/Embedding Pipeline Fixes Complete

Fixed 4 bugs and 2 enhancements from the code review batch:

### Bugs Fixed
1. **Force-index reference workspace clears primary embeddings** -- routed `clear_all_embeddings` to reference DB via `workspace_db_path()` instead of handler's primary DB
2. **Watcher snapshots None embedding provider** -- changed to `Arc<RwLock<...>>` shared between workspace and watcher, propagation at both lazy-init write-back sites (`embeddings.rs:81` and `nl_embeddings.rs:132`)
3. **vector_count under-reports** -- reads `embedding_count()` from workspace DB after pipeline instead of using `stats.symbols_embedded`
4. **matches_filter ignores exclude_tests** -- added `is_test_path()` check to semantic candidate filter before RRF merge

### Enhancements
5. **No-op index spawns embeddings** -- gated on `files_processed > 0 || orphans_cleaned > 0 || force`
6. **Per-file embeddings miss lang_configs** -- threaded `LanguageConfigs` through `dispatch_file_event`

### Key Discovery
Code review caught that `initialize_embedding_provider()` propagation was dead code: always called on workspace clones (which have `watcher: None`). Had to add propagation at the actual write-back sites in `embeddings.rs` and `nl_embeddings.rs`.

### All 8 xtask dev buckets green.
