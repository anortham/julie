---
id: checkpoint_d7fa350e
timestamp: 2026-03-23T00:19:41.459Z
git:
  branch: main
  commit: 4bcf8178
  files:
    - .claude/settings.local.json
summary: "## Track C: v6 Phase 2 — COMPLETE"
type: checkpoint
symbols:
  - DaemonDatabase
  - generate_workspace_id
  - workspace_db_path
  - format_trend_table
  - test_phase2_daemon_db_full_flow
next: v6 Phase 3 planning (if any), dogfood the new daemon mode in a real session
---

## Track C: v6 Phase 2 — COMPLETE

All 20 tasks (A1-A8, B1-B4, C1-C6, I1-I2) are now completed and the dev tier is green.

### What was accomplished in this session

**C4: query_metrics trend category**
- New `src/tools/metrics/trend.rs`: `format_delta`, `format_comparison`, `format_trend_table`
- Manual Gregorian calendar for timestamp→date (avoids chrono dep)
- "trend" match arm in mod.rs: reads daemon_db snapshot history, shows comparison vs previous snapshot
- 10 unit tests in `src/tests/tools/metrics/trend_tests.rs`

**C5+: Registry migration (Watchers teammate) + test suite fixes**
- `registry_service.rs` deleted — daemon.db is the authoritative registry
- `handler.rs`, `startup.rs`, `health.rs` updated to DaemonDatabase paths
- All workspace commands (add, refresh, stats, list) are now daemon-mode-only; return helpful errors in stdio mode
- **Key insight**: `ManageWorkspaceTool::add` and `refresh` require `handler.daemon_db` — tests using `new_for_test()` (stdio mode) cannot call these and expect success

**Test suite fixes for daemon-mode-only operations**
- `primary_workspace_bug.rs`: replaced `registry_service.get_primary_workspace_id()` with `generate_workspace_id()` — registry.json no longer written in stdio mode
- `get_symbols_reference_workspace.rs`: rewrote to manually create `SymbolDatabase` at `workspace.workspace_db_path(&ref_id)` and populate it — this is the only way to set up a reference workspace DB in stdio mode for tests
- `isolation.rs`: fixed undefined `workspace` variable (`→ primary_workspace.path()`)
- `xtask/test_tiers.toml`: raised `tools-misc` timeout 300s → 450s for slow dogfood test

**I1: Integration test** (committed in prior context)
- `test_phase2_daemon_db_full_flow` covers: DaemonDatabase schema, WorkspacePool get_or_init, session sharing, reference workspace add/list, snapshot capture, tool call history, disconnect_session

**I2: Documentation**
- CLAUDE.md: Mode field updated, Per-Workspace Isolation now documents stdio vs daemon topologies
- AGENTS.md: synced from CLAUDE.md
- WORKSPACE_ARCHITECTURE.md: complete rewrite showing both mode storage layouts

### Key decisions / learnings

- **Reference DB path in stdio mode**: `handler.get_database_for_workspace(id)` uses `primary.workspace_db_path(id)` which is `{primary_root}/.julie/indexes/{id}/db/symbols.db`. Manually creating `SymbolDatabase` at that path is the correct test pattern when daemon mode isn't available.
- **Orphaned test code**: C5's Watchers teammate removed registry_service.rs but left orphaned test bodies without function signatures. These compile errors needed separate fixes.
- **xtask timeout**: `test_target_minimal_mode_includes_body_for_child_symbols` indexes the full julie project and takes ~164s. Combined with other bucket tests it exceeded the 300s hard timeout. Raised to 450s.

### Final state
- Branch: `main`
- Last commits: 4bcf8178 (docs), 2e859223 (docs), 5533abb0 (registry migration), 0fbe6e39 (I1 test), de9b8569 (C6 deprecation), 0ef64cef (C4 trend)
- Dev tier: **8/8 buckets pass** (~372s total)
