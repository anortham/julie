---
id: checkpoint_d933f7a7
timestamp: 2026-03-23T19:38:34.230Z
tags:
  - v6
  - code-review
  - dogfood
  - phase3
  - agent-team
  - comprehensive
git:
  branch: main
  commit: c02eb2d9
  files:
    - .claude/settings.local.json
    - TODO.md
summary: "## V6 Comprehensive Code Review Complete"
type: checkpoint
impact: "Identified 13 critical and 25+ high issues before v6 release. Key
  areas: transaction safety in indexing, daemon lifecycle management, workspace
  ID consistency, reference workspace support. Core algorithms (search, RRF,
  tokenization) are solid."
next: Update TODO.md with all findings (clearing completed items), then build a
  TeamCreate agent team to fix criticals and highs collaboratively.
---

## V6 Comprehensive Code Review Complete

### What
Ran a 5-agent expert review team across Julie's entire codebase, each agent focused on a functional area: daemon architecture, search engine, navigation/analysis tools, indexing pipeline, and handler/core infrastructure. All agents used Julie's own tools (510+ tool calls total, dogfooding the v6 daemon mode).

### Key Findings
- **13 CRITICAL** issues: transaction safety gaps in indexing, daemon lifecycle races, force-reindex workspace ID bug, reference workspace name resolution wrong, KNN score conversion questionable
- **25+ HIGH** issues: session drain missing on SIGTERM, auto-indexing race, parser pool dead code, unbounded dedup map, rename events dropped, Struct kind not routed to deep_dive formatter
- **26 MEDIUM**, **20 LOW**, **30+ POSITIVE** design notes

### Top Criticals
1. **H-C1**: Force-reindex workspace ID double-formatted (`julie_julie_c0`), silently fails
2. **D-C3**: `accept()` errors kill entire daemon (EMFILE = dead server)
3. **I-C1/C2**: Fresh indexing has no outer transaction; index drops outside transaction
4. **N-C1**: `resolve_source_names` always queries primary DB (wrong for reference workspaces)
5. **I-C4**: Watcher double-reads file content creating hash race

### Phase 3 Dogfood Results
- Daemon restart required to pick up new binary (added auto-restart TODO)
- Shared EmbeddingService working: CodeRankEmbed on MPS, sidecar shared across sessions
- All 8 MCP tools validated: fast_search, deep_dive, fast_refs, get_symbols, get_context, query_metrics, rename_symbol, manage_workspace
- `get_context` missed `EmbeddingService` for directly relevant query (search quality gap)
- `query_metrics` file_pattern `src/daemon/**` returns empty (glob matching bug)
