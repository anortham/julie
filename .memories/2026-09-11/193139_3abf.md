---
id: checkpoint_3abfb9f7
timestamp: 2026-09-11T19:31:39.723Z
tags:
  - test-speed
  - item-3
  - task-3
git:
  branch: main
  commit: ab6b8077
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 3 landed: the uncovered scenario test passes"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Task 3 landed: the uncovered scenario test passes

**WHAT**: `src/tests/request_scenarios.rs` apply call adds `--standalone`, matching the `cli_subcommand` and `cli_tool` helpers.

**WHY**: `test_scenario_2_refactoring_pipeline` ran in no test bucket and failed on main with exit 1: without `--standalone` the CLI opened the index store the fresh fixture's MCP child was still creating (`INTERNAL_ERROR: Failed to initialize workspace: open checkout store`). The flags `--dry-run=false` and `--occurrence all` are fine.

**HOW**: Worker diagnosed by capturing stderr, applied plan outcome (a), no product code touched.

**IMPACT**: One of the four known failures among the 319 uncovered tests is fixed. Observation for the finding: other scenario and parity tests call the CLI without `--standalone` after `rpc` calls and could race under load.
