---
id: checkpoint_76d18a51
timestamp: 2026-09-11T22:34:42.508Z
tags:
  - deployment-story
  - task-8
  - finding
  - runbook
  - review
git:
  branch: main
  commit: 91c7968c
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 8 done: finding, ledger, brief; runbook corrected after review"
briefId: machine-service-one-rust-service-per-machine-repla
evidence:
  - "cargo xtask test full at 18403740: 5/5 commands in 41.7 s"
  - "cargo xtask test dev on the docs working tree: 2210 passed, 13 CLI tests
    passed, 15.5 s"
  - "node --test hooks/*.test.cjs at plugin 1dec280: 21 pass"
  - "scratchpad/shim-env-livecheck.jsonl: JULIE_WORKSPACE from /tmp returns
    launch.rs:52, 1960 instruction bytes"
next: Write .memories/autonomous-run-2026-09-11-deployment-story.md, commit it
  with its checkpoint, report to the owner.
---

## Task 8 done: finding, ledger, brief; runbook corrected after review

- **WHAT:** `docs/findings/2026-09-11-deployment-story.md` (verdict, what shipped per design section, Task 6 evidence, six harness results with doc URLs, what the checks changed, owner runbook, side findings, deferred). Ledger rows: run.cjs fix, shim `JULIE_WORKSPACE` test, six harness checks (live), config restore, `cargo xtask test full` at 18403740 (5/5, 41.7 s; clippy 0 errors), live `JULIE_WORKSPACE` shim check, `dev` on the docs working tree (2210 + 13 pass), plugin tests at 1dec280 (21 pass). Plan and design checkboxes ticked. Brief: item 4 done, item 5 next. Release notes: Codex and Antigravity rows now show the config block; Hermes row no longer says "printed".
- **WHY the runbook changed:** an adversarial review (3 reviewers, 25 confirmed findings) found that `release.yml` dispatches the plugin's `Update Plugin` workflow on plugin `main` at the end of the release job, and that plugin `main` on GitHub is the workflow's orphan commit `09a2c23` (7.18.1) with no history in common with `v8-deployment`. The old runbook (tag first, merge the plugin branch later, push by hand) could not work: the old workflow on `main` would have published an 8.0.0 plugin with the 7.18 layout, and the merge would fail. New order: `git push --force origin v8-deployment:main` in the plugin repo first, then merge and tag julie; the auto-dispatch then runs the v8 workflow on the v8 layout; the owner reviews `origin/main` and pushes nothing.
- **Other review fixes:** harness-check SHA attribution (service from 9e38fdba, packed shim from 622c616d, not 18403740); Codex probe env described in full (nine variables, none names a project); README skills table paths for the three plugin caches; Node.js 22.5 prerequisite; Codex hook-trust step; "one install path per harness"; passive sentences; Tools (10); ledger worker row command and timestamps.
- **Environment:** a mistaken shim run from `/tmp` (env var applied to `printf`, not the shim) indexed `/tmp` into the stray `tmp_e9671acd` checkout and left `/tmp/.julie/logs/`, which broke one root-detection test in `dev`; removed, `dev` green.
- **IMPACT:** Next: run report in `.memories/`, then the owner runs the runbook. Never push or tag.
