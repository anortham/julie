---
id: checkpoint_0aeddab1
timestamp: 2026-09-11T19:44:05.443Z
tags:
  - docs
  - testing
  - xtask
  - test-speed
git:
  branch: main
  commit: ab6b8077
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 5: docs name only the three test tiers"
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
---

## Task 5: docs name only the three test tiers

**WHAT:** Every live instruction file now names only `cargo xtask test dev|dogfood|full` and the exact-test edit loop (`cargo nextest run --lib <name>`).

- `CLAUDE.md` / `AGENTS.md`: Quick Reference block, TDD step 6, and the whole RUNNING TESTS section replaced by one three-tier table, the edit loop, the rules, subagent rules, ledger scope labels, known failures, fixture rebuild. Section shrank from about 130 lines to about 65.
- `README.md`, `docs/TESTING_GUIDE.md`, `docs/DEVELOPMENT.md`, `docs/DEPENDENCIES.md`, `docs/ADDING_NEW_LANGUAGES.md`, `docs/TREE_SITTER_UPGRADES.md`, `docs/TREE_SITTER_QUALITY_BAR.md` (Release Gates table only), `docs/plans/verification-ledger-template.md`, `.claude/hooks/session-start-tests.cjs`, `.claude/hooks/julie-routing-block.md`, both `dead-code-audit/SKILL.md` files, `src/handler/tools/mod.rs` doc comment.

**WHY:** Task 4 removed buckets, `changed`, budgets, warm/cold accounting. The docs-contract test in `xtask/tests/docs_contract_tests.rs` was RED until the docs matched.

**HOW:** Commands copied verbatim from `xtask/src/runner.rs` (DEV, DOGFOOD consts) and `xtask/src/cli.rs` (Tier names). Thread cap now documented as `.config/nextest.toml` test group `dogfood`, `max-threads = 6`.

**Judgment call:** the historical ledger rows in `docs/TREE_SITTER_QUALITY_BAR.md` (lines ~200-246) still name retired commands. The task said they are history and stay, so they were left intact with a one-sentence note above them. The final grep therefore still lists those rows; nothing else.

**Verification:** `cargo nextest run -p xtask docs_contract` 9/9 pass. `diff CLAUDE.md AGENTS.md` empty.
