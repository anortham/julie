---
id: checkpoint_a6b8a980
timestamp: 2026-09-11T21:23:00.254Z
tags:
  - deployment-story
  - handoff
  - task-7
git:
  branch: main
  commit: 91c7968c
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Handoff: deployment story mid Task 7 (harness checks)"
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
next: Fix run.cjs findArchive in julie-plugin, rerun harness checks on the 8.0.0
  service, restore user configs, write the finding, run dev at final HEAD.
---

## Handoff: deployment story mid Task 7 (harness checks)

- **Done and committed:** Tasks 1 to 6 (julie `deployment-story` at 9e38fdba; plugin `v8-deployment` at 6ed348b). Dev tier green at 622c616d; the service-instructions fix (9e38fdba) is not yet under a dev run.
- **Found during Task 7:**
  - `hooks/run.cjs findArchive` takes the first readdir match. With both 7.18.0 and 8.0.0 archives in `bin/archives/`, it re-extracted 7.18.0 over `bin/x86_64-unknown-linux-gnu/`, so every harness check so far ran the OLD binary (serverInfo 7.18.0, "No results"). Fix: pick the newest match (`localeCompare` numeric) or the archive matching the plugin version; add a two-archive test in `hooks/run.test.cjs`; then delete `bin/x86_64-unknown-linux-gnu/` and rerun.
  - The dev shim `target/release/julie-server` (8.0.0) refuses the 7.18.1 service ("service version ... does not match; run: julie-server service restart", exit 3). A rebuild plus `service restart` was started in the background (task b5kpnwi63); confirm `service status` shows 8.0.0.
  - Codex installed the plugin (`~/.codex/plugins/cache/julie-plugin/julie/7.18.0`) but the session saw no julie tool; `codex mcp list` shows only the user-level `julie` (dev shim, then broken by the version mismatch). Verify whether Codex loads plugin `mcp.json`/`.codex-plugin` mcpServers (doc: developers.openai.com/plugins/build/plugins).
  - Claude Code (`claude -p --plugin-dir`) and OpenCode both loaded the plugin and called `fast_search` (mcp connected). Hermes and Antigravity runs were backgrounded by the user (tasks b2aw2qu9b, binet3ekn); results unread.
- **User environment changed (restore after Task 7):** Codex: marketplace `julie-plugin` + plugin `julie@julie-plugin` added (backup `scratchpad/backup/codex-config.toml`; remove with `codex plugin remove julie@julie-plugin` and `codex plugin marketplace remove julie-plugin`). Hermes: `mcp_servers.julie` added (backup `scratchpad/backup/hermes-config.yaml`; `hermes mcp remove julie`). OpenCode: created `~/.config/opencode/opencode.json` (did not exist before; delete it) and ran `install-opencode.cjs` (undo with `node bin/install-opencode.cjs --uninstall`). Antigravity: `agy plugin install /home/murphy/source/julie-plugin` may have run (`agy plugin list`; `agy plugin uninstall julie`). Cursor: not touched.
- **Remaining:** fix findArchive, rerun the six harness checks against the 8.0.0 service, restore configs, Task 8 (finding `docs/findings/2026-09-11-deployment-story.md` with owner runbook, ledger rows for `dev` at final HEAD and plugin tests, brief update), finishing-a-development-branch. Owner merges, tags, pushes.
