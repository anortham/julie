---
id: checkpoint_ea272ee0
timestamp: 2026-09-11T20:54:32.757Z
tags:
  - deployment-story
  - release
  - sidecar
git:
  branch: main
  commit: 91c7968c
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 1 done: release archives carry the sidecar"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Task 1 done: release archives carry the sidecar

- **WHAT:** New `.github/scripts/pack-release.sh` pins sidecar 0.1.0 (asset name and sha256 per target), downloads it with `gh release download`, checks the checksum, drops the sidecar README, renames its LICENSE, and packs `julie-server` plus the sidecar files at the archive root. `release.yml` calls the script and its stale PyTorch/CUDA comments are gone.
- **WHY:** Semantic search needs `julie-semantic-sidecar` beside `julie-server`; nothing installed it before.
- **HOW:** Verified on this machine: dry-run for four targets matches the published `.sha256` files; a real Linux pack produced a 55 MB archive with both binaries; sha mismatch exits 1.
- **IMPACT:** Task 6 reuses the script for the local end-to-end check. Windows path relies on `unzip`, `7z`, `sha256sum` in Git Bash (not runnable here).
- **Side note:** the worker's Julie MCP calls failed with "could not start service: No such file or directory" at 20:52; the developer service restarted at 20:52:40 and is up now. Watch for a repeat.
