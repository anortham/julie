---
id: checkpoint_c66c5a4f
timestamp: 2026-09-11T13:27:45.154Z
tags:
  - revival
  - review-only
  - machine-service
  - paging
  - evaluation
git:
  branch: main
  commit: 2c404bbc
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: machine-service-one-rust-service-per-machine-repla
type: learning
---

## WHAT
Reviewed landed revival at main 2c404bbc with three read-only Sol reviewers. User explicitly permits review/planning only; another session owns .worktrees/hybrid-auto. No source edits, builds, tests, commits or restarts. This checkpoint is review evidence, not authorization to implement.

## WHY
Assess whether the simplification makes Julie effective again and what remains before comparison with Miller.

## HOW
Used existing Julie release CLI read tools, git source, gate findings and recorded benchmark outputs. Confirmed: GET /status runs manage_workspace status, which opens full CheckoutStores and retains them in handler ref_store_cache; these can duplicate runtime-owned stores. Design memory bound/LRU is absent. RuntimeFactory::acquire awaits cold initialization under global runtimes write lock. Paging trailers omit workspace and non-default filters across search/refs/symbols/impact. Tracked .agents/skills/editing still mandates deleted rewrite/rename tools while .claude copy is updated. Evaluation has 23 natural-language search cases, scores one expected path, and compares Miller auto to explicit Julie variants; it is diagnostic evidence, not a held-out product contest. Current live service may run another worktree's binary; source findings were checked against main. RSS samples during ongoing benchmarks are not a controlled baseline.

## IMPACT
Recommend preserve facts + immutable snapshots + one semantic child. Prioritize lightweight status and canonical bounded store ownership, faithful paging, coherent client guidance, then held-out agent tasks/freshness/concurrent-worktree comparison. Treat full-graph re-resolution on edits and shared child query/batch contention as measured follow-ups, not reasons to add infrastructure now. Hybrid auto remains the other session's work.
