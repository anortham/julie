---
id: checkpoint_68fcb839
timestamp: 2026-09-11T21:00:52.009Z
tags:
  - deployment-story
  - instructions
  - hooks
git:
  branch: main
  commit: 91c7968c
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 3 done: one instructions file in julie"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Task 3 done: one instructions file in julie

- **WHAT:** `JULIE_AGENT_INSTRUCTIONS.md` (1,960 bytes) is the only routing text: rules, tools, a new Workflows section, paging with `offset`. The dev hook `.claude/hooks/session-start.cjs` prints it. `cargo xtask sync-plugin` mirrors it to the plugin (`sync_instructions`, `SyncReport.instructions_updated`). Deleted `julie-routing-block.md`, `pretool-edit.cjs`, `pretool-agent.cjs`, and `.agents/skills/`. CLAUDE.md and AGENTS.md carry the standalone-CLI evidence sentences and the updated Plugin Distribution section. Tests repointed: `test_agent_instructions_recommend_standalone_for_quick_dogfood_checks` reads CLAUDE.md; `docs_contract_tests_instructions_carry_the_full_guidance` reads the instructions with a 2,000-byte ceiling.
- **WHY:** Two routing texts drifted; the plugin hook and the MCP instructions now print the same file.
- **HOW:** Two rules (get_symbols before Read, fast_refs before modifying) became the "Do not read a file without get_symbols first" sentence and the Refactor workflow line to fit the budget.
- **IMPACT:** Batch A (tasks 1 to 5) complete. Next: lead `cargo xtask test dev`, then Task 6 end-to-end. Side finding: Julie MCP failed for every worker ("could not start service: No such file or directory") while the lead's calls worked; the release CLI `deep-dive` stalled past 120 s for one worker. `cargo xtask sync-plugin` without `--plugin-root` fails from a worktree (default root is the worktree parent).
