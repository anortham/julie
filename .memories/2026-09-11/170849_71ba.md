---
id: checkpoint_71badee4
timestamp: 2026-09-11T17:08:49.902Z
tags:
  - index-hygiene
  - phase-6
  - gate
  - finding
git:
  branch: main
  commit: 537d9f8e
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Index hygiene plan: all seven tasks done, gates green, live check passed"
briefId: machine-service-one-rust-service-per-machine-repla
type: checkpoint
evidence:
  - system 4 / dev 30 / full 47 buckets pass at 964d14dc
  - service bucket + changed pass at 84c75ba5
  - "restart 2 and 3: births unchanged, vectors unchanged"
---

## Index hygiene plan: all seven tasks done, gates green, live check passed

- **WHAT:** Branch `index-hygiene` holds seven product commits (bca54911..84c75ba5) plus the finding `docs/findings/2026-09-11-index-hygiene.md` and ledger `docs/plans/2026-09-11-index-hygiene-ledger.md`. Every plan acceptance box is ticked.
- **WHY:** Brief item 2 (registry and index hygiene). The service deleted live indexes on transient failures and restarts, tests polluted `~/.julie`, the service log lived in a tempdir, and dead registry rows never got pruned.
- **HOW:** Workflow with Opus implementer, reviewer and gate agents per task, serial. Task 1 needed three fix rounds and a lead cap ruling (marker assertion added by the lead, four commits squashed). Task 3 two rounds (zombie pid, child reaping). Task 5's gate caught env-derived test paths. The live check exposed that the service never installed a tracing subscriber; fixed at 84c75ba5.
- **IMPACT:** Two consecutive restarts on eleven open workspaces: 11/11 birth times unchanged, vector counts identical, one service pid, sweep summary in `~/.julie/logs/julie-service.log.<date>`. The pre-fix pollution was caught in the act: `~/.julie/indexes` was empty at 16:45Z after worker test runs, registry rows intact, no orphan events (temp-registry sweep path). Deferred: dashboard ErrorBuffer layer never installed (item 7); partial vector backfill does not resume after restart; `new_files` repair on every restart; RSS 2.9 GB with eleven workspaces; stray `/tmp` registry row.

Next: finishing-a-development-branch (owner merges locally, no push authority), then rebuild main release + copy sidecar + restart, then brief update and item 3 (test speed).
