---
id: checkpoint_3b94e146
timestamp: 2026-09-11T19:31:13.709Z
tags:
  - test-speed
  - item-3
  - task-1
git:
  branch: main
  commit: ab6b8077
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 1 landed: deadlines instead of 10 s waits"
briefId: machine-service-one-rust-service-per-machine-repla
---

## Task 1 landed: deadlines instead of 10 s waits

**WHAT**: `connect_or_start_within(paths, spawn, deadline)` in `src/service/client.rs`; `connect_or_start` wraps it with 10 s. The two service client tests pass a 200 ms deadline (0.21 s each, were 10 s). `test_search_after_indexing_complete` polls `search_ready` and asserts the search result (0.04 s, was 10 s); it now indexes first because `initialize_workspace_with_force` does not index.

**WHY**: Three tests set the 10 s floor of the whole-workspace run.

**HOW**: Worker implemented under parallel-lead-commit; lead reviewed the diff inline: spec compliant, no extra seams.

**IMPACT**: Whole-workspace floor drops to the 5 s lock-wait tests. Plan `docs/plans/2026-09-11-test-speed-plan.md` Task 1 boxes ticked.
