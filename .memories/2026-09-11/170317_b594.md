---
id: checkpoint_b5942def
timestamp: 2026-09-11T17:03:17.842Z
tags:
  - index-hygiene
  - service
  - logging
  - task-7
git:
  branch: main
  commit: 537d9f8e
  worktree: /home/murphy/source/julie
  files:
    - .codex/config.toml
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Service process installed no tracing subscriber
briefId: machine-service-one-rust-service-per-machine-repla
type: incident
context: "Live restart check after Tasks 1-6 of the index-hygiene plan: the new
  $JULIE_HOME/logs/julie-service.log.<date> held only ProjectLog tool_call
  lines; the start-sweep info line and repair warnings never appeared."
evidence:
  - "cargo nextest run --lib status_reports_a_service_log_under_the_julie_home:
    RED then PASS"
  - "cargo xtask test bucket service: 7 commands pass"
  - "cargo xtask test changed: dashboard, service, service-process pass"
next: Commit, rebuild release, restart twice with disk evidence, write finding
  and ledger.
---

## Service process installed no tracing subscriber

- **WHAT:** `ServiceApp::new` now calls `install_file_tracing(logs_dir, "julie-service.log", "info")`. The durable-roots test accepts `logs/` as a fourth root. The service-log test now polls for the "Cleanup sweep finished at service start" line.
- **WHY:** No code path in the service process ever installed a tracing subscriber (`install_file_tracing` had zero callers). Every `info!`/`warn!` from the service, the watcher, the sweep, and the new repair-failure warning was discarded. Task 4 wired only the handler `ProjectLog`, so the file existed but carried nothing operators need.
- **HOW:** Install at `ServiceApp::new` so the in-process service tests cover it (nextest runs one process per test, so the global subscriber is fresh). Default filter is `info` because the watcher and runtime lines come from `julie_runtime`/`julie_core` targets; `RUST_LOG` still overrides.
- **IMPACT:** The service log now carries tracing lines and tool calls in one file. The dashboard `ErrorBuffer` layer is still never installed (its `errors` list is always empty); deferred to plan item 7 (troubleshooting).

## Live evidence so far
- Before the branch gate `~/.julie/indexes` was already empty: worker test runs before Task 5 landed swept the ten corpus indexes through a temp registry (no `orphan_index_dir` events in the live db, 50 `auto_prune missing_path` rows for `/tmp/.tmp*` test workspaces). Registry rows stayed at 12.
- system/dev/full tiers green (4/30/47 buckets), registry rows still 12, indexes dir untouched by tests after Task 5.
- Restart 1 on the new binary: single service pid, `/status.log` reported, ten corpus repos reopened and rebuilt (births 16:55-16:57Z), sidecar ready, vectors backfilling.
