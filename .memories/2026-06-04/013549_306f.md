---
id: checkpoint_306f9dc7
timestamp: 2026-06-04T01:35:49.625Z
tags:
  - julie-extractors
  - v2.1.0
  - upgrade
  - source-regions
  - brainstorm
  - wip
  - doc-comments
  - search
  - migration
  - version-skew
git:
  branch: julie-extractors-2.1
  commit: 009a5bc1
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: julie-extractors v2.1.0 migration done; source_regions consumption
  brainstorm in progress
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
symbols:
  - SourceRegion
  - SourceRegionKind
  - ExtractionResults.source_regions
  - SEMANTIC_INDEX_ENGINE_VERSION
next: Continue the source_regions consumption brainstorm conversationally — get
  user's primary-value direction, propose 2-3 approaches, then design doc. Run
  dev+system as the combined branch gate after the feature is implemented.
---

## julie-extractors v2.1.0 migration done; source_regions consumption brainstorm in progress

**Where we are:** On branch `julie-extractors-2.1`. Scope B chosen (migrate + consume source_regions, breadth across 8 langs).

### Done — migration baseline (commit `009a5bc1`)
- Bumped julie-extractors v2.0.3→v2.1.0 in ALL THREE workspace pins (top, julie-core, julie-index). Root cause of the "108 errors" was version skew (Phase 1 crate split added 2 new pin sites) → two julie_extractors versions in the graph; 107/108 errors were spurious type-mismatches that vanished once pins matched.
- Only real API change: new `ExtractionResults.source_regions` field (defaulted empty in TextOnly path; behavior identical to v2.0.3).
- Synced `SEMANTIC_INDEX_ENGINE_VERSION` → `...ecmascript-swift-shape-v3.source-regions-v1` (forces reindex).
- Fixed stale CLAUDE.md/AGENTS.md bucket name `parser-upgrade`→`extractor-dep-integration`.
- Gates: `cargo check --workspace --all-targets` green; `extractor-dep-integration` bucket 2/2 (engine_version lockstep + real_world_parser_upgrade_contracts). dev+system NOT yet run (deferred to after the feature, one combined branch gate).

### In progress — brainstorm: how julie consumes source_regions
**SourceRegion shape** (julie-extractors base/types.rs): `kind` (Comment|DocComment|StringLiteral|Embedded), `containing_symbol_id: Option<String>` (links DocComment→documented symbol), span (line/col/byte) — **NO text** (spans only; julie must read text from files). 8 langs: Rust/JS/JSX/TS/TSX/Python/HTML/Vue.
**Value angles:** (1) doc-comment→symbol → surface docs in deep_dive/get_symbols (best fit for 'intelligence per token'); (2) searchable comment/doc/string text in Tantivy; (3) embedded-lang regions (speculative). Tool ceiling (12) → enrich existing tools, no new tool.
**Process note:** user wants a CONVERSATION, not early MC questions (rejected my multi-select). Asked them what they'd like to clarify; awaiting their direction on primary value.
