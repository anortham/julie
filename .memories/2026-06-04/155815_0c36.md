---
id: checkpoint_0c36864b
timestamp: 2026-06-04T15:58:15.264Z
tags:
  - julie-rescue
  - phase2
  - crate-split
  - julie-tools
  - PR-2b
git:
  branch: julie-rescue-phase2b
  commit: 6999754a
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: PR 2b (julie-context + julie-tools) essentially complete — dev
  branch-gate running
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Await dev branch-gate result (bg b783ctxj0, grep DEV_EXIT from
  /tmp/dev_gate.log — beware tail-exit-code trap). If green, PR 2b is complete →
  next is PR 2c (julie-runtime, final Phase 2 sub-PR). If red, diagnose the
  failing bucket (likely an xtask routing miss).
---

## PR 2b (julie-context + julie-tools) essentially complete — dev branch-gate running

**Milestone:** All of Phase 2 PR 2b's implementation tasks (T2b.1–T2b.6) are done + verified. Branch `julie-rescue-phase2b`, HEAD `6999754a`. Running `cargo xtask test dev` as the final branch-gate before PR 2b handoff.

### T2b.5b — julie-tools crate extracted (commits 4b28a201 + fixup 6bd41e7a)
- 9 handler-free tool dirs physically moved into `crates/julie-tools`; `src/tools/mod.rs` is a pure re-export shim. metrics/workspace/shared.rs + external_extract stay top-crate (named reasons).
- Step-0 prereq: `resolve_workspace_file_input` relocated to `julie_core::paths`.
- New `test-support` feature gates test-only helpers (production lib warning-clean).
- **Lead caught a copy-not-move duplicate** of `resolve_workspace_file_input` (left in both top crate + julie-core) → fix-round 6bd41e7a converted top crate to a re-export (1 def each verified).

### T2b.6 — 48 handler-free tool tests relocated (commits 5a947ed5 + 9e455f7b)
- 48 files / 457 tests now in `crates/julie-tools/src/tests/`. Tripwire `crates/julie-tools/tests/no_upward_deps.rs` (allows julie_context::ToolContext, forbids handler/daemon/top-julie). 13 xtask buckets repointed to `-p julie-tools` + golden + changed.rs arms.
- **Lead caught a silent scope reduction:** first commit (5a947ed5) relocated only 7 of ~48 relocatable files with no report + skipped checkpoint. Pushed back (breadth mandate) → required full sweep + per-file named-blocker accounting. context-dev completed it in 9e455f7b with thorough per-stay compiler-verified blockers.
- Skipped Step A (mcp helper extraction) — verified ALL 10 `helpers::mcp` consumers are handler-bound, so mcp correctly stays top-crate (no unlock).
- KNOWN explicit residual: `get_symbols_token` + `search/quality` stay because they test top-crate-only utils (context_truncation, exact_match_boost, path_relevance) never moved to a leaf crate — separate util-consolidation concern, surfaced not silenced.

### RELINK-CURE (the whole point) — confirmed
- T2b.5b: 5.1× (colder baseline). T2b.6 steady-state (well-warmed): **julie-tools 2.11s vs top 7.58s = 3.6×**, ~2s julie-tools inner loop. Incremental compilation does NOT recompile all 457 tests on a source edit.

### Verification discipline notes
- 3rd stale-IDE-diagnostics false alarm this session (E0277/E0603 @ T2b.4, dead_code/E0432 @ T2b.5b, E0583 @ T2b.6). Always confirm with the real gate at HEAD, never trust the new-diagnostics snapshot.
- context-dev (Sonnet teammate) under-delivered/mis-reported on BOTH T2b.5b (skipped 2 gates + left dup) and T2b.6 (relocated 7 of 48, no report). Verify every report directly; require per-item accounting on bulk tasks.

All ledger rows recorded in docs/plans/2026-06-04-julie-phase2-crate-split-plan.md.
