---
id: checkpoint_d91a7215
timestamp: 2026-06-04T16:49:09.552Z
tags:
  - julie-rescue
  - phase2
  - crate-split
  - julie-runtime
  - boundary
git:
  branch: julie-rescue-phase2c
  commit: 2c5bf463
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - docs/plans/2026-06-04-julie-phase2-crate-split-plan.md
summary: PR 2c (julie-runtime) boundary RE-VALIDATED against post-2b HEAD —
  design doc was stale
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: decision
decision: julie-runtime = watcher + workspace ONLY; indexing-orchestration stays
  top-crate (handler-bound); 2a already pre-severed the watcher's tools deps
alternatives:
  - "Follow the stale design doc and try to move the 8 commands + indexing
    orchestration to runtime — rejected: they are impl ManageWorkspaceTool and
    name handler/daemon, so they cannot live below the handler"
  - Move request_db UP to handler/daemon as the doc suggested — viable but
    WorkspaceConnectionPool::request_db in julie_core is cleaner (fully
    downward, no handler bloat) since it uses only the pool
next: Create T2c tasks, dispatch T2c.1 + T2c.1b (independent) in parallel to the
  visible team with inline review
---

## PR 2c (julie-runtime) boundary RE-VALIDATED against post-2b HEAD — design doc was stale

Merged PR #25 (2b) → `main` @ `2c5bf463`, created branch `julie-rescue-phase2c`, then re-validated the 2c scope against the real post-2b tree (the design doc was written pre-2b).

**Key discovery — the design doc's 2c scope is obsolete:**
- The indexing-orchestration (`tools/workspace/indexing/{route,index,incremental,pipeline,processor,embeddings,finalize}.rs`) is `impl ManageWorkspaceTool` + names `crate::handler` (6×) & `crate::daemon` (4×). ALL of `src/tools/workspace/**` (ManageWorkspaceTool + 10 command files, none facade-swapped in 2b) STAYS top-crate. It does NOT move to runtime.
- **2a already pre-severed the watcher's `crate::tools` deps**: `indexing::state`→`julie_core::indexing_state`, `indexing::file_policy`→`julie_core::file_policy`, `finalize::resolve_pending_relationships`→`julie_pipeline::finalize`, health enums→`julie_core::health_types` (all shims in place).

**Net: julie-runtime = `watcher/**` + `workspace/**` ONLY** — cleaner/narrower than the design doc.

**Validated edges (post-2b):**
- watcher: 0 handler/daemon refs; only genuine top-crate edge = `crate::startup::scan_workspace_files`.
- workspace: exactly 1 daemon ref = `request_db` (R3); health-enum imports already in core.
- Reverse-deps collapse to the proven `lib.rs` re-export shim (`crate::watcher` 2 refs + `crate::workspace` 21 refs → `pub use julie_runtime::{watcher,workspace}`).

**Corrected T2c tasks** (written into `docs/plans/2026-06-04-julie-phase2-crate-split-plan.md`):
- T2c.1: move `request_db` off `JulieWorkspace` onto `WorkspaceConnectionPool` in julie_core (uses only the pool, never self). 2 call sites.
- T2c.1b: relocate `scan_workspace_files` + `is_code_file` `src/startup.rs`→julie_core (deps already in core). 3 call sites + test.
- T2c.2: create `crates/julie-runtime`; move watcher+workspace; repoint 6 import families; lib.rs shims; delete `events.rs.tmp`.
- T2c.3: relocate 10 watcher test files (~186 marks, all handler-free) + `env_paths`/`root_detection`; extract R5 markers (`make_isolated_workspace_root`/`mark_workspace_root`) to julie-test-support; handler/daemon-bound workspace tests stay (R2).
- T2c.4: tripwire (forbid handler/daemon/tools/top-julie; allow core/index/pipeline) + xtask routing + 0-match orphan scan.
