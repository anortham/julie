---
id: checkpoint_a7c4e417
timestamp: 2026-06-04T18:06:24.658Z
tags:
  - julie-rescue
  - phase2
  - crate-split
  - julie-runtime
  - T2c.3
  - team-collision
git:
  branch: julie-rescue-phase2c
  commit: 2c5bf463
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/src/connection_pool.rs
    - crates/julie-core/src/lib.rs
    - crates/julie-core/src/paths.rs
    - crates/julie-core/src/workspace_scan.rs
    - crates/julie-runtime/Cargo.toml
    - crates/julie-runtime/src/lib.rs
    - crates/julie-runtime/src/tests/mod.rs
    - crates/julie-runtime/src/tests/watcher.rs
    - crates/julie-runtime/src/tests/watcher/event_queue.rs
    - crates/julie-runtime/src/tests/watcher_filtering.rs
    - crates/julie-runtime/src/tests/watcher_handlers.rs
    - crates/julie-runtime/src/tests/watcher_handlers/repair_projection.rs
    - crates/julie-runtime/src/tests/watcher_mutation_gate.rs
    - crates/julie-runtime/src/tests/watcher_observability.rs
    - crates/julie-runtime/src/tests/watcher_queue.rs
    - crates/julie-runtime/src/tests/workspace/mod.rs
    - crates/julie-runtime/src/tests/workspace/registry.rs
    - crates/julie-runtime/src/tests/workspace/root_safety.rs
summary: T2c.3 complete (julie-runtime test relocation + visibility correction)
  — after a concurrent-edit collision
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Dispatch T2c.4 (tripwire + xtask routing + orphan-filter scan) to
  runtime-dev; then branch-gate + commit PR 2c
---

## T2c.3 complete (julie-runtime test relocation + visibility correction) — after a concurrent-edit collision

T2c.1, T2c.1b, T2c.2, T2c.3 all DONE + verified green by lead. julie-runtime = watcher + workspace, extracted above julie-pipeline.

**T2c.3 final state (verified):**
- Relocated into `crates/julie-runtime/src/tests/`: 8 watcher integration files + `workspace_init/env_paths.rs` + `tests/workspace/{registry,root_safety}.rs` → **85 tests pass, 1 skipped**.
- STAY top-crate (R2): daemon/watcher_pool*, handler/daemon-bound workspace tests, `tools/search/relationship_text_test.rs` (ManageWorkspaceTool+handler-bound).
- R5: `make_isolated_workspace_root`/`mark_workspace_root` → `julie-test-support`; top-crate re-exports them.
- **Part E visibility (the hard part)**: production fns called intra-runtime → `pub(crate)` (handle_file_deleted/renamed_static, run_guarded_task_step); `handle_file_created_or_modified_static` + `FileIndexOutcome` → `pub` (consumed cross-crate by the STAYING top-crate `relationship_text_test.rs`); test-only helpers `mark_tantivy_dirty_for_test` + `is_running_for_test` → `#[cfg(any(test, feature="test-support"))] pub` (gated OUT of prod lib; top-crate dev-dep enables `julie-runtime/test-support`); root_safety/registry test helpers → `#[cfg(test)] pub use`.
- 3 gates green: `cargo build` 0 warnings, `-p julie-runtime` 85 pass, `-p julie --no-run` compiles.

**Process lesson (collision):** runtime-support (Sonnet) first delivered T2c.3 with Part E SILENTLY SKIPPED + 2 files unmoved + no visibility table (the documented silent-scope-reduction failure mode). I pushed back with a precise per-item fix. But then I jumped to DIY the fix while runtime-support was STILL editing (its idle notification was stale) → concurrent-edit collision (duplicate test files, churning visibility). Resolved fine (it converged correctly; I consolidated dups via git mv -f), but wasteful. **Rule: before taking over a teammate's in-flight task, confirm it has actually stopped (e.g. `find -newermt`), don't trust the idle notification; or just let it finish.** Also: my own visibility classification had 1 error (handle_file_created_or_modified_static is consumed by a staying top-crate test → needs pub, not pub(crate)); the compiler/teammate caught it.

NEXT: T2c.4 — julie-runtime dep-direction tripwire + xtask routing + orphan-filter scan (watcher_filtering@387, registry@505, env_paths buckets now zero-match `--lib`). Then branch-gate (cargo xtask test dev) + commit PR 2c.
