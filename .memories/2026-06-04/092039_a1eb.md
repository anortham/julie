---
id: checkpoint_a1ebf4f9
timestamp: 2026-06-04T09:20:39.321Z
tags:
  - julie-rescue
  - phase2
  - crate-split
  - julie-pipeline
  - PR-2a
git:
  branch: julie-rescue-phase2a
  commit: 73767fe1
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: Julie Rescue PR 2a COMPLETE — julie-pipeline extracted + tripwire/routing (U4)
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - julie-pipeline tripwire 2/2 (allowlist negative-tested)
  - cargo nextest run -p julie-pipeline = 144 across 2 binaries (incl tripwire)
  - cargo nextest run -p xtask 168/168
  - rg crate::tools zero in src/utils,watcher/filtering,indexing_core
  - smoke tier green (was RED via core-embeddings)
  - relink cure 6.2s vs 18.2s ~2.9x wall
  - HEAD 7ce28607 / 73767fe1
symbols:
  - sidecar_root_path
  - core-embeddings bucket
  - core-pipeline bucket
  - buckets_for_path
  - file_policy shim
next: Full `cargo xtask test dev` branch-gate running in background; on green,
  finish branch (push + PR 2a to main per Phase 0/1 discipline) pending user
  confirmation of the main-merge milestone. Then PR 2b (julie-context +
  ToolContext facade + julie-tools).
---

## Julie Rescue PR 2a COMPLETE — julie-pipeline extracted + tripwire/routing (U4)

Closed out PR 2a of the Phase 2 crate split on branch `julie-rescue-phase2a` (13 commits ahead of main, not yet pushed).

### What landed (U4, the final PR 2a task)
- **Dep-direction tripwire** `crates/julie-pipeline/tests/no_upward_deps.rs`. Source guard forbids `julie::`/`julie_context`/`julie_tools`/`julie_runtime`; manifest guard is **allowlist-based** (only julie-core/index/extractors/test-support are legal downward deps) so any future higher crate is auto-rejected without naming it (R7). Negative-tested: dropping julie-index from the allowlist correctly fails.
- **xtask routing**: new `core-pipeline` bucket (`cargo nextest run -p julie-pipeline`) in dev+full + sort order + golden manifest spec; `changed.rs` arms (embeddings→+core-embeddings; indexing_core/resolver/finalize→+workspace-init/integration/tools-workspace) + 4 new routing unit tests (crate-split arms had none).
- **PR 2a severance completed**: repointed `src/watcher/filtering.rs` + the file_policy unit test off the `crate::tools` shims onto `julie_core` directly; trimmed shim. `rg crate::tools` now zero in src/utils, src/watcher/filtering.rs, src/indexing_core.

### Two real bugs caught by the U4 gate
1. **U3 regression**: U3 moved 118 embedding tests into julie-pipeline but left `core-embeddings` filtering for them → zero-match nextest exits 4 → **dev+smoke were silently RED at HEAD**. Fixed by dropping dead filters (coverage now in core-pipeline). This is the Phase-1 orphaned-`--lib`-filter lesson biting again.
2. **cfg(test) false signal**: `cargo build` flagged a file_policy shim re-export "unused" while a `cfg(test)` consumer still needed it; trimming on that broke the test compile — `nextest --no-run` is the authority.

### Correction to a carried assumption
`cargo nextest run -p <crate>` **DOES** run the crate's integration tripwire binary (verified core/index/pipeline all "N tests across 2 binaries"). nextest does NOT skip it — no separate `cargo test --test` command needed.

### Relink cure (headline metric)
Touch a pipeline src file: pipeline dev relink **6.2s** vs top-crate test-binary relink **18.2s** → **~2.9× wall / ~4.1× build cure**. In line with Phase 0 (5.8×) / Phase 1 (3.2×).
