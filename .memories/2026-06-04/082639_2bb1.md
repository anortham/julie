---
id: checkpoint_2bb1d6d7
timestamp: 2026-06-04T08:26:39.189Z
tags:
  - julie-rescue
  - phase2a
  - U3
  - julie-pipeline
  - crate-split
git:
  branch: julie-rescue-phase2a
  commit: 5a0450c5
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
    - Cargo.lock
    - Cargo.toml
    - crates/julie-core/Cargo.toml
    - crates/julie-core/src/lib.rs
    - crates/julie-core/src/walk.rs
    - crates/julie-pipeline/Cargo.toml
    - crates/julie-pipeline/src/embeddings/factory.rs
    - crates/julie-pipeline/src/embeddings/init.rs
    - crates/julie-pipeline/src/embeddings/log_fields.rs
    - crates/julie-pipeline/src/embeddings/metadata.rs
    - crates/julie-pipeline/src/embeddings/mod.rs
    - crates/julie-pipeline/src/embeddings/pipeline.rs
    - crates/julie-pipeline/src/embeddings/sidecar_bootstrap.rs
    - crates/julie-pipeline/src/embeddings/sidecar_embedded.rs
    - crates/julie-pipeline/src/embeddings/sidecar_protocol.rs
    - crates/julie-pipeline/src/embeddings/sidecar_provider.rs
    - crates/julie-pipeline/src/embeddings/sidecar_supervisor.rs
    - crates/julie-pipeline/src/finalize.rs
    - crates/julie-pipeline/src/indexing_core/analysis.rs
    - crates/julie-pipeline/src/indexing_core/batch.rs
summary: "U3 complete: `julie-pipeline` crate created + engine modules relocated"
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
evidence:
  - cargo nextest run -p julie-pipeline → 142/142 pass
  - cargo nextest run -p julie-core → 140/140 pass
  - cargo nextest run -p julie --no-run → 0 errors
  - rg tripwire → ZERO hits
  - CLI smoke → results returned
next: "Commit with message 'refactor: extract julie-pipeline crate (Phase 2a
  U3)', then proceed to U4: julie-pipeline tripwire + xtask routing"
---

## U3 complete: `julie-pipeline` crate created + engine modules relocated

### WHAT
Created `crates/julie-pipeline` and relocated the indexing + embedding engine into it:
- `src/indexing_core/**` → `crates/julie-pipeline/src/indexing_core/`
- `src/embeddings/**` → `crates/julie-pipeline/src/embeddings/`
- `resolve_pending_relationships` + resolver deps → `crates/julie-pipeline/src/finalize.rs` + `crates/julie-pipeline/src/resolver.rs` (+ submodules)
- `src/utils/walk.rs` → `crates/julie-core/src/walk.rs` (needed by discovery.rs)
- Handler-free unit tests relocated to `crates/julie-pipeline/src/tests/`

Re-export shims placed at old paths so callers in the top crate compile unchanged.

### WHY
Phase 2a of Julie Rescue — split relink tax by extracting `julie-pipeline` leaf crate. Per DAG: `core → index → pipeline → top`.

### HOW
- `julie-pipeline` Cargo.toml deps: `julie-core`, `julie-index`, `julie-extractors` + async/util crates + `embeddings-sidecar` feature
- Import remaps: `crate::database::*` → `julie_core::database::*`, `crate::extractors::*` → `julie_extractors::*`, `crate::analysis/search` → `julie_index::*`, etc.
- `pub(crate)` items promoted to `pub` where called across crate boundaries (sidecar_bootstrap, sidecar_supervisor, sidecar_provider, sidecar_embedded, metadata, resolver, extraction)
- `test_sidecar_root_path_succeeds_from_source_checkout` stays in top crate only — CARGO_MANIFEST_DIR sensitive

### IMPACT
- `crates/julie-pipeline`: 142/142 tests pass
- `julie-core`: 140/140 pass, tripwire green
- Top crate `cargo nextest run -p julie --no-run`: zero errors
- `rg "crate::(handler|daemon|tools|watcher|workspace)" crates/julie-pipeline/src/` = ZERO hits
- CLI smoke: `julie-server search "SymbolDatabase" --standalone --json` returns valid results
- Note: test files also left in top-crate tree (not fully removed) — duplicates compile clean
