---
id: checkpoint_e10911d0
timestamp: 2026-06-04T13:46:25.721Z
tags:
  - julie-rescue
  - phase2
  - crate-split
  - julie-tools
  - T2b.5b
git:
  branch: julie-rescue-phase2b
  commit: 7d8303c0
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: T2b.5a ledger recorded + T2b.5b (julie-tools extraction) dispatched
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
next: Await context-dev's T2b.5b extraction report; inline-review the diff (esp.
  pub(crate)→pub visibility leaks); then dispatch T2b.6 (test relocation +
  tripwire + bucket routing).
---

## T2b.5a ledger recorded + T2b.5b (julie-tools extraction) dispatched

**Where we are:** Phase 2 PR 2b. HEAD `7d8303c0`. T2b.1–T2b.5a complete (9 tool dirs now fully handler-free). T2b.5b dispatched to context-dev (teammate) with a complete repoint analysis I derived by reading the tree.

### Full repoint map for the 9-dir move (`search, get_context, spillover, impact, deep_dive, navigation, symbols, editing, refactoring`)
- `crate::search::*` → `julie_index::search::*` (incl. `scoring::is_nl_like_query` — the known nl_embeddings concern, RESOLVED: lives in julie-index, top crate aliases `crate::search = julie_index::search` via src/lib.rs:19)
- `crate::analysis::*` → `julie_index::analysis::*`
- `crate::extractors::*` (47) → `julie_extractors::*` (julie-tools needs julie-extractors as a NEW direct git-dep, tag v2.1.0; core/index do NOT re-export it)
- `crate::language::*` → `julie_extractors::language::*`
- `crate::database::*` → `julie_core::database`
- `crate::mcp_compat::*` → `julie_core::mcp_compat`
- `crate::embeddings::*` → `julie_pipeline::embeddings`
- `crate::utils::{serde_lenient,token_estimation,cross_language_intelligence,file_utils,language}` → `julie_core::*` (all top-crate utils submodules used by tools are julie_core re-export shims)
- `crate::tools::shared::{NOISE_CALLEE_NAMES,OptimizedResponse}` → `julie_core::shared::*` (shared.rs is ALREADY a julie_core shim — do NOT move it)
- `crate::tools::<moved sibling>` → `crate::` (julie-tools internal)

### The one blocker (Step 0 prereq)
`crate::utils::paths::resolve_workspace_file_input` (3 call sites) lives in TOP-crate `src/utils/paths.rs`, NOT julie_core::paths. But that whole module depends ONLY on `julie_core::{paths, workspace_errors}` → move its impl (display_path, resolve_workspace_file_input, WorkspaceFileInputResolution, relative_within_workspace, to_absolute_native) into `julie_core::paths` + leave top-crate shim. Then repoint → `julie_core::paths::resolve_workspace_file_input`.

### Scope decisions (verified, not guesses)
- **Move:** 9 tool dirs only.
- **Stay top-crate:** `src/tools/metrics` + `src/tools/workspace` (ManageWorkspaceTool, handler/daemon-bound); `src/tools/shared.rs` (already a julie_core shim); `src/external_extract/` (CLI layer — depends on top-crate `cli_tools`, consumers are cli.rs/main.rs, NOT an MCP tool).
- **Tool TESTS stay in src/tests/tools/ for #7** (compile via the `src/tools/mod.rs` shim) — relocation is T2b.6 because it's coupled to xtask bucket rewiring.

### julie-tools deps
core, index, context, pipeline (path) + julie-extractors (git) + anyhow, tracing, serde, serde_json, schemars, rmcp, tantivy, uuid, tree-sitter, regex, diff_match_patch_rs, async-trait, tokio. dev-dep: julie-test-support.

### Shim design (`src/tools/mod.rs`)
`pub use julie_tools::{9 dirs}` + keep the existing type re-exports (FastSearchTool, DeepDiveTool, etc.) + keep `pub mod metrics; pub mod workspace; pub mod shared; pub use workspace::ManageWorkspaceTool;`.

### T2b.6 prep (done, read-only)
Inventoried xtask `test_tiers.toml` buckets referencing `tests::tools::*`. `workspace::*` (235-256,482) + `metrics::` (338) STAY `--lib` (tools don't move). Search/get_context/get_symbols/editing/deep_dive/nav/blast/spillover/refactoring buckets reference moved-tool tests → each repoints to `-p julie-tools` (if handler-free, relocated) or stays `--lib` (if handler-bound). Test split: 162 tool-test files, 68 reference a handler (stay), ~94 handler-free candidates (relocate). Finalize per-file routing after extraction lands.

### Gate for #7
`nextest -p julie-tools --no-run` + `-p julie --no-run` + `-p julie-core --no-run` + `cargo build` 0-warn + relink-cure measurement (touch julie-tools src, compare `-p julie-tools --no-run` vs `-p julie --no-run` wall/build; U4 baseline ~2.9×/4.1×). context-dev to STOP-and-report before T2b.6.
