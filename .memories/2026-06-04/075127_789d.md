---
id: checkpoint_789d31ef
timestamp: 2026-06-04T07:51:27.162Z
git:
  branch: julie-rescue-phase2a
  commit: 5a0450c5
  files:
    - .codex/config.toml
    - .miller/.symbols.db.init.lock
    - .miller/indexer.lock
    - .miller/logs/miller-20260531.jsonl
    - .miller/logs/miller-20260603.jsonl
    - .miller/logs/miller-35698-20260531.jsonl
    - .miller/logs/miller-38072-20260531.jsonl
    - .miller/logs/miller-43360-20260531.jsonl
    - .miller/symbols.db
    - .miller/symbols.db.julie-extract.lock
summary: Julie Rescue Phase 2 PR 2a — U1 + U2 done & verified, U3 in flight
briefId: julie-rescue-de-slop-packaging-keep-the-moat
type: checkpoint
---

## Julie Rescue Phase 2 PR 2a — U1 + U2 done & verified, U3 in flight

Executing PR 2a (extract `julie-pipeline`) via TeamCreate team `julie-phase2a` (Opus lead + Sonnet `pipeline-dev`), branch `julie-rescue-phase2a` (off main @ 4f2c2353). Sequential chain U1→U2→U3→U4 with lead inline review; **lead re-verifies every gate independently** (the implementer gave a false-green once).

### Done & lead-verified
- **U1** (`435fc2f6`): relocated shared leaves → julie-core (blacklist consts, workspace leaf modules, external_extract::paths, health state enums+SystemStatus, utils::{serde_lenient,file_utils,token_estimation,language,cross_language_intelligence,string_similarity}, mcp_compat, workspace_errors). Severed §3.2 utils→tools edges. Required a fix round: `cargo build` passed but top-crate TEST binary failed (`sanitize_name` unimported in src/tests/tools/workspace/registry.rs) — the Phase-1 lesson confirmed live.
- **U2** (`63b3810c`): file_policy.rs + indexing_state.rs → julie-core; embedding-log-fields cluster → src/embeddings/log_fields.rs (severs embeddings→workspace inversion at init.rs:16); extract_symbols_static→`extractors::extract_canonical` free fn (severs indexing_core→ManageWorkspaceTool). Both inversions rg-confirmed gone.
- Ledger rows recorded + committed (`0b158450`, `5a0450c5`).

### In flight
- **U3** (`pipeline-dev`): create crates/julie-pipeline (deps core+index, replicate embeddings-sidecar feature), move indexing_core/** + embeddings/** + handler-free indexing functions, relocate ~230 handler-free unit tests INTO the crate, top-crate re-export shims. Session-state orchestration (route/index/task-reg/analyze_batch) STAYS top-crate for 2c.
- **U4** pending: julie-pipeline tripwire + xtask routing + relink-cure measurement.

### Hard-won gotchas this session
- Crate-split gate MUST be `cargo nextest run -p <crate> --no-run` (compiles cfg(test)); `cargo build`/`check` give false greens.
- nextest's default profile SKIPS the `tests/no_upward_deps.rs` tripwire integration binaries ("4 binaries skipped"); run them via `cargo test -p <crate> --test no_upward_deps`. **U4/xtask routing must account for this or the tripwire silently won't run in tiers.**
- `#[cfg(test)]` in a dependency crate is invisible to dependents — symbols used by top-crate tests must be unconditionally `pub` in the lower crate.</description>
<tags>julie-rescue,phase-2,pr-2a,julie-pipeline,crate-split,team,in-progress</tags>
<next>Verify U3 when pipeline-dev reports (re-run -p julie-pipeline + -p julie --no-run + tripwires + CLI smoke independently); then U4 (tripwire + xtask routing + relink-cure measurement); then finishing-a-development-branch → PR 2a.</next>
<unknowns>Whether all ~230 pipeline/embeddings tests are truly handler-free (some may need julie-test-support or stay up-stack); embeddings-sidecar feature propagation; whether any embeddings test needs the Python sidecar to run</unknowns>
</invoke>

