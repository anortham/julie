---
id: checkpoint_dbe92824
timestamp: 2026-08-06T01:33:16.500Z
tags:
  - dependency-upgrade
  - julie-extractors
  - v2.27.0
  - v7.18.0
  - erlang
  - xml
  - reindex
  - ranking
git:
  branch: feat/julie-extractors-v2.27.0
  commit: 8593ed71
  files:
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - README.md
    - crates/julie-core/Cargo.toml
    - crates/julie-core/src/database/helpers.rs
    - crates/julie-core/src/database/identifiers.rs
    - crates/julie-core/src/shared.rs
    - crates/julie-core/src/test_support/db/rows.rs
    - crates/julie-core/src/tests/database/relationships.rs
    - crates/julie-index/Cargo.toml
    - crates/julie-index/languages/erlang.toml
    - crates/julie-index/languages/xml.toml
    - crates/julie-index/src/analysis/test_quality.rs
    - crates/julie-index/src/analysis/test_roles.rs
    - crates/julie-index/src/search/index.rs
    - crates/julie-index/src/search/index/mutation.rs
    - crates/julie-index/src/search/index/query/execution.rs
    - crates/julie-index/src/search/index/types.rs
    - crates/julie-index/src/search/language_config.rs
    - crates/julie-index/src/search/projection/apply.rs
    - crates/julie-index/src/search/reranker.rs
    - crates/julie-index/src/search/schema.rs
    - crates/julie-index/src/search/scoring.rs
    - crates/julie-index/src/tests/analysis/test_roles_tests.rs
    - crates/julie-index/src/tests/search/c3_enriched_schema_tests.rs
    - crates/julie-index/src/tests/search/language_config_embedded_tests.rs
    - crates/julie-index/src/tests/search/reranker_tests.rs
    - crates/julie-index/src/tests/search/unified_doc_index_test.rs
summary: Executed the julie-extractors v2.27.0 upgrade — all tasks landed, gates green
type: checkpoint
symbols:
  - SEMANTIC_INDEX_ENGINE_VERSION
  - lifecycle_role_from_name
  - is_function_scoped_binding
  - declaration_role
  - FUNCTION_SCOPED_BINDING_PENALTY
  - DOC_LANGUAGES
  - NON_EMBEDDABLE_LANGUAGES
next: Await dogfood result, then commit in reviewed groups, tag v7.18.0, and
  write substantive release notes.
confidence: 5
---

## Executed the julie-extractors v2.27.0 upgrade — all tasks landed, gates green

Branch `feat/julie-extractors-v2.27.0`, nothing committed yet. Executed via subagent-driven development: 4 parallel workers + lead-owned migration.

### WHAT LANDED

**Tasks 1+2 (lead):** Applied the pre-verified migration patch — all 6 manifests to v2.27.0, 39 API break sites fixed. xtask contract test RED→GREEN, renamed `extractor_dependency_release_is_v2_27_0`.

**Task 3 (worker):** Bumped `SEMANTIC_INDEX_ENGINE_VERSION` with `+extractors-tag=v2.27.0` to force the one-time reindex. **The worker improved on spec**: the new guard test reads the pinned tag directly out of root `Cargo.toml` via `include_str!` rather than duplicating the literal — so the NEXT re-pin mechanically fails until someone bumps the engine version. The false-green bug cannot recur.

**Task 4 (worker):** `docs/DEPENDENCIES.md` v2.16.0→v2.27.0 + matching assertion in `xtask/tests/docs_contract_tests.rs`.

**Task 5 (worker):** Erlang + XML language parity. New `erlang.toml`/`xml.toml` configs (real language knowledge, not stubs), registered in the `include_str!` table. XML added to `DOC_LANGUAGES` and `NON_EMBEDDABLE_LANGUAGES` (its symbols are name-promoted elements = data, not definitions). Three extension allowlists updated. **Fixed `lifecycle_role_from_name` language-agnostically** — the old `starts_with("after")` rule missed Erlang `end_per_suite` AND Ginkgo `JustAfterEach`; new rule reads the name's WORDS via existing `split_camel_case`, with `post` deliberately excluded (CT's `post_init_per_suite` runs after setup, not teardown).

**Task 6 (worker):** Language count 34→36 across ~16 files. **NOT 38** — jsx/tsx are aliased variants, so Julie's original "34" was correct.

**Task 7 (worker):** C# `Variable` rank safety. **Corrected my brief**: the exact-title bonus lives in `reranker.rs` `kind_boost` (Variable ties Field/Property at 20.0), not `scoring.rs`. Required plumbing a new Tantivy `declaration_role` field through 8 files in `search/` because extractor metadata was not reachable at scoring time. Penalty 90.0, de-boost not exclude. Naming collision with Julie's path-derived `role` handled by calling it `declaration_role` everywhere with cross-referencing doc comments.

### KEY CORRECTION FROM USER
I misread "update it and push a new release" as un-retiring Julie. **User corrected: Julie stays retired.** Reverted `TODO.md` fully and restored README's `## Retired` section, adding only that v7.18.0 is a maintenance release with nothing further planned. Also dropped the now-false clause "which Miller consumes at a newer version than Julie's final pin" — Julie at 2.27.0 is now AHEAD of Miller's 2.20.0 pin.

### VERIFICATION
- `cargo fmt --check` PASS, `cargo check --workspace --all-targets` clean
- `cargo xtask test bucket extractor-dep-integration` PASS (3/3)
- `cargo xtask test dev` PASS (27 buckets, 255.8s warm)
- dogfood running

### ENVIRONMENT NOTE
`cargo-nextest` was NOT installed on this machine; the entire xtask runner shells out to it, so no gate could run. Installed prebuilt 0.9.143 to `~/.cargo/bin`.

### KNOWN DEFERRED
- `is_glob_pattern` (`query_preprocessor.rs:106`) still language-biased, missing ~20 extensions. Principled fix is deriving from `supported_extensions_for_indexing()`. Behavior change beyond upgrade scope.
- `html.toml` has no `[literal_carriers]` — pre-existing, drops HTML attribute literals.
- One unrelated `cargo fmt` line in `crates/julie-runtime/src/watcher/mod.rs` swept in; kept so branch passes `fmt --check`.
