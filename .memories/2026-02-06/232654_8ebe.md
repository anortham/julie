---
git:
  branch: main
  commit: d0edc3b
  dirty: false
id: checkpoint_698678be_4ae514
tags:
- release
- v2.1.0
- milestone
timestamp: 1770420414
type: checkpoint
---

## v2.1.0 Release — Shipped Successfully

### What's in v2.1.0
- **Removed `output_format`** from `get_symbols` (auto-selects code/lean based on body presence)
- **Fixed `edit_symbol` `replace_body`** — now uses `find_any_symbol()` supporting structs, enums, classes, not just functions/methods
- **Generic name filtering** in `trace_call_path` downstream — ~85 names (clone, to_string, next, iter, etc.) filtered out
- **Dedup by symbol ID** in both `trace_upstream` and `trace_downstream`
- **Fixed `fast_explore(dependencies)`** — key name mismatches (`kind`→`relationship_kind`, `line`→`start_line`, `children`→`dependencies`), BFS dedup, generic filtering, accurate count via recursive `count_deps()`
- **Docs cleanup** — deleted ~795k of stale docs (RAG_TRANSFORMATION, JULIE_2_PLAN, TODO, archive/, debug/, plans/)
- **11 memory checkpoint files** committed

### Release Details
- Commit: `d0edc3b`
- Tag: `v2.1.0`
- GitHub Actions: all 5 jobs passed (Windows, macOS Intel, macOS ARM, Linux, Create Release)
- 45 files changed, 560 insertions, 16,090 deletions
- 758 tests pass
