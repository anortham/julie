---
id: checkpoint_50bdbcf6
timestamp: 2026-07-22T12:13:01.905Z
tags:
  - decision
  - build
  - macos
  - rustup
  - release
  - module-boundaries
  - phase-2
  - xtask-runner
  - plan
git:
  branch: codex/julie-improvement-roadmap
  commit: 73f37791
  files:
    - docs/plans/2026-07-22-macos-release-toolchain.md
    - docs/plans/2026-07-22-runner-module-boundary.md
summary: Planned the release-toolchain prerequisite and Phase 2A runner split
briefId: julie-quality-improvement-roadmap
type: decision
decision: Stabilize the proven macOS release toolchain mismatch before executing
  Phase 2A, while keeping the release-build and runner-boundary work in separate
  approved plans.
alternatives:
  - Ignore the linker output because cargo exits successfully; rejected because
    Homebrew compiler_builtins declares macOS 26.0 while Julie supports macOS
    11.
  - Raise Julie's deployment target; rejected by the approved roadmap and
    product support contract.
  - Suppress linker warnings; rejected because it hides incompatible object
    metadata.
  - Combine all four Phase 2 splits; rejected because the roadmap requires a
    separately approved plan per split.
impact: Unblocks a reproducible release build and an evidence-grounded first
  Phase 2 module split without changing product behavior.
evidence:
  - Homebrew cargo build --release passed with object-version warnings from
    macOS 26.0/26.5 objects against target 11.0.
  - vtool showed Homebrew compiler_builtins minos 26.0 and rustup 1.97.0
    compiler_builtins minos 11.0.
  - Official rustup 1.97.0 release control build finished in 3m09s with zero
    linker warnings.
  - Miller inspection confirmed xtask/src/runner.rs is 1242 lines with stable
    public callers in xtask main, changed selection, and runner test suites.
symbols:
  - xtask::runner
  - run_tier
  - run_named_buckets
  - run_bucket
  - ProcessCommandExecutor
  - render_summary
  - prebuild_test_binary
next: Commit the plans and Goldfish artifacts locally, then wait for explicit
  approval before implementation.
confidence: 5
---

## Planned the release-toolchain prerequisite and Phase 2A runner split

- **What:** Wrote separate implementation plans for a warning-free official Rust build and the behavior-preserving `xtask::runner` module boundary.
- **Why:** Homebrew Rust 1.97.1 now supplies `compiler_builtins` objects marked for macOS 26.0 while Julie targets macOS 11.0; `xtask/src/runner.rs` remains 1242 lines and is the first Phase 2 split.
- **How:** Reproduced the warnings under Homebrew, inspected object load commands, then completed a clean release control build with official rustup 1.97.0 plus `MACOSX_DEPLOYMENT_TARGET=11.0` with no linker warning. The runner plan keeps every public symbol and behavior behind private prebuild, execution, and rendering modules.
- **Impact:** The two plans are ready for user approval. Once approved, the toolchain prerequisite can make bare local release builds trustworthy before Phase 2A starts.
