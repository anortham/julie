---
id: checkpoint_7c75c314
timestamp: 2026-07-22T22:28:38.728Z
tags:
  - release
  - v7.17.0
  - code-review
  - bug-fix
  - projection-staleness
git:
  branch: main
  commit: 82acb59b
  files:
    - Cargo.lock
    - Cargo.toml
    - crates/julie-pipeline/src/indexing_core/web_edges.rs
    - crates/julie-runtime/src/tests/watcher_handlers/repair_projection.rs
    - crates/julie-runtime/src/watcher/handlers.rs
    - crates/julie-runtime/src/watcher/mod.rs
    - crates/julie-tools/src/impact/formatting.rs
    - crates/julie-tools/src/impact/mod.rs
    - crates/julie-tools/src/tests/blast_radius_formatting_tests.rs
    - docs/release-notes/v7.17.0.md
    - docs/site/index.html
    - src/tests/external_extract/info.rs
    - xtask/src/changed/mapping/front.rs
    - xtask/src/changed/policy.rs
    - xtask/test_tiers.toml
    - xtask/tests/changed_tests.rs
    - xtask/tests/support/manifest_contract_expected.rs
summary: Pre-release review + fixes for v7.17.0
type: checkpoint
evidence:
  - "cargo nextest --workspace: 2985 passed / 1 flake / 54 skipped"
  - "flake passes in isolation:
    test_primary_read_handlers_do_not_block_on_legacy_db_mutex"
  - "TDD red→green: staleness + web_callers pagination"
next: Commit all changes, tag v7.17.0, push main + tag, ensure substantive
  GitHub release notes
---

## Pre-release review + fixes for v7.17.0

Reviewed 62 commits since v7.16.0 (base `e3b99896` → `82acb59b`) with a 7-lane adversarial workflow + full `cargo nextest --workspace`, then fixed every actionable finding and prepped the v7.17.0 release.

### Fixes landed (all TDD where behavior changed)
- **Silent search-staleness regression** (`6765a8b4` introduced it): watcher handler erroring *after* the SQLite commit but before the Tantivy apply left the projection falsely `Ready`. Fixed `dispatch_file_event` Err arms (Created/Modified + rename target) to queue `tantivy_dirty` → batch stamps `Stale`. New test `test_watcher_handler_error_after_commit_marks_projection_stale` (added `#[cfg(test)] FAIL_AFTER_COMMIT_FOR_TEST` hook in handlers.rs).
- **Stale schema tripwire**: `extract_contract_version_and_schema_are_at_phase3b_coordinated_values` pinned schema 28, actual is 30 (migration_030 web_edges). Bumped to 30 + rewrote the misleading "Miller bridge" comment (Julie has no Miller dependency; Miller is a downstream consumer, contract stayed v3).
- **Dead code**: deleted `facts_contain_web_patterns` (zero callers, false docstring).
- **Unbounded output**: `blast_radius mode=web` web_callers now paginated with spillover (new field + `store_list_overflow`), test `test_web_callers_overflow_marker_appears_when_truncated`.
- **xtask stale mapping**: `src/startup.rs` routed to non-existent `lifecycle` bucket → swapped to `registry`; guarding test now uses the real manifest so it can't lie.
- **Coverage gap**: `external_extract` tests (incl. the schema tripwire) now run in the dev-tier `registry` bucket (+ contract snapshot updated).

### Deliberately NOT changed
- **#6 xtask budget under-pricing** (get-symbols declared 40s vs ~140s warm): a conscious maintainer choice (`d0c2e2cc`); "fixing" forces a tier-budget redesign. Flagged for user decision, not auto-changed.

### Release prep
- Version 7.16.0 → **7.17.0** (Cargo.toml, Cargo.lock, docs/site/index.html). CLAUDE.md/AGENTS.md already in sync.
- Wrote `docs/release-notes/v7.17.0.md`.

### Verification
- Full `cargo nextest --workspace`: 2985 passed, 54 skipped, 1 "failed" = `test_primary_read_handlers_do_not_block_on_legacy_db_mutex` — confirmed a load-induced 800ms-timeout flake (PASSES in isolation, 9.7s; unrelated to my changes).
- Release APPROVED by user. Next: commit + tag v7.17.0 + push.
