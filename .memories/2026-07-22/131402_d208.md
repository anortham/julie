---
id: checkpoint_d2084ecb
timestamp: 2026-07-22T13:14:02.454Z
tags:
  - refactor
  - xtask
  - changed-selection
  - plan-conformance
  - routing
  - phase-2b
git:
  branch: codex/julie-improvement-roadmap
  commit: 8914055a
  files:
    - xtask/src/changed/mapping.rs
    - xtask/src/changed/mapping/crates.rs
    - xtask/src/changed/mapping/front.rs
    - xtask/src/changed/mapping/product.rs
summary: Restored the approved optional mapping seams
briefId: julie-quality-improvement-roadmap
type: checkpoint
impact: Eliminates a plan mismatch before exact-SHA verification.
evidence:
  - "cargo check -p xtask: pass"
  - "cargo nextest run -p xtask --test changed_tests: 43/43 pass"
  - "cargo fmt -p xtask: pass"
  - "git diff --check: pass"
  - mapping files remain <= 415 lines
symbols:
  - mapping::buckets_for_path
  - mapping::front::buckets_for_path
  - mapping::crates::buckets_for_path
  - mapping::product::buckets_for_path
next: Commit the correction and run exact-commit verification.
---

## Restored the approved optional mapping seams

The pre-ledger plan audit found that the first implementation preserved routing behavior but used empty slices as private fallthrough markers.

- **Why:** The approved Phase 2B design explicitly requires each rule group to return `Option` and the dispatcher to chain `front`, `crates`, and `product` with `or_else`.
- **How:** Added private slice-returning helpers inside each rule group, exposed `Option` at the module boundary, and restored the exact ordered dispatcher.
- **Impact:** The implementation now conforms to both the behavioral contract and the approved module-interface design.
