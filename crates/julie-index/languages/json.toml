[tokenizer]
# Character sequences to preserve as single tokens
preserve_patterns = []

# Naming conventions to recognize
naming_styles = ["camelCase", "snake_case", "PascalCase"]

# Prefixes/suffixes with semantic meaning
meaningful_affixes = []

[variants]
strip_prefixes = []
strip_suffixes = [".json"]

[scoring]
important_patterns = []

[embeddings]
# Default: global EMBEDDABLE_KINDS only, variable_ratio = 0.20
extra_kinds = []
