//! MCP Compatibility Layer — relocated to `julie_core::mcp_compat`.
//!
//! All items re-exported so existing `crate::mcp_compat::*` import sites
//! compile unchanged.
pub use julie_core::mcp_compat::*;
